# インポートとエクスポート

*日本語 | [English](import-export.md)*

図の入出力は、ツールバーの「取込」「出力」ボタンから行います（実データベースからの取込は別ボタン「DB取込」です。[データベース連携](database.ja.md)を参照してください）。

| 形式 | インポート | エクスポート |
|---|:-:|:-:|
| 実データベース（5 方言） | ✅ | —（差分同期・DDL で書き戻し） |
| C# コード（生成物 .g.cs） | ✅ | —（コード生成で書き出し） |
| SQL DDL | — | ✅ |
| DBML | ✅ | ✅ |
| Mermaid（erDiagram） | ✅ | ✅ |
| Excel 定義書 | ✅ | ✅ |
| HTML 定義書 | — | ✅ |
| スキーマ JSON（配置情報なし） | — | ✅ |
| PNG / SVG | — | ✅ |
| 印刷 / PDF | — | ✅ |

## インポート

「取込」ボタンからファイルを選ぶと、現在の図を取込内容で置き換えます（実行前に置換確認があります）。取込後は自動整列が適用されます。

### DBML（.dbml）

`Table` ブロックと `Ref:` 行からなる DBML を取り込みます。対応範囲は QuickER の DBML エクスポートと往復可能なサブセットです。

- 列設定: `pk` / `ref` / `null` / `not null` / `note: '...'`
- 関係: `Ref:` 行の `-`（1 対 1）、`<`（1 対 多）、`<>`（多 対 多）。`>`（多 対 1）は未対応
- 未対応: `Project` / `Enum` / `Indexes` / `TableGroup` / 複数行 `Note` ブロック

列を持たないテーブルには既定の PK 列（`ID int`）が補われます。DBML は方言情報を持たないため、図の対象 DB は取込前のまま維持されます。

### Mermaid（.mmd / .mermaid）

`erDiagram` 記法を取り込みます。DBML と同じく方言情報を持たないため、対象 DB は維持されます。

### Excel 定義書（.xlsx）

QuickER がエクスポートした定義書を再取込できます。シートの役割は非表示の定義タグで判定するため、シート名を変更・翻訳しても取り込めますが、**他のアプリで作成した Excel を直接取り込むことはできません**（手元の定義書資産を移行する場合は、いったん QuickER の定義書形式に転記してください）。定義書には対象 DBMS が埋め込まれており、取込時に方言ごと復元されます。件数の不一致や重複、未定義のテーブル参照は取込エラーになります。

### C# コード（.cs）

コード生成ツールバーの「コード取込」ボタンから、QuickER が **`IncludeDataAnnotations` ON で生成した本体 `.g.cs`** を選ぶと、Roslyn の構文解析のみ（コンパイル・アセンブリ読込なし）で図を復元し、現在の図へマージ取込します。`[Table]` / `[Column]` / `[Key]` / `[Required]` / `[DbColumnMeta]` / `[DbTableMeta]` / `[NavigationReference]` 属性を持つクラスだけが対象で、Repository などのインフラクラスや手書き POCO は無視されます（対象クラスが 0 件のときは、生成物の本体 `.g.cs` を選ぶよう案内するエラーになります）。

列型は方言中立トークンから現在の図の方言のネイティブ型へ展開します（展開できないトークンは警告のうえそのまま採用）。**コードは方言中立のため、取込後も図の対象 DB は取込前のまま維持されます**。多対多リレーション・`ON DELETE` / `ON UPDATE`・FK 制約名はコードに存在しないため、既存図へのマージ取込では、端点（両端テーブル名・列名）が一致するリレーションの参照アクション・制約名と、両端が生存する多対多リレーションを現在図から温存します（コードで消えた通常のリレーションは図からも消えます）。他の取込と同様、構造差分や壊れる名前付きクエリがあるときは置換確認が出ます。

CLI の `quicker reverse` は、同じ解析でマージなしの新規図（スキーマのみ JSON）を書き出します（[CLI リファレンス](cli.ja.md) を参照）。

## エクスポート

「出力」ボタンから形式を選びます。

### SQL DDL（.sql）

図の対象 DBMS の方言で CREATE 文一式を出力します。

### DBML / Mermaid

テキスト形式へ書き出します。DBML の出力は上記インポートと同じサブセット（`Table` ブロック＋ `Ref:` 行）で、書き出したファイルはそのまま再取込できます。dbdiagram.org などの DBML 対応ツールや、Mermaid を表示できる GitHub / ドキュメントツールとの連携に使えます。

### Excel 定義書（.xlsx）

テーブル一覧、リレーション一覧、テーブル別の詳細シート（列の型・必須・キー・説明）からなる定義書を出力します。対象 DBMS がファイルに埋め込まれるため、この定義書は QuickER への再取込で図に戻せます。

### HTML 定義書（.html)

外部参照も JavaScript も含まない自己完結の単一 HTML を出力します。サイドバーのナビゲーション、概要（対象 DBMS・テーブル数・リレーション数）、テーブル一覧、リレーション一覧（ON DELETE / ON UPDATE を含む）、テーブル別詳細で構成され、ブラウザだけで閲覧できるため、開発者以外の関係者への配布に向きます。

図を修正したら定義書を再出力するだけで最新化できるので、「ドキュメントだけ古い」状態を避けられます。

### スキーマ JSON（.json）

図の保存形式（[ER 図の編集](er-editor.ja.md) を参照）から配置情報（座標・色などの layout）を除いた、スキーマ定義と名前付きクエリ定義だけの JSON（`{ version, schema }`）を出力します。配置情報を持たないため差分が安定し、テーブル定義そのものをレビュー・バージョン管理したいときに向きます。このファイルは QuickER で「開く」と読み込め、配置情報がないため全体が自動整列されます（＝可逆）。通常保存（layout 込みの `.json`）とは別に、配置の揺れを含めずスキーマだけを共有したい場合に使います。

### PNG / SVG（画像）

図全体を画像として出力します。画面のズーム状態や選択表示の影響を受けません。

### 印刷 / PDF

ツールバーの「印刷」（Ctrl+P）で印刷オプションダイアログが開きます。

- **図のタイトル** — ヘッダに印字するタイトル
- **印刷サイズ** — 「用紙 1 ページに縮小して印刷」（既定）/「原寸大で印刷」（用紙サイズを図の実寸に合わせる・PDF 出力向け）
- **印刷日時をヘッダに印字する**（既定 ON）

Microsoft Print to PDF などの PDF プリンタを選べば、そのまま PDF 出力になります。

## 関連ページ

- [データベース連携](database.ja.md) — 実 DB からの取込と差分同期
- [ER 図の編集](er-editor.ja.md) — 図の保存形式（git 管理できる JSON）
