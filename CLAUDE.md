# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

QuickER は WPF 製の ER 図デザイナ。DB スキーマのインポート／DDL 生成（SQL Server / PostgreSQL / MySQL / Oracle / SQLite の5方言）、Scriban テンプレートによる C# コード生成、AI チャットによる図の操作（MCP サーバ内蔵）、外部 AI エージェント向け stdio MCP サーバ（CLI `quicker mcp`）を持つ。コメント・コミットメッセージは日本語。ただし**生成物（.g.cs の XmlDoc・コメント・実行時例外メッセージ、.g.md）に埋め込む固定文は英語が正本**（製品の中立言語＝resx の中立と同じ英語。テンプレート `Templates/CSharpRuntime/*.scriban` 内の全コメントは生成出力そのものなので英語で書く。図の Description 由来のユーザーデータは無加工。API リファレンスは `IncludeJapaneseApiDocs`／CLI `--api-docs-ja` で日本語版 `{ベース名}.ja.g.md` を併産可能＝テンプレート対訳ペア `ApiReferenceDoc.scriban`/`ApiReferenceDoc.ja.scriban` は構造パリティテストが同時更新を強制。生成出力の CJK 混入は GeneratedOutputEnglishGuardTests が検知）。**samples/ 配下は手書きコード（Program.cs のコメント・出力・データ）・図の Description も英語**（生成物と同居する国際向けショーケースのため。サンプルの README.ja.md のみ日本語）。

## コマンド

```powershell
dotnet build QuickER.slnx                                # ビルド
dotnet test QuickER.slnx --no-build                      # 全テスト
dotnet test tests/QuickER.Tests/QuickER.Tests.csproj --filter "FullyQualifiedName~DdlGeneratorTests"   # 単一テストクラス
csharpier format .                                       # 整形（グローバルツール）。コード修正後は必ず実行
```

- テストは net10.0-windows / WPF 依存のため **Windows でのみ実行可能**（CI も windows-latest）
- **テストのフォルダ構成は src のプロジェクト構成をミラー**する（`tests/QuickER.Tests/{Model|Document|Provider|SqlServer|PostgreSql|MySql|Oracle|Sqlite|CodeGen.CSharp|CodeGen.UI|Db.UI|AI|AI.UI|AI.Chat|AI.Mock|Extensibility|Gui|Gui.Common|Cli|Mcp|Mcp.Tools}/`＝SUT の所在プロジェクトに対応・namespace はフォルダ追従で `QuickER.Tests.{フォルダ}`。Gui/ 配下はさらに Services/ViewModels/Views/UndoRedo をミラー）。横断フォルダは `GeneratedFixture/`（ドリフト検知＝パス不動・regen スクリプトが参照）・`Integration/`・`Resources/`（resx パリティ）・`Samples/`・`TestDoubles/`（共有テストダブル）・`TestSupport/`（WPF/STA 起動ヘルパ）
- `tests/QuickER.Tests/Integration/` は実 DB テストで 2 サブフォルダに分かれる: `Dialects/`＝アプリ自身の DB 面サービス検証（`*IntegrationTests` 接尾辞。CommentSync / ConnectionStringFactory / DdlRoundTrip / SchemaSync）・`GeneratedRuntime/`＝生成コードの実行時挙動検証（`*RuntimeTests` 接尾辞。SaveHook / NamedQuery / BinaryColumn / Remote / EF Core 方言等）。共有フィクスチャ（*ContainerFixture / SqliteTempDatabase）は Integration 直下。SQL Server / PostgreSQL / MySQL / Oracle は Testcontainers の実コンテナを使い、**Docker 不在時は自動スキップ**される（フィクスチャが検出）。この開発機では Docker 稼働＝全件実行・スキップ 0 が正常値。CI では Linux コンテナが使えないため常にスキップされる。SQLite 系は実ファイル DB（SqliteTempDatabase）を使うため **Docker 不要＝CI でも常時実行**される
- Docker 必須テストには `[Trait("RequiresDocker", "true")]` が付与されており、`--filter "RequiresDocker=true"`（または `!=` で除外）で選別実行できる（例外: `Integration/Dialects/SqlServerSchemaSyncIntegrationTests` は localhost の実 SQL Server 接続＝Docker 不使用・接続不可時スキップの第 3 機構）
- 生成コードの Roslyn コンパイル検証（GeneratedCodeCompilationTests）は Docker 不要で常時実行される

## アーキテクチャ

依存方向（上が被依存側。矢印はプロジェクト参照）：

```
QuickER.Model        意味モデル（依存ゼロ。X/Y/色などの視覚情報を持たない）
  ▲
QuickER.CodeGen.CSharp    DB非依存のC#コード生成エンジン（Scriban・テンプレートは埋め込みリソース）
  ▲
QuickER.Provider     DB抽象化の共通基盤（DdlGeneratorBase、インポータ共有部品）
  ▲
QuickER.SqlServer / PostgreSql / MySql / Oracle / Sqlite    方言プロバイダ（5実装で対称構造）
  ▲
QuickER.Gui (WPF) / QuickER.Cli                    合成ルート（全プロジェクトを参照）

QuickER.Document → Model    保存単位 DiagramDocument = schema(意味) + layout(視覚) の1ファイル
QuickER.Mcp          MCP ツール定義の正本（ToolDefinition・ErDiagramToolCatalog＝英語）と stdio サーバホスト・file 注入（依存は ModelContextProtocol SDK のみ・WPF 非依存）
QuickER.Mcp.Tools → Model, Document, Mcp, CodeGen.CSharp    ファイルベースの ER 図ツール実行ホスト（ステートレスに読込→変更→保存。set_query の DSL/生 SQL 検証に CodeGen.CSharp の Queries を使用。CLI `quicker mcp` が合成）
QuickER.AI       → Model, Settings, Mcp    AIチャットエンジン共通基盤 AI/MCP/ASP.NET Core（WPF非依存。VM操作は IErDiagramToolHost 経由でアプリ側）
QuickER.Settings     設定 JSON の汎用永続化ストア JsonSettingsStore<T> と原子的ファイル書き込み AtomicFile（依存ゼロ・net10.0。AI / CodeGen.UI / Document / Db.UI が参照）
QuickER.Extensibility → Model, Provider    フィーチャーモジュール契約（IFeatureModule / FeatureToolbarItem / IErDiagramHost。WPF 非依存・net10.0）
QuickER.Gui.Common   → Gui.Abstractions    汎用 WPF 部品（MessageBoxDialogService / WpfFileDialogService / PasswordBoxBehavior / InformationDetailsDialog）

機能プロジェクト（WPF。Gui を参照しない＝Gui が参照する。ランチャー・アダプタは各機能側にあり、Gui は IErDiagramHost 実装とカタログだけを持つ）:
QuickER.Gui.Abstractions  アプリ汎用 UI サービス抽象（IDialogService / IFileDialogService。実装は Gui.Common / Gui 側）
QuickER.AI.UI      → AI, Gui.Abstractions    AI 共有 UI 部品（接続パネル・添付・ApiKeyStore・WpfUiDispatcher）
QuickER.AI.Chat    → AI, AI.UI, Extensibility, Gui.Abstractions, Gui.Common    チャット機能（ErDesign プロファイル・ER 設計ツール定義・チャットダイアログ・AiChatFeatureModule）
QuickER.AI.Mock    → AI, AI.UI, Extensibility, Gui.Abstractions, Gui.Common, CodeGen.CSharp, Provider    モック生成機能（モックフォルダ方式のセッション・ツール・ストア・プレビュー一式・単一 HTML バンドル出力・WPF モックプロジェクト生成・MockGenerationFeatureModule）
QuickER.CodeGen.UI → CodeGen.CSharp, Extensibility, Gui.Abstractions, Provider, Settings, SqlServer, Sqlite    C# コード生成／クエリ定義ダイアログ・CodeGenerationFeatureModule
QuickER.Db.UI      → Extensibility, Gui.Abstractions, Gui.Common, Provider, SqlServer, Sqlite    DB 取込／DB 同期ダイアログ・接続プロファイル（connections.json＋DPAPI シークレット）・DbToolsFeatureModule
```

押さえるべき設計判断：

- **意味と視覚の分離**: `Entity`（Model）は座標・色を持たない。視覚状態は `EntityViewModel` と `EntityLayout`（Document）が保持。エクスポータ・生成器は意味モデル `ErDiagram` のみを消費する
- **CodeGen.CSharp の DB 非依存**: 型解決（DB型→C#型）はプロバイダ側の責務。CodeGen.CSharp は解決済み `CSharpTypeInfo` の辞書を入力に受け取る
- **プロバイダの対称性**: 5方言は同じ構造（SchemaImporter / DdlGeneratorBase 派生 / 型マップ / 実 DB 統合テスト。統合テストは SQL Server / PostgreSQL / MySQL / Oracle が Testcontainers、SQLite は実ファイル DB）。方言 SQL の説明コメントは SQL Server 版と同水準に揃える。スキーマインポータの基底クラス化は**意図的に見送り**（方言差分が大きい）。共有部品は ForeignKeyRelationshipBuilder / UniqueColumnSetBuilder / SchemaTableEntry。**既知の割り切り**: 各方言の Identifier はドット付きテーブル名を 5 方言対称に `"a"."b"` へ分割クォートする（SqliteIdentifierTests が固定）。SQLite では `"a"."b"` が「アタッチ済み DB a のテーブル b」を意味するため、dbo 以外のスキーマを含む SQL Server 由来の図を sqlite へ切り替えて DDL/同期を実行すると `unknown database` エラーになり得る（対称性維持を優先し修正しない・2026-08-05 判断）
- **DB 同期の 2 段構造（planner ＋ 方言別レンダリング）**: スキーマ同期は「`SchemaDiffService.Compute`（差分検出・方言中立）→ `SyncPlanner.BuildPlan`（選択済み差分を固定順セクション化＝方言中立の `SyncPlan`）→ `ISyncScriptBuilder.Build`（方言別に SQL 化）」の 2 段。方言差は `SyncDialectCapabilities`（`SupportsAlterColumn` / `SupportsForeignKeyAlter` / `SupportsDescriptions` / `PersistsForeignKeyConstraintNames` / `ColumnReorder` / `AlterColumnRequiresForeignKeyRebuild`）でプランナーへ宣言する。逐次 DDL 方言（SQL Server / PostgreSQL / MySQL / Oracle）は差分をそのままセクション化。**SQLite はテーブル再構築方式**（`SyncPlan.Rebuilds`／`TableRebuildPlan`＝「live＋選択差分のみ」の合成スキーマで新テーブル作成→データ移送→DROP→RENAME→補助オブジェクト再作成・FK 検査は `foreign_key_check` を実行側が実検査・PRAGMA はトランザクション外＝`SyncPlanner` が合成に live スキーマ〔`SyncPlanContext`〕を要求する）。**列順同期（Phase 3）は SQLite（再構築へ畳む）と MySQL（ネイティブ `ALTER TABLE ... MODIFY ... AFTER`＝`SyncPlan.Reorders`／`TableReorderPlan`・最長増加部分列を不動として移動列を最小化）のみ**で、選択可能な `ReorderColumns` 差分項目（既定未選択）として出す。他 3 方言（`ColumnReorder=None`）は従来どおり選択不可の案内項目のみ（VM 側）。検出は「共通列の相対順序比較」（追加・削除を除いた双方に存在する列の順序差＝真ん中への列追加＋並び替えを 1 回の同期で完結できる）。**主キー同期（AlterPrimaryKey）**＝PK 列構成（順序付き名前リスト）の差をテーブル単位 1 項目で検出（既定未選択・IsDestructive）。旧 PK 制約名は取込まず方言カタログの実行時逆引きで DROP（SQL Server=sys.key_constraints 動的 SQL・PostgreSQL=pg_constraint DO ブロック・MySQL/Oracle=名前不要の DROP 構文・PK なしは no-op・解除のみは ADD なし）・ADD は `PK_{テーブル名}` 固定命名。逐次方言は **3 フェーズ**（`SyncPlanSection.PrimaryKeyPhase`＝Drop→AlterColumn→Add の順で同一 Kind のセクションを 2 分割＝旧 PK 列の NULL 許容化を含む変更も 1 回の同期で成立。PK 変更なしの計画は出力バイト不変）・SQLite は再構築へ畳む。**依存 FK の自動 drop→re-add**＝選択済み AlterPrimaryKey の被参照 FK（全方言無条件）と FK 関与列の AlterColumn（`AlterColumnRequiresForeignKeyRebuild`＝SQL Server のみ true・Msg 5074 対策)を、プランナーが live リレーションから合成項目として Drop/Add セクションへ注入（明示 Drop 選択とはシグネチャで重複排除・明示 Drop した FK は再 add しない）。被参照列が新 PK から外れる再 ADD は `SyncPlan.Warnings`（候補キー喪失・言語中立 record）として実行確認に表示（UNIQUE 制約を取込まないため実行ブロックはしない＝誤爆回避。失敗時 SQL Server/PG はロールバック・**MySQL/Oracle は FK 消失の部分適用**＝統合テストで現状固定）。**複合 FK の劣化ガード**＝取込警告（`SchemaImportResult.Warnings`）を同期ダイアログが受け、選択不可の案内項目（`SchemaDiffKind.Advisory`＝SectionOrder 外でレンダラーは無視）を先頭表示・関与する FK 差分は `CompositeForeignKeyGuard` の照合で選択不可へ格下げ・**SQLite は複合 FK 子テーブルの再構築をテーブル単位でブロック**（成功して静かに単列 FK へ置き換わる唯一の経路を封鎖・他テーブルの同期は継続）。実行失敗後は差分を自動再計算する（MySQL/Oracle の部分適用後に古い差分を表示し続けない）
- **フィーチャーモジュール方式（着脱可能なプラグイン）**: DB 取込・DB 同期・AI チャット・AI モック生成・C# コード生成・クエリ定義は、契約 `QuickER.Extensibility`（`IFeatureModule`＝Id / ConfigureServices / Initialize〔既定 no-op・Build 直後〕/ CreateToolbarItems / OnMainWindowClosing・`FeatureToolbarItem`〔INotifyPropertyChanged 実装クラス。Tooltip / BeginsGroup は実行中に切替可＝対象 DBMS 切替に追従してツールチップを差し替える機構（現在は全方言が DB 同期対応のため実質定数だが機構は維持）。各モジュールの先頭アイテムは BeginsGroup=true・集約後の全体先頭のみ App が false へ矯正〕・`IErDiagramHost`）を介した静的モジュール合成で Gui へ結線する。依存方向は「プラグイン（Db.UI / AI.Chat / AI.Mock / CodeGen.UI の各 FeatureModule）→ 契約 ← ホスト（Gui）」で、**Gui は機能の型を知らない**（機能プロジェクト参照はカタログのためだけ）。ホスト能力は `MainViewModelErDiagramHost`（Gui）が `IErDiagramHost`（IsEmpty / **IsDirty**〔未保存変更の有無＝図を失う操作の確認水準の選択用。ダーティなら ConfirmWarning・クリーンなら Confirm＝`DialogServiceExtensions.ConfirmDiscard`〕/ GetDiagram / Providers / **TargetDbms** / AutoArrangeNewDiagram / ExecuteTool / **ReplaceDiagram**〔方言採用＋履歴なし置換＋自動整列＋fit＝DB 取込用〕/ **ReplaceQueries**〔差し替え＋AutoSave〕/ **ColumnRenamed** / **TargetDbmsChanged** イベント）として提供し、ランチャー・アダプタ・実行サービスは各機能プロジェクト側（DB=`DbImportCommandService`〔SchemaSignature 比較の置換確認込み〕/ `DbSyncCommandService`〔CanRun・ツールチップは TargetDbmsChanged 購読で再評価〕・AI=`ErDiagramHostChatAdapter` / `ErDiagramHostMockDiagramSource`・コード生成=`CSharpGenerationCommandService`〔CurrentProvider は GetDiagram().TargetDbms から解決〕/ `QueryDefinitionCommandService` / `QueryConditionRenameFollower`〔列リネーム時の DSL 条件追従。Initialize で購読・書き換え発生時のみ ReplaceQueries〕・ダイアログ提示は presenter シーム）。AI エンジン `QuickER.AI` は Extensibility を知らない（`IErDiagramToolHost` はプラグイン内アダプタで包む＝層の純度維持）。ツールバーのモジュールボタンは MainWindow の WrapPanel＋`MainViewModel.FeatureToolbarItems`／`FeatureToolbarItemGroups`（App が起動時に集約設定・BeginsGroup 境界のグループごとにコードビハインドが ItemsControl を生成して WrapPanel へ挿入＝ウィンドウ幅不足時はくくり単位で折返し・モジュール 0 個ならセパレータごと畳む）。切り離し後の `IAppDialogService` は印刷ダイアログ 1 メソッドのみ。同梱一覧は `FeatureModuleCatalog`（Gui）で、ビルドプロパティ **`IncludeDbToolModules=false`**（DB ツールなし構成＝Db.UI が外れる。スキーマ取込・差分・同期エンジンはプロバイダ層在住＝CLI の scaffold と共有のため残る）・**`IncludeAiModules=false`**（AI なし構成＝出力に QuickER.AI*.dll が一切出ない）・**`IncludeCodeGenModules=false`**（コード生成 UI なし構成＝CodeGen.UI が外れる。エンジン CodeGen.CSharp は型解決のためプロバイダ層が参照しており残る）を独立に指定できる（csproj の条件付き ProjectReference と `QUICKER_DBTOOL_MODULES` / `QUICKER_AI_MODULES` / `QUICKER_CODEGEN_MODULES` 定数が連動）。ツールバー文言・機能内メッセージの resx は各機能プロジェクト側（Db.UI / AI.Chat / AI.Mock / CodeGen.UI）。モジュールを抜いた構成では列リネームのクエリ条件追従も止まる（生成前の Guid 参照検証・DSL パースが壊れを警告するため許容）
- **外部 MCP サーバ（CLI `quicker mcp`・stdio）**: 外部 AI エージェント（Claude Code / Codex 等）向けに ER 図編集 10 ツール＋名前付きクエリ 3 ツール（set_query / list_queries / remove_query。set_query は (テーブル名, クエリ名) 照合の一発定義 upsert＝既存 Id 温存・全戻り形×全実装方式対応・定義時に DSL 構文〔QueryConditionParser〕/列名・@パラメータ実在/生 SQL〔RawSqlAnalyzer・未宣言＝拒否・未使用/複文＝警告のみ〕/構造を検証し失敗は保存しない・order_by は list/single/projection のみ・型トークンの内容検証は生成時任せ＝方言非依存維持）＋コード生成 3 ツール（generate_csharp / generate_ddl ＋情報系 get_generation_config_schema＝quicker.json の全キーを機械可読 JSON で返す自己記述カタログ〔`GenerationConfigSchema`〕。docs 不在の外部エージェントが config を自己発見的に書ける）を stdio MCP で公開する。初期化時に設計既定ルールの instructions（`ErDiagramToolCatalog.ServerInstructions`＝英語。命名はパスカルケース単数形・既存様式追従・単一 PK・FK 手順など。**誘導のみで強制なし**＝ルール外の図も扱える柔軟性を維持）を返し、add_entity のツール説明にも命名既定を記載する。**ステートレス設計**＝`file`（図 JSON パス）は `FileParameterInjector` がツール単位で機械注入し（図を対象にするツールのみ・引数不要の get_generation_config_schema は対象外）、毎呼び出しが「読込→変更→保存」で完結（`QuickER.Mcp.Tools` の `DocumentErDiagramToolHost`）。ツール定義の正本は `QuickER.Mcp.ErDiagramToolCatalog`（**英語**・内蔵チャットと共有＝AI.Chat の `ErDiagramToolDefinitions` は委譲＋LLM SDK 形式変換のみ・CJK 混入は英語ガードテストが検知）。意味論は GUI 側 `ErDiagramDynamicTools` のミラー（パリティテストが結果 ErDiagram の一致を検証）。安全策＝変更系はファイル不在（`create_diagram` へ案内）・非 DiagramDocument JSON・新フォーマット版（IsNewerFormat）を拒否し、`create_diagram` は既存ファイルを上書きしない。**レイアウトはサーバが書かない**＝新規作成はスキーマのみ文書（GUI で開くと全体自動整列）・既存ファイルへの追加分は GUI 読込時に `AutoLayoutService.LayoutAppend`（既存不動・空き領域へ追記配置。マージ取込の新規テーブルも同経路）が配置。コード生成ツールは合成ルート CLI 側（`CodeGenToolSet`＝`GenerationExecutor` の ParseResult 非依存コアを流用・`quicker generate` の挙動はバイト不変）。**stdout は JSON-RPC 専用チャネル**のため mcp 起動時に `Console.Out` を stderr へ退避・stdio E2E テスト（実プロセス起動）がプロトコル純度を最終検証する。GUI で開いている図は外部変更検知で追従する（下記）
- **document-based 化＋外部変更検知（GUI）**: 「現在のファイル」フルパス（`MainViewModel.CurrentFilePath`＝開く/名前を付けて保存でのみ遷移・インポートや図置換ではパス維持）＋Ctrl+S 上書き保存／Ctrl+Shift+S 名前を付けて保存・タイトル「ファイル名\*」の未保存マーク（終了確認なし）。**自動保存（AppData）は復旧専用のまま分離**＝現在ファイルへの書き込みは Ctrl+S のみ（自動保存を現在ファイルへ直結すると編集のたびにユーザーファイルを上書きし MCP 同時編集と衝突するため）。復元メタは gui-settings.json の CurrentDocument セクション（FilePath/LastKnownHash/IsDirty）＝起動時に「前回のファイル＋未保存変更」を復元。ダーティ判定は `UndoRedoManager` の変更世代カウンタ（読込/保存時点の世代と比較。Undo で保存時点へ戻っても安全側の「変更あり」）。外部変更は `DocumentFileWatcher`（WPF 非依存・400ms デバウンス・SHA-256 ハッシュ比較＋保存前後 Suspend/Resume の自己書き込み抑制二重化・書き込み途中の読込リトライ）で検知し、**クリーンなら無確認で自動再読込**（ビューポート維持＝fit-to-window 抑制・履歴クリア・ステータスバーの一時通知）・**ダーティなら確認ダイアログ**（「続行」選択後は同一ハッシュの再確認を抑止）。破損/新フォーマット文書は取り込まず現状維持＋通知。起動時の復元後も同規則で外部変更をチェックする
- **AI モック生成（モックフォルダ方式）**: ER 図から Web モック画面を起こす AI チャット機能（`QuickER.AI.Mock`）。**保存単位はフォルダ**＝`mock.json` マニフェスト（画面一覧・遷移・改訂履歴・スキーマスナップショット）＋画面ごとの `*.html`＋共有 `style.css` のフラット構成（`MockFolderStore`／`MockManifest`）。会話開始時にフォルダを指定し（空フォルダ＝新規 `CreateNew`／既存モックフォルダ＝再開）、以降は 1 ターンごとにライブ保存する。**再開は状態再開**＝会話履歴は保存せず、フォルダの現状（画面一覧・遷移・改訂・共有 CSS・最新スキーマ）をプロンプトへ注入して続きから作る（バックエンド非依存＝API キー/Codex/Claude Code で同じ再開ができる）。ツールは英語正本 4 つ（`save_screen`〔完全 HTML の upsert・遷移宣言・改訂メモ〕/ `remove_screen` / `save_stylesheet` / `get_screen`。定義は `MockFolderDesignTools`＝`ErDiagramToolCatalog` と同流儀）で、**検証は警告のみ**（`MockContentValidator`＝外部参照/リンク切れ/style.css 未参照は警告・拒否は空/非 HTML〔`<html>` 不含〕のみ）。プレビュー（`MockPreviewPanel`＝WebView2）は**フォルダ内 `*.html` のみ許可**（許可フォルダ付きプレビュー要求）で画面間リンク遷移に追従し、サイドバー選択と相互同期する。単一 HTML 出力は `MockBundleExporter`（決定的なハッシュルーター結合＝各画面 body を `<section data-screen>` へ連結・`href="Foo.html"`→`#Foo`・共有 CSS インライン化）。設計書出力は `MockDesignDocExporter`（決定的・AI 不使用・モックフォルダ直下 `README.md`＝画面一覧＋Mermaid 遷移図＋**画面×エンティティ CRUD 表**＋regex による画面項目抽出。見出しは UI 言語追従〔resx `MockDoc_*`〕・改行は LF 固定）＝初回は「設計書出力」ボタンのオプトインで書き出し、以降は README.md が在る限り画面 save/remove に無音で自動追従して再生成する（save_stylesheet・スキーマ更新では再生成しない＝内容に影響しないため）。CRUD 表は save_screen の `entities` 申告〔省略=既存維持・空配列=消去・正規化 C→R→U→D・図に無い名前は警告のみ〕を画面×エンティティ表として遷移図の直後に載せる（行＝全画面〔マニフェスト順・未宣言画面は空セル行〕・列＝宣言エンティティ名の初出順・セルは正規化済み操作文字列。宣言ゼロならセクション省略）。**第 2 ステップ（モックプロジェクト生成・ターゲット 2 種〔WPF/Blazor〕・3 バックエンド対応・メンテナンスレベル）**＝**プロンプト・README-QuickER.md・ER スキーマ本文（MockSchemaSerializer）は英語固定**（「対話チャット＝UI 言語追従・ヘッドレス実行と機械向け診断＝英語固定」の線引き。警告・検証エラーも英語固定・成功文は UI 追従。CJK 混入は英語ガードテスト群が検知）＝決定的スキャフォールド（`MockProjectScaffoldService`＝データ層コード＋csproj＋README＋モックフォルダを `design/mock/` へ同梱）→ 実行器抽象 `IMockProjectAgent`（選択中バックエンドに連動・追加指示欄を任意でプロンプト末尾へ連結）で UI 層を生成 → 共有オーケストレータ `MockProjectAgentRunner` が全体タイムアウト・成果物検証・最終 `dotnet build` を独立検証（実行器の自己申告を信じない）。実行器 3 実装＝**Claude Code**（ヘッドレス・自己修正）/ **Codex**（既存 app-server 統合を Cwd=出力先・approval never・workspace-write で駆動・自己修正・dynamicTools 不使用。エージェント型 2 実装はプロンプト正本 `MockProjectPromptBuilder` を共有）/ **API キー**（`emit_file` 固定パイプライン＝共通部→各画面の決定的一発生成・読取/ビルドツールは与えない・パス保護〔Generated/design/README/.sln/.csproj と `..`/絶対パス拒否〕・中間ビルド失敗時のみ修正 1 回・UI に「自己修正なし」注記）。ターゲットは `MockProjectTarget` 2 種（blazor 既定＝Blazor Web App・グローバル InteractiveServer・モック HTML/style.css を可能な限り移植／wpf）で、差分は `MockProjectTargetProfile`（csproj/README・プロンプトフラグメント・UI 成果物パターン *.xaml/*.razor）へ集約＝プロンプトは `MockProjectPromptBuilder` 単一正本＋フラグメント合成・UI は第 2 ステップの ComboBox で選択
- **生成コードの汎用性**: 生成される C# は CommunityToolkit 等の UI フレームワークに依存させない（WPF 以外でも使える設計）。Repository 生成は単一キー・アプリ側採番前提（複合キー・DB自動採番は対象外）
- **store-generated 列の書き込み除外（rowversion / timestamp）**: DB が値を生成する列は生成 Entity にマーカー属性 `[StoreGeneratedColumn]` を刻み（判定は型マッパーの `CSharpTypeInfo.IsRowVersion`＝`rowversion`/`timestamp`・付与はオプション非依存で無条件）、`EntitySaveMetadata` が QuickER 版 Repository の INSERT / BulkInsert / UPDATE の対象から除外する（`InsertProperties`＝全列−store-generated・`NonKeyProperties` からも除外）。DB が採番するため明示挿入は実行時エラー（`Cannot insert an explicit value into a timestamp column.`）になるのを防ぐ。**SELECT では取得する**（`SelectProperties`/`AllProperties` は不変＝並行性トークンは読める）。EF Core は Fluent の `IsRowVersion()` が既に store-generated として扱うため非適用・InMemory は SQL 非使用で無関係。楽観排他（UPDATE の WHERE に rowversion を使う）はスコープ外。属性定義は `[UnboundedBinaryColumn]` と同型の emit ゲートで常時出力（固定 infra が参照するため）
- **Save フック（ISaveHook）**: グラフ保存（`SaveAsync` の単一/複数の 2 形態のみ・直接 Insert/Update/Delete と BulkInsert は素通り）の各操作の前後に処理を差し込む機構。**オプションなし・常時生成**（未登録なら完全 no-op＝既存挙動不変）。契約は Core（`ISaveHook<TEntity>`＝両メソッド既定実装付き・`SaveOperation`・`ISaveHookContext`・`ISaveHookRegistry`/`ISaveHookInvoker`）。DI 登録は `ISaveHook<T>`（0..N）＋各 `AddGenerated*` が `TryAddScoped<ISaveHookRegistry>`（keyed 版はファクトリで `GetService` 明示）・Repository 基底/具象に省略可能な第 2 引数 `ISaveHookRegistry? saveHooks = null`（手動 new は不変）。**Before は `EntityGraphSaver` の RowState 分岐で発火**（ExecuteAsync 内でないため update→insert 切替の二重発火を回避）・登録順で最初の `false` で短絡＝その 1 件を単独スキップ（`HashSet<EntityBase>(ReferenceEquality)` の skip 集合を `AcceptChanges` へ渡し RowState 据え置き）。整合はフック実装者責任＝削除は子から順のため「root だけ false」は子が消え root が残る・矛盾スキップは FK 制約→例外→全ロールバック。**After はコミット前＋同一トランザクション参加の `ISaveHookContext`**（`WriteBinaryColumnAsync`＋ファイル糖衣・`ExecuteSqlAsync`＝Save と blob/監査のアトミック性・context の型は After 対象エンティティ型に束縛・除外列なし図の WriteBinary は NotSupported）・`insertWhenUpdateMissing` は Before=Update / After=実操作 Insert。実装先差分＝ADO は各 DML 直後に After（`SqlSaveHookContext`＝進行中 (connection, transaction) 共有）・EF Core は SaveChanges 後に一括（`EfCoreSaveHookContext`＝バイナリ NotSupported・ExecuteSql は対応）・InMemory は擬似（バイナリはストア・ExecuteSql NotSupported・**After 例外時はストア変更が残る**＝実トランザクションなし）・リモートは**サーバー側 DI で発火**（クライアント側 RowState はスキップを反映せず Unchanged＝既知の制限）。EF Core/InMemory/マルチターゲット/UseRuntimePackages と直交。実 DB 検証は SaveHook{Ado,EfCore,InMemory,SqlServer,Remote}RuntimeTests（パリティは `SaveHookRuntimeTestsBase`・CI 常時）
- **EF Core モード（GenerateEfCore）**: 既存 Entity をそのまま EF Core に乗せる方言非依存の QuickErDbContext＋EF Core 版 Repository を生成し、DI 登録（AddGenerated{方言}Repositories ⇔ AddGeneratedEfCoreRepositories）の差し替えだけでQuickER 版 Repository と交換できる。GenerateEfCore=false のとき生成物に EF Core への依存は一切出ない（using 含む）。スキーマ作成は従来どおり DDL 生成の責務で、EF Core は既存スキーマへの接続専用（Migrations / HasColumnType は範囲外）
- **DB 定義メタ属性（`[DbColumnMeta]` / `[DbTableMeta]`）**: 生成 Entity を「DB 定義の自己記述ドキュメント」にする定義用メタ（将来の C#→ErDiagram リバースの布石）。列に方言中立の型トークン（`CanonicalTypeToken`。例 `string(50)` / `decimal(10,2)` / `int32`・小文字・-1=max）と説明、テーブルに説明を刻む。付与は `IncludeDataAnnotations` ON かつ Entity 生成時のみで**対象 DB・Repository/EF Core 設定に依らない**。トークンは図の方言 `ITypeCatalog.TryParse` → `CanonicalTypeToken.Format` を `DiagramCodeGenerator` の後処理（`CanonicalTypeTokenAttacher`）で主辞書へ付加する（マッパ実装は無変更・解析不能な自由記述型はトークン null＝属性省略）。実行時型付けの `[SqlColumnType]`（SQL Server 専用・Size ガード）とは**責務分離**で無関係。canonical 由来のため可搬図では方言に依らず同一トークン（PortableFixtureDialectIndependenceTests が保証。**可搬フィクスチャの文字列は Unicode で統一すること**＝Ansi/Unicode 差でトークンが割れるため。lessons.md 参照）
- **ランタイム配布の折衷（インライン既定＋NuGet パッケージ参照モード）**: 生成コードのスキーマ非依存な固定コード（ランタイム）は既定で従来どおりインライン出力（自己完結・依存最小）。`UseRuntimePackages`（CLI `--use-runtime-packages`／GUI チェックボックス）で固定コードを出力せず、NuGet パッケージ `QuickER.Runtime`（共通基盤＋方言中立契約・依存ゼロ）／`QuickER.Runtime.SqlServer`／`QuickER.Runtime.Sqlite`（各方言エンジン・ADO 依存は方言別に排他）／`QuickER.Runtime.EntityFrameworkCore`（EF Core 共通部品・EF Core Relational 依存・ADO/DI なし）への参照で賄うモードに切り替わる（**4 パッケージ**）。パッケージ用ソース（`src/QuickER.Runtime*/QuickERRuntime*.g.cs`・4 本）の正本は Scriban テンプレートで、`RuntimePackageSourceRenderer` が「空図＋全機能 ON＋固定名前空間＋internal→public」でレンダリングしたものをチェックイン（**手編集禁止**）。DI 登録拡張（AddGenerated*Repositories）はエンティティ別登録を含むスキーマ依存物なので**常に生成側**（パッケージには入れない）。**EF Core 生成（GenerateEfCore）とパッケージ参照モードは併用可**＝EF Core 固定 infra を `TContext : DbContext` ジェネリック化したことで具象 QuickErDbContext 依存が生成側（DI 登録・エンティティ別実装）だけに残り、EF Core エンジンは EF Core パッケージへ切り出せる。パッケージモードでも QuickErDbContext・Fluent 構成・EfCore{Entity}Repository・AddGeneratedEfCoreRepositories は**常に生成側**に出力する。EF Core パッケージの EntitySaveMetadata は EF Core が使うメンバー（KeyProperty/AllProperties/PropertyByColumn/ColumnList/CascadeNavigations）だけの縮小版で、方言クォート焼き込みの SQL 文メンバー（TableName/*Sql）はQuickER 版 Repository コンテキスト（`repositories`）限定にガードして持たない（**マルチターゲット×EF Core の排他は別理由＝契約の型同一性で残る**）。PackageReference の届け方は「案内のみ」（生成ヘッダ＋ダイアログ/CLI 出力。csproj には触らない）で、バージョンはロックステップ（Directory.Build.props の `VersionPrefix`＝パッケージ版＝ツール版。案内の版解決は `RuntimePackages.ResolveGuidanceVersion`）。公開は `.github/workflows/publish.yml`（workflow_dispatch・dry_run 既定 true・認証は Trusted Publishing＝GitHub OIDC の一時キーで長期 API キー不使用）
- **名前付きクエリ（ErDiagram.Queries）**: 図に保存するクエリ定義（`QueryDefinition`・エンティティ/列は Guid 参照）から Repository メソッドを生成する。条件は簡易 DSL（UI 表記も「簡易 DSL」。比較・AND/OR/NOT・括弧・IS [NOT] NULL・[NOT] LIKE・[NOT] IN・CONTAINS/STARTSWITH/ENDSWITH。パーサ＝`QueryConditionParser`・診断はローカライズ済み）で、**単一の C# ラムダ式へエミット**（`QueryConditionCSharpEmitter`）して既存 `Query()` パイプライン（式木→`SqlExpressionTranslator`／EF Core Where 直適用）に流す＝方言 SQL 翻訳は実行時・エミッタに方言分岐なし。VO 列は `Vo.Create(値)` 比較・VO×IN はメソッド冒頭でリスト持ち上げ。戻り形は 一覧/単一/件数/射影（DTO 生成・寛容マッパー互換）＋スカラー（生 SQL／手動実装専用）。実装方式の UI 表記は「簡易 DSL」「生 SQL」「手動実装 (partial クラス)」（enum は Dsl/Sql/Manual のまま）。実装の出し分けは統一規則「**SQL（または DSL 共有本体）が与えられていない実装先は手動実装（manual）**＝契約宣言のみ生成・実装はユーザーが partial クラスへ通常メソッドで書く（漏れはコンパイルエラー）」——EF Core×生 SQL・SQL 辞書にない方言・インメモリがすべて同じ規則に落ちる。型トークン解決は列型と同じくプロバイダ層（`QueryParameterTypeResolver`・列と同一経路で解決）。LIKE の意味論はランタイムのエスケープ設計に合わせ「リテラルは % 位置で分解・`LIKE @p` は部分一致（値リテラル扱い）」で、生 LIKE パターンは生 SQL の担当。生 SQL バインダはコレクション値を `@名0,@名1,...` へ IN 展開（空リストは `(NULL)`＝不一致）。生 SQL は生成前に静的検証（レベル 1・方言非依存＝`RawSqlAnalyzer`。文字列/コメント/`[..]`/`".."` 内・`@@` システム変数はパラメータ扱いしない 1 パス走査）を通し、**未宣言パラメータ＝警告＋該当クエリのみ生成スキップ**（実行時必失のため。Guid 参照検証と同方針）・**未使用パラメータ／複文（; の後にコード）＝警告のみで生成継続**（GUI/CLI 双方に効く・ダイアログは SQL 入力欄近くにインライン表示＋未宣言は OK ブロック）。**EF Core Sqlite は decimal のサーバーサイド比較・並び替え非対応**のため、SQLite×EF Core で使う図の DSL 条件・並び順は整数キー等に限る（実 DB パリティは NamedQueryAdo/EfCoreRuntimeTests＝CI 常時実行）
- **リモート契約生成（GenerateRemoteContracts）**: リモート操作用インターフェイス `I{Entity}RemoteRepository` を**追加生成**する bool オプション（既定 false。CLI `--generate-remote-contracts`／GUI「リモート対応」チェック＝DB アクセス配下・「なし」時は非表示。3 階層構想 Phase 2）。ランタイム共通部は**オプションに依らず常時**、基底分割 `IRemoteRepository<TEntity, TKey>`（CRUD＋Save×2＝ネットワーク境界を越えられる操作）← `IRepository`（＋Query()・生 SQL 3 種・BulkInsert）を非破壊で出力する（既存利用コードは無変更でコンパイル可）。ON のとき `I{Entity}RemoteRepository`（IRemoteRepository 継承＋**名前付きクエリ全部**＝Dsl/Sql/Manual ともシグネチャはリモート可能）が追加され、既存の `I{Entity}Repository` はそれと IRepository を継承する**全機能面のまま**（純粋に追加的＝ON にしても既存コードは一切壊れない）。**実装クラス・DI 実装登録は従来どおり全機能面基準**（間仕切りは純粋にインターフェイス水準・エンジンに分岐なし）で、リモート面は同一インスタンスへの転送として追加登録（keyed 版も同様）。EF Core・InMemory・マルチターゲット・UseRuntimePackages と直交。リモート面だけに依存したコードは、実体を Web サービス経由の実装（下記リモートサービス生成）へ差し替えてもコンパイル時に安全。実 DB 検証は RemoteContractRuntimeTests（CI 常時実行）
- **リモートサービス生成（GenerateRemoteServices）**: リモート面を **HTTP + JSON** で提供するクライアント／サーバー実装を生成する bool オプション（既定 false。CLI `--generate-remote-services`／GUI「リモート対応」行の 2 つ目チェック。ON はリモート面を自動含意。3 階層構想 Phase 3）。転送方式は gRPC でなく HTTP+JSON＝**実証済みの JSON 資産を転用**（EntityBase.Clone と同じ意味論の `RemoteJson.Options`＝VO コンバータ・RowState 込み・IgnoreCycles。**IgnoreReadOnlyProperties は指定しない**＝クエリ転送エンベロープの匿名型が get-only で空 JSON になるため）。(1) クライアント＝`Http{Entity}RemoteRepository`（BCL の HttpClient のみ・`AddGeneratedHttpRemoteRepositories(baseAddress | HttpClient ファクトリ)`・名前付きクエリは**実装方式に依らず全部**転送メソッド化）を本体生成物へ同梱し、(2) サーバー＝`MapGeneratedRemoteEndpoints(prefix="/quicker")`（Minimal API・`POST {prefix}/{エンティティ}/{操作}`・RouteGroupBuilder を返し認可付与可）を**別ファイル** `{ベース名}.RemoteServer.g.cs` へ出力（ASP.NET Core の FrameworkReference 必須のため本体へ連結しない。バケット RemoteServer）。クライアント側固定部（RemoteJson・RemoteRepositoryException・エンベロープ・HttpRemoteRepository 基底）は共有 infra＝**Core パッケージへも収載**（BCL のみ＝依存ゼロ維持）で、per-entity クライアント・DI 登録はスキーマ依存物として常に生成側。**例外は型を復元**: SaveConflictException⇔HTTP 409＋構造化 JSON（RemoteError）・その他⇔500＋RemoteRepositoryException＝直結⇔リモートで catch が変わらない。グラフ保存成功後はクライアント側でも RowState を確定（[NavigationReference(Cascade)] 走査＝EntityGraphSaver.AcceptChanges と同じ意味論）。**BulkInsert・Query()・生 SQL はリモート面に無いため転送対象外**（設計どおり）。実 HTTP 検証は RemoteServiceAdo/EfCoreRuntimeTests（Kestrel を 127.0.0.1 空きポートで in-process 起動・CI 常時実行）
- **無制限バイナリ列の除外（ExcludeUnboundedBinaryColumns）**: `varbinary(max)` / `image` / 長さ宣言なし BLOB / `bytea` 等の**無制限バイナリ列**を、QuickER 版 Repository の SELECT / UPDATE 対象から外す bool オプション（既定 false。CLI `--exclude-unbounded-binary-columns`／GUI チェック）。判定は**宣言ベース**でプロバイダの型マッパー（`CSharpTypeInfo.IsUnboundedBinary`＝`varbinary(max)`/`image`/長さなし BLOB は真・`rowversion`/`timestamp`/`binary(n)`/`varbinary(n)` は偽）が行い、生成 Entity のプロパティへマーカー属性 `[UnboundedBinaryColumn]` を刻む（除外列一覧は Info 診断で通知）。ランタイム（`EntitySaveMetadata`）は属性をリフレクションで読み列集合を分割する＝**INSERT / BulkInsert は全列**（初回書き込みで blob を渡せる）・**SELECT は縮小列**（除外列は未取得状態＝nullable は null・非 nullable は空配列）・**UPDATE は縮小列**（除外列に触れず既存 blob を温存）。除外列に「null でも空でもない」値を残したまま UPDATE / SaveAsync（グラフカスケード）/ InMemory 更新 / リモートクライアント送信を試みると `InvalidOperationException`（サイレントなデータ取りこぼしを防ぐ・除外列の更新は生 SQL `ExecuteSqlAsync` へ誘導）。生 SQL のエンティティ取得は縮小列が必須だが、SELECT に除外列を含めれば opportunistic にマップされる。名前付きクエリの DSL 射影が除外列を参照していればサーバー側刈り込み SELECT に含まれ値が取れる（WHERE 参照も可）。**EF Core（DbSet 経由）は非適用**＝EF Core の列選択は EF Core の責務のため全列読み書き。InMemory は完全パリティ（読み取り複製の除外列 strip・更新ガード・射影は完全クローンから）。除外が効く図でも呼び出しごとに blob 込みで取得できる**読み取りオプトイン `SqlQuery<T>.WithUnboundedBinary()`**（`SqlQueryPlan` の bool フラグ・API は常時生成で除外列なしは no-op）を Query() に持ち、ON かつ Include なしのとき SQL Server は FOR JSON を使わず全列プレーン SELECT（Base64 膨張回避）・SQLite も全列 SELECT・InMemory は strip なしの完全クローンを返し（RowState=Unchanged の正当なエンティティ）、EF Core は元々全列のため no-op。Include との併用は終端で `InvalidOperationException`（両順序を BuildPlan で検出）。効果は ToListAsync / FirstOrDefaultAsync のみ（取得後の除外列更新は既存 UPDATE ガードで throw＝生 SQL へ誘導）。VO・マルチターゲット・EF Core・InMemory・リモートと直交。除外列ごとに**ストリーミング Stream アクセサ `Read/Write{Column}Async`**（＋ファイル糖衣 `Read/Write{Column}ToFile/FromFileAsync`）も追加生成する（`GenerateRepositories && ExcludeUnboundedBinaryColumns` かつ除外列があるときのみ）＝O(チャンク)・blob 全量をメモリに載せない唯一の手段。**契約の挿入先はリモート契約の有無で変わる**＝OFF なら全機能面 `I{Entity}Repository`・ON（GenerateRemoteContracts/Services）なら**リモート面 `I{Entity}RemoteRepository` へ移設**（全機能面はリモート面を継承＝純粋に追加的・利用コード無変更。ファイル糖衣の `this` 対象も追従。ランタイム共通の基底 `IRemoteRepository` には載せない＝エンティティ固有のため per-entity 面のみ）。契約・薄い委譲実装（QuickER 2 方言＋InMemory は固定 infra のエンジンへ委譲・列名は `EntitySaveMetadata` 経由で焼き込まない）・EF Core 版 Repository は `NotSupportedException`・ファイル糖衣は拡張メソッド静的クラス（Stream 版へ 1 行委譲）を `CSharpGenerationModelBuilder.BinaryStream.cs` が整形済みブロックとして組み立てる。エンジンは SQL Server=`SequentialAccess`＋`GetStream`／Stream 値の `SqlParameter`（`VarBinary`,-1）・SQLite=`SqliteBlob`＋`zeroblob`（rowid 前提・QuickER の SQLite DDL は WITHOUT ROWID 非生成）。`CanSeek` でない Stream は `length` 必須（方言中立に統一・欠落は `ArgumentException`）。`source=null` で SET NULL・行なし/NULL は false。INSERT 専用メソッドはなく「INSERT→Write の 2 段」。楽観排他はスコープ外。**リモートサービス生成（GenerateRemoteServices）時は HTTP でストリーミング転送**する第 2 形式の専用エンドポイントを追加＝JSON エンベロープ（POST+Base64）と意図的に別形式で `GET/PUT/DELETE {prefix}/{エンティティ}/{列名}?id=`（キーは URL クエリ・本文は生 blob・`application/octet-stream`）。GET は応答開始を最初の書き込みまで遅延するラッパーストリーム（`DeferredOctetStreamBody`）で「false→404・true(空含む)→200」を本文ストリーミングと両立（先に本文を書くと 404 に切り替えられない問題の解法）。PUT は成功 204・行なし 404・`Content-Length` 欠落(chunked)は 411、**このエンドポイントのみ `IRequestSizeLimitMetadata` メタデータでリクエストサイズ制限を解除**（既定 30MB 超を追加設定なしで許可＝DoS 面のため認可併用推奨・RouteGroupBuilder で上書き可）。DELETE は `Write(id,null)`＝NULL 化（0 バイト PUT＝空ボディと構造的に区別）。クライアント（`Http{Entity}RemoteRepository`）は `ResponseHeadersRead`＋`CopyToAsync`／`StreamContent`（Content-Length 付き）＋非シークは送信前 `ArgumentException` 検証、キー直列化は `RemoteJson.Options`（VO 内包値）を URL エンコードしサーバーが同設定で復元。クライアント固定部（`FormatKeyForQuery`・Download/Upload ヘルパー）は `HttpRemoteRepository` 基底＝Core パッケージ収載。`WithUnboundedBinary()`/`Query()`/生 SQL のリモート化はスコープ外。実 DB 全経路検証は BinaryColumnAdo/EfCore/Remote/InMemory 各テスト（WithUnboundedBinary の取得・Include 併用例外・取得後 Update ガード・Stream の往復/SET NULL/行なし/非シーク length 必須/ファイル糖衣往復・**リモートは HTTP 越しの往復/404 false/DELETE→NULL/空ボディ 0 バイト/送信前 length 例外/31MB でのサイズ制限解除**を含む・CI 常時実行）
- **DB アクセスの排他と単独出力**: Repository バケットは「共通契約」（GenerateRepositories || GenerateEfCore で出力）と「QuickER 版 Repository 実装」（GenerateRepositories 時のみ）に分割されており、**GenerateEfCore 単独の生成物には QuickER 版 Repository 実装の ADO 依存（Microsoft.Data.SqlClient / Microsoft.Data.Sqlite）が一切出ない**（ガードテストあり）。QuickER 版 Repository は `RepositoryDialects` で方言を選ぶ多DB対応（未指定は既定 `sqlserver` の単一へフォールバック。`sqlserver`＝FOR JSON／`sqlite`＝プレーン SELECT＋IncludeLoader マルチクエリ・LIMIT/OFFSET・strftime。旧単数キー `RepositoryDialect` は廃止済み）で、単一指定では片方言のみを出力し方言間の ADO 依存も排他（ガードテスト双方向）。`RepositoryDialects` に複数方言を指定するとマルチターゲット出力になり、中立契約（IRepository / I{Entity}Repository / ISqlExecutor など）を `RepositoryNamespace` に 1 回・方言別実装を `.SqlServer` / `.Sqlite` の方言別 namespace に出力する（分割時のファイルは `Repositories.g.cs`＋`Repositories.SqlServer.g.cs` 等）。**EF Core 実装（GenerateEfCore）も分割時は同じ流儀で `Repositories.EfCore.g.cs`・`{RepositoryNamespace}.EfCore`（契約 namespace のサブ名前空間へ導出専用＝個別の名前空間オプションは持たない）に出力する**。DI は方言別拡張＋keyed 版（AddGeneratedSqlServerRepositories / AddGeneratedSqliteRepositories・`object? serviceKey` 付き）で、単一契約型を keyed 解決して同一プロセスで複数 DB へ書き分けられる（実 DB 結合は MultiTargetRepositoryRuntimeTests＝Docker 依存・keyed 解決の型検証は MultiTargetRepositorySqliteKeyedResolutionTests＝CI 実行）。マルチターゲット（実効方言 2 つ以上）と GenerateEfCore は排他で、併用指定は診断エラーになる。GUI の生成ダイアログは DB アクセスをラジオ3択（なし既定／QuickER 版 Repository＝常時選択可・対象 DB チェック群 [SQL Server / SQLite] で選択（既定=図の方言、未対応方言の図では空・最低1つ必須）／EF Core）で排他選択させる。両方 ON はパリティ検証用に CLI／オプション直指定でのみ可能
- **GUI の配布（Velopack・2 チャンネル）**: release.yml（手動トリガ）が win-full（self-contained）/ win-lite（フレームワーク依存）の 2 チャンネルで Setup.exe＋Portable zip を生成する。エントリポイントは `Program.Main`（`VelopackApp.Build().Run()` を WPF 初期化より前に実行。csproj の `StartupObject`＋`EnableDefaultApplicationDefinition=false` とセットで崩さない）。起動時自動更新のフィードは `UpdateFeed.GitHubRepositoryUrl`（**リポジトリ公開時に URL を設定する。現状空文字＝チェック無効**）で、環境変数 `QUICKER_UPDATE_FEED` にローカルフィードのパスを入れると E2E 検証できる（Velopack 依存は `IAppUpdater` seam の裏に隔離・UpdateService は単体テスト済み）

## 不変条件（ビルド・型検査では検出できず、壊すと静かに回帰する）

- 生成テンプレートは保守性のため `Templates/CSharpRuntime/*.scriban` の部品ファイル群へ物理分割されており（`_00_HeaderAttributes` 〜 `_09_RemoteServer` の 10 本）、`ScribanCSharpRenderer.LoadTemplate` が `TemplatePartResourceNames` の固定順で単純連結して 1 本のテンプレート本文へ復元する（Scriban の include は使わず連結結果を 1 回だけ解析）。各部品は元の連続行範囲そのままで、**並べ替え・境界での行編集をしない**（連結結果が分割前とバイト完全一致であることが不変条件＝生成コードのバイト一致の前提）。部品は全て **CRLF 固定**（.gitattributes の `*.scriban text eol=crlf` で強制）。部品を編集する場合は連結順（番号順）と CRLF を崩さないこと
- **テンプレート変更時は** `tests/QuickER.Tests/GeneratedFixture/` の固定フィクスチャ **10 個**（GeneratedFixture.g.cs＝SQL Server 全カバレッジ・PortableFixture.g.cs＝方言可搬な EF Core 単独出力・SqlitePortableFixture.g.cs＝SQLite 方言のQuickER 版 Repository＋EF Core 併存・MultiTargetPortableFixture.g.cs＝sqlserver/sqlite マルチターゲットQuickER 版 Repository・EF Core なし・keyed DI・InMemoryFixture.g.cs＝インメモリ Repository・QueryFixture.g.cs＝名前付きクエリ入りの SQLite 方言＋EF Core 併存・RemoteContractFixture.g.cs＝QueryFixture と同一図で GenerateRemoteContracts=true＝リモート面追加の比較対照・RemoteServiceFixture＝同図で GenerateRemoteServices=true。**本体＋RemoteServiceFixture.RemoteServer.g.cs の 2 ファイル構成**＝サーバーファイルはテストプロジェクトで実コンパイルされ ASP.NET Core 参照の検証を兼ねる・BinaryFixture＝ExcludeUnboundedBinaryColumns=true の SQLite 方言＋EF Core＋InMemory＋リモート併存＝無制限バイナリ除外の全経路検証。**本体＋BinaryFixture.RemoteServer.g.cs の 2 ファイル構成**・SqlServerBinaryFixture.g.cs＝BinaryFixture と同一図の SQL Server 方言版（QuickER 版 Repository 単独＝最小構成）＝SQL Server ストリーミングエンジン・除外の FOR JSON 縮小 SELECT・WithUnboundedBinary のプレーン SELECT 経路の実 DB 検証用（SqlServerBinaryColumnRuntimeTests・Testcontainers・Docker 依存）。図は BinaryFixtureDefinition.Build() へ委譲）の再生成が必要。各ドリフトテスト（GeneratedFixtureDriftTests / PortableFixtureDriftTests / SqlitePortableFixtureDriftTests / MultiTargetPortableFixtureDriftTests / InMemoryFixtureDriftTests / QueryFixtureDriftTests / RemoteContractFixtureDriftTests / RemoteServiceFixtureDriftTests / BinaryFixtureDriftTests / SqlServerBinaryFixtureDriftTests＝出力ファイル構成そのものも検知対象）が「再生成→差分ゼロ」を検証し、失敗メッセージに再生成手順がある。再生成は次の 1 コマンドでまとめて処理する（環境変数 `QUICKER_REGEN_FIXTURES` を立てるとドリフト検知と同一経路で上書き。実処理は FixtureDriftHarness に集約）：

  ```powershell
  $env:QUICKER_REGEN_FIXTURES=1; dotnet test tests/QuickER.Tests/QuickER.Tests.csproj --filter "FullyQualifiedName~Drift"; $env:QUICKER_REGEN_FIXTURES=$null
  ```

  再生成後は環境変数なしで同じテストを流し、緑（ドリフトなし）を確認する（MultiTargetPortableFixture はマルチ辞書オーバーロードで生成するため、FixtureDriftHarness の該当オーバーロード経由で再生成される）。同じ 1 コマンドで**ランタイムパッケージ用ソース 4 本**（`src/QuickER.Runtime*/QuickERRuntime*.g.cs`＝Core/SqlServer/Sqlite/EntityFrameworkCore・RuntimePackageSourceDriftTests が検証・.gitattributes で eol=crlf 固定）と、**動くサンプルのチェックイン生成物 6 本（2 サンプル）**（ec-order＝`EcOrder.g.cs`（SQLite 方言・QuickER 版 Repository・VO なし）・`EcOrder.g.md`（--generate-api-docs 同梱出力・英語正本）・`EcOrder.sql`／ec-order-remote＝`EcOrderRemote.g.cs`＋`EcOrderRemote.RemoteServer.g.cs`（GenerateRemoteServices の 2 ファイル構成）・`EcOrderRemote.sql`。EcOrderSampleDriftTests / EcOrderRemoteSampleDriftTests が検証。生成コードは**実 CLI と同一経路**で照合＝実 `quicker generate` が生成した物と同一であることを保証。DDL は非決定的な `-- 生成日時:` 行のみ正規化して比較・.gitattributes で eol=crlf 固定）も再生成される
- ランタイムパッケージ 4 csproj の依存集合は**完全一致で固定**（RuntimePackageProjectDependencyGuardTests）: Core=PackageReference ゼロ／SqlServer=Microsoft.Data.SqlClient のみ／Sqlite=Microsoft.Data.Sqlite＋SQLitePCLRaw ピンのみ／EntityFrameworkCore=Microsoft.EntityFrameworkCore.Relational のみ。DI 系・ADO・他方言を足すとガードが落ちる（依存を足す前に「それは本当にパッケージの責務か（スキーマ依存物ではないか）」を疑う）
- DDL 出力の先頭コメントは `-- DDL auto-generated by QuickER`（DdlGeneratorBase.cs／SqliteDdlGenerator.cs で同一文言）。生成 SQL に乗る固定文（DDL ヘッダ・生成日時行・多対多コメント・Oracle の ON UPDATE 注意コメント・同期スクリプトのセクション見出し／スキップコメント）は英語が正本で、`DdlOutputEnglishGuardTests`（ASCII のみの図・差分から生成した DDL／同期スクリプトに CJK 混入がないことを検証）が回帰を検知する
- `PasswordBoxBehavior`（QuickER.Gui/Views）の DP 既定値は **null 必須**。string.Empty にすると空文字バインド時に PasswordChanged が購読されず入力が VM に届かない（PasswordBoxBehaviorTests が守る）。バインド先 VM プロパティは空文字で初期化する
- 生成ランタイムの SqlParameter は `[SqlColumnType]` 属性由来の明示型。文字列 Size の「宣言長超過なら値長」ガードを固定 Size にするとサイレントなデータ破損を招く

これらのファイルに触るときは、対応するテスト（DdlGeneratorTests / PasswordBoxBehaviorTests / CSharpCodeGenerationServiceTests / GeneratedFixtureDriftTests）を必ず実行する。

## コーディング規約

- 既存コードと同様、ビヘイビアや主要メソッドに分かりやすい日本語コメントを付ける（**samples/ 配下の手書きコードは例外で英語コメント**）
- `if` / `for` / `switch` / `try` 等のブロック前後には空行を1行入れる（最終判断は CSharpier の挙動を優先）
- 文字列リテラルの定数化は一貫性重視：一部の表示文言だけを定数化せず、やるなら同種をまとめて整理する
- ビルド警告は可能な限り解消する

## ワークフロー

- `git add` / `git commit` は**ユーザーの明示的な指示があるまで実行しない**（変更はワーキングツリーに残してレビューを待つ）。ブランチ作成はタスク開始時に行ってよい
- 計画・教訓は `tasks/todo.md` / `tasks/lessons.md` に記録する運用。**tasks/ はリポジトリに含めない**（メンテナのローカルで非公開管理・.gitignore 済み）。tasks/ が存在する環境では、セッション開始時に lessons.md を確認する
- レイアウト等ビジュアルに直結する機能は、機能テストに加えて見た目の品質指標（占有面積・充填率・交差数・線長）を実測してから完了とする（lessons.md 参照）
