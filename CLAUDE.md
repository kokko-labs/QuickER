# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

QuickER は WPF 製の ER 図デザイナ。DB スキーマのインポート／DDL 生成（SQL Server / PostgreSQL / MySQL / Oracle / SQLite の5方言）、Scriban テンプレートによる C# コード生成、AI チャットによる図の操作（MCP サーバ内蔵）、外部 AI エージェント向け stdio MCP サーバ（CLI `quicker mcp`）を持つ。コメント・コミットメッセージは日本語。ただし**生成物（.g.cs の XmlDoc・コメント・実行時例外メッセージ、.g.md）に埋め込む固定文は英語が正本**（製品の中立言語＝resx の中立と同じ英語。テンプレート `Templates/CSharpRuntime/*.scriban` 内の全コメントは生成出力そのものなので英語で書く。図の Description 由来のユーザーデータは無加工。API リファレンスは `IncludeJapaneseApiDocs`／CLI `--api-docs-ja` で日本語版 `{ベース名}.ja.g.md` を併産可能＝テンプレート対訳ペア `ApiReferenceDoc.scriban`/`ApiReferenceDoc.ja.scriban` は構造パリティテストが同時更新を強制。生成出力の CJK 混入は GeneratedOutputEnglishGuardTests が検知）。**samples/ 配下は手書きコード（Program.cs のコメント・出力・データ）・図の Description も英語**（生成物と同居する国際向けショーケースのため。サンプルの README.ja.md のみ日本語）。

## コマンド

```powershell
dotnet build QuickER.slnx                                # ビルド
dotnet test QuickER.slnx --no-build                      # 全テスト
dotnet test tests/QuickER.Tests/QuickER.Tests.csproj --filter "FullyQualifiedName~SqlServerDdlGeneratorTests"   # 単一テストクラス
csharpier format .                                       # 整形（グローバルツール）。コード修正後は必ず実行
```

- テストは 2 プロジェクト構成: `tests/QuickER.Tests`（net10.0-windows / WPF 依存＝**Windows でのみ実行可能**）と `tests/QuickER.Tests.Integration`（net10.0・WPF 非依存＝クロスプラットフォーム。実 DB 統合＋生成コードのランタイムテスト）。CI は windows-latest（全体ビルド＋全テスト）＋ubuntu-latest（Integration のみ・実コンテナ＝実 DB 検証の唯一の CI 経路）の 2 ジョブ
- **テストのフォルダ構成は src のプロジェクト構成をミラー**する（`tests/QuickER.Tests/{Model|Document|Provider|SqlServer|PostgreSql|MySql|Oracle|Sqlite|CodeGen.CSharp|CodeGen.UI|CodeReverse.CSharp|Db.UI|AI|AI.UI|AI.Chat|AI.Mock|Extensibility|Settings|Gui|Gui.Common|Cli|Mcp|Mcp.Tools}/`＝SUT の所在プロジェクトに対応・namespace はフォルダ追従で `QuickER.Tests.{フォルダ}`。Gui/ 配下はさらに Behaviors/Services/ViewModels/Views/UndoRedo をミラー）。横断フォルダは `Docs/`（CLAUDE.md 整合ガード＝依存図・フォルダ一覧を実体と機械照合）・`GeneratedFixture/`（ドリフト検知＝パス不動・regen スクリプトが参照）・`PublicApi/`（ランタイムパッケージ公開 API 面の承認ファイル）・`Resources/`（resx パリティ）・`Samples/`・`TestDoubles/`（共有テストダブル）・`TestSupport/`（WPF/STA 起動ヘルパ）。実 DB 統合・ランタイムテストは別プロジェクト `QuickER.Tests.Integration`（次項）
- `tests/QuickER.Tests.Integration/` は実 DB テストの独立プロジェクト（net10.0・WPF 非依存＝ubuntu の CI ジョブで実行可能）で 2 サブフォルダに分かれる: `Dialects/`＝アプリ自身の DB 面サービス検証（`*IntegrationTests` 接尾辞。CommentSync / ConnectionStringFactory / DdlRoundTrip / SchemaSync）・`GeneratedRuntime/`＝生成コードの実行時挙動検証（`*RuntimeTests` 接尾辞。SaveHook / NamedQuery / BinaryColumn / Remote / EF Core 方言等）。共有フィクスチャ（*ContainerFixture / SqliteTempDatabase）はプロジェクト直下、`GeneratedFixture/` サブフォルダにはインメモリ系ランタイムテスト（`RuntimeTestMatrixTests` の走査規約＝「Generated* 名前空間の `*RuntimeTests`」を保つため名前空間ごと同居）。**生成フィクスチャ（.g.cs・*FixtureDefinition・manual partial）は `tests/QuickER.Tests/GeneratedFixture/` の実体を Compile リンクで二重コンパイル**する（実体パスは不動＝ドリフト検知・再生成は QuickER.Tests 側のまま。manual partial は生成側が契約宣言のみを出す設計のため .g.cs と同一アセンブリ必須）。SQL Server / PostgreSQL / MySQL / Oracle は Testcontainers の実コンテナを使い、**Docker 不在時は自動スキップ**される（フィクスチャが検出）・**厳格モード `QUICKER_REQUIRE_DOCKER=1` ではスキップへ変換せず失敗**する（壊れた Docker 構成がスキップ緑に化けない＝CI の ubuntu ジョブと Docker 稼働の開発機で設定する）。この開発機では Docker 稼働＝全件実行・スキップ 0 が正常値。CI は ubuntu ジョブ（integration-tests-linux）が実コンテナ＋厳格モードで全件実行し、windows ジョブでは Linux コンテナが使えないためスキップされる。SQLite 系は実ファイル DB（SqliteTempDatabase）を使うため **Docker 不要＝両ジョブとも常時実行**される
- **生成コードのランタイムテストは「共通基底 1＋バックエンド派生 N＋派生固有 Fact」の形に揃える**（2026-08-16 の再編）。バックエンド非依存のシナリオは基底の `[Fact]` が持ち、派生はリポジトリ生成・シード・「他者による更新」の作り方だけを差し込む（`SaveHookRuntimeTestsBase` / `ConcurrencyRuntimeTestsBase<TEntity, TConflictException>` / `NamedQueryRuntimeTestsBase` / `UniquenessCheckRuntimeTestsBase<TOrder>`）。**生成物はフィクスチャごとに別 namespace へ出る**ため `SaveConflictException` も `ConcurrencyMode` も `OrderEntity` も基底からは名指しできない＝エンティティ型・競合例外型だけを型引数で受け、値の読み書きは派生のアダプタへ委ねる（VO 有効の図と無効の図が同じシナリオを共有できる）。**バックエンドによって存在しない面（リモートの `Query()`・インメモリの生 SQL）は条件スキップでなくサブクラス階層で分ける**（`NamedQueryRawSqlRuntimeTestsBase` / `UniquenessCheckLocalRuntimeTestsBase<TOrder>`＝スキップ 0 の原則）。新しいバックエンドを足すときは派生を 1 つ書くだけで全共通シナリオが自動的に掛かる（＝機能×実装の網に穴が開かない）
- Docker 必須テストには `[Trait("RequiresDocker", "true")]` が付与されており、`--filter "RequiresDocker=true"`（または `!=` で除外）で選別実行できる（例外: `Integration/Dialects/SqlServerSchemaSyncIntegrationTests` は localhost の実 SQL Server 接続＝Docker 不使用・接続不可時スキップの第 3 機構）
- 生成コードの Roslyn コンパイル検証（GeneratedCodeCompilationTests）は Docker 不要で常時実行される

## アーキテクチャ

依存方向（上が被依存側。矢印はプロジェクト参照）：

```
QuickER.Model        意味モデル（依存ゼロ。X/Y/色などの視覚情報を持たない）
  ▲
QuickER.CodeGen.CSharp    DB非依存のC#コード生成エンジン（Scriban・テンプレートは埋め込みリソース）
  ▲
QuickER.Provider     DB抽象化の共通基盤（DdlGeneratorBase、インポータ共有部品）
  ▲
QuickER.SqlServer / PostgreSql / MySql / Oracle / Sqlite    方言プロバイダ（5実装で対称構造）
  ▲
QuickER.Gui (WPF) / QuickER.Cli                    合成ルート（全プロジェクトを参照）

QuickER.Document → Model, Settings    保存単位 DiagramDocument = schema(意味) + layout(視覚) の1ファイル
QuickER.Mcp          MCP ツール定義の正本（ToolDefinition・ErDiagramToolCatalog＝英語）と stdio サーバホスト・file 注入（依存は ModelContextProtocol SDK のみ・WPF 非依存）
QuickER.Mcp.Tools → Model, Document, Mcp, CodeGen.CSharp    ファイルベースの ER 図ツール実行ホスト（ステートレスに読込→変更→保存。set_query の DSL/生 SQL 検証に CodeGen.CSharp の Queries を使用。CLI `quicker mcp` が合成）
QuickER.AI       → Model, Settings, Mcp    AIチャットエンジン共通基盤 AI/MCP/ASP.NET Core（WPF非依存。VM操作は IErDiagramToolHost 経由でアプリ側）
QuickER.Settings     設定 JSON の汎用永続化ストア JsonSettingsStore<T> と原子的ファイル書き込み AtomicFile（依存ゼロ・net10.0。AI / Cli / CodeGen.UI / Document / Db.UI / Gui が参照）
QuickER.CodeReverse.CSharp → Model, Provider    C# リバース取込のパーサ（Roslyn 構文解析のみ・コンパイル/アセンブリ読込なし。Roslyn 依存はこのプロジェクトへ隔離。Cli と CodeGen.UI が参照）
QuickER.Extensibility → Model, Provider    フィーチャーモジュール契約（IFeatureModule / FeatureToolbarItem / IErDiagramHost。WPF 非依存・net10.0）
QuickER.Gui.Common   → Gui.Abstractions    汎用 WPF 部品（MessageBoxDialogService / WpfFileDialogService / PasswordBoxBehavior / InformationDetailsDialog）

機能プロジェクト（WPF。Gui を参照しない＝Gui が参照する。ランチャー・アダプタは各機能側にあり、Gui は IErDiagramHost 実装とカタログだけを持つ）:
QuickER.Gui.Abstractions  アプリ汎用 UI サービス抽象（IDialogService / IFileDialogService。実装は Gui.Common / Gui 側）
QuickER.AI.UI      → AI, Gui.Abstractions    AI 共有 UI 部品（接続パネル・添付・ApiKeyStore・WpfUiDispatcher）
QuickER.AI.Chat    → AI, AI.UI, Extensibility, Gui.Abstractions, Gui.Common, Mcp, Mcp.Tools    チャット機能（ErDesign プロファイル・ER 設計ツール定義・チャットダイアログ・AiChatFeatureModule）
QuickER.AI.Mock    → AI, AI.UI, Extensibility, Gui.Abstractions, Gui.Common, CodeGen.CSharp, Provider, Mcp    モック生成機能（モックフォルダ方式のセッション・ツール・ストア・プレビュー一式・単一 HTML バンドル出力・WPF モックプロジェクト生成・MockGenerationFeatureModule）
QuickER.CodeGen.UI → CodeGen.CSharp, CodeReverse.CSharp, Extensibility, Gui.Abstractions, Gui.Common, Provider, Settings, SqlServer, Sqlite    C# コード生成／クエリ定義ダイアログ・CodeGenerationFeatureModule
QuickER.Db.UI      → Extensibility, Gui.Abstractions, Gui.Common, Provider, Settings, SqlServer, Sqlite    DB 取込／DB 同期ダイアログ・接続プロファイル（connections.json＋DPAPI シークレット）・DbToolsFeatureModule
```

押さえるべき設計判断：

- **意味と視覚の分離**: `Entity`（Model）は座標・色を持たない。視覚状態は `EntityViewModel` と `EntityLayout`（Document）が保持。エクスポータ・生成器は意味モデル `ErDiagram` のみを消費する
- **CodeGen.CSharp の DB 非依存**: 型解決（DB型→C#型）はプロバイダ側の責務。CodeGen.CSharp は解決済み `CSharpTypeInfo` の辞書を入力に受け取る。この契約のため Provider は CodeGen.CSharp をプロジェクト参照する（Scriban が上位層の依存クロージャへ推移的に乗るが、Provider 自身は Scriban の型を使わない）。`CSharpTypeInfo` を中立アセンブリへ分離して参照を切る案は**検討のうえ見送り**（実利が「IncludeCodeGenModules=false 構成から Scriban.dll が 1 本消える」だけで、契約 1 型のためのアセンブリ新設は投機的な間接層・2026-08-25 判断）
- **プロバイダの対称性**: 5方言は同じ構造（SchemaImporter / DdlGeneratorBase 派生 / 型マップ / 実 DB 統合テスト。統合テストは SQL Server / PostgreSQL / MySQL / Oracle が Testcontainers、SQLite は実ファイル DB）。方言 SQL の説明コメントは SQL Server 版と同水準に揃える。スキーマインポータの基底クラス化は**意図的に見送り**（方言差分が大きい）。共有部品は ForeignKeyRelationshipBuilder / UniqueConstraintImportBuilder / SchemaTableEntry。**既知の割り切り**: 各方言の Identifier はドット付きテーブル名を 5 方言対称に `"a"."b"` へ分割クォートする（SqliteIdentifierTests が固定）。SQLite では `"a"."b"` が「アタッチ済み DB a のテーブル b」を意味するため、dbo 以外のスキーマを含む SQL Server 由来の図を sqlite へ切り替えて DDL/同期を実行すると `unknown database` エラーになり得る（対称性維持を優先し修正しない・2026-08-05 判断）
- **DB 同期の 2 段構造（planner ＋ 方言別レンダリング）**: スキーマ同期は「`SchemaDiffService.Compute`（差分検出・方言中立）→ `SyncPlanner.BuildPlan`（選択済み差分を固定順セクション化＝方言中立の `SyncPlan`）→ `ISyncScriptBuilder.Build`（方言別に SQL 化）」の 2 段。方言差は `SyncDialectCapabilities`（`SupportsAlterColumn` / `SupportsForeignKeyAlter` / `SupportsDescriptions` / `PersistsForeignKeyConstraintNames` / `ColumnReorder` / `AlterColumnRequiresForeignKeyRebuild`）でプランナーへ宣言する。逐次 DDL 方言（SQL Server / PostgreSQL / MySQL / Oracle）は差分をそのままセクション化。**SQLite はテーブル再構築方式**（`SyncPlan.Rebuilds`／`TableRebuildPlan`＝「live＋選択差分のみ」の合成スキーマで新テーブル作成→データ移送→DROP→RENAME→補助オブジェクト再作成・FK 検査は `foreign_key_check` を実行側が実検査・PRAGMA はトランザクション外＝`SyncPlanner` が合成に live スキーマ〔`SyncPlanContext`〕を要求する）。**列順同期（Phase 3）は SQLite（再構築へ畳む）と MySQL（ネイティブ `ALTER TABLE ... MODIFY ... AFTER`＝`SyncPlan.Reorders`／`TableReorderPlan`・最長増加部分列を不動として移動列を最小化）のみ**で、選択可能な `ReorderColumns` 差分項目（既定未選択）として出す。他 3 方言（`ColumnReorder=None`）は従来どおり選択不可の案内項目のみ（VM 側）。検出は「共通列の相対順序比較」（追加・削除を除いた双方に存在する列の順序差＝真ん中への列追加＋並び替えを 1 回の同期で完結できる）。**主キー同期（AlterPrimaryKey）**＝PK 列構成（順序付き名前リスト）の差をテーブル単位 1 項目で検出（既定未選択・IsDestructive）。旧 PK 制約名は取込まず方言カタログの実行時逆引きで DROP（SQL Server=sys.key_constraints 動的 SQL・PostgreSQL=pg_constraint DO ブロック・MySQL/Oracle=名前不要の DROP 構文・PK なしは no-op・解除のみは ADD なし）・ADD は `PK_{テーブル名}` 固定命名。逐次方言は **3 フェーズ**（`SyncPlanSection.PrimaryKeyPhase`＝Drop→AlterColumn→Add の順で同一 Kind のセクションを 2 分割＝旧 PK 列の NULL 許容化を含む変更も 1 回の同期で成立。PK 変更なしの計画は出力バイト不変）・SQLite は再構築へ畳む。**依存 FK の自動 drop→re-add**＝選択済み AlterPrimaryKey の被参照 FK（全方言無条件）と FK 関与列の AlterColumn（`AlterColumnRequiresForeignKeyRebuild`＝SQL Server のみ true・Msg 5074 対策)を、プランナーが live リレーションから合成項目として Drop/Add セクションへ注入（明示 Drop 選択とはシグネチャで重複排除・明示 Drop した FK は再 add しない）。被参照列が「同期後の PK」とも「同期後に存在する UNIQUE 制約」とも**完全一致**しない再 ADD は `SyncPlan.Warnings`（候補キー喪失・言語中立 record）として実行確認に表示（**証明ベース**＝同期後 UNIQUE 集合は live − 選択済み DropUniqueConstraint ＋ 選択済み AddUniqueConstraint から厳密合成・自然キー UNIQUE＋代理 PK の定番構成では警告しない。⊆ 判定は不可＝`(id)→(id,code)` 拡張で 4 方言中 3 方言が失敗する。取込外の素の一意インデックスで実際には通る構成があるため実行ブロックはしない＝誤爆回避。失敗時 SQL Server/PG はロールバック・**MySQL/Oracle は FK 消失の部分適用**＝統合テストで現状固定）。**複合 FK はフル対応（2026-08-07・劣化ガード撤去済み）**＝FK 差分・暗黙再構築・候補キー証明はペアリスト（被参照列は集合照合）基準で動き、MySQL の「プレフィックス FK が成功して静かに壊れる」ハザードは完全ペア再作成で構造的に消滅（KEY_COLUMN_USAGE 直検証の統合テストで固定）・SQLite の複合 FK 子テーブル再構築ブロックも撤去（全ペア保持で再構築）。旧 `CompositeForeignKeyGuard`／取込警告（`SchemaImportResult.Warnings`）／`SchemaDiffKind.Advisory`・`ToAdvisory` は撤去済み。実行失敗後は差分を自動再計算する（MySQL/Oracle の部分適用後に古い差分を表示し続けない）
- **UNIQUE 制約のモデル正本化（2026-08-06）**: 一意制約の正本は `Entity.UniqueConstraints`（`UniqueConstraint`＝Id / Name?〔null=生成時合成〕/ ColumnIds〔列 Guid・宣言順〕）。取込は 5 方言とも**真の UNIQUE 制約のみ**（SQL Server=`is_unique_constraint=1`・SQLite=`origin='u'`・PostgreSQL=`contype='u'`・Oracle=`'U'`・MySQL=区別不能のため全一意インデックス〔プレフィックス／関数インデックスは除外〕）で、制約名は実名を永続化（SQLite の自動名 `sqlite_autoindex_*` のみ null）。未設定名は生成時に `UQ_{テーブル}_{列連結}` を合成（規則の正本は `UniqueConstraint.SynthesizeName`＝Model 側〔Provider を参照しない MCP ツールホストと共有〕・方言 SafeName の適用は `UniqueConstraintNaming`）。DDL は 5 方言とも PK 制約行の直後に `CONSTRAINT … UNIQUE` 句（**CREATE TABLE の制約行〔PK→UNIQUE〕の組み立ては `TableConstraintLineBuilder`〔Provider〕が単一正本**＝DDL 生成と逐次 4 方言の同期 AddTable の計 5 呼び出し箇所が共有・2026-09-13。どちらか一方に再インライン化すると「同期で作ったテーブルだけ UNIQUE が無い」乖離が静かに再発する＝mutation で UNIQUE 出力除去は DDL 側 12＋同期側 12 の 24 件が赤）・**1対1 判定はモデルの UniqueConstraints 参照**（取込時判定・以降の自動再計算なし）・`SchemaSignature` へ含む（UNIQUE だけの差でも置換確認が出る）・文書 JSON は v1 のまま後方互換（旧ファイルは空リスト）。差分同期は制約単位の `AddUniqueConstraint`（既定選択）/ `DropUniqueConstraint`（既定未選択・IsDestructive）＝**列集合照合**（名前差だけでは差分を出さない＝合成名 null と実名の揺れ対策）・セクション順は DropFK→DropUnique→PK:Drop→AlterColumn→PK:Add→…→AddUnique→AddFK・SQL Server は AlterColumn×UNIQUE 構成列の暗黙 drop→re-add を注入（Msg 5074 対策・明示 Drop とはシグネチャ重複排除）・明示 DropUnique×被参照 FK は警告（`UniqueConstraintDropMayBreakForeignKey`・方言中立）・SQLite は再構築へ畳む（**`SchemaAuxiliaryObject` の UniqueConstraint Kind は廃止**＝Auxiliary はインデックス・トリガーのみ。再構築の CREATE TABLE は合成後定義の正本から名前付き UNIQUE 句を出力）。GUI はプロパティパネルの UNIQUE 制約カード（正本=ColumnIds・構成列編集は**列選択コンボボックスの行リスト**〔行の並び＝宣言順・候補は同じ制約の他行が未使用の列＋自行の選択に絞って重複選択を構造的に排除・＋は未選択の空スロットを 1 つだけ追加するビュー状態でモデル未反映＝列を選んだ時点で確定・×は行を外す〕で、行 VM〔`UniqueConstraintMemberViewModel`〕は正本からの導出・選択編集は `EntityViewModel` 経由で MainViewModel へ中継して `ChangeUniqueConstraintColumnsCommand` 1 回に畳む・追加/削除/構成列変更は Undo コマンド・制約名は DiagramChangeTracker 追跡）＋キー欄の UQ マーク（優先度 PK>FK>UQ・判定と配色は `ColumnKeyMarkPalette` へ 1 本化＝画面 XAML／ベクタ印刷／SVG／寸法実測の 4 箇所が共有・コンパクト表示は従来どおり PK/FK 行のみ）。**列削除はその列を含む制約ごと削除**（縮めて別の意味の制約に変質させない。GUI=RemoveColumnCommand の 1 Undo 単位・MCP の remove_column も同カスケード）。周辺露出は MCP/内蔵チャット（set_unique_constraint〔(テーブル, 列集合) 照合 upsert〕/ remove_unique_constraint・get_diagram_summary 反映）・DBML（単一列無名=列設定 `unique`・複合/名前付き=`Indexes { … [unique, name: …] }`・往復可）・Mermaid（**単一列制約のみ** UK＝複合はフォーマットに構文が無く非出力の割り切り）・Excel/HTML 定義書（キー列 `UQ{n}`・同番=同一制約で複合表現・Excel 取込は制約名が落ち合成名になる）。**重複事前チェックの契約と実装は基底に 1 回だけ（2026-09-04・移設 P2）**＝`CheckUniquenessAsync` の宣言はランタイム共通の `IRemoteRepositoryCore<TEntity,TKey>`（per-entity 面の宣言は削除＝継承経由で利用側は無変更）・実装は 4 つのリポジトリ基底（方言 2＋EfCore＋InMemory）と `HttpRemoteRepositoryCore`（転送）に各 1 回。per-entity に残るのは `UniquenessConstraintSet<TEntity>`（制約テーブル＋自分自身除外を 1 レコードへ束ねた 3 行 override・制約ゼロのエンティティは override ごと省略）と `CollectCustomUniquenessChecks` partial フックへの橋渡しのみ（フックは存続＝挿入の唯一手段）。基底の実行順は「制約テーブル→カスタムフック」の従来順を厳守（共通実行子への合成は順序が変わるため不採用と判断済み）。**DI 登録も縮約**＝非 keyed は `AddScoped<I契約, 実装>()` 1 行（省略可能引数のコンストラクタ注入が旧 factory の `GetService` と同意味論＝`ISqlExecutor` / `ISaveHookRegistry` は未登録なら既定 null。唯一の意味差は「利用者が partial で第 2 コンストラクタを足すとコンテナがそちらを選び得る」）・keyed は明示 factory を維持（keyed 依存は属性なしに注入できない）。mutation 実証済み＝制約テーブルを空にすると Uniqueness/ValidateUnique 系 25 件だけ赤・橋渡し除去でカスタムフックの 6 件（5 バックエンド）だけ赤。**EF Core 生成（HasIndex/IsUnique）は意図的に非対応**（既存スキーマ接続専用で実行時効果がないため）。**C# リバースは往復対応（2026-08-07）**＝`[UniqueConstraint]` をパーサーが読取（プロパティ名→[Column] 逆写像。属性なし＝制約なしでコードが正・フォールバックなし＝旧生成コードと区別不能なため）。あわせて FK の ConstraintName / OnDelete / OnUpdate も `[NavigationReference]` の任意名前付き引数として往復（既定値は出力省略・旧生成コードの未指定分は `ReverseMergePostProcessor`＝fallback 専用が GUI マージで現在図から補完・CLI リバースでも FK 定義が完全復元）
- **リレーションの複数列対応（複合 FK フル対応・2026-08-07）**: リレーションの列対応の正本は `Relationship.ColumnPairs`（`RelationshipColumnPair`＝親列 Guid×子列 Guid・宣言順。旧 `SourceColumnId`/`TargetColumnId` は**後方互換なしで廃止**＝旧文書 JSON の列ペア情報は読み捨て・Version 1 のまま）。**列ペアが唯一の正本で推測フォールバックなし**＝列名解決は `ForeignKeyColumnPairResolver`（Provider）へ 1 本化し、ペア 0 件（多対多・未設定）・解決不能な列を含む FK は DDL の FK 句・FK 差分の対象外として明示スキップ（旧来の「親=先頭 PK・子=合成名推測」「差分の 4 段引き当て」は撤去）。取込は 5 方言とも全ペアを宣言順で正本化（劣化なし・取込警告機構は廃止）・1対1 判定は従来から列集合ベースで不変。GUI はリレーションパネルの**ペア行エディタ**（1 行=親列＋子列コンボ・両側確定で 1 履歴・親列候補は **PK ∪ UNIQUE 制約構成列**・ManyToMany は編集不可＝種別切替で全ペアクリアをグループ 1 履歴）・作成時は**親 PK 全列を自動ペア化**（列ごとに ForeignKeyColumnResolver の命名マッチ・重複子列排除・引き当て不能列はペアなし）・列削除は当該リレーションの**全ペアをクリア**（線・種別・制約名は残す・1 Undo 単位・MCP remove_column とパリティ）。MCP/内蔵チャットの add_relationship は `source_columns`/`target_columns`（並行配列・同数・宣言順・**省略時は親 PK 全列自動ペア化**）・remove_relationship は複数一致時に `constraint_name` で特定（無指定なら候補列挙エラー＝旧「先頭を黙って削除」は廃止）・get_diagram_summary に列ペアと制約名を表示。DBML は複合 Ref 構文 `Ref: p.(a,b) < c.(x,y)` の出力＋取込（**取込の列再推論は廃止＝Ref 行の列名が正本**・不整合行はペアだけ破棄）・Excel/HTML 定義書は列セルをカンマ区切り複数列表記（`FK{n}` は複合の全構成子列に同番＝`UQ{n}` と同流儀）・Mermaid は列構文が無いため出力現状維持・取込推論を PK 全列ペア化へ統一。**生成コード（C#）はペア 2 組以上を対象外**（`CodeGen_Warning_RelationshipCompositeSkipped` でナビゲーション/EF Core Fluent をスキップ・単一ペアは従来どおり＝単一ペア図の生成物はバイト不変・C# リバースも単一列のみ）
- **完了通知の提示先（GUI・2026-08-08）**: 「**外部とのやり取り＝モーダル／ER 図ファイル自身の読み書き＝ステータスバーの一時通知**」で使い分ける。外部形式の入出力（エクスポート／インポート）・DB 取込・C# コード取込は `IDialogService.ShowInformation`、上書き保存／名前を付けて保存／開くは `MainViewModel.NotifyStatus`（5 秒で「準備完了」へ戻る・外部変更検知と同じ経路。ファイル名はタイトルバーに出るため文言へ入れない）。**失敗の報告は提示先に依らず常にモーダル**（原因の例外文を連結）。**完了に添える内訳・警告があるときは提示先に依らず `ShowInformationDetails`**（単文の完了に大型ダイアログは出さない＝コード生成の診断提示・エクスポートの欠落告知・コード取込の解析警告が同じ規則）。印刷は他アプリの流儀に倣い無通知。規則の正本は `IDialogService` の XmlDoc（アイコンの使い分け規約と同居）。この線引きなら機能モジュールからメインのステータスバーへ触る必要が無いため `IErDiagramHost` に通知メソッドは持たせない。**標準 MessageBox の本文へ載せる項目一覧は `DialogItemList.Format`（Gui.Abstractions・上限 30 件で「他 N 件」の 1 行へ畳む・文言は呼び出し側の `Common_MoreItems` を渡す）へ集約**する（DB 取込・コード取込の「取込で消える名前付きクエリ」告知が共有。標準 MessageBox はスクロールしないため、件数が多いとボタンが画面外へ出る）。**確認に判断材料の一覧を添えるときは `ConfirmWarningDetails`**（`InformationDetailsDialog` の警告確認モード＝スクロール可能な詳細領域＋OK／キャンセル。一覧は畳まず全件＝対象 DBMS 切替の事前確認が使用。実行可否を判断させる画面で「他 N 件」の中身を確かめられないのは判断材料として不十分・二節構成は 30 件上限でも MessageBox からあふれ得るため）。DB 同期の完了は「ダイアログ内ステータス＝簡潔な状態／モーダル＝結果の要約（実行バッチ数）」と文言の役割を分ける（同一文字列を 2 箇所へ出すと重複した通知に見えるため）
- **出力形式の情報欠落告知（Mermaid / DBML・2026-08-08）**: 形式が構造的に表現できない情報を、出力完了時に内訳として告知する。判定は各エクスポータの `DetectOmissions`（出力ロジックと同居＝「出力を変えたのに検出が古いまま」の乖離を防ぐ）が**図の実際の中身から**行い（説明が空の図で「説明が落ちる」とは言わない）、言語中立の `ExportOmissionKind` を返す（件数は持たない＝「何が落ちるか」が伝われば十分。表示側 `DiagramExportService.DescribeOmission` が resx で文言化＝`SyncPlan.Warnings` と同じ流儀。エクスポート／インポートのコマンド実行本体は `DiagramExportService` / `DiagramImportService`〔Gui/Services・2026-08-25 に MainViewModel から抽出〕で、VM は 1 行委譲と `IDiagramTransferHost` の実装だけを持つ）。提示は `ShowInformationDetails`（要約＝完了文＋導入文・詳細＝項目の箇条書き＝型変換警告と同じ形式）で、**形式ごとにセッション 1 回**だけ（Mermaid は NOT NULL 列がある限りほぼ必ず立ち、毎回出すと通知が形骸化する。`_omissionNotifiedFormats`＝未対応方言フォールバック `_fallbackWarningShown` と同じ流儀）。**Mermaid の欠落は 9 種**（テーブル説明／メモ／列の説明／NULL 許可／複合 UNIQUE 制約／UNIQUE 制約名／FK 列ペア／参照アクション／名前付きクエリ）。**DBML はテーブル説明を標準 `Note:` 行・参照アクションを Ref 行の設定ブロック（`delete` / `update`・既定 NoAction は出力省略・DBML の `restrict` は取込で NoAction へ倒れる）で往復対応済み**のため欠落はメモと名前付きクエリの 2 種だけ（メモは `Note` を説明に使うため独自拡張なしには載せられない割り切り）。あわせて他ツール製 DBML の `Note:` 行をカラム定義として誤取込していた相互運用の不具合も解消した。**SQL DDL・Excel/HTML 定義書・画像は告知しない**（落ちるものが「その形式の目的上、出さないのが正しいもの」ばかりで、告知すると本当に危険な欠落が埋もれる）
- **フィーチャーモジュール方式（着脱可能なプラグイン）**: DB 取込・DB 同期・AI チャット・AI モック生成・C# コード生成・クエリ定義は、契約 `QuickER.Extensibility`（`IFeatureModule`＝Id / ConfigureServices / Initialize〔既定 no-op・Build 直後〕/ CreateToolbarItems / OnMainWindowClosing・`FeatureToolbarItem`〔INotifyPropertyChanged 実装クラス。Tooltip / BeginsGroup は実行中に切替可＝対象 DBMS 切替に追従してツールチップを差し替える機構（現在は全方言が DB 同期対応のため実質定数だが機構は維持）。各モジュールの先頭アイテムは BeginsGroup=true・集約後の全体先頭のみ App が false へ矯正〕・`IErDiagramHost`）を介した静的モジュール合成で Gui へ結線する。依存方向は「プラグイン（Db.UI / AI.Chat / AI.Mock / CodeGen.UI の各 FeatureModule）→ 契約 ← ホスト（Gui）」で、**Gui は機能の型を知らない**（機能プロジェクト参照はカタログのためだけ）。ホスト能力は `MainViewModelErDiagramHost`（Gui）が `IErDiagramHost`（IsEmpty / **IsDirty**〔未保存変更の有無＝図を失う操作の確認水準の選択用。ダーティなら ConfirmWarning・クリーンなら Confirm＝`DialogServiceExtensions.ConfirmDiscard`〕/ GetDiagram / Providers / **TargetDbms** / AutoArrangeNewDiagram / ExecuteTool / **ReplaceDiagram**〔方言採用＋履歴なし置換＋自動整列＋fit＝DB 取込用〕/ **ReplaceQueries**〔差し替え＋AutoSave〕/ **ColumnRenamed** / **TargetDbmsChanged** イベント）として提供し、ランチャー・アダプタ・実行サービスは各機能プロジェクト側（DB=`DbImportCommandService`〔SchemaSignature 比較の置換確認込み〕/ `DbSyncCommandService`〔CanRun・ツールチップは TargetDbmsChanged 購読で再評価〕・AI=`ErDiagramHostChatAdapter` / `ErDiagramHostMockDiagramSource`・コード生成=`CSharpGenerationCommandService`〔CurrentProvider は GetDiagram().TargetDbms から解決〕/ `QueryDefinitionCommandService` / `QueryConditionRenameFollower`〔列リネーム時の DSL 条件追従。Initialize で購読・書き換え発生時のみ ReplaceQueries〕・ダイアログ提示は presenter シーム）。AI エンジン `QuickER.AI` は Extensibility を知らない（`IErDiagramToolHost` はプラグイン内アダプタで包む＝層の純度維持）。ツールバーのモジュールボタンは MainWindow の WrapPanel＋`MainViewModel.FeatureToolbarItems`／`FeatureToolbarItemGroups`（App が起動時に集約設定・BeginsGroup 境界のグループごとにコードビハインドが ItemsControl を生成して WrapPanel へ挿入＝ウィンドウ幅不足時はくくり単位で折返し・モジュール 0 個ならセパレータごと畳む）。切り離し後の `IAppDialogService` は印刷ダイアログ 1 メソッドのみ。同梱一覧は `FeatureModuleCatalog`（Gui）で、ビルドプロパティ **`IncludeDbToolModules=false`**（DB ツールなし構成＝Db.UI が外れる。スキーマ取込・差分・同期エンジンはプロバイダ層在住＝CLI の scaffold と共有のため残る）・**`IncludeAiModules=false`**（AI なし構成＝出力に QuickER.AI*.dll が一切出ない）・**`IncludeCodeGenModules=false`**（コード生成 UI なし構成＝CodeGen.UI が外れる。エンジン CodeGen.CSharp は型解決のためプロバイダ層が参照しており残る）を独立に指定できる（csproj の条件付き ProjectReference と `QUICKER_DBTOOL_MODULES` / `QUICKER_AI_MODULES` / `QUICKER_CODEGEN_MODULES` 定数が連動）。ツールバー文言・機能内メッセージの resx は各機能プロジェクト側（Db.UI / AI.Chat / AI.Mock / CodeGen.UI）。モジュールを抜いた構成では列リネームのクエリ条件追従も止まる（生成前の Guid 参照検証・DSL パースが壊れを警告するため許容）
- **外部 MCP サーバ（CLI `quicker mcp`・stdio）**: 外部 AI エージェント（Claude Code / Codex 等）向けに ER 図編集 12 ツール（カタログ正本の 11＋Mcp.Tools 側が足す create_diagram。一意制約の set_unique_constraint〔(テーブル, 列集合) 照合の upsert・既存 Id 温存〕/ remove_unique_constraint を含む）＋名前付きクエリ 3 ツール（set_query / list_queries / remove_query。set_query は (テーブル名, クエリ名) 照合の一発定義 upsert＝既存 Id 温存・全戻り形×全実装方式対応・定義時に DSL 構文〔QueryConditionParser〕/列名・@パラメータ実在/生 SQL〔RawSqlAnalyzer・未宣言＝拒否・未使用/複文＝警告のみ〕/構造を検証し失敗は保存しない・order_by は list/single/projection のみ・型トークンの内容検証は生成時任せ＝方言非依存維持）＋コード生成 3 ツール（generate_csharp / generate_ddl ＋情報系 get_generation_config_schema＝quicker.json の全キーを機械可読 JSON で返す自己記述カタログ〔`GenerationConfigSchema`〕。docs 不在の外部エージェントが config を自己発見的に書ける）を stdio MCP で公開する。初期化時に設計既定ルールの instructions（`ErDiagramToolCatalog.ServerInstructions`＝英語。命名はパスカルケース単数形・既存様式追従・単一 PK・FK 手順など。**誘導のみで強制なし**＝ルール外の図も扱える柔軟性を維持）を返し、add_entity のツール説明にも命名既定を記載する。**ステートレス設計**＝`file`（図 JSON パス）は `FileParameterInjector` がツール単位で機械注入し（図を対象にするツールのみ・引数不要の get_generation_config_schema は対象外）、毎呼び出しが「読込→変更→保存」で完結（`QuickER.Mcp.Tools` の `DocumentErDiagramToolHost`）。ツール定義の正本は `QuickER.Mcp.ErDiagramToolCatalog`（**英語**・内蔵チャットと共有＝AI.Chat の `ErDiagramToolDefinitions` は委譲＋LLM SDK 形式変換のみ・CJK 混入は英語ガードテストが検知）。意味論は GUI 側 `ErDiagramDynamicTools` のミラー（パリティテストが結果 ErDiagram の一致を検証）。安全策＝変更系はファイル不在（`create_diagram` へ案内）・非 DiagramDocument JSON・新フォーマット版（IsNewerFormat）を拒否し、`create_diagram` は既存ファイルを上書きしない。**レイアウトはサーバが書かない**＝新規作成はスキーマのみ文書（GUI で開くと全体自動整列）・既存ファイルへの追加分は GUI 読込時に `AutoLayoutService.LayoutAppend`（既存不動・空き領域へ追記配置。マージ取込の新規テーブルも同経路）が配置。コード生成ツールは合成ルート CLI 側（`CodeGenToolSet`＝`GenerationExecutor` の ParseResult 非依存コアを流用・`quicker generate` の挙動はバイト不変）。**stdout は JSON-RPC 専用チャネル**のため mcp 起動時に `Console.Out` を stderr へ退避・stdio E2E テスト（実プロセス起動）がプロトコル純度を最終検証する。GUI で開いている図は外部変更検知で追従する（下記）
- **document-based 化＋外部変更検知（GUI）**: 「現在のファイル」フルパス（`MainViewModel.CurrentFilePath`＝開く/名前を付けて保存でのみ遷移・インポートや図置換ではパス維持）＋Ctrl+S 上書き保存／Ctrl+Shift+S 名前を付けて保存・タイトル「ファイル名\*」の未保存マーク（終了確認なし）。**自動保存（AppData）は復旧専用のまま分離**＝現在ファイルへの書き込みは Ctrl+S のみ（自動保存を現在ファイルへ直結すると編集のたびにユーザーファイルを上書きし MCP 同時編集と衝突するため）。復元メタは gui-settings.json の CurrentDocument セクション（FilePath/LastKnownHash/IsDirty）＝起動時に「前回のファイル＋未保存変更」を復元。ダーティ判定は `UndoRedoManager` の変更世代カウンタ（読込/保存時点の世代と比較。Undo で保存時点へ戻っても安全側の「変更あり」）。外部変更は `DocumentFileWatcher`（WPF 非依存・400ms デバウンス・SHA-256 ハッシュ比較＋保存前後 Suspend/Resume の自己書き込み抑制二重化・書き込み途中の読込リトライ）で検知し、**クリーンなら無確認で自動再読込**（ビューポート維持＝fit-to-window 抑制・履歴クリア・ステータスバーの一時通知）・**ダーティなら確認ダイアログ**（「続行」選択後は同一ハッシュの再確認を抑止）。破損/新フォーマット文書は取り込まず現状維持＋通知。起動時の復元後も同規則で外部変更をチェックする
- **AI モック生成（モックフォルダ方式）**: ER 図から Web モック画面を起こす AI チャット機能（`QuickER.AI.Mock`）。**保存単位はフォルダ**＝`mock.json` マニフェスト（画面一覧・遷移・改訂履歴・スキーマスナップショット）＋画面ごとの `*.html`＋共有 `style.css` のフラット構成（`MockFolderStore`／`MockManifest`）。会話開始時にフォルダを指定し（空フォルダ＝新規 `CreateNew`／既存モックフォルダ＝再開）、以降は 1 ターンごとにライブ保存する。**再開は状態再開**＝会話履歴は保存せず、フォルダの現状（画面一覧・遷移・改訂・共有 CSS・最新スキーマ）をプロンプトへ注入して続きから作る（バックエンド非依存＝API キー/Codex/Claude Code で同じ再開ができる）。ツールは英語正本 4 つ（`save_screen`〔完全 HTML の upsert・遷移宣言・改訂メモ〕/ `remove_screen` / `save_stylesheet` / `get_screen`。定義は `MockFolderDesignTools`＝`ErDiagramToolCatalog` と同流儀）で、**検証は警告のみ**（`MockContentValidator`＝外部参照/リンク切れ/style.css 未参照は警告・拒否は空/非 HTML〔`<html>` 不含〕のみ）。プレビュー（`MockPreviewPanel`＝WebView2）は遷移が**フォルダ直下 `*.html` のみ許可**（許可フォルダ付きプレビュー要求）・サブリソース（img/script/css/fetch/WebSocket）は `WebResourceRequested` で**既定拒否**〔`IsResourceAllowed`＝許可フォルダ内なら拡張子不問。遮断は無言＝外部参照は保存時に `MockContentValidator` が警告済み〕で、画面間リンク遷移に追従し、サイドバー選択と相互同期する。単一 HTML 出力は `MockBundleExporter`（決定的なハッシュルーター結合＝各画面 body を `<section data-screen>` へ連結・`href="Foo.html"`→`#Foo`・共有 CSS インライン化）。設計書出力は `MockDesignDocExporter`（決定的・AI 不使用・モックフォルダ直下 `README.md`＝画面一覧＋Mermaid 遷移図＋**画面×エンティティ CRUD 表**＋regex による画面項目抽出。見出しは UI 言語追従〔resx `MockDoc_*`〕・改行は LF 固定）＝初回は「設計書出力」ボタンのオプトインで書き出し、以降は README.md が在る限り画面 save/remove に無音で自動追従して再生成する（save_stylesheet・スキーマ更新では再生成しない＝内容に影響しないため）。CRUD 表は save_screen の `entities` 申告〔省略=既存維持・空配列=消去・正規化 C→R→U→D・図に無い名前は警告のみ〕を画面×エンティティ表として遷移図の直後に載せる（行＝全画面〔マニフェスト順・未宣言画面は空セル行〕・列＝宣言エンティティ名の初出順・セルは正規化済み操作文字列。宣言ゼロならセクション省略）。**第 2 ステップ（モックプロジェクト生成・ターゲット 2 種〔WPF/Blazor〕・4 バックエンド対応・メンテナンスレベル）**＝**プロンプト・README-QuickER.md・ER スキーマ本文（MockSchemaSerializer）は英語固定**（「対話チャット＝UI 言語追従・ヘッドレス実行と機械向け診断＝英語固定」の線引き。警告・検証エラーも英語固定・成功文は UI 追従。CJK 混入は英語ガードテスト群が検知）＝決定的スキャフォールド（`MockProjectScaffoldService`＝データ層コード＋csproj＋README＋モックフォルダを `design/mock/` へ同梱）→ 実行器抽象 `IMockProjectAgent`（選択中バックエンドに連動・追加指示欄を任意でプロンプト末尾へ連結）で UI 層を生成 → 共有オーケストレータ `MockProjectAgentRunner` が全体タイムアウト・成果物検証・最終 `dotnet build` を独立検証（実行器の自己申告を信じない。`DotnetBuildRunner` は MSBuild の自動 import を 5 プロパティで無効化＝`ImportDirectory{Build,Solution}{Props,Targets}`／`ImportDirectoryPackagesProps`＝生成物を置き場所から独立させる。実測でソリューション・プロジェクト双方に有効）。実行器 4 実装＝**Claude Code**（ヘッドレス・自己修正・許可ツール `Edit`/`Write`/`MultiEdit`/`Bash`＋`acceptEdits`＝**Bash は無制限**）/ **Codex**（既存 app-server 統合を Cwd=出力先・approval never・workspace-write で駆動・自己修正・dynamicTools 不使用）/ **Copilot**（copilot CLI をヘッドレス実行・自己修正・ER 設計ツールは非公開＝組込みのファイル編集/シェル実行に委ねる・`AllowWorkspaceTools`＝出力フォルダ配下のみ自動承認。エージェント型 3 実装はプロンプト正本 `MockProjectPromptBuilder` を共有）/ **API キー**（`emit_file` 固定パイプライン＝共通部→各画面の決定的一発生成・読取/ビルドツールは与えない・パス保護は**拡張子ホワイトリスト**〔プロファイル宣言（WPF=.xaml/.cs・Blazor=.razor/.css/.cs）∩ 中央上限集合 `MockProjectEmitTools.SupportedEmitExtensions`＝実効の許可集合。拒否メッセージへ許可拡張子を列挙〕＋保護フォルダ〔Generated/design/**obj/bin**〕＋README/.sln/.csproj/NuGet.Config・`..`/絶対パス/`:` 拒否・中間ビルド失敗時のみ修正 1 回・UI に「自己修正なし」注記）。**エージェント型の実行権限は QuickER 側で絞らない**（2026-08-22 判断）＝ユーザーが導入した CLI をその CLI 自身の権限モデルで動かす方針で、QuickER が設計した境界は API キー方式だけ（Claude Code は `Bash` から任意コマンドを実行できる＝承知のうえの非対称。4 バックエンドの権限差の正本は `docs/ai-chat.md` の「注意」節の表）。ターゲットは `MockProjectTarget` 2 種（blazor 既定＝Blazor Web App・グローバル InteractiveServer・モック HTML/style.css を可能な限り移植／wpf）で、差分は `MockProjectTargetProfile`（csproj/README・プロンプトフラグメント・UI 成果物パターン *.xaml/*.razor）へ集約＝プロンプトは `MockProjectPromptBuilder` 単一正本＋フラグメント合成・UI は第 2 ステップの ComboBox で選択
- **生成コードの汎用性**: 生成される C# は CommunityToolkit 等の UI フレームワークに依存させない（WPF 以外でも使える設計）。Repository 生成は単一キー・アプリ側採番前提（複合キー・DB自動採番は対象外）。**SQL Server の BulkInsert は `SqlBulkCopyOptions.CheckConstraints` を常時付ける**（既定では FK / CHECK が検査されず、行単位 INSERT が弾く行を一括追加だけが通す非対称になっていた。`FireTriggers` は付けない＝QuickER の DDL はトリガー非生成）。**`BulkInsertAsync` の契約は 4 実装（SqlServer / Sqlite / EF Core / InMemory）で共通**＝null 要素はスキップ（グラフ保存のリスト内 null と同じ流儀）・戻り値は実際に挿入した行数（スキップ分は数えない）・空コレクションは接続を開かず 0・冒頭で `cancellationToken.ThrowIfCancellationRequested()`。正本は `IRepository.BulkInsertAsync` の XmlDoc で、パリティは GeneratedRuntimeParityTestsBase（SqlServer / EF Core）・GeneratedSqliteRuntimeTestsBase（Sqlite / EF Core）・InMemoryFixtureRuntimeTests が固定する（バッチ全体の主キー事前検証は InMemory だけの上乗せ）。**SQLite 版はコマンドを 1 回だけ用意して値だけ差し替える**（`EntitySaveMetadata.RebindInsertParameters`）＝XmlDoc が既にそう主張していたのに行ごとに `new SqliteCommand` していた（＝毎行 parse/plan）のを 2026-08-16 に実装側で揃えたもので、性能の doc は挙動の doc より検証されにくく静かに嘘になる（分解計測で発覚・洗い替えの速度の源）
- **EditModel のロード意味論と検証経路（2026-08-11）**: **確定値が正本・`BindingXxx` はその表示投影**。Mapper の `ApplyToEditModel` は Entity の値を確定値プロパティへ直接代入し（そのため確定値の setter は `private` でなく `internal`）、`RevertInput()` で入力文字列を確定値から導出する＝ロードは無損失（入力文字列の再パースを経由しないため `DateTime` の秒未満や `Kind` のように表示形式が表せないものが落ちない）。**行編集のキャンセル（`CancelEdit`）も同じ理由で確定値を正本にする**＝`BeginEditCore` は確定値フィールド（`_xSnapshot`）と RowState を退避し、`CancelEditCore` は確定値を内部セッターで戻したうえで `ExecuteRevert(RevertCore)` により入力文字列を導出し直す〔`RevertCore` の `SetError(null)` 経路で、取り消した入力が生んだエラーも消える〕。**戻した直後に `ClearDuplicateErrors(DuplicateErrorSource.Database)` も呼ぶ**（2026-08-13）＝確定値の復元はロード（`ExecuteLoad`）の中で走るため確定値セッターの `!IsLoading` ブロックが動かず、DB 照合が付けた重複エラーだけがキャンセル後も残って `Validate()` が永続的に false になっていた。クリアするのは `Database` 枠のみ＝確定値セッターと同じ規則（照合対象の値が消えた以上その所見は無効／`Siblings` はコレクションの現状についての所見なので次の兄弟間検証が判断する）。バインディング文字列を戻してセッターに再パースさせる旧実装は、無損失ロードで保たれた秒未満・Kind を「編集して取り消した」だけで落とし、RowState は Unchanged のままなので劣化値が黙って保存され得た。**カスケード子の登録（`ChildLink`）は単一子・コレクションとも遅延アクセサ形**（`AddChild("X", () => X)` / `AddChildren("Orders", () => Orders)`）で、登録時のインスタンスを捕捉しない＝`ApplyToEditModel` がコレクションを丸ごと差し替えても Validate / CollectErrors / HasGraphChanges / AcceptChanges は現在のコレクションを見る（旧実装はコレクションだけ捕捉形で、再ロード後に破棄済みの旧コレクション〔大抵は空〕を検証していた）。**コレクション ChildLink の検証は `EditModelCollection<T>.Validate` へ委譲**するため、親の `Validate(includeChildren: true)` だけで兄弟間の重複検出（`EditModelUniquenessValidator`）まで走る。**エラーは 2 ストア**＝`_errors`（必須・変換・VO・OnValidate。`SetError` / `ClearErrors` の担当）と `_duplicateErrors`（重複値。`SetDuplicateError` / `ClearDuplicateErrors` の担当）で、互いに触らず `HasErrors` / `GetErrors` / `CollectOwnErrors` が合成する（**合成結果は必ずリスト化して返す**＝遅延クエリを返すと、受け取ったバインディング層が後で列挙する時点でストアが書き換わっており、全プロパティ形では `Clear` 中の辞書を列挙し得た・2026-08-13）＝同一プロパティで変換エラーと重複エラーが共存でき、重複解消後の再検証が変換エラーを巻き添えで消さない（変換エラーはバインディングの setter からしか再生成されないため、消すと不正入力が画面に残ったまま `Validate` が true になり旧確定値が黙って保存される）。**ストア内はさらに「登録したチェック」で分離**し、各チェックは自分が付けたエラーだけを付け外しする（2026-08-12）＝(1) `_errors` の項目は「必須チェック由来か」を持ち、必須チェック（生成 `ValidateSelf`）は他の入力エラーがある欄には `SetRequiredError` で上書きせず〔変換できない文字列が入っている欄を「必須です」で塗り潰さない＝原因が上流の変換エラーを見せ続ける〕、値が入れば `ClearRequiredError` で自分の分だけ消す〔確定値へ直接代入されたケースでも消える。旧実装は消す経路が無く、値を入れても必須エラーが残った〕。(2) `_duplicateErrors` の項目は**チェックごとのスロット**（`DuplicateErrorSource`＝`Siblings`＝コレクション要素どうし / `Database`＝DB 照合 の 2 枠を 1 プロパティに持つ record struct。両枠が空になった時点でエントリごと除去＝`Count` が「有無」の判定であり続ける。**枠の引き当ては switch 式＋既定 throw**＝未定義の `DuplicateErrorSource` は `ArgumentOutOfRangeException`〔`ConcurrencyModes.Validated` と同じ流儀。二分岐の三項演算子では公開 API 経由の `(DuplicateErrorSource)99` が無警告で Database 枠へ落ちていた〕・2026-08-14）で、`SetDuplicateError` は自分の枠だけを書き `ClearDuplicateErrors(source)` は自分の枠だけを消す〔旧実装は 1 プロパティ 1 スロットで、後から走ったチェックが先の所見を上書きし、そのチェックが取り下げた時点で「もう一方は依然として重複している」事実ごと消えていた＝片方を解消しただけで `Validate` が真になり得た。さらに旧々実装は一括クリアで、保存直前の `Validate` が直前の `ValidateUniqueAsync` の結果を静かに消していた。公開 API の署名は不変・2026-08-13〕。引数なしの `ClearDuplicateErrors()` は全ソース削除で、**Mapper のロードだけが呼ぶ**（判定対象の値そのものが入れ替わるため。`RevertInput` は入力エラーのみ担当＝XmlDoc もその線引きで書く）。**確定値が変われば `Database` 枠はモデル全体で取り下げる**〔確定値セッターの `!IsLoading` ブロックが `ClearDuplicateErrors(DuplicateErrorSource.Database)` を呼ぶ。照合したのは編集前の値なので結論は陳腐化し、複合制約は構成列すべての組で判定しているため 1 列でも変われば同じモデルの DB 由来所見はすべて無効。`Siblings` 枠は次の検証が判断するので触らない・2026-08-13〕。`OnValidate` が登録したカスタムエラーはフック側の持ち物（生成コードは触らない）。あわせて周辺の意味論も同日に整理: **バイナリ列のロードは防御的コピー**（`byte[]` は `(byte[]?)entity.X?.Clone()`・VO 列は `entity.X?.CopyValue()`＝`ValueObjectBinaryBase.CopyValue()` が写した配列で同値の VO を作る〔VO クラス名を書かないので分割生成の Mapper が値オブジェクト名前空間へ依存しない〕。無損失ロード＝確定値の直接代入にした副作用で Entity と配列を共有していた）・**日付のみ（canonical トークン `date`）の列は `ToString("d")` で表示**（既定の `ToString()` が常に付ける "0:00:00" を出さない。入力側は無変更で往復・`datetime2` 等は従来どおり・VO 列は `X?.Value.ToString("d")`）・**EditModel の親参照ナビは NULL 許容で初期化子なし**（生成コードが一度も代入しないため `= null!` の「初期化済みのふり」をやめる。カスケード親の実体は `ParentModel`）・**`EditModelCollection` は削除した同一インスタンスを戻すと削除追跡を解除して RowState を削除前へ復元**（`_removed` は削除前の状態を併記して保持。戻した行を「削除＋挿入」で二重保存しない）・**`RemoveAll` は先頭から削除**して `RemovedItems` がコレクション順を保つ・**`IsLoading` / `IsReverting` はカウンタ**（フック等からの再入で内側の完了が外側のフラグを解かない）。**削除マーク行（`IsRemoved`）は検証・エラー集約の対象外＝読み側フィルタ**（2026-09-03）＝`EditModelBaseCore.Validate` は先頭で true（子への再帰も省く＝削除される部分木の検証は全部死に仕事）・`EditModelCollection<T>.Validate` と `CollectErrors` 系は削除行の部分木をスキップ・**DB 照合糖衣の共有ボディ `EditModelBaseCore.CheckDatabaseUniquenessAsync` も先頭で true**（2026-09-10。ここだけ漏れていて「`ValidateUniqueAsync` は false なのに `CollectErrors` は空」の自己矛盾になっていた。per-EditModel 側は無変更＝ガードは共有ボディ 1 箇所）〔保存に使われるのはキーだけなのに、削除予定行の変換エラーが保存を恒久ブロックしていた。`EditModelUniquenessValidator` の `!IsRemoved` 除外と対称化〕。**エラーストアには触れない**＝行自身の `HasErrors`/`GetErrors` は残り（行単位の表示は生きる）、削除を取り消して行が復活すればエラーも検証対象へ戻る（変換エラーはバインディング setter からしか再生成されないため、マーク時クリア方式は「不正入力が画面に残ったまま Validate が true」の既知の罠を再現する）。EditModelRemovedValidationTests が固定・mutation 実証済み（先頭 return 除去で該当テストだけが赤）。**機械的定型は列テーブル＋基底ループへ移設済み（2026-09-04・移設 P5）**＝生成 EditModel は per-column の `EditModelColumn<TSelf>` 表（表示名・Binding 名・IsRequired・確定値アクセサ・入力文字列化デリゲート）を持ち、`ValidateSelf` 必須ループ・`RevertCore`・`BeginEditCore`/`CancelEditCore`（基底の `object?[]` スナップショット＝モデルごと再利用・null 許容値型はボクシング）・`RegisterDuplicateError` は基底実装が表を駆動する（**上記の意味論〔SetRequiredError の非上書き・ClearRequiredError の自己分離・CancelEdit の復元順＝restore→ExecuteRevert→ClearDuplicateErrors(Database)→OnCancelEditCore→RowState〕は場所が基底へ移っただけで不変**＝Lifecycle/Uniqueness/RemovedValidation/RoundTrip/BinaryVo 往復の全テストが無修正緑・mutation 3 点実証済み）。ストレージモデル（確定値プロパティ・フィールド・Binding setter 本体）は意図的に不変＝列テーブルのラムダが実在メンバへアクセスする形（序数配列化は第 2 期候補）。Mapper も外枠（ExecuteLoad 開閉・RevertInput・ClearDuplicateErrors・RowState 復元）は基底テンプレートメソッドで、生成側は `LoadColumns` / `CompleteLoad` の 2 abstract（`RevertInput`＋`ClearDuplicateErrors` が列コピーと子ロードの**間**に挟まる順序保存のため 1 本化しない）。EditModel の文言フック 4 種（CustomizeRequired/Parse/DuplicateErrorMessage・CustomizePropertyDisplayName・実装はテスト専用のみ）は P1 と同じ規則で廃止＝`EditModelMessages` リゾルバへ安定キー（propertyName 等）を追加。意味系フック（OnValidate/OnBeginEdit/OnCancelEdit/OnEditModelLoaded 等）は存続で、`OnCancelEditCore`（protected virtual）は生成クラスが partial フックを復元ロードの内側へ差し込むための内部配線＝利用者向け拡張点の増減なし
- **store-generated 列の書き込み除外（rowversion / timestamp）**: DB が値を生成する列は生成 Entity にマーカー属性 `[StoreGeneratedColumn]` を刻み（判定は型マッパーの `CSharpTypeInfo.IsRowVersion`＝`rowversion`/`timestamp`・付与はオプション非依存で無条件）、`EntitySaveMetadata` が QuickER 版 Repository の INSERT / BulkInsert / UPDATE の対象から除外する（`InsertProperties`＝全列−store-generated・`NonKeyProperties` からも除外）。**除外は「その方言の DB が値を採番するか」の能力フラグ `dialect_assigns_store_generated`＋`repositories` のときだけ**（2026-08-14 導入・2026-08-16 に方言名比較から能力フラグへ置換）＝採番するのは SQL Server だけなので、他方言のエンジンでは同じ列を通常列として全書き込み対象にする（`insertProperties = columns`・`nonKeyProperties` は PK を除くだけ）。フラグの正本は `RepositoryDialectVariables.AssignsStoreGenerated`（C# 側）で、**方言を持たないスコープ（InMemory / EF Core の固定部＝`repository_dialect` は単に `RepositoryDialects[0]`）では意味を持たない**ため、参照箇所は必ず `repositories` と併せて判定する（`RowVersionProperty` の XmlDoc は `repositories && 能力`／`repositories`／`in_memory`／EF Core の 4 アームで、インメモリは擬似版採番＋Optimistic 検証・EF Core は Fluent 側の版ガードを述べる。両アームは StoreGeneratedCapabilityGenerationTests が非ドリフトで固定）。sqlite 単独生成では図の `timestamp` が日時解決＝属性なしのため従来から通常列で、意味が変わるのは「マルチターゲットで属性付き Entity を SQLite エンジンが扱う」ケースのみ（下記「マルチターゲット × rowversion」）。DB が採番するため明示挿入は実行時エラー（`Cannot insert an explicit value into a timestamp column.`）になるのを防ぐ。**SELECT では取得する**（`SelectProperties`/`AllProperties` は不変＝並行性トークンは読める）。EF Core は Fluent の `IsRowVersion()` が既に store-generated として扱うため非適用・InMemory は SQL 非使用で無関係。属性定義は `[UnboundedBinaryColumn]` と同型の emit ゲートで常時出力（固定 infra が参照するため）
- **rowversion による楽観排他（2026-08-10）**: **rowversion 列の有無がそのままポリシー**（オプトインなし・列があれば全バックエンドで版チェックが効き、無い図の生成物・挙動は不変）。`UpdateAsync` / `SaveAsync` は省略可能引数 `ConcurrencyMode`（`Optimistic` 既定 / `ForceOverwrite`＝明示的 last-write-wins）を受ける。**利用者向けの意味論（「行なし」と「版が古い」の別・グラフ保存の全ロールバック・版の反映・バックエンド間の契約同一性・`DeleteAsync(id)` は版で守られない）の正本は `docs/code-generation.md`（.ja）の「rowversion による楽観排他」節**で、ここには実装の骨格と「壊すと静かに回帰する判断」だけを書く。**未定義値は入口で fail-fast**＝全 12 実装（ADO / リモートクライアント / InMemory / EF Core の各 Update＋Save×2）が先頭で共有ヘルパー `ConcurrencyModes.Validated` を通し `ArgumentOutOfRangeException`（内部の 2 値分岐は「Optimistic なら版ガード・さもなくば強制」のままで、検証がないと `(ConcurrencyMode)99` が黙って強制上書き側へ落ちる）。**単一 `UpdateAsync` の「行なし＝false」と「版が古い＝`SaveConflictException`」は実在確認で区別**し、`insertWhenUpdateMissing` も同じ線引き（行なし→INSERT 切替・版違いは競合＝INSERT へ倒すと主キー重複に化けるため）。**`SaveConflictException` は再試行の材料を構造化して持つ**＝`Reason`（`SaveConflictReason.NotFound` / `Modified` / メッセージのみのコンストラクタ経由は `Unknown`）・`EntityTypeName`・`Key`（2 ファクトリが充填＝メッセージ解析を不要にする）。**リモートでも復元する**＝409 の `RemoteError` へ `Reason` / `EntityType` / `Key` を載せ（この 3 つは SaveConflict 以外では null・欠落した旧ボディは `Unknown` へ退化）、クライアントが同じプロパティを持つ例外を組み直す。**Save フックの After はコミット前のため旧版が見える**。実装先＝QuickER 版 ADO は **`sqlserver` 方言のみ**（`WHERE ... AND [row_ver] = @original` ＋ `OUTPUT INSERTED` で版を回収し **`RowVersionCollector` がコミット後に適用**＝ロールバック時に幻の版を残さない。0 件時は `RowVersionConcurrency.RowExistsAsync` で実在確認〔問いは存在の有無だけなので専用の `EntitySaveMetadata.ExistsByIdSql`＝`SELECT CASE WHEN EXISTS (SELECT 1 …) THEN 1 ELSE 0 END`。全列 SELECT して捨てていた〕）・EF Core は `DbUpdateConcurrencyException` を `GetDatabaseValuesAsync` で分類し同じ契約へ変換（ForceOverwrite は `OriginalValues.SetValues(stored)` で再試行。**フックあり経路は版の反映をコミット後へ倒す**＝EF は `SaveChanges` 内で新版をエンティティへ書くため、退避した旧版を After の間だけ書き戻し `CommitAsync` 成功後に新版を再適用する＝After が例外を投げてロールバックしても「DB に無い版」が残らず同一インスタンスの再保存が偽競合にならない。同一プロパティ型のまま往復するので VO の Wrap は不要。フックなし経路は暗黙トランザクション＝対象外）・InMemory は単調増加 8 バイトの擬似版（big-endian）＋**`InMemorySaveStaging` の copy-on-write で保存単位を all-or-nothing 化**〔`InMemoryWriteScope` の Put/Insert/Remove と After 経路の `WriteColumnValue` は overlay（(型, キー)→スナップショット・null＝削除マーク）へ**ステージングするだけでストアを書かない**。読みは overlay 優先→ストアの合成なので、保存は自分の書き込みだけを見る＝同一保存単位内の二重更新競合・同キー二重 INSERT の DuplicateKeyError・Removed 後の Exists=false は従来どおり。first-touch でストアの行参照をベースラインへ記録し、全フェーズ成功後の `Store.Publish(staging, mode)` が lock 内で **①検証（ベースラインと現在行を `ReferenceEquals` 比較）→②一括適用→③版スタンプ解決**を行う。**①の判定順は (a) 重複キー →(b) 存否 →(c) 版で、存否が版より先**（2026-08-13 に入れ替え＝無くなった行に対する版比較は何も語れず、旧順では rowversion を持つ型が (c) で止まって「行が消えている」事実に到達できなかった。SQL Server の UPDATE 0 行を `ExistsByIdSql` で「消えた」と「変わった」に分ける二分と同一意味論）。(a)「ベース null＝この保存が挿入した行なのに現在は存在」は mode に依らず `DuplicateKeyError`・(b) は **mode にも rowversion の有無にも依らず**適用・(c) は残っている行にだけ効く＝**rowversion を持つ型かつ Optimistic のときだけ** `SaveConflictException.Modified`（版なし型と ForceOverwrite は検証なしで後勝ち）。**後勝ちは復活までは含まない**＝更新として開始した行を他者が削除していれば `SaveConflictException.NotFound` で失敗する〔さもなければ削除済みの行を UPDATE が蘇らせる。黙って捨てるのも不可＝rows は保存フェーズで計上済み・`AcceptChanges` も無条件のため「戻り値 1 件・RowState=Unchanged・ストアに行なし」になる（実測）〕。**staged が削除マークなら競合にしない**（既に無い行の削除は実 DB でも no-op・版の有無に依らない）。**Phase 2 の例外も Phase 3（After）の例外もステージングを捨てるだけ**＝ストアを 1 度も書いていないため巻き戻す対象が無く、After 待機中に割り込んだ他者の更新を消さない（旧 undo ジャーナル方式は無条件の書き戻しで他者の更新を消していた）。**版スタンプは版の値でなく行を記録**して公開後のスナップショットから読み直す＝After の `WriteBinaryColumnAsync` が版をもう一段進めても呼び出し元には最終版が渡る（旧方式は Phase 2 時点の版を配り、成功直後の再保存が偽競合になっていた）。版カウンタは戻さず `Interlocked.Increment`。単一 Update/Insert/BulkInsert/Seed は staging なし＝ストアへ直接・即時スタンプで挙動不変。**`ExecuteDeleteAsync` も staging を使う**＝カスケード走査が途中で `NotSupportedException`（循環カスケード）になっても部分削除を残さない（実 DB は文が丸ごとロールバックする）・公開は `ForceOverwrite`＝一括削除は条件を述べるだけで特定行の版を主張しないため版検証はしない〕・**リモートは `ConcurrencyMode` をリクエストへ載せ、Insert / Update / Save の応答が版対応表（`RemoteRowVersionEntry`＝CLR 型名＋`RemoteJson` でシリアライズしたキー）を運びクライアントが書き戻す**（共有ヘルパー `RemoteEntityGraph`。旧エンベロープ〔Mode 欠落〕は既定の Optimistic へ落ちる）。**同一エンティティに rowversion 列が 2 本以上ある図は生成時の診断エラー**（版の読み書きは全経路が `FirstOrDefault` で「1 型につき 1 本」を前提にしており、SQL Server も 1 テーブル 1 本しか許さない＝黙って生成すると「どちらが版か」が暗黙に決まった生成物が出て DDL 適用で初めて落ちる。判定は `CSharpTypeInfo.IsRowVersion`＝`[StoreGeneratedColumn]` の付与条件と同一・列型辞書が確定した後に検証）。**主キー列が rowversion の図も同じ検証で生成時エラー**（2026-08-13。DB 採番＝`[StoreGeneratedColumn]` により INSERT / UPDATE の対象から外れるため、生成 INSERT がキー列を送らず・行の同一性が更新のたびに変わる値に乗り・インメモリ辞書のキーも壊れる。DB 取込では自然に発生し得る構成なので黙って通さない）。既知の制限＝他方言に `rowversion` 型が無く対象外・`SqlBulkCopy` は生成値を返せず版未反映・`OUTPUT` 句はトリガーのあるテーブルで使えない（QuickER の DDL はトリガー非生成）・「行なし削除」は ADO 黙認／EF グラフ例外の既存非対称のまま・rowversion 列は `[DbColumnMeta]` トークンなし＝C# リバース対象外・生 SQL／バイナリ Stream アクセサは非対象。**VO 併用時（GenerateValueObjects=true）は版プロパティが VO 型**（例 `RowVerValue : ValueObjectBinaryBase`）になるため、リフレクション経由の版の読み書きは全経路で開き直す＝書き戻しは `SqlValueObjectActivator.Wrap`（ADO の `OUTPUT` 戻り・`RowVersionCollector.Apply`・InMemory の `StampRowVersion`・`RemoteEntityGraph.WriteRowVersion`）、読み出しは `SqlParameterValue.Unwrap`（InMemory の版比較・`RemoteEntityGraph.ReadRowVersion`＝転送は常に生 byte[]）。EF Core は反射代入を通らず Fluent の `.IsRowVersion().HasConversion(...)` 併記のまま動く。**DB 採番列は EditModel の入力必須にしない**＝rowversion 列は `IsRequired=false`＋ Mapper の Entity へのコピーは「入力があるときだけ代入」（未入力は現在値維持）。さもないと新規行の EditModel 保存が `"{列} has no input value."` で必ず失敗する。実 DB 検証は SqlServerConcurrencyRuntimeTests（Docker）／EfCoreConcurrencyRuntimeTests（Docker）／InMemoryConcurrencyRuntimeTests・RemoteConcurrencyRuntimeTests（CI 常時＝リモートはインメモリ実体を Kestrel で公開）と、VO 併用の ConcurrencyVo{SqlServer,EfCore}RuntimeTests（Docker）／ConcurrencyVo{InMemory,Remote}RuntimeTests（CI 常時）
- **マルチターゲット × rowversion（案 B・2026-08-14）**: サーバー＝SQL Server・ローカル＝SQLite のハイブリッド構成を組めるようにする「生成対応のみ」の対応（同期ロジックはアプリ側）。①**型統合**＝マルチ辞書のいずれかの方言解決が `IsRowVersion=true` の列は型不一致エラーにせず、その解決（`byte[]`＋`IsRowVersion`）で主辞書を統一する（`MultiDialectTypeReconciler.ReconcileRowVersionTypes`＝実効方言の指定順で先に行バージョンと解決した方言を採る・`DiagnoseTypeMismatches` は統一済み列 ID をスキップ）。統一した列は Info 診断 `CodeGen_Info_MultiTargetRowVersionColumns` で通知（同じ表記でも綴りで食い違いの中身が変わる＝`timestamp` は sqlite で `DateTime`〔日時の別名〕・`rowversion` は sqlite で `string`〔未知の型〕。どちらも統一先は同じ）。②**書き込み除外の方言ゲート**（上記 store-generated 節）。③**DDL 型変換**＝`CanonicalTypeKind.RowVersion` を新設し SqlServer が `rowversion`/`timestamp` を解析（`TryFormat` は代表表記 `rowversion`）・Sqlite の `TryFormat` は `BLOB` へ落とす（他 3 方言は `default: return false` のまま＝変換不能一覧に載る従来どおり）。落ちるとき `ColumnTypeConversion.MakeNullable` が立ち NOT NULL を解除する（ローカルは採番しないので未同期行が空になる。判定は「変換先を読み直しても RowVersion のままか」＝方言名で分岐しない。GUI は `ChangeTargetDbmsCommand` が NULL 許容の変更も同じ 1 履歴で巻き戻す）。**`CanonicalTypeTokenAttacher` は行バージョン列にトークンを刻まない**（`IsRowVersion` でスキップ＝中立トークンは「DB が採番する」を運べず、刻むと C# リバースがただのバイナリ列として復元して版ガードが黙って消える。既存の「rowversion 列は `[DbColumnMeta]` トークンなし」を新しい canonical 種別を足しても維持するための明示ガード）。④**契約 doc の非対称告知**＝実効方言に「採番するエンジン」と「しないエンジン」が混在するとき（`store_generated_dialects_differ`）だけ、共有契約の `mode` param doc 3 箇所と `ConcurrencyMode` enum へ「値を採番しないエンジンはこの引数を無視する」1 文を足す（契約は 1 回しか出ないため実装側 doc では補えない・単一方言の出力はバイト不変）。⑤**既知の乖離**＝SQLite 側は版ガードなし（`ForceOverwrite` は no-op）・ミラーの鮮度は誰も保証しない・`BLOB` を SQL Server へ戻しても `varbinary(max)`（非可逆）。InMemory / EF Core / リモートは現状維持（InMemory は SQL Server 代役＝擬似版採番・EF Core はマルチターゲットと排他）。フィクスチャ＝`MultiTargetRowVersionFixture.g.cs`（1 テーブル `sync_items`・sqlserver/sqlite）・実 DB 検証は MultiTargetRowVersionRuntimeTests（Docker＝版ガード維持＋keyed DI での同期最小形）／MultiTargetRowVersionSqliteRuntimeTests（CI 常時＝ミラー列の読み書き・変換 DDL の BLOB/NULL 許容）
- **双方向同期支援（GenerateSyncSupport・2026-08-16）**: サーバー＝SQL Server・ローカル＝SQLite のハイブリッド構成で「オフライン編集の再生（アップロード）＋サーバー変更の差分取得（ダウンロード）」を行う支援コードを生成する bool オプション（既定 false。CLI `--generate-sync-support`／GUI「双方向同期の支援コードを生成する」＝対象 DB が両方言のときのみ表示）。**利用者向けの意味論（競合ポリシー 3 択・削除伝搬・洗い替えの使い分け・blob 既定の挙動・後勝ちの取引・既知の割り切り）の正本は `docs/code-generation.md`（.ja）の「双方向同期の支援」節**で、ここには実装の骨格と「壊すと静かに回帰する判断」だけを書く。**対象は Repository 契約を持つ全テーブル（単一主キー）で、rowversion 列の有無は対象でなくモードを決める**（版あり＝増分＋競合検出／版なし＝後勝ち・全量スキャン）。生成時エラー＝実効方言が sqlserver+sqlite のちょうど 2 つでない／`GenerateRepositories` が false／同期可能テーブルが 0 件（`CodeGen_Error_SyncSupportRequiresEligibleTables`）。対象一覧は Info 診断（`CodeGen_Info_SyncSupportTables`）で FK 順のまま通知。**サーバー側の追加スキーマはゼロ**で、ローカルにだけ共有テーブル 2 つを `SyncJournal.EnsureCreatedAsync` が実行時 ensure する＝`quicker_sync_journal`（オフライン編集）と `quicker_sync_ack`（`(table_name, key_text)` 主キー・`server_row_version BLOB NOT NULL`＝サーバーが受理したアップロードに採番した版の**行単位の受理記録**）。①**アンカーは保存せず導出**＝再開点はローカルのミラー版列の `MAX`。正しさの前提は「サーバーの変更を rowversion 昇順で取得し、バッチをローカル 1 トランザクション（`SaveAsync(IEnumerable)`）で適用し、**ミラー版を書くのはダウンロードだけ**」の 3 点で、途中で落ちてもローカルは順序付きストリームの接頭辞を保持し、その最大値が次の再開点になる（どれか 1 つでも崩すと再開点が嘘になる）。3 つ目が抜けると順序付きストリーム外の版が未取得行を追い越して恒久取りこぼしになる（＝アップロードの書き戻しを廃止した理由・2026-09-11）。**ack はアンカーではない**＝行単位の記録で、そこから再開点を導く処理はどこにも無い（だからミラーへ書けない版をここへは書ける）。上限は `MIN_ACTIVE_ROWVERSION()`（未コミットのトランザクションを跨いだ読み飛ばしを防ぐ・1 パス 1 回）。差分 SQL の `@anchor` / `@ceiling` は NULL を取り得るため `(@anchor IS NULL OR ...)` で「制約なし」を明示し、**比較相手は `CAST(@anchor AS binary(8))` へ明示キャスト**する（生 SQL のパラメータは型なし＝NULL が nvarchar と推論され `Implicit conversion from data type nvarchar to timestamp is not allowed.` で落ちる）。②**FK 順**＝テーブルは図から求めたトポロジカル順（親→子）で、ダウンロードは親から drain し、削除伝搬とジャーナルの削除再生は逆順（子→親）（循環は解けた分だけ先に出し残りは宣言順）。③**ローカル変更の追跡はデコレータ方式**（`Journaling{Entity}Repository`）＝`ISaveHook` はグラフ保存でしか発火せず直接 CRUD を素通しするためフックでは穴が開く。全書き込み入口（Insert / Update / Delete / BulkInsert / SaveAsync×2＋除外列がある図では `Write{列}Async`）が記録する。**グラフ保存はカスケード全体を記録する**（2026-08-19）＝Runtime.Sync 固定の汎用走査 `SyncGraphRecorder`（2026-09-04・移設 P3 で per-entity 生成ミラーから置換＝保存側と「同じ走査の 2 実装」を構造的に解消。`EntityBaseCore.EnumerateCascadeChildren` をこの用途で public 化して保存側と同じ列挙を歩く・レジストリは全記述子〔構築時除外に依らない＝従来の静的生成と同挙動〕・未登録の中間型も降下は続け記録は登録型のみ）が子孫も記録する（Removed は cascadeDelete なら部分木を子から順に Delete・Added/Updated は Upsert・Unchanged は記録なしで cascadeSave なら子へ再帰。生成対象は「同期対象をルートとするカスケード閉包のうち自身または子孫に対象を含む型」）。**ルートの RowState しか見ない実装は 3 形の実バグになる**（実測で確定＝①カスケード子が上がらない ②同じランの削除伝搬が未記録のローカル子行を消す＝同期 1 回で子データが両側から喪失 ③カスケード削除の再生がサーバー FK に阻まれ恒久ハード失敗。SyncRuntimeTestsBase のカスケード 3 シナリオが固定）。**記録は journal-first**（生成 Repository が接続を内部管理し業務書き込みと同一トランザクションに乗れないため、意図を先に記録する）。業務側が失敗して残った余分なエントリは、アップロードが「ローカルの現在行を読み直して送る」ため無害化される（行が無ければ破棄＝`Discarded`・競合ではない）。**生 SQL（`ExecuteSqlAsync`）と `Query().ExecuteDeleteAsync` は記録対象外**（変更行が文・述語からは読めない＝docs 明記の割り切り）。**Save フックは素通し**＝`ISaveHook<T>` は従来どおり発火し、エンジンがダウンロードで適用する行に対しても発火する（`SyncSession` が抑えるのはジャーナル記録だけ）。④**アップロード**＝ジャーナルを (テーブル, キー) ごとに最新 1 件へ畳んで再生。版ありの original は **max(ミラー版, ack 版)**（`SyncTableBase.MaxRowVersion`＝big-endian 符号なし比較＝先頭ゼロを除いた有効バイト長→辞書式。null/空はどちらも「版なし」。**ack 優先固定は不可**＝掃除漏れでミラー > ack が残ると偽競合へ戻る）。max が null＝オフライン挿入としてサーバー存在確認後に `InsertAsync`（既にあれば `DuplicateOnServer`）・非 null は `WriteRowVersion(local, max)` してから `UpdateAsync(entity, Optimistic)`（**ack があればミラー版なしでも Update 経路**＝エコー未着のオフライン挿入が偽 `DuplicateOnServer` にならない。ガードはエンティティから版を読むので書いてから送る）・削除は max(`entry.OriginalRowVersion`, ack) を載せた `RowState=Removed` のスタブを `SaveAsync` へ（キーだけの `DeleteAsync` は無条件削除になるため使えない）。**競合ポリシーは 3 種の競合すべてに効く**（2026-09-03）＝`LocalWins` は版競合（ModifiedOnServer）だけでなく DuplicateOnServer（ForceOverwrite 更新→0 行なら Insert）・MissingOnServer（`MarkAdded`＋Insert＝**サーバーが消した行をローカル編集が復活させる**）も解決する。実体は `SyncTableBase.ForceOverwriteOrInsertAsync`（private protected＝公開 API 面は不変）で **LWW 再生と同じ本体を共有**＝「ローカルが勝つ」の意味が版あり/なしで割れない（旧実装は 2 分岐が policy を見ず、MissingOnServer×LocalWins は「毎回競合報告・削除伝搬はジャーナルが守る」の永久ループだった）。**`ServerWins` は落とす前にサーバー行を読み直して適用する**（2026-09-13・B4）＝ジャーナルを落とすだけでは「サーバーが変えていない行」のローカル編集が残り（ダウンロードが降ろすのは版が進んだ行だけ）、エントリも消えているため以後どのランも直さない。捨てる各キーを `Writer.GetByIdAsync` で読み、**版プロパティをローカル行の現在値〔ローカル行なしは null〕へ差し替えてから** `SyncTableBase.ApplyServerRowsAsync`（＝ダウンロードと同じ `ApplyBatchAsync` を通す＝ローカルに無いキーは挿入＝ローカル削除の取り消しもここで成る）で適用する〔**サーバーの版をミラーへ書くと①の 3 点目が破れて B1 と同型の恒久取りこぼし**になる。戻した行は次のダウンロードが順序どおり到達するまでミラー版なし＝ローカル新規行と同じ状態〕。適用は**エントリ削除より前**（途中で落ちても次ランが同じ適用を繰り返して落とす。逆順は「編集が残りエントリだけ消える」窓を作る）・FK 順は親→子・サーバーに行が無ければ何もせず削除伝搬へ委ねる（`PropagateDeletes=false` なら残る）。**適用した行は ack へ記録する**（2026-09-13・H3＝`ApplyServerRowsAsync` が版差し替え〔`WriteRowVersion`〕の**前に**読んだサーバー版を `SyncAppliedServerRow`〔KeyText × 版〕のリストで返し、engine が非空版だけ `RecordAckAsync`。順は **apply→ack→drop**〔ack 前に落ちれば次ランが同じ適用を繰り返して記録・ack 後 drop 前に落ちても再適用は冪等で ack も同値 upsert〕。記録しないと、ミラー据え置きの帰結として「この行のローカル内容＝サーバー版 V の内容」を語るものが端末に何も無くなる＝**復活行はサーバー版がアンカー以下でダウンロードが二度と来ない**ため再編集の original が null＝偽 `DuplicateOnServer`、適用後にダウンロードが落ちたケースは original が旧版のまま＝偽 `ModifiedOnServer` になる。掃除は既存 (i)〜(iv) のままで足りる〔長く残る ack はそれが正しい original で、ダウンロードがその行へ到達すればエコー判定→版のみ書き→(i) で消える〕。副作用として同ランのダウンロードは適用行をエコーと判定する＝内容の再適用が省かれ `Downloaded` に数えない）。`ISyncTable.ApplyServerRowsAsync` は既定実装が空リストを返す（外部実装の互換＝`DownloadAsync(context)` と同じ流儀）。mutation 実証済み（適用除去→新規 7 件だけ赤・版差し替え除去→アンカー安全性の 2 件だけ赤・`RecordAckAsync` 除去→ServerWins×ack の 2 件とアンカー安全性〔エコー件数の表明が ack に依存する〕だけ赤）。**削除再生は冒頭でローカル行を probe**（`LocalRowSurvivedDeleteAsync`＝行がまだ在れば「業務削除は着地しなかった」として Discarded・版あり/LWW の両経路）＝journal-first の「失敗エントリは無害化」が delete でも成立する（旧実装は probe なし＝FK 違反等で失敗したローカル削除を次の同期がサーバーで完遂し、削除伝搬が生き残ったローカル行まで消した）。**settle はエントリ単位で即時＋ack 先行**（2026-09-11＝ラン末尾一括の `settledIds` もラン内メモも撤去。upsert は `RecordAckAsync`〔`ServerRowVersion` が非空のときだけ〕→`RemoveAsync` の順、delete は `RemoveAcksAsync`→`RemoveAsync` の順＝どちらも「補助記録が先・エントリが後」。**両方残ったまま落ちても無害**＝次ランが ack 込みの original で同内容を再送してサーバーが再び受理する／delete の再送は行なし削除＝no-op。原理的に閉じられないのは「サーバー受理直後・ack 書き込み前」の窓だけで docs の既知の制限へ明記。逆順にすると『ack なし・ミラー旧』が残って偽競合へ戻る）。**`SyncJournal.RemoveAsync` の 500 件チャンク**（`InClauseChunkSize`＝方言エンジンの IncludeLoader と同名同値）は残る。**ミラー版の読み出し生成式（`BuildRowVersionRead`）は VO なら無条件 `?.Value`**＝列の NULL 許容宣言で分岐しない（マルチターゲット変換がローカル rowversion 列を意図的に NULL 許容へ落とす／削除記録はキーだけのスタブから読む、の両方で「NOT NULL 宣言でも実行時 null」が構造的に成立する。非 VO は `byte[]?` の型なりで元から安全）。SyncRuntimeTestsBase の LocalWins×Duplicate/Missing・削除 probe・600 件 settle の各シナリオと SyncVoRuntimeTests が固定・mutation 実証済み（分岐除去・probe 除去・`.Value` 復帰のいずれも該当テストだけが赤）。後勝ちは全テーブル一律「`Update(ForceOverwrite)`→行なしなら `Insert`・削除は無条件」（版の読取も実在確認もなし・`ConflictPolicy` 無視）。**成功後の版はミラーへ書かず ack へ記録する**（`SyncUploadOutcome.ServerRowVersion` / `UploadedWith` が engine へ運び engine が `RecordAckAsync`＝①の 3 点目。ミラーへ書き戻す実装は B1＝ダウンロードの恒久取りこぼし）。④'**ダウンロード側の突き合わせ**（2026-09-11）＝pendingKeyTexts はアップロード完了後に 1 回読み（削除伝搬と同じ集合を行適用にも使う）、ack はテーブルごとに `ReadAcksAsync` で読んで `SyncDownloadContext.AcknowledgedRowVersions` へ渡し、バッチ（版昇順）の行ごとに **(1) pending なら打ち切り**〔その行を含め以降を適用せず、テーブル順でそれより後ろの全テーブル（版なし含む）も同ランはダウンロードしない＝FK 子孫の粗い上位集合。打ち切りは `SyncResult.Truncations`（`SyncDownloadTruncation`＝自テーブル打ち切りは PendingKeyText・連動は TruncatedByTableName）で必ず露出する。先頭が pending なら applied=0 の明示分岐で正常終了＝進捗ガードの網に入れない〕→**(2) ack 一致（キー一致 AND 版 `SequenceEqual`）なら内容適用と blob コピーを飛ばし版だけ Suppress 内でミラーへ書く**〔Downloaded に数えない。**キーだけの照合は不可**＝アップロード後に他者が触った行を取りこぼす。ローカル行が消えていたら通常適用へフォールバック＝アンカーが進まず進捗ガードが誤爆するため〕→**(3) それ以外は従来どおり ForceOverwrite 適用**。適用は「到着順の連続ラン単位」で流す（content をため、エコー行の版書き込みの前に flush＝どのコミット境界でも接頭辞性が保たれる）。版なしテーブルは打ち切らず**スキップして続行**（アンカー非依存）だが、前段の打ち切りには連動する。engine 側は `DownloadCoreAsync`（private）が打ち切りを運び public な `DownloadAsync` が従来のタプルへ 1 行委譲、`UploadCoreAsync` は不要になり public `UploadAsync` へ統合（**単独呼び出しでも ack は DB から読む＝エコー最適化も偽競合防止も効く**）。④''**ack の掃除は 4 点**＝(i) テーブルのダウンロード完了後に「版を書いたキーのうち ack を持つもの」（`SyncDownloadOutcome.SettledAckKeyTexts`＝エコー・通常適用の両方。**コミット後に消す**＝コミット前に消してコミットが失敗すると『ack なし・ミラー旧』の窓が戻る。コミット後・削除前に落ちる逆窓は ack が残るだけで、次ランのエコー判定→版のみ書き→削除で自己回復＝安全側）・(ii) 削除エントリの settle 成功時（上記）・(iii) **削除伝搬が消したキー**（`PropagateDeletesAsync` が件数でなく `IReadOnlyList<string>` を返し engine が `RemoveAcksAsync`＝放置すると「サーバー削除→同キー再作成」が死んだ版を original に採って偽 `MissingOnServer` になる）・(iv) `RefreshAsync` がスコープ内テーブルの ack を全削除（何も消す前に）。**`SyncJournal.RemoveTableAsync` / `RemoveAllAsync` は ack に触れない**（未送信のローカル変更を捨てることは、サーバーが既に受理した内容について何も言っていない）。⑤**ループ防止**＝エンジン自身の書き込み（ダウンロード適用・ジャーナル掃除・版の書き戻し）は `SyncSession.Suppress()`（`AsyncLocal<int>` のカウンタ）で記録を抑制する（抑えないと降ろした行が即座にローカル変更として記録され、同じ行が 2 つの DB を往復し続ける）。⑥**削除伝搬はキー全比較**（サーバーの全キー集合とローカルの突合＝O(テーブル)・既定 ON・`SyncOptions.PropagateDeletes` で off）。⑦**差分ソース抽象**＝エンジンは `ISyncServerSource<TEntity, TKey>`（アンカー超の行を `SyncChangeBatch<TEntity>`〔行＋`HasMore`〕で返す・全キー列挙・上限取得・アップロード先 `Writer`・版なし用のキー順ページング `GetFirstPageAsync`/`GetPageAfterAsync`〔既定実装は NotSupported＝版ありソース互換〕）に対してだけ動き、直結実装（sealed 固定型 `DirectSyncSource<,>` / `VersionlessDirectSyncSource<,>`＝旧 per-entity `{Entity}DirectSyncSource` は 2026-09-04 廃止）は既存の生 SQL 面と既存 Repository だけで組む（新しい ADO コードを書かない）。`Writer` の型は**リモート面 `IRemoteRepositoryCore<,>`**＝再生が使うのは「ネットワークを越えられる操作」だけなので、直結リポジトリと HTTP クライアントが同じ位置に立てる。**継続の判定はソースが返す `HasMore`**（行数とバッチサイズの比較を呼び出し側で行わない）。**ダウンロードには進捗ガードがある**＝バッチを適用したのに再導出したアンカー（版なしはキー）が動かなければ `InvalidOperationException`（アンカーを無視するソースと版列が空の行は同じ形になり、放置すると同じバッチを永久に適用し続ける＝mutation でハングを確認済み）。**HTTP 経路**＝`GenerateSyncSupport && GenerateRemoteServices` のとき、サーバー側は既存のエンドポイントグループへ同期専用の 4 本（`SyncCeiling` / `SyncChanges` / `SyncKeys` ＋版なしテーブルにのみマップする `SyncPage`・操作名の正本は `RemoteSyncOperations`）が加わり、クライアント側は sealed 固定型 `HttpSyncServerSource<,>`（`HttpRemoteRepositoryCore` 派生なので `Writer` は自分自身＝固定ランタイム内の派生はシムを通らず Core 直付け）が記述子を受けて立つ（旧 per-entity `Http{Entity}SyncSource` は 2026-09-04 廃止）。**エンドポイントは差分ソースの薄い remoting**＝ハンドラは DI から `ISyncServerSource<,>` を解決して呼ぶだけで意味論の実装は 1 つ・**アップロードは新設せず既存の CRUD／保存エンドポイントを通る**・**ステートレス**（再開点は毎リクエストの anchor、上限は ceiling として送られる）。その裏返しとして**上限は呼び出し側の値**＝`SyncCeiling` が返した値を同じパスで送り返す限りにおいてのみ導出アンカーの保証が成立する（勝手に大きな ceiling を送ると、その下で実行中のトランザクションの行を恒久的に読み飛ばす）。バッチサイズは 0 以下を 400 で拒む（上限は設けない＝取り放題を止めるのはグループの認可）。⑧**パッケージと DI**＝固定エンジン（`SyncEngine` / `SyncJournal` / `SyncTableBase` 階層 / `SyncSession` / 結果・競合レコード／リモートサービス併用時は同期エンベロープと `HttpSyncServerSource<,>`）は第 7 の `QuickER.Runtime.Sync`（Core への ProjectReference のみ・PackageReference ゼロ）で、分割時は `Runtime.Sync.g.cs`。per-entity（記述子・デコレータ・直結／HTTP 差分ソース・DI 登録）は `Repositories.Sync.g.cs`。**DI は「ローカル半分」と「サーバー半分」に分かれ、転送経路の選択は 1 行の差**＝`AddGeneratedSyncEngine(localKey)`＋`AddGeneratedDirectSyncSources(serverKey)` または `AddGeneratedHttpSyncSources(baseAddress ｜ HttpClient ファクトリ)`（`AddGeneratedSyncSupport` は全直結構成のための合成）。キー引数は `null` で非 keyed 登録を指す。デコレータ差し替えは keyed / 非 keyed のどちらでも**元のファクトリを保ったまま**行う（元の登録が factory 登録であることに依存）。⑨**洗い替え（RefreshAsync）**＝同期対象テーブルを全消し（子→親）→サーバー全行の流し込み（親→子）。**適用子は新設せず中立面だけで構成**（削除は `ISqlExecutor.ExecuteSqlAsync`・書き込みは `IRepositoryCore.BulkInsertAsync`）・**カーソルは差分ソースの `GetChangesAsync` をバッチ末尾行の版で回す**ため追加エンドポイントなしで両経路が動く。未送信ジャーナルが残っていれば**何も消す前に** `SyncPendingChangesException` で拒否（`Force` のときだけ破棄）。**クラッシュ安全は per-table/per-batch トランザクション**＝生成 Repository は接続を内部管理し外部トランザクションに乗れないため単一トランザクション化は不可能（ADO 型を Sync バケットへ持ち込むことになり割に合わない）。代わりに「削除は子→親・書き戻しは親→子・版の昇順」でどのコミット境界も有効な再開点になる。`BatchSize` の既定は **2000**（差分同期の 500 と別＝1 バッチ＝1 コミットで速さはここから来る）。実測は通常同期比 **3〜4.5 倍**（比の上限は「サーバー読み出しは両経路共通」という構造から来る）。**`SyncRefreshOptions.Mode` の既定は `LastWriteWins`（＝全テーブル）**＝洗い替えは「ローカルをサーバーの姿にする」操作で、版なし子が版あり親を FK 参照する図では `Versioned` 既定だと親の全消しが FK に阻まれ**構造的に失敗する**（実測で発覚して補正した既定・版ありだけの図では両者同一）。⑩**無制限バイナリ列の除外（`ExcludeUnboundedBinaryColumns`）との併用（2026-08-17）**＝併用可で、運ぶかは実行時引数 `SyncOptions.IncludeUnboundedBinary`（既定 false）が決める。**行の転送は除外列を含まない**＝差分走査の SELECT は除外列があるとき `*` から明示列挙へ切り替える（生 SQL のエンティティ取得は SELECT に含まれた列を opportunistic にマップするため、`*` のままだと除外列まで降りてきてローカル UPDATE のガードに引っかかり同期が落ちる）。ON では行の転送後に列を 1 本ずつコピーする＝**面は `ISyncBinaryColumns<TKey>`**（ローカル＝記述子・サーバー直結＝`{Entity}DirectSyncSource`・サーバー HTTP＝`Http{Entity}SyncSource` の 3 実装で、エンジンはこの面しか見ない＝転送経路に依らず意味論は 1 つ。`ISyncTable.UnboundedBinaryColumnNames` と `ISyncBinaryColumns.UnboundedBinaryColumnNames` は記述子上で**同じ 1 つのプロパティ**が満たす＝エンジンが読む名前とコピーが回す名前がずれない）。**コピーは一時ファイル経由**（読みは渡されたストリームへ押し出し・書きは引き出すため両端を直結できず、かつ書き込みは長さを前もって要求する。O(チャンク) を保ったまま繋ぐのがファイル）。HTTP は既存のバイナリエンドポイントを再利用＝新規エンドポイントなし。**アップロード後はサーバー行を読み直してその版をランへ報告する**（`ReadServerRowVersionAsync`）＝blob の書き込みも行の版を進めるため、挿入・更新が返した版をそのまま報告すると④'(2) のメモ照合が外れ、同じランのダウンロードが自分の行を blob ごと取り戻す（実 SQL Server で mutation により実証）。**記録は `IncludeUnboundedBinary` に依らず常時**（何を送るかは送信側の判断で、生成時には決まらない）。**洗い替えの損失ガード**＝除外列があると `IncludeUnboundedBinary` か `DiscardLocalUnboundedBinaries` のどちらかが立っていない限り**何も消す前に** `SyncUnboundedBinaryLossException` で拒否する（未送信ジャーナルの拒否より前＝構造的な前提を先に見る）。⑪**版なしテーブルの後勝ち（2026-08-19）**＝`SyncOptions.Mode`（`Versioned` 既定＝版ありだけを同期し版なしには一切触れない〔記録は続く〕／`LastWriteWins`＝全テーブル対象・ラン全体で意味論を 1 つに統一）。版なしダウンロードは**毎回キー昇順の全量スキャン**（`SELECT TOP … WHERE key > @afterKey ORDER BY key`・O(テーブル)/ラン＝大きいテーブルは rowversion 追加へ誘導）で、版ありは後勝ちランでも増分ダウンロードを維持する（意味同一・転送効率のみ）。ceiling は**スコープ内の版ありテーブル**にだけ問う（全版なしなら null）。固定基底は**サブクラス階層**＝`SyncTableBase`（共有＝バッチ適用・削除伝搬・blob コピー・LWW 再生 `UploadLastWriteWinsAsync`＋サーバー版の読み出し `ReadServerRowVersionAsync`。旧 `OnLastWriteWinsUploadedAsync` フックは版書き戻し廃止に伴い撤去）← `SyncTable`（版あり）/`VersionlessSyncTable`（版なし）で、per-entity は `SyncTableDescriptor<TEntity,TKey>`（テーブル名・ルート名・SQL・キー/版 Func・バイナリ列）を生成データとして持ち、版の有無で sealed 固定型 `SyncTable<,>` / `VersionlessSyncTable<,>` を選ぶ（2026-09-04 descriptor 化＝旧 `{E}SyncTable` 派生クラスは廃止。Journaling デコレータは P2 の契約引き上げを受けて CRUD/Save/CheckUniqueness 転送が基底へ集約され、per-entity には named query・blob アクセサの転送＋記録だけが残る。DI は型引数付きヘルパ AddSyncTable/AddDirectSource/AddHttpSource の 1 行/エンティティ）。除外は 2 層＝**ラン単位 `SyncOptions.ExcludedEntityTypes`**（記録は継続・未知型は ArgumentException で黙殺しない）と**構築時 `AddGeneratedSyncEngine(…, excludeFromSync:)`**（＝ローカル専用宣言。デコレータで包まず記述子も登録しない＝記録ゼロ・全ラン対象外。旧イディオム「ローカル専用は版列なしテーブルへ」は撤廃済みでこちらが正）。残留エントリの掃除は `SyncJournal.RemoveTableAsync`/`RemoveAllAsync()`（実行中に呼ばない・ack には触れない＝④''）。未送信拒否・Force 破棄・blob 損失ガード・ServerWins の全破棄はいずれも**ランのスコープ内のみ**集計する。⑫**フィクスチャと検証**＝`SyncFixture`（親子 2 テーブル＋版なし子 `sync_notes`〔版あり親を FK 参照する混在トポロジ〕・親テーブルに除外列 `attachment`・リモートサービス併用の 2 ファイル構成）。実 DB 検証は共通基底 `SyncRuntimeTestsBase`（全シナリオ＝blob 交差 10・LWW 系 10・洗い替え・カスケード 3・アップロード×ダウンロードの噛み合わせ 6〔B1 の取りこぼし・競合行の内容保持・他者更新の非エコー判定・打ち切りの FK 安全性・解決後の追いつき・削除競合の非復活〕・**ack 系 5**〔打ち切り裏の再編集＝偽 ModifiedOnServer／ダウンロード例外後の再編集＝偽競合なし＋`Downloaded==0`／打ち切り裏のオフライン挿入の再編集＝偽 DuplicateOnServer／打ち切り裏の行のローカル削除＝偽 DeleteConflict／削除伝搬の ack 掃除＝偽 MissingOnServer〕・**ServerWins の読み直し 4**〔サーバー未変更行の復元／ローカル削除行の復活〈削除行の版がアンカーの下に潜る形で作る〉／サーバー削除行は削除伝搬が決着／ミラー版据え置きのアンカー安全性＋適用行のエコー判定〕・**ServerWins×ack 2**〔復活行の再編集＝偽 DuplicateOnServer／適用後にダウンロードが落ちたあとの再編集＝偽 ModifiedOnServer〕を含む）＋転送別の派生 2 つ＝SyncSqliteRuntimeTests（直結・CI 常時＝サーバー役も SQLite で版採番と版ガードを代役）／SyncHttpRuntimeTests（HTTP・CI 常時＝in-process Kestrel・アンカーの Base64 往復・400 拒否・サーバー不在）、および SyncSqlServerRuntimeTests／SyncSqlServerHttpRuntimeTests（Docker＝本物の rowversion・`MIN_ACTIVE_ROWVERSION()`・実 `varbinary(max)`・409 競合）。加えて SyncConstructionExclusionTests（構築時除外・登録時 fail-fast）と SyncRefreshBenchmarkRuntimeTests（**時間は表明せず** `ITestOutputHelper` へ出力し、表明は 3 経路の内容ダイジェスト一致のみ）。mutation 実証済み＝スコープフィルタ除去・LWW 分岐除去のどちらも該当シナリオだけが赤になり、ack 側も「アップロードのミラー書き戻し復活→B1＋ack 系 1」「replay をミラーのみ→ack 系 4」「ack upsert 除去→ack 系 4＋エコー最適化 3」「ダウンロードの ack 読み込み除去→`Downloaded==0` 系のみ」「掃除 (iii) 除去→ack 系 1」と切り分かる。**max 規則そのものの防御は単独 mutation では観測できない**（ack 優先固定へ変えても掃除 (i) が ack を消すため差が出ず、(i) も同時に壊して初めて偽競合になる＝理屈上の二重化として保持する）。
- **Save フック（ISaveHook）**: グラフ保存（`SaveAsync` の単一/複数の 2 形態のみ・直接 Insert/Update/Delete と BulkInsert は素通り）の各操作の前後に処理を差し込む機構。**オプションなし・常時生成**（未登録なら完全 no-op＝既存挙動不変）。契約は Core（`ISaveHook<TEntity>`＝両メソッド既定実装付き・`SaveOperation`・`ISaveHookContext`・`ISaveHookRegistry`/`ISaveHookInvoker`）。DI 登録は `ISaveHook<T>`（0..N）＋各 `AddGenerated*` が `TryAddScoped<ISaveHookRegistry>`（keyed 版はファクトリで `GetService` 明示）・Repository 基底/具象に省略可能な第 2 引数 `ISaveHookRegistry? saveHooks = null`（手動 new は不変）。**DI なし経路も公式サポート**＝`SaveHookRegistry`（`new SaveHookRegistry().Add<T>(hook)` の連鎖でリポジトリのコンストラクタへ渡す。追加順に発火＝DI 版と同じ意味論・型消去した辞書＋型付き `SaveHookInvoker<T>` でリフレクションなし・**組み立てはスレッドセーフでない**＝リポジトリへ渡す前に全部追加し以後は読み取り専用）／DI 経路の登録糖衣 `AddSaveHook(services, object hook)`（インスタンスが実装する `ISaveHook<TEntity>` を全部走査して singleton 登録＝**エンティティ型ごとの分岐を登録コードから消す**。1 つも実装していなければ「黙って何も登録しない」を避けるため `ArgumentException`）。**Before は `EntityGraphSaver` の RowState 分岐で発火**（ExecuteAsync 内でないため update→insert 切替の二重発火を回避）・登録順で最初の `false` で短絡＝その 1 件を単独スキップ（`HashSet<EntityBaseCore>(ReferenceEquality)` の skip 集合を `AcceptChanges` へ渡し RowState 据え置き）。整合はフック実装者責任＝削除は子から順のため「root だけ false」は子が消え root が残る・矛盾スキップは FK 制約→例外→全ロールバック。**After はコミット前＋同一トランザクション参加の `ISaveHookContext`**（`WriteBinaryColumnAsync`＋ファイル糖衣・`ExecuteSqlAsync`＝Save と blob/監査のアトミック性・context の型は After 対象エンティティ型に束縛・除外列なし図の WriteBinary は NotSupported）・`insertWhenUpdateMissing` は Before=Update / After=実操作 Insert。実装先差分＝ADO は各 DML 直後に After（`SqlSaveHookContext`＝進行中 (connection, transaction) 共有）・EF Core は SaveChanges 後に一括（`EfCoreSaveHookContext`＝バイナリ NotSupported・ExecuteSql は対応）・InMemory は擬似（バイナリはストア・ExecuteSql NotSupported・**After 例外時もストア変更は残らない**＝実トランザクションはないが `InMemorySaveStaging` の copy-on-write で保存単位を all-or-nothing 化＝全書き込みをステージングし全フェーズ成功後に `Store.Publish` が一括公開するため、失敗した保存は一度もストアへ届かない。After の `WriteBinaryColumnAsync` はステージング済みの行の上に積む＝保存フェーズの更新と blob が同じ単位で公開される。**ストアが持つのは行であってグラフではない**＝Put / Insert のスナップショットは**列だけを写し取る**〔`InMemoryDataStore.CopyColumns`＝新規インスタンスへ `EntitySaveMetadata.AllProperties` を代入し、ナビゲーションはコンストラクタ既定のまま（コレクションは空・単一参照は null）。byte[]（と内包 byte[] の VO）だけは複製して共有しない＝旧 `EntityBaseCore.Clone` の値意味論を列レベルで維持。**グラフごとの JSON 複製をやめた理由は 2 つ**＝(a) 子を複製した直後に捨てていたため n ノードのグラフ保存が各ノードの部分木を複製する超線形コストだった (b) 落とさないと Include なし取得が RowState=Added の子を返し〔SQL 版はナビ空〕・その戻り値の再保存が子を二重 INSERT する。読みは Include が子行から組み立てるため影響なし〕）・リモートは**サーバー側 DI で発火**（**スキップは保存応答で伝搬する**＝サーバーは保存成功後のグラフから「`HasChanges` かつ非 Removed＝スキップ集合」を版対応表と同じ走査・同じ照合で収集し `RemoteSaveResult.Skipped`〔`RemoteEntityRef`＝CLR 型名×`RemoteJson` キー・既定 null で旧ボディ互換〕へ載せ、クライアントの `AcceptChanges` が一致行を `MarkUnchanged` しない＝直結と同じく未保存のまま残る。旧「クライアントは Unchanged 確定」の既知制限は解消済み）。EF Core/InMemory/マルチターゲット/UseRuntimePackages と直交。実 DB 検証は SaveHook{Ado,EfCore,InMemory,SqlServer,Remote}RuntimeTests（パリティは `SaveHookRuntimeTestsBase`・CI 常時）
- **グラフ取得糖衣（IncludeGraph・2026-08-28）**: `Query().IncludeGraph()`＝グラフ保存が辿るのと同じ子方向カスケード閉包を Include ツリーへ展開する per-entity 生成拡張メソッド（静的クラス `SqlQueryExtensions`・契約 interface 不変・オプションなし常時生成＝Repository 契約が出る構成のみ・カスケード子を持たない葉は `=> query;` の no-op）。**利用者向けの意味論の正本は `docs/code-generation.md`（.ja）の「グラフ取得（IncludeGraph）」節**。**Include ツリーは「1 ナビゲーション 1 ノード」が不変条件**（2026-08-28 のデデュープ修正で確立）＝実行器は子コレクションを置換でバインドし（SQLite IncludeLoader / InMemory IncludeAttacher）SQL Server は同名 JSON プロパティが衝突するため、同一ナビの重複ノードは先行の孫を静かに消すか実行時エラーになる。Include（2 形）/ ThenInclude（2 形）/ AddIncludeNodes の全経路が `SqlQuery<T>.MergeInclude`（internal 静的＝判定の単一正本・PropertyInfo 照合は参照一致＋Name×DeclaringType フォールバック）を通って既存ノードへマージし、EF Core 流の分岐 Include（`Include(A).ThenInclude(B)`＋`Include(A).ThenInclude(C)`）が 4 実行器で成立する。**`AddIncludeNodes` はツリーをディープコピーして取り込む**＝デデュープは既存ノードの再利用なので、コピーしないと `IncludeGraph().Include(...).ThenInclude(...)` が生成拡張の static 共有ツリーを変異させ以後の全クエリを汚染する（mutation 実証済み＝デデュープ除去で 9 件赤・コピー除去で汚染が他テストクラスへ波及し 8 件赤。IncludeGraph ランタイムスイートの重複 Include マージ／兄弟分岐／共有ツリー不変性シナリオが固定）。閉包計算（`CSharpGenerationModelBuilder.IncludeGraph.cs`）が組んだツリーを `SqlQuery<T>.AddIncludeNodes`（infra_visibility）へ直接渡す構造は不変。閉包はナビ宣言順 DFS の edge-skip（パス上に既出のテーブルへ向かう辺は展開しない・スキップした辺は Info 診断 `CodeGen_Info_IncludeGraphSkippedNavigations` で名指し）＝自己参照・相互参照ナビは空のまま返る。実行器 4 実装は無変更。実 DB 検証は IncludeGraph{SqlServer,Ado,EfCore,InMemory}RuntimeTests（3 階層チェーン＋取得→編集→保存の往復・パリティは `IncludeGraphRuntimeTestsBase`）と自己参照 edge-skip の IncludeGraphSelfReference{SqlServer,Ado,InMemory,EfCore}RuntimeTests（EF Core セルは FK 列の VO 型統一〔次項〕で 2026-08-28 に NotApplicable から昇格済み＝QueryFixture の自己参照テーブル nodes を使う）
- **FK 列の値オブジェクト型統一（2026-08-28）**: GenerateValueObjects=true のとき、リレーションの子側列は**親側列の VO 型を共有**する（`ship_customer_id` → `CustomerIdValue`＝「同じ識別子は同じ値型」。利用者向けの規則は docs/code-generation.md（.ja）の値オブジェクト節が正本）。**動機は EF Core の制約**＝モデル検証は FK と参照先 PK の **CLR 型一致**を要求し HasConversion は判定に関与しないため、列名ごとの別型では FK 列名≠PK 列名の図（自己参照では必然）で生成 DbContext がモデル検証ごと落ち、その図の EF Core 経路が丸ごと使用不能になる（**Fluent 側では原理的に直せない**＝生成時の型解決だけが修正面。シャドウ FK 化は実プロパティへ値が入らずパリティを壊すため不可）。実装は「列 Id → VO キー」の**単一マップ**（`CSharpGenerationModelBuilder.ValueObjects.cs` の `BuildValueObjectKeys`）で、VO を使う全経路（Entity プロパティ・EditModel・EF Fluent・DSL クエリ束縛・同期記述子）は `ResolveValueObject` 経由で自動追従する＝**列名→VO 型の独自解決を下流に作らないこと**。チェーン（FK の FK）は不動点まで・循環は「循環に属する列の親辺を全部落としてから解決」＝走査順非依存の決定性。同一子列が異なる型へ解決される親を複数参照は生成時エラー（`CodeGen_Error_ValueObjectForeignKeyTypeConflict`）・下地 C# 型が食い違うペアは統一しない・統一列は Info 診断（`CodeGen_Info_ValueObjectForeignKeyUnified`）で名指し（型名が変わる破壊的変更のため）。**グループの定義の正（型・長さ・PK 優先判定）は自列名でそのキーを名乗る native 列に限定**（寄ってきた FK 列が PK のとき VO 名が FK 列名に化けるのを防ぐ）。FK 列名＝PK 列名の図と VO 無効の図は出力バイト不変。検証は ValueObjectForeignKeyUnificationTests（統一・チェーン・循環・退化エラー・Info）／ValueObjectForeignKeyEfCoreModelTests（生成→コンパイル→実 `DbContext.Model` アクセスでモデル検証が通ることを固定）
- **EF Core モード（GenerateEfCoreRepositories）**: 既存 Entity をそのまま EF Core に乗せる方言非依存の QuickErDbContext＋EF Core 版 Repository を生成し、DI 登録（AddGenerated{方言}Repositories ⇔ AddGeneratedEfCoreRepositories）の差し替えだけでQuickER 版 Repository と交換できる。GenerateEfCoreRepositories=false のとき生成物に EF Core への依存は一切出ない（using 含む）。スキーマ作成は従来どおり DDL 生成の責務で、EF Core は既存スキーマへの接続専用（Migrations / HasColumnType は範囲外）
- **DB 定義メタ属性（`[DbColumnMeta]` / `[DbTableMeta]`）**: 生成 Entity を「DB 定義の自己記述ドキュメント」にする定義用メタ（将来の C#→ErDiagram リバースの布石）。列に方言中立の型トークン（`CanonicalTypeToken`。例 `string(50)` / `decimal(10,2)` / `int32`・小文字・-1=max）と説明、テーブルに説明を刻む。付与は `IncludeDataAnnotations` ON かつ Entity 生成時のみで**対象 DB・Repository/EF Core 設定に依らない**。**`[Column]` はこのオプションの対象外＝常時付与**（2026-09-10）＝永続化の構造マッピングであり、**生成ランタイムの列判定 2 箇所（`EntityBaseCore.GetValueProperties`＝`HasSameValues` / `GetValueHashCode` が読む値プロパティ集合・`EntitySaveMetadata.Build`＝INSERT/SELECT/UPDATE の SQL と InMemory の列コピーが読む列集合）が `[Column]` を許可リストとして読む**のが正本（旧「`DeclaringType != EntityBaseCore` かつ非ナビ」の除外リストは、拡張シム `EntityBase` や具象エンティティ partial へ足した setter 付きプロパティを実在しない SQL 列に化けさせた＝InMemory は緑のまま実 DB だけが Invalid column name で落ちる）。帰結＝**手書きプロパティは `[Column("実在列名")]` を付けたときだけ列**（オプトイン）で、`[Table]` / `[Key]` は従来どおり `IncludeDataAnnotations` 側＝Repository 契約生成時の必須条件も従来どおり。using は Entity／Runtime／Repository／EfCore／InMemory の各バケットで `System.ComponentModel.DataAnnotations.Schema` を無条件要求（`GeneratedFileUsings`）。ExtensionShimAcceptanceTests の 2 本（シム追加プロパティは列でない／`[Column]` 付き手書きプロパティは列になる）と annotations OFF 生成テストが固定。トークンは図の方言 `ITypeCatalog.TryParse` → `CanonicalTypeToken.Format` を `DiagramCodeGenerator` の後処理（`CanonicalTypeTokenAttacher`）で主辞書へ付加する（マッパ実装は無変更・解析不能な自由記述型はトークン null＝属性省略）。実行時型付けの `[SqlColumnType]`（SQL Server 専用・Size ガード）とは**責務分離**で無関係。canonical 由来のため可搬図では方言に依らず同一トークン（PortableFixtureDialectIndependenceTests が保証。**可搬フィクスチャの文字列は Unicode で統一すること**＝Ansi/Unicode 差でトークンが割れるため。lessons.md 参照）
- **層別フォルダ出力（LayeredOutput・2026-08-18）**: 分割生成のファイルを DDD 風の層フォルダへ振り分ける bool オプション（既定 false。CLI `--layered-output`＋層フォルダ 4 フラグ／GUI「層別フォルダへ振り分けて出力する」チェック＋パス 4 欄〔サーバー欄はリモートサービス ON のみ表示・ON にすると出力モードが分割へ切り替わる。含意は両方向で、ラジオは常時操作可＝「1 ファイルにまとめる」を選ぶと層別チェックが外れる〕。ON は SplitFilesByCategory を自動含意し、生成側の分割判定は `CodeGenerationOptions.EffectiveSplitFilesByCategory` へ一本化）。バケット→層の対応は固定＝domain（Entity / VO / 契約 `Repositories.g.cs` / インライン時の `Runtime.g.cs`。契約はDDDのポート＝EditModel→契約依存が「プレゼン→ドメイン」参照に収まり、Runtime コア同居はパッケージモードで domain csproj が QuickER.Runtime を参照する構造と対称）・presentation（EditModel / Mapper）・infrastructure（方言実装 / EntityFrameworkCore / InMemory / Sync / Http と対応する `Runtime.{...}` 固定 infra）・server（`RemoteServer.g.cs`＋`Runtime.AspNetCore.g.cs`＝FrameworkReference 制約で通常のクラスライブラリ不可）・`*.g.md` は out_dir 直下（どの csproj にも属さない。**汎用オプション `ApiDocsSubdirectory`**〔CLI `--api-docs-subdir`／GUI「出力先サブフォルダ」欄＝GenerateApiDocs 連動〕で docs/ 等のサブフォルダへ移せる＝層別に依らず全出力モードで有効・検証は LayerDirectoryValidator を共有・エラー文言／resx キーは「出力サブフォルダ」へ一般化済み〔`CodeGen_Error_InvalidOutputSubdirectory`〕）。参照方向は presentation→domain←infrastructure←server。層判定の正本は `GeneratedFilePlanner.LayerOf`（**Buckets＋ContractOnly＋MultiDialect から導出**。ContractOnly 単独では EF Core／インメモリ単独構成の契約ファイルが方言実装レイアウトを通らず ContractOnly=false で出るため誤分類する＝MultiDialect 条件が必須）。フォルダは層単位の相対パス 4 オプション（`DomainLayerDirectory` 等・既定 Domain/Presentation/Infrastructure/Server・複数階層可＝`MyApp.Domain/Generated`・空白は既定へフォールバック）で、検証は生成前診断と `GeneratedFileWriter` の防御が `LayerDirectoryValidator`（単一正本）を共有（絶対パス・ドライブ指定・`..`・不正文字を拒否。診断は名前空間検証と同じく**使われる層のみ**＝Plan から導出）。**名前空間の既定は層フォルダに追従する**（2026-08-18 の追加決定＝当初の「配置のみ変更・バイト不変」を同日中に置換。層ルート＝フォルダパスの `/`→`.` 変換〔`LayerNamespaceRoot`。`MyApp.Domain/Generated`→`MyApp.Domain.Generated`〕・各バケットは `{層ルート}.{DefaultSuffix}`＝domain は `.Entities`/`.ValueObjects`/`.Repositories`/`.Runtime`・presentation は `.EditModels`/`.Mappers`・server は `.RemoteServer`/`.AspNetCore`。**インフラ層は固定 infra と per-entity を系統ごとに同一サブ名前空間へ統合**〔`{I}.SqlServer`＝Runtime.SqlServer.g.cs＋Repositories.SqlServer.g.cs。方言間は SqlExecutor/EntitySaveMetadata 等の同名型があるため単一名前空間化は構造的に不可〕・方言実装が契約名前空間の下にぶら下がる通常分割のねじれも解消・`RootNamespace` は導出既定に現れない。明示の名前空間オプションは従来どおり最優先。名前空間として成立しないフォルダは、その層で導出が実際に使われる場合のみ診断エラー〔`LayerNamespaceDerivationUsed`＝全バケット明示指定なら許容〕）。**固定 infra の可視性は層別出力で public**（2026-08-18・第 7 次指示書対応＝多アセンブリ配置では `internal` な Owner/OwnerModel・IncludeNode・CascadeNavigation 等を別層の生成物が参照できず 418 件のビルドエラーになる。既存の `infra_visibility` テンプレート変数を `LayeredOutput ? "public" : "internal"` で切替＝**パッケージ配布と同一規則**〔同じ型は QuickER.Runtime で既に public・IncludeNode は公開型 SqlQueryPlan の契約の一部＝「公開 API に出さない」という指摘の受け入れ条件 3 は既存仕様と矛盾するため反証済み〕。InternalsVisibleTo 生成は不採用＝アセンブリ名は確実に知り得ない。型の層移動も不成立＝IncludeNode/CascadeNavigation は domain 側の SqlQuery/EntityGraphSaver 自身が参照する）。**不変条件＝層分け ON/OFF でスキーマ依存ファイルは namespace 宣言・using 行以外一致・Runtime 系はさらに可視性のみ差・非層別モードの出力はバイト不変**（LayeredOutputTests が固定・.g.md のファイル一覧表は導出後の実名前空間を載せる）。**多アセンブリ検証**＝生成物を層ごとに別 Roslyn コンパイル＋参照グラフ（Infrastructure→Domain・Presentation→Domain・Server→Domain+Infrastructure）で組む 4 プロジェクト分割コンパイルテスト（GeneratedCodeCompilationTests・mutation 実証済み＝単一コンパイルでは internal が見えて境界越えを検出できない盲点を塞ぐ）。モード切替で旧位置のファイルは削除しない（docs 明記）。**生成コードのサブフォルダ（CodeSubdirectory・2026-08-27）**＝`.g.cs` を層フォルダ／出力ディレクトリのさらに 1 段下へ出す相対パス（既定 null＝オプトイン・CLI `--code-subdir`／GUI「出力先」カードの出力先パス直下の行〔＝メインとサブを隣接させる。出力モード・層別に依らず常時表示〕）。**層別・分割・非分割の全出力モードで有効**で、生成コードと手書きコードを同一 csproj 内で分けるためのもの（SDK 形式は `**/*.cs` を暗黙 glob するため csproj 変更不要）。**名前空間には一切追随しない**＝`ResolveLayerDirectory`（`LayerNamespaceRoot` が読む）へは混ぜず、合流点は `GeneratedFilePlanner.Plan` の出口 1 箇所（内部は `PlanCore`＋`ApplyOutputDirectories`）だけ＝**非分割は PlanCore が早期 return するため、分割パス末尾で付与すると素通りする**（旧 `ApplyLayerDirectories` の位置）。追随させると手書き partial が `.Generated.` 名前空間を名乗る羽目になるのが非追随の理由で、逆に名前空間へ出ないため**識別子検証は課さない**（`generated-code` も可＝既存の一般則「名前空間導出に実際に使われる値だけ検証」の帰結）。`.g.md` は追随しない（置き場の正本は `ApiDocsSubdirectory` だけ＝暗黙フォールバックを作らない）。検証は `LayerDirectoryValidator` 共有で出力モードに依らず効く。**不変条件＝サブフォルダの有無で生成テキストはバイト不変（配置だけが変わる）**（CodeSubdirectoryTests・mutation 実証済み＝`ResolveLayerDirectory` へ混ぜると当該 3 テストが赤）。サンプルは移行しない（ec-order / ec-order-remote は `--out` を `Generated/` へ向ける従来構成のまま＝層別出力を使っておらず移行は別サイズの作業）。**HTTP クライアントの派生バケット分離（同日・分割モードの構成変更）**＝`Http{Entity}RemoteRepository`・`AddGeneratedHttpRemoteRepositories`・OwnedHttpClient は契約ファイルから独立バケット Http（テンプレートガード `render_http_client`）へ常時分離し、分割時は `Repositories.Http.g.cs`（層分けでは infrastructure 行き）・非分割時は従来位置に同居（本体スペックへ Http バケット同居＝出力バイト不変）。**名前空間は契約と同一の `{RepositoryNamespace}` に据え置き**（EfCore 等の導出サブバケットと意図的に別規則＝サブ名前空間にすると型 FQN と非分割出力まで破壊されるため。ファイル構成のみの破壊に留める）。契約ファイルの HTTP 系 using は `GeneratedFileUsings` の Repository ケースへ足した `spec.EmitSharedInfra` ゲートで落ち（固定 infra を持つ Runtime.g.cs・非分割本体・パッケージソースのコアは従来どおり保持）、クライアント固定基底（`HttpRemoteRepositoryCore` / RemoteJson）は従来どおり Runtime コア在住
- **ランタイム配布の折衷（インライン既定＋NuGet パッケージ参照モード）**: 生成コードのスキーマ非依存な固定コード（ランタイム）は既定で従来どおりインライン出力（自己完結・依存最小）。`UseRuntimePackages`（CLI `--use-runtime-packages`／GUI チェックボックス）で固定コードを出力せず、NuGet パッケージ `QuickER.Runtime`（共通基盤＋方言中立契約・依存ゼロ）／`QuickER.Runtime.SqlServer`／`QuickER.Runtime.Sqlite`（各方言エンジン・ADO 依存は方言別に排他）／`QuickER.Runtime.EntityFrameworkCore`（EF Core 共通部品・EF Core Relational 依存・ADO/DI なし）／`QuickER.Runtime.InMemory`（インメモリ基盤・依存ゼロ＝BCL のみ・Core への ProjectReference だけ）／`QuickER.Runtime.AspNetCore`（リモートサーバーの固定エンジン・PackageReference ゼロ＋`Microsoft.AspNetCore.App` の FrameworkReference のみ）／`QuickER.Runtime.Sync`（双方向同期エンジン・依存ゼロ＝BCL のみ・Core への ProjectReference だけ）への参照で賄うモードに切り替わる（**7 パッケージ**）。**分割生成（SplitFilesByCategory）のファイル構成はパッケージと 1:1 対称**＝固定ランタイムは `Runtime.g.cs`（`{Runtime}`＝共有基盤＋方言中立契約）／`Runtime.SqlServer.g.cs`・`Runtime.Sqlite.g.cs`（`{Runtime}.{方言}`）／`Runtime.EntityFrameworkCore.g.cs`／`Runtime.InMemory.g.cs`／`Runtime.AspNetCore.g.cs`／`Runtime.Sync.g.cs` の 7 系統へ集約し、`Repositories*` 系（＋`RemoteServer.g.cs`）は per-entity 契約・実装・DI 登録・DbContext・per-entity エンドポイントなどスキーマ依存物だけへ純化する（命名は「ファイル名・名前空間サフィックス＝パッケージ名サフィックス」の単一規則＝**EfCore 略記は廃止**〔旧 `Repositories.EfCore.g.cs` / `.EfCore` 名前空間〕。**C# の型名は別軸で不変**＝`EfCore{Entity}Repository`・`QuickErDbContext`・`AddGeneratedEfCoreRepositories`。Runtime 系は有効にした機能の分だけ出る＝方言ファイルは QuickER 版 Repository 生成時・EF Core / InMemory / AspNetCore は各オプション〔GenerateEfCoreRepositories / GenerateInMemoryRepositories / GenerateRemoteServices / GenerateSyncSupport〕有効時）。この構成のため `UseRuntimePackages` の意味は「**Runtime 系ファイルを 1 本も出さず、生成コードの using が固定パッケージ名前空間（`QuickER.Runtime*`）を指す**」だけになり、`Repositories*` 系はモードの on/off で内容不変（`SplitRuntimeSymmetryTests` が「Runtime 系ファイルの型集合＝対応パッケージの型集合」「Runtime 系にスキーマ依存型が混ざらない」「パッケージモードで Runtime 系が 1 本も出ない」「スキーマ依存ファイルはモード間で同一」を固定）。**インメモリ Repository（GenerateInMemoryRepositories）もパッケージ参照モードと併用可**（`QuickER.Runtime.InMemory` 新設により旧併用不可診断は撤去済み）。パッケージ用ソース（`src/QuickER.Runtime*/Runtime*.g.cs`・7 本）の正本は Scriban テンプレートで、`RuntimePackageSourceRenderer` が「空図＋全機能 ON＋固定名前空間＋internal→public」でレンダリングしたものをチェックイン（**手編集禁止**）。DI 登録拡張（AddGenerated*Repositories）はエンティティ別登録を含むスキーマ依存物なので**常に生成側**（パッケージには入れない）。**EF Core 生成（GenerateEfCoreRepositories）とパッケージ参照モードは併用可**＝EF Core 固定 infra を `TContext : DbContext` ジェネリック化したことで具象 QuickErDbContext 依存が生成側（DI 登録・エンティティ別実装）だけに残り、EF Core エンジンは EF Core パッケージへ切り出せる。パッケージモードでも QuickErDbContext・Fluent 構成・EfCore{Entity}Repository・AddGeneratedEfCoreRepositories は**常に生成側**に出力する。EF Core パッケージの EntitySaveMetadata は EF Core が使うメンバー（KeyProperty/AllProperties/PropertyByColumn/ColumnList/CascadeNavigations）だけの縮小版で、方言クォート焼き込みの SQL 文メンバー（TableName/*Sql）はQuickER 版 Repository コンテキスト（`repositories`）限定にガードして持たない（**マルチターゲット×EF Core の排他は別理由＝契約の型同一性で残る**）。PackageReference の届け方は「案内のみ」（生成ヘッダ＋ダイアログ/CLI 出力。csproj には触らない）で、バージョンはロックステップ（Directory.Build.props の `VersionPrefix`＝パッケージ版＝ツール版。案内の版解決は `RuntimePackages.ResolveGuidanceVersion`）。公開は `.github/workflows/publish.yml`（workflow_dispatch・dry_run 既定 true・認証は Trusted Publishing＝GitHub OIDC の一時キーで長期 API キー不使用）
- **拡張シム層（Generation Gap・2026-09-09 / VO は 2026-09-10 に共通ルート化 / 同日に Repository・Mapper まで広げて全域統一）**: 生成コードの基底は**例外ゼロで** 2 層＝**実装の置き場が固定ランタイムの `*Core`**（`EntityBaseCore` / `EditModelBaseCore` / `EditModelBaseCore<TSelf>` / `ValueObjectBaseCore<TSelf, TValue>` / `IValueObjectCore` / `IRemoteRepositoryCore<TEntity, TKey>` / `IRepositoryCore<TEntity, TKey>` / `SqlServerRepositoryCore` / `SqliteRepositoryCore` / `EfCoreRepositoryCore<TEntity, TKey, TContext>` / `InMemoryRepositoryCore` / `HttpRemoteRepositoryCore` / `MapperBaseCore<TEntity, TEditModel>`）、**利用者が拡張する面が「現行名を名乗る生成 partial」**（`EntityBase` / `EditModelBase<TSelf>` / VO 基底 7 本 / 非ジェネリックマーカー `IValueObject` / `IRemoteRepository<,>` / `IRepository<,>` / バックエンド実装基底 5 本 / `MapperBase<,>`）。**拡張面はスキーマ依存側のバケット（Entity / ValueObject / EditModel / Repository 契約 / 各バックエンド実装 / Mapper）へ、パッケージ参照モードでも必ず出す**＝これが層の存在理由そのもの（コンパイル済みパッケージのクラスへは partial を書けず、`UseRuntimePackages` の切り替えで利用者の拡張コードが書き直しになる）。**per-型の宣言句は現行名のまま**（`: EntityBase` / `: EditModelBase<X>` / `: ValueObjectStringBase<X>` / `I{E}Repository : IRepository<E, K>` / `{E}Repository : SqlServerRepository<E, K>(…)` / `{E}Mapper : MapperBase<E, M>`）＝拡張面を経由して Core へ着地するので、per-型出力の差分は基底の追加だけに収まり手書き VO レシピも名前不変（全 17 フィクスチャ＋2 サンプルの per-型宣言句 221 行がバイト不変であることを機械照合済み）。**VO は「共通ルート 1 本＋系統別 6 本」の単一鎖**＝`ValueObjectBase<TSelf, TValue>`（`ValueObjectBaseCore` を継承）を全 VO が必ず通り、`ValueObjectOrderedBase` / `ValueObjectStringBase` / `ValueObjectBooleanBase` / `ValueObjectBinaryBase` / `ValueObjectGuidKeyBase` がその直接の派生・`ValueObjectDateTimeBase` は `ValueObjectOrderedBase` の派生。**系統別 6 本は実装ごと生成側にあり（クラス本体が系統別の拡張点を兼ねる＝別途のシムを置かない）、マーカー `IValueObject` を実装するのはルート 1 本だけ**（系統別へは継承で波及）＝ルートへ足したメンバーが値の形に依らず全 VO へ届く（旧構造の「系統別 `*Core` ＋兄弟枝のルートシム」はルートが全 VO の拡張点にならないため不採用）。系統別クラスの emit ガードは per-VO コードと同じ `generate_value_objects && render_value_objects`＝Runtime 系ファイル・ランタイムパッケージには 1 本も出ない。**ランタイム側へ寄せた共有部は 3 型**＝`ValueObjectComparisons`（非ジェネリック `IComparable.CompareTo` の型不一致 throw〔例外メッセージとパラメータ名 `obj` は不変〕と、Ordered 演算子の null 順序比較〔null 最小・両 null 等値〕。生成側の 3 系統は 1 行転送）/ `ValueObjectBinaryOperations`（`AreEqual`＝null 扱い＋`SequenceEqual`・`ComputeHashCode`＝`HashCode.AddBytes` で全バイト混合〔structural comparer が末尾 8 要素しか混ぜない根拠の doc ごと在住〕）/ `IStringMatchValueObject<TSelf>`（Contains / StartsWith / EndsWith の string 版・TSelf 版 計 6 メンバー。**拡張面ではなく機構の契約**＝ジェネリック契約と同じ分類で、Ordinal 比較と `ArgumentNullException` 契約の正本もここ）。**文字列一致の翻訳判定はインターフェイスマップで引く**（4 エンジン＝Core の `SqlQuery` null 引数ガード・SqlServer / Sqlite の `SqlExpressionTranslator`・EF Core の `ValueObjectStringMethodTranslator`）＝「`call.Method.DeclaringType` が `IStringMatchValueObject<>` を実装し、かつ `GetInterfaceMap(...).TargetMethods` に `call.Method` が含まれる」。**`IsAssignableFrom`＋名前一致にしてはいけない**＝具象 VO も面を継承実装しているため、利用者が per-VO partial へ書いた自前の `Contains` が中身を一度も実行されないまま列の LIKE へ化ける（`SqlExpressionTranslatorStringMatchTests` が両アームを固定・mutation 実証済み）。TSelf 版オーバーロードも契約に含めるのが必須＝含めないと VO 引数の `Contains` が翻訳対象から外れて実 DB テストが割れる。EF Core 側は判定が返す閉じた契約の型引数から TSelf を取る。**TSelf 束縛の static ファクトリ（Boolean の `True`/`False`・DateTime の `Now`/`Today`・GuidKey の `Create()`）はクラスに置くしかない**＝継承 static だからこそ `IsActiveValue.True` の呼び形が成立する（静的ヘルパーへ移すと呼び形が変わる）。**ジェネリック契約 `IValueObject<TSelf>` / `IValueObject<TSelf, TValue>` は名前不変かつ拡張面ではない**（静的ファクトリ契約＝ランタイム在住。アリティ 1 の継承元だけ `IValueObjectCore`）。**`*Core` 型は 1 つも `partial` を名乗らない**（2026-09-10）＝`EntityBaseCore` / `EditModelBaseCore`(＋`<TSelf>`) / `ValueObjectBaseCore` / `MapperBaseCore` / `IRemoteRepositoryCore` / `IRepositoryCore` / `{方言}RepositoryCore` / `EfCoreRepositoryCore` / `InMemoryRepositoryCore` / `HttpRemoteRepositoryCore` の 11 宣言（テンプレート内で分割宣言している型はゼロ）。拡張面はシム側だけという線引きを言語レベルで担保する（インラインモードで利用者が Core のインターフェイスへ既定実装を書くと、シム側の既定実装とダイヤモンドになり CS0121 を招く）。**拡張面（`EntityBase`）へ足した読み書きプロパティは列ではない**＝`[Column]` 許可リスト（DB 定義メタ節）により SQL・`HasSameValues`・列コピーの対象外で、JSON 経路（`ToJson`/`Clone`・リモート転送）にだけ載る。**固定ランタイム内の参照は 1 つ残らず Core 名**（`is IValueObjectCore` 判定・`EntitySaveMetadata` 等の引数型を含む）＝固定コードが生成型を参照するとパッケージ側から生成側の型を見ることになり成立しない（XmlDoc の cref も同じ理由で生成型を指せない＝文言化するか新設ヘルパーへ向ける）。VO の拡張面だけは Core が値引数コンストラクタしか持たないため**転送コンストラクタが構造上必須**（空ボディの原則に対する唯一の例外）。層別出力での配置は Entity/VO＝domain・EditModel＝presentation（per-型コードと同じバケットの帰結）。**Repository 契約シムの価値は「既定実装が DI 越しに届く」こと**＝`IRepository<TEntity, TKey>` の partial へ**既定実装付き**のメンバーを足すと、DI で受け取った `I{Entity}Repository` 参照から——全エンティティ・全バックエンド・デコレータ越しでも——直接呼べる（インターフェイスの既定実装は派生インターフェイス型の参照から見え、`this` へのインターフェイス呼び出しは仮想ディスパッチでデコレータの実装へ落ちる）。**追加は既定実装付きに限る**（本体のないメンバーは生成リポジトリ全部を壊す＝QuickER はその実装を出さない）。シムの `IRepository<,>` は Core だけでなく**シムの `IRemoteRepository<,>` も継承する**＝リモート面へ足した既定実装が全機能面の参照からも見える（この 2 本目の継承辺が層の要）。**実装側は独立 5 枝**（`SqlServerRepository` / `SqliteRepository` / `EfCoreRepository` / `InMemoryRepository` / `HttpRemoteRepository`＝共通の親を持たない。用途は per-entity partial の手書き実装が使う `protected` 共有ヘルパの置き場）で、**コンストラクタ引数を正確に転送する**（省略可能引数の既定値も写して手動 new の書き味を不変に保つ＝VO シムと同じ「空ボディ原則の例外」扱い）。**固定ランタイム内の派生はシムを通らず Core 直付け**（`HttpSyncServerSource` → `HttpRemoteRepositoryCore`）＝ランタイムは生成型を参照できない構造的必然で、ユーザー partial がそこへ届かないのは正しい意味論。**インターフェイスを持つ拡張面は Repository 契約と VO マーカーだけ**＝前者は利用側のアクセス経路がそもそもインターフェイス、後者は開ジェネリックの共通ルート `ValueObjectBase<TSelf, TValue>` を非ジェネリック 1 型で束ねる必要があるため。Entity / EditModel / Mapper は具象クラスで持つ単一実装なので対応するインターフェイスは設けない。**`DbContext` は外部型なのでシムを置かない**＝EF Core の `DbContext` はパッケージの型で、拡張点は生成される `QuickErDbContext` 自身（元から partial）。利用者向けの拡張レシピ（マーカーへのインターフェイス注入・ルートへのメンバー追加・Repository 契約の既定実装・実装側 protected ヘルパ・拡張メソッド・モード移植性の保証・インターフェイスの線引き）の正本は `docs/code-generation.md`（.ja）の「生成される基底クラスの拡張」節。検証は `ExtensionShimGenerationTests`（出力＝インライン／分割／パッケージ／層別／マルチ方言で全拡張面〔Entity・EditModel・VO マーカー＋共通ルート＋系統別・契約 2・実装 5・Mapper〕の emit 先と per-型宣言句を固定）と `ExtensionShimAcceptanceTests` / `ExtensionShimBinaryAcceptanceTests` / `ExtensionShimRepositoryAcceptanceTests`（実フィクスチャ上で partial 拡張が全 Entity・全 VO・全系統・全リポジトリ・全 Mapper へ届くこと。Repository は InMemory フィクスチャ＝DB 不要で、既定実装がデコレータ越しに転送されることも実測）
- **名前付きクエリ（ErDiagram.Queries）**: 図に保存するクエリ定義（`QueryDefinition`・エンティティ/列は Guid 参照）から Repository メソッドを生成する。条件は簡易 DSL（UI 表記も「簡易 DSL」。比較・AND/OR/NOT・括弧・IS [NOT] NULL・[NOT] LIKE・[NOT] IN・CONTAINS/STARTSWITH/ENDSWITH。パーサ＝`QueryConditionParser`・診断はローカライズ済み）で、**単一の C# ラムダ式へエミット**（`QueryConditionCSharpEmitter`）して既存 `Query()` パイプライン（式木→`SqlExpressionTranslator`／EF Core Where 直適用）に流す＝方言 SQL 翻訳は実行時・エミッタに方言分岐なし。VO 列は `Vo.Create(値)` 比較・VO×IN はメソッド冒頭でリスト持ち上げ。戻り形は 一覧/単一/件数/射影（DTO 生成・寛容マッパー互換）＋スカラー（生 SQL／手動実装専用）。実装方式の UI 表記は「簡易 DSL」「生 SQL」「手動実装 (partial クラス)」（enum は Dsl/Sql/Manual のまま）。実装の出し分けは統一規則「**SQL（または DSL 共有本体）が与えられていない実装先は手動実装（manual）**＝契約宣言のみ生成・実装はユーザーが partial クラスへ通常メソッドで書く（漏れはコンパイルエラー）」——EF Core×生 SQL・SQL 辞書にない方言・インメモリがすべて同じ規則に落ちる。型トークン解決は列型と同じくプロバイダ層（`QueryParameterTypeResolver`・列と同一経路で解決）。LIKE の意味論はランタイムのエスケープ設計に合わせ「リテラルは % 位置で分解・`LIKE @p` は部分一致（値リテラル扱い）」で、生 LIKE パターンは生 SQL の担当。**NULL 許容列の文字列一致は「列が NULL でないこと」を AND した形へエミットする**（`(x.Memo != null && x.Memo!.Contains(p))`・否定 `NOT LIKE` も同じ前提の内側＝NULL 行はどちらの向きでも一致しない・2026-08-16）。SQL の LIKE は NULL 行を UNKNOWN で落とすため SQL 側では意味が変わらないが、**インメモリ実行器は式木をコンパイルして実際に評価する**ため、前提を書かないと NULL 行で `NullReferenceException` になり 3 実装先の観測結果が割れていた（旧実装のコメント「式は翻訳されるだけで実行はされないため安全」が誤り＝インメモリ実装が反例。NamedQueryInMemoryRuntimeTests の新設で検出）。NULL 非許容列の生成物はバイト不変。文字列一致（Contains/StartsWith/EndsWith）の null 引数は全バックエンドで ArgumentNullException（SqlQuery.Where の共有ガードで fail-fast 統一・2026-08-10）。**値の側が null になる `==` / `!=` は `IS [NOT] NULL` へ補償**する（`SqlExpressionTranslator.VisitComparison`＝リテラル null だけでなく変数・式の評価結果が null の場合も対象・VO は Unwrap 後で判定。従来は評価済み null が `col = @p`（@p=NULL）としてバインドされ SQL の 3 値論理で全行 UNKNOWN になり、InMemory〔式木コンパイル＝C# 意味論〕・EF Core〔null 補償あり〕と 3 者不一致だった。実害は一意性チェックの自分自身除外＝主キー未設定の新規行で重複を静かに見逃す。**列の側も同じ理由で補償する**＝非 null 値との `!=` は `(col <> @p OR col IS NULL)` を出力する〔素の `col <> @p` は列が NULL の行で UNKNOWN になり静かに脱落するが、C# も EF Core も「NULL は非 null 値と等しくない＝一致」と扱う。実測でも ADO 1 件・EF Core 2 件・InMemory 2 件と割れていた。列の NULL 許容性は式木から確実に判定できないため**無条件補償**＝非 NULL 列では追加した選言が成立しないだけで意味不変・両方言共通。`==` 側は補償しない〕。**列同士の `!=` も同じ理由で補償する**〔`(a <> b OR (a IS NULL AND b IS NOT NULL) OR (a IS NOT NULL AND b IS NULL))`＝EF Core と同形・C# の「片側だけ NULL は不一致／両側 NULL は一致」に揃える。素の `a <> b` はどちらかが NULL の行を UNKNOWN で落とす〕。**列同士の `==` も対称に補償する**〔`(a = b OR (a IS NULL AND b IS NULL))`＝EF Core と同形。素の `a = b` は両側 NULL の行を UNKNOWN で落とすが、C# も EF Core も「両側 NULL は一致」と扱う（実測で ADO だけが脱落）。旧記述の「列同士の `==` は SQL も C# も NULL 側を不一致と扱うため無補償」は両側 NULL のケースを見落とした誤りで、2026-08-13 に是正。**列 vs 値の `==` は従来どおり無補償**＝こちらは「NULL 列は非 null 値と一致しない」で SQL と C# が実際に揃う〕。**否定 `!(==)` / `!(!=)` は演算子を反転して同じ経路へ流す**〔`NOT (...)` で包むと補償の外側に出て `!(x.Col == v)` だけが NULL 行を落とし、`x.Col != v` と結果が割れる（実測で ADO 1 件 vs EF Core/InMemory 2 件）。反転は `VisitComparison(binary, parameters, negate: true)` が先頭で NodeType を入れ替えるだけで、等値以外の否定は従来どおり `NOT (...)`・2026-08-13〕。**二重否定は単項 Not の入口で畳み込む**〔オペランドがさらに単項 Not なら、その被演算子をそのまま翻訳して両方の否定を消す。畳み込まないと `!(!(x != v))` の外側は「オペランドが等値比較でない」ため `NOT (...)` 枝へ落ち、内側の反転と合成されて `NOT ([col] = @p)`＝NULL 行脱落になった（列同士も同様）。再帰的に効くので三重以上も正しく畳む・2026-08-13〕。**IN（コレクション Contains）も同じ理由で補償する**（2026-09-03）〔null 要素はパラメータ化せず列の `IS NULL` へ畳む＝混在リストは `(col IN (@…) OR col IS NULL)`・その否定は `(col NOT IN (@…) AND col IS NOT NULL)`。**NOT IN は null 要素が無くても列側を無条件補償**＝`(col NOT IN (@…) OR col IS NULL)`（素の `NOT (col IN …)` は NULL 列の行を UNKNOWN で落とすが、C# も EF Core も「null は非 null 値のリストに含まれない＝否定で真」と扱う。`!=` の列側無条件補償と同じ線引き）。否定は単項 Not の入口で等値反転と同列に `BuildInClause(negate: true)` へ畳む（`NOT (...)` で包むと補償の外に出る）。空リストは IN=`1 = 0`／NOT IN=`1 = 1`・全 null リストは `IS [NOT] NULL` 単独＝いずれも C# の `Contains` と同結論。SqlExpressionTranslatorInClauseTests（SQL 文字列固定）と Sqlite/SqlServerTranslatorOperatorRuntimeTests（実 DB で C# 意味論と突き合わせ）が固定・mutation 実証済み＝NOT IN の列側補償を外すと単体 3 件＋実 DB 2 件だけが赤〕。関係演算子〔< <= > >=〕は null 対応の SQL 対応物が無いため補償せずパラメータのまま＝意図的な線引き。**日付部品（`x.Col.Year` 等）への変換は読み出し元の宣言型が `DateTime` / `DateOnly` / `DateTimeOffset`（と各 nullable）のときだけ**〔メンバー名だけを見ていると、VO の partial 実装などで `Year` を足した瞬間に日付でない列が `YEAR([col])` へ化ける。該当しない参照は既存の値評価経路へ落ちて `NotSupportedException`＝明示的に失敗する・2026-08-13〕。**ナビゲーションプロパティは列でないため翻訳不能として弾く**〔`IsColumn` が `[NavigationReference]` 付きプロパティを検出して `NotSupportedException`（外部キー列で絞るよう案内）。従来は「ラムダ引数直下のプロパティ参照」だけで列判定していたため `Where(x => x.Customer == null)` が `[Customer] IS NULL` という存在しない列の SQL になり実行時 DB エラー＝InMemory/EF Core は翻訳できるため 3 者不一致だった唯一の箇所・属性判定は ConcurrentDictionary でキャッシュ・2026-08-12〕。あわせて**一意性チェックの自分自身除外は「主キーが null を取り得る型のとき条件付き」**で生成する〔防御の二重化。非 NULL 値型 PK の図の生成物はバイト不変〕・2026-08-11）。生 SQL バインダはコレクション値を `@名0,@名1,...` へ IN 展開（**置換の境界は左右両方**＝`(?<![@0-9A-Za-z_])@名(?![0-9A-Za-z_])`。右だけだと `@@ROWCOUNT` のようなシステム変数の末尾まで置換して SQL が壊れた。テキスト置換なので文字列リテラル/コメント内の同名も置換される＝docs に明記。空リストは `(NULL)` 展開で `IN` は不一致＝正・`NOT IN` は全行 UNKNOWN で「全件除外」になるため空なら SQL を分岐せよ、も docs に明記）。生 SQL は生成前に静的検証（レベル 1・方言非依存＝`RawSqlAnalyzer`。文字列/コメント/`[..]`/`".."` 内・`@@` システム変数はパラメータ扱いしない 1 パス走査）を通し、**未宣言パラメータ＝警告＋該当クエリのみ生成スキップ**（実行時必失のため。Guid 参照検証と同方針）・**未使用パラメータ／複文（; の後にコード）＝警告のみで生成継続**（GUI/CLI 双方に効く・ダイアログは SQL 入力欄近くにインライン表示＋未宣言は OK ブロック）。**EF Core Sqlite は decimal のサーバーサイド比較・並び替え非対応**のため、SQLite×EF Core で使う図の DSL 条件・並び順は整数キー等に限る（実 DB パリティは NamedQueryAdo/EfCoreRuntimeTests＝CI 常時実行）
- **リモート契約生成（GenerateRemoteContracts）**: リモート操作用インターフェイス `I{Entity}RemoteRepository` を**追加生成**する bool オプション（既定 false。CLI `--generate-remote-contracts`／GUI「リモート対応」チェック＝DB アクセス配下・「なし」時は非表示。3 階層構想 Phase 2）。ランタイム共通部は**オプションに依らず常時**、基底分割 `IRemoteRepositoryCore<TEntity, TKey>`（CRUD＋Save×2＝ネットワーク境界を越えられる操作）← `IRepositoryCore`（＋Query()・生 SQL 3 種・BulkInsert）を非破壊で出力する（per-entity 契約が名指しするのは拡張シムの `IRemoteRepository<,>` / `IRepository<,>`）（既存利用コードは無変更でコンパイル可）。ON のとき `I{Entity}RemoteRepository`（拡張シム `IRemoteRepository` 継承＋**名前付きクエリ全部**＝Dsl/Sql/Manual ともシグネチャはリモート可能）が追加され、既存の `I{Entity}Repository` はそれと拡張シム `IRepository` を継承する**全機能面のまま**（純粋に追加的＝ON にしても既存コードは一切壊れない）。**実装クラス・DI 実装登録は従来どおり全機能面基準**（間仕切りは純粋にインターフェイス水準・エンジンに分岐なし）で、リモート面は同一インスタンスへの転送として追加登録（keyed 版も同様）。EF Core・InMemory・マルチターゲット・UseRuntimePackages と直交。リモート面だけに依存したコードは、実体を Web サービス経由の実装（下記リモートサービス生成）へ差し替えてもコンパイル時に安全。**Repository 契約（QuickER 版 / EF Core / インメモリ）が 1 つも無い構成でのリモート指定は生成時エラー**（`CodeGen_Error_RemoteRequiresContract`＝リモート面は契約を拡張する形でしか出ないため、止めないと診断ゼロで何も生成されない。GUI はチェック欄ごと隠し非表示時は保存値も落とすため、踏むのは CLI / MCP / 手書き config）。実 DB 検証は RemoteContractRuntimeTests（CI 常時実行）
- **リモートサービス生成（GenerateRemoteServices）**: リモート面を **HTTP + JSON** で提供するクライアント／サーバー実装を生成する bool オプション（既定 false。CLI `--generate-remote-services`／GUI「リモート対応」行の 2 つ目チェック。ON はリモート面を自動含意。3 階層構想 Phase 3）。**利用者向けの意味論（直列化・例外型の復元・400 分類・liveness・楽観排他の転送・RowState 確定・500 の詳細非公開・HttpClient の所有権と寿命・更新系のリトライ禁止・keyed 登録・バイナリ転送エンドポイント）の正本は `docs/code-generation.md`（.ja）の「リモートサービス」節**で、ここには実装の骨格と「壊すと静かに回帰する判断」だけを書く。転送方式は gRPC でなく HTTP+JSON＝**実証済みの JSON 資産を転用**（`EntityBaseCore.Clone` と同じ意味論の `RemoteJson.Options`＝VO コンバータ・RowState 込み・IgnoreCycles。**`IgnoreReadOnlyProperties` は指定しない**＝クエリ転送エンベロープの匿名型が get-only で空 JSON になるため）。(1) クライアント＝`Http{Entity}RemoteRepository`（BCL の HttpClient のみ・`AddGeneratedHttpRemoteRepositories(baseAddress | HttpClient ファクトリ)`。Insert / Update / Save / SaveMany は直結実装（ADO / EF Core / InMemory）と同じく入口で `ArgumentNullException.ThrowIfNull` を通す〔**例外型・パラメータ名まで直結と一致させるのが契約の一部**＝実装差し替えで catch が変わらない。従来は null をそのまま直列化してサーバー奥の別例外＝500 へ化けていた〕・名前付きクエリは実装方式に依らず全部転送メソッド化）を本体生成物へ同梱し、(2) サーバー＝`MapGeneratedRemoteEndpoints(access, prefix="/quicker")`（Minimal API・`POST {prefix}/{エンティティ}/{操作}`・RouteGroupBuilder を返し追加ポリシー付与可。**第 1 引数 `RemoteAccess` は既定値なしの必須**＝RequireAuthorization はグループ全体〔health 含む〕へ既定認可ポリシー適用・AllowAnonymous はメタデータ無付与〔`[AllowAnonymous]` を付けてホストの FallbackPolicy を弱体化させない〕・未定義値はマップ時 `ArgumentOutOfRangeException`。全行の読み書き削除面の開放/保護を誰も書いていない既定値に決めさせない＝exposeErrorDetails / allowUnboundedUploads と同じ「危険は明示させる」文法・2026-08-25。実 HTTP 検証は RemoteAccessRuntimeTests）を**別ファイル** `{ベース名}.RemoteServer.g.cs` へ出力（ASP.NET Core の FrameworkReference 必須のため本体へ連結しない。バケット RemoteServer）。クライアント側固定部（`RemoteJson` / `RemoteRepositoryException` / エンベロープ / `HttpRemoteRepositoryCore` 基底）は共有 infra＝**Core パッケージへも収載**（BCL のみ＝依存ゼロ維持）で、per-entity クライアント・DI 登録はスキーマ依存物として常に生成側。**サーバー側固定部も同じ扱い**＝リクエスト読み取り・例外分類・応答書き込み・汎用 CRUD マッピング・バイナリ転送の補助は `RemoteServerEngine`（＋トップレベル型 `RemoteBadRequestException` / `RemoteErrorDetailPolicy` / `DisableRequestBodySizeLimit` / `DeferredOctetStreamBody`）へ切り出され、**分割生成では `Runtime.AspNetCore.g.cs`・パッケージ参照モードでは `QuickER.Runtime.AspNetCore`** が持つ（非分割インラインでは同じサーバーファイルへ同居）。per-entity 側は `RemoteServerEngine.` 修飾で呼ぶ＝方言エンジン参照と同型で、**モード間で per-entity 出力はテキスト不変**。**CRUD＋保存＋重複事前チェック・バイナリ転送・同期エンドポイントのマップはエンジンのジェネリックメソッドへ集約済み（2026-09-04・移設 P4）**＝per-entity の `Map{E}Endpoints` は `MapCrud<TEntity,TKey,TRemote>(group, ルート名)` 1 呼び出し（26→9 行）＋バイナリ列ごとの `MapBinaryColumn<TKey>(group, ルート, allowUnbounded, read/write デリゲート)`（57→11 行/列。キー復元はハンドラのラムダ内＝不正 `?id=` の 400 分類・411 の判定位置・DELETE=`write(null)` の構造を 1 バイトも変えない）＋同期は `MapSyncEndpoints` 系（SyncPage は版なしテーブルへの張り分けを per-entity 側の呼び分けで維持）で、**名前付きクエリのエンドポイントだけが per-entity のまま**（確定仕様）。`{E}CheckUniquenessRequest` record は廃止＝既存 `RemoteEntityRequest<TEntity>` と完全同型のため（private record＝公開 API 非破壊・ワイヤ互換）。`RequireSyncSources` は `(Type, string)[]` の表＋固定 foreach（素の `Type[]` にしないのは例外メッセージの型名表記〔`int` 等の C# キーワード形〕をテストが固定しており、実行時導出だと `Int32` に化けるため＝報告名は生成時に書き下ろす）。mutation 実証済み＝Update 配線破壊で UpdateAsync 経由の 40 件だけ赤・SyncPage 張り分け破壊で HTTP 経路 17 件だけ赤（直結 65 件は全緑）。**`OnServerError` の partial 契約はアセンブリを跨げないため metadata 経由で繋ぐ**＝生成側 `GeneratedRemoteEndpoints` が `RaiseServerError`（メソッドグループ＝partial 未実装でも実在）を `RemoteErrorDetailPolicy(exposeErrorDetails, RaiseServerError)` としてグループのメタデータに載せ、エンジンの `WriteServerErrorAsync` がそこから呼び出す（順序・挙動は完全不変・エンドポイント不一致時は「隠す＋フックなし」の安全側既定）。500 の詳細非公開スイッチも同じメタデータに乗る〔ハンドラ 1 つも署名を変えない・同一プロセスの複数グループが独立〕ため、500 を書く 4 ラッパーは共通ヘルパー `WriteServerErrorAsync(context, ex)`〔ログ→フック→本文の 3 手順を 1 箇所へ〕を呼ぶだけになる。**400/409 はスイッチの影響を受けず、400/409 ではログも `OnServerError` も呼ばない**（両者は 500 専用）。応答書き込みは共通ヘルパー `WriteErrorAsync(context, statusCode, type, message, conflict?, correlationId?)` へ集約。**クライアント側の分類で落としやすい 3 点**＝(a) **成功ステータスなのに本文が期待した JSON でない応答も `RemoteRepositoryException`**〔`InvokeAsync` の成功パスが `JsonException` / `NotSupportedException` を包み直す＝応答しているのは生成エンドポイントではない別物（プロキシやポータルの 200）で実体は転送の失敗。素の `JsonException` では他のリモート失敗と同じ catch で拾えない。**JSON リテラル `null` の本文は正当な JSON＝例外で出ないため明示チェックで同じ分類にする**（2026-08-26）＝クライアントの実体は private `InvokeCoreAsync(…, allowNullResult)` 1 本で、null が正当な結果の操作（GetById・単一戻り形・null 許容スカラー＝**内側の型の `?` サフィックスで振り分け**）だけが `InvokeNullableAsync` 経由＝200＋`null` を従来どおり「該当行なし」で返す（RemoteResponseBodyRuntimeTests が両アーム固定）〕・(b) **VO 併用時は `ValueObjectValidationException` も同じ 2 フィルタ（成功パス／`ReadErrorAsync`）へ含める**〔`ValueObjectJsonConverter.Read` の `TVo.Create` 検証違反は JsonException で出ないため素通りしていた〕・(c) **失敗応答の本文を読めなかった理由も保全する**〔`ReadErrorAsync` が解析失敗の例外を `ToException` まで運び innerException へ載せる＝「本文が RemoteError でなかった」と「message が空の RemoteError だった」を呼び出し側が区別できる〕。**400 分類の実装規則**＝境界は「`ReadRequestAsync` と `ParseKeyFromQuery` の 2 関数内の失敗」で、内部マーカー `RemoteBadRequestException` に包み直して 4 ラッパーが catch する（例外型の列挙をしない）。**必須フィールド欠落も 400**＝エンベロープは positional record のため `{}` は Entity / Entities / 参照型キーが既定の null のまま通り、リポジトリ奥の null 引数例外＝500＋ログ＋フックへ化けていた。共有ヘルパー `Required<T>(T? value, string name)`〔無制約ジェネリック＝値型キーは素通し〕を Insert / Update / Save / SaveMany の Entity・SaveMany の Entities・GetById / Delete の Id へ適用し、**per-entity のマップを組み立てるモデルビルダー側（`CSharpGenerationModelBuilder.Remote.cs`）も同じ規則**＝重複事前チェック（CheckUniqueness）の entity と名前付きクエリの参照型パラメータ〔VO・string・`IReadOnlyList<T>`〕を包む（判定は `QueryPayloadParameter.IsReferenceType`＝列由来は `IsUnderlyingReferenceType`／VO・トークン由来は `CSharpTypeInfo.IsReferenceType`）。DI 解決も per-entity 側は `RemoteServerEngine.Repository<T>(context)` 経由へ統一する。**リスト内 null 要素は 400 にしない**＝3 実装先の SaveAsync がいずれも `entity is not null` で除外＝直結の契約どおり黙って無視するのが正で、応答収集側〔`CollectRowVersions` / `CollectSkipped`〕の null ガードで NRE→500 を塞ぐ。**未定義の `ConcurrencyMode` 値も 400**（enum は JSON で任意の数値を受けるため Update / Save / SaveMany の 3 ハンドラが `ValidatedMode` で `Enum.IsDefined` 検証＝素通しすると版チェックが黙って無効化される）・**名前付きクエリのページング引数も 400**（`ValidatedPaging(take, skip)`＝`take<=0` / `skip<0` はクエリパイプラインが `ArgumentOutOfRangeException` で拒否する値で、素通しすると呼び出し側の不備が 500＋ログ＋フックへ化ける。2 つは常に対で出るのでまとめて 1 回検証する）で、**生成クライアントはその 400 を出さない**＝ページング付きの転送メソッドは先頭で直結と同じ例外（`ArgumentOutOfRangeException`・順序は Skip→Take・ParamName は `SqlQuery.Skip/Take` の引数名 `count`・文言も同一）で弾き HTTP を投げない〔`ArgumentNullException.ThrowIfNull` と同じ「例外型・パラメータ名まで直結と一致させる」契約。文言の複製は `_05_QueryPipeline` とモデルビルダーの 2 箇所になるため、一致はパリティテスト（RemoteService 22）が固定する。サーバー側 400 は手書きクライアント向けの防御として残す〕・Kestrel の `BadHttpRequestException` は**ステータス素通し**（ボディサイズ超過の 413 が 500 へ化けていた不具合の修正）。**分類 catch は 4 ラッパーとも `when (!Response.HasStarted)` で守る**＝応答が書き出し始めたあとの失敗ではステータスを変えられず、エラー本文を書くと部分応答へ継ぎ足しになる。`ExecuteAsync` と blob 3 executor（Download / Upload / Delete）はいずれも開始済み用の `catch (Exception)` を 1 本持ち、`LogServerError` だけ通して rethrow する〔ホストが接続を中断＝クライアントには破損応答でなく切断が見える。`OnServerError` は 500 応答の拡張点なので 500 を出さないこの経路では呼ばない。blob 側は本文をストリーミングする分むしろ開始済みで落ちやすいのに、この 1 本だけが無く失敗が痕跡を残さず消えていた・2026-08-13〕。クライアント切断（RequestAborted 由来の `OperationCanceledException`）は 4 ラッパーすべてで無応答に統一（バイナリの Upload / Delete だけ専用 catch がなく 500＋ログへ化けていた同種の誤分類も解消）。**共有ヘルパー `RemoteEntityGraph`（走査・キー直列化・版プロパティ解決）は Core パッケージ収載の `public` 静的クラス**＝サーバーファイルは別プロジェクトへ置ける前提のため `internal` にできない。**`PingAsync` は `HttpRemoteRepositoryCore` 基底に置く**＝直結実装と共有する契約に liveness の意味がないためリモート面インターフェイスには出さない（呼ぶには具象 `Http{Entity}RemoteRepository` へキャストする）。ルートは定数 `RemotePaths`（`DefaultPrefix` = `"/quicker"`・`HealthRoute`）へ一本化しサーバー既定値・クライアント・サンプルが同じ値を読む。**BulkInsert・Query()・生 SQL はリモート面に無いため転送対象外**（設計どおり）。**DI 登録は非 keyed・keyed の 2 系統×（ベースアドレス版／HttpClient ファクトリ版）＝4 本**（2026-08-16）＝keyed 版は方言別拡張の keyed 版と対で「サーバー＝HTTP・ローカル＝SQLite」のハイブリッド構成を組むための部品。共有 HttpClient（`OwnedHttpClient`）も同じキーで `AddKeyedSingleton` する＝非 keyed 登録とも別キーとも衝突せず、コンテナ所有（破棄も従来どおり）を保つ。実 HTTP 検証は RemoteServiceAdo/EfCoreRuntimeTests（Kestrel を 127.0.0.1 空きポートで in-process 起動・CI 常時実行）と RemoteErrorDetailRuntimeTests（500 の非公開/透過・相関 ID のログ突き合わせ・400/409 の対照・旧ボディ退化・バイナリ経路）・RemoteKeyedRegistrationRuntimeTests（Kestrel 2 台をキー別に登録して往復・非 keyed 解決との分離・同一キー後勝ち・コンテナ所有の破棄）。
- **無制限バイナリ列の除外（ExcludeUnboundedBinaryColumns）**: `varbinary(max)` / `image` / 長さ宣言なし BLOB / `bytea` 等の**無制限バイナリ列**を、QuickER 版 Repository の SELECT / UPDATE 対象から外す bool オプション（既定 false。CLI `--exclude-unbounded-binary-columns`／GUI チェック）。判定は**宣言ベース**でプロバイダの型マッパー（`CSharpTypeInfo.IsUnboundedBinary`＝`varbinary(max)`/`image`/長さなし BLOB は真・`rowversion`/`timestamp`/`binary(n)`/`varbinary(n)` は偽）が行い、生成 Entity のプロパティへマーカー属性 `[UnboundedBinaryColumn]` を刻む（除外列一覧は Info 診断で通知）。ランタイム（`EntitySaveMetadata`）は属性をリフレクションで読み列集合を分割する＝**INSERT / BulkInsert は全列**（初回書き込みで blob を渡せる）・**SELECT は縮小列**（除外列は未取得状態＝nullable は null・非 nullable は空配列）・**UPDATE は縮小列**（除外列に触れず既存 blob を温存）。除外列に「null でも空でもない」値を残したまま UPDATE / SaveAsync（グラフカスケード）/ InMemory 更新 / リモートクライアント送信を試みると `InvalidOperationException`（サイレントなデータ取りこぼしを防ぐ・除外列の更新は生 SQL `ExecuteSqlAsync` へ誘導）。**EditModel / Mapper は除外列へ rowversion とほぼ同じ規則を適用する**（2026-09-03）＝常時必須にはせず（`IsRequired` へ `IsExcludedUnboundedBinary`〔オプションとの AND 済みフラグ・正本は BuildEditModelProperty/BuildValueObjectEditModelProperty〕を AND）、Mapper の `ApplyToEntity` は入力が非 null のときだけ代入（未入力は初期化子のまま＝未取得状態を維持し `IsUnset` ガードを素通り→UPDATE が既存 blob を温存）。さもないと VO 有効 × NOT NULL 除外列（初期化子 `= null!`）で「通常フェッチ→通常列だけ編集→保存」が必須エラーと `?? throw` の 2 段で必ず落ちる（VO なし非 NULL は初期化子が空配列のため露見しない＝旧フィクスチャ行列の盲点）。帰結: Mapper 経由では NULL 許容除外列を NULL へ戻せない（生 SQL / `Write{列}Async(null)` へ誘導）。**ただし NOT NULL の除外列は新規行（RowState=Added）でのみ必須**（`IsRequiredWhenAdded`＝列テーブルの独立フラグ・2026-09-04 の再レビュー N-1 対応）＝当初の「新規行の供給漏れは DB エラーに委ねる」判断を**覆した**もの。根拠: VO×NOT NULL では null の INSERT は DB の NOT NULL 違反で必ず落ちるため、正当な 2 段方式（INSERT→Write）は空の値（VO は `Create([])`・非 VO は空配列＝初期値で自然成立）を入れる形でしか成立せず、空値は非 null なので Added 条件必須を素通り＝**この検証は DB 行き確定の null だけを列名つきで止め、正当な経路を何も塞がない**（「Added 条件は 2 段方式を塞ぐ」という当初の不採用理由は、当該組み合わせで 2 段方式が null で成立するという誤った前提だった。docs の「INSERT は null または空」も同時に是正）。判定は基底 ValidateSelf ループの `IsRequired || (IsRequiredWhenAdded && RowState==Added)` で、Added を抜けた行の必須エラーは else 枝の `ClearRequiredError` が取り下げる。ExcludedBinaryRequiredWhenAddedGenerationTests＋BinaryVoEditModelRuntimeTests の 2 段方式実測が固定・mutation 実証済み（RowState 条件除去で新テストだけが赤）。WithUnboundedBinary 取得後の Mapper 経由保存が UPDATE ガードで拒否される挙動は設計どおり不変。組合せは BinaryVoFixture＋BinaryVoEditModelRuntimeTests が固定・mutation 実証済み（非 null ゲート除去で往復テストだけが赤）。生 SQL のエンティティ取得は縮小列が必須だが、SELECT に除外列を含めれば opportunistic にマップされる。名前付きクエリの DSL 射影が除外列を参照していればサーバー側刈り込み SELECT に含まれ値が取れる（WHERE 参照も可）。**EF Core（DbSet 経由）は非適用**＝EF Core の列選択は EF Core の責務のため全列読み書き。InMemory は完全パリティ（読み取り複製の除外列 strip・更新ガード・射影は完全クローンから・**更新は除外列の blob を温存**＝行ごと差し替える構造上、更新（＝置換前の行がある書き込み）では「未取得状態のままの除外列」を前スナップショットから引き継ぐ〔`UnboundedBinaryColumns.PreserveUnset`。挿入は引き継ぎ元がなく全列書き込み＝実 INSERT と同じ〕。さもないとガードが通る正常系〔GetById→編集→Update〕で blob が黙って消える）。除外が効く図でも呼び出しごとに blob 込みで取得できる**読み取りオプトイン `SqlQuery<T>.WithUnboundedBinary()`**（`SqlQueryPlan` の bool フラグ・API は常時生成で除外列なしは no-op）を Query() に持ち、ON かつ Include なしのとき SQL Server は FOR JSON を使わず全列プレーン SELECT（Base64 膨張回避）・SQLite も全列 SELECT・InMemory は strip なしの完全クローンを返し（RowState=Unchanged の正当なエンティティ）、EF Core は元々全列のため no-op。Include との併用は終端で `InvalidOperationException`（両順序を BuildPlan で検出）。効果はエンティティ形の取得（ToListAsync / FirstOrDefaultAsync）と**射影のフォールバック経路**（列抽出不能なセレクタ＝エンティティを全列取得してから射影する経路。方言側はフォールバックが ToListAsync 委譲のため元から拾っていたのを InMemory も揃えた）で、件数・存在確認・刈り込みが成立する射影には影響しない（取得後の除外列更新は既存 UPDATE ガードで throw＝生 SQL へ誘導）。VO・マルチターゲット・EF Core・InMemory・リモートと直交（**双方向同期とも併用可**＝行の転送からは外れ、`SyncOptions.IncludeUnboundedBinary` のときだけ列単位でコピーされる。同期節⑫を参照）。除外列ごとに**ストリーミング Stream アクセサ `Read/Write{Column}Async`**（＋ファイル糖衣 `Read/Write{Column}ToFile/FromFileAsync`）も追加生成する（`GenerateRepositories && ExcludeUnboundedBinaryColumns` かつ除外列があるときのみ）＝O(チャンク)・blob 全量をメモリに載せない唯一の手段。**契約の挿入先はリモート契約の有無で変わる**＝OFF なら全機能面 `I{Entity}Repository`・ON（GenerateRemoteContracts/Services）なら**リモート面 `I{Entity}RemoteRepository` へ移設**（全機能面はリモート面を継承＝純粋に追加的・利用コード無変更。ファイル糖衣の `this` 対象も追従。ランタイム共通の基底 `IRemoteRepositoryCore` には載せない＝エンティティ固有のため per-entity 面のみ）。契約・薄い委譲実装（QuickER 2 方言＋InMemory は固定 infra のエンジンへ委譲・列名は `EntitySaveMetadata` 経由で焼き込まない）・EF Core 版 Repository は `NotSupportedException`・ファイル糖衣は拡張メソッド静的クラス（Stream 版へ 1 行委譲）を `CSharpGenerationModelBuilder.BinaryStream.cs` が整形済みブロックとして組み立てる。エンジンは SQL Server=`SequentialAccess`＋`GetStream`／Stream 値の `SqlParameter`（`VarBinary`,-1）・SQLite=`SqliteBlob`＋`zeroblob`（rowid 前提・QuickER の SQLite DDL は WITHOUT ROWID 非生成）。`CanSeek` でない Stream は `length` 必須（方言中立に統一・欠落は `ArgumentException`）。`source=null` で SET NULL・行なし/NULL は false。INSERT 専用メソッドはなく「INSERT→Write の 2 段」。楽観排他はスコープ外。**リモートサービス生成（GenerateRemoteServices）時は HTTP でストリーミング転送**する第 2 形式の専用エンドポイントを追加＝JSON エンベロープ（POST+Base64）と意図的に別形式で `GET/PUT/DELETE {prefix}/{エンティティ}/{列名}?id=`（キーは URL クエリ・本文は生 blob・`application/octet-stream`）。GET は応答開始を最初の書き込みまで遅延するラッパーストリーム（`DeferredOctetStreamBody`）で「false→404・true(空含む)→200」を本文ストリーミングと両立（先に本文を書くと 404 に切り替えられない問題の解法）。PUT は成功 204・行なし 404・`Content-Length` 欠落(chunked)は 411・id 欠落/不正は 400（GET / DELETE も同じ）。**「行なし/NULL」の 404 には marker `RemoteError{Type:"NotFound"}` を載せ、クライアントで `false` へ畳むのはこの marker 付きだけ**（本文なしの素の 404＝ベースアドレス/プレフィックス誤り・消えたルート・プロキシ自身の応答は `RemoteRepositoryException(404)` として送出＝設定ミスが「データなし」に化けない。判定は「本文を 1 度だけ読んで `Type` を見る」段階形で、解析失敗は例外側へ倒す）。411 にも他の分類済み拒否と同じ `RemoteError{Type:"BadRequest"}` 本文を付ける（ボディのドレインは不要＝早期 return のまま 3KB〜50MB の chunked すべて 411 が正常受信されることを実測で確認済み）、**リクエストサイズ制限の解除はオプトイン**＝`MapGeneratedRemoteEndpoints(access, prefix, exposeErrorDetails, bool allowUnboundedUploads = false)` の第 4 引数が true のときだけ、このエンドポイントへ `IRequestSizeLimitMetadata` メタデータ（`DisableRequestBodySizeLimit`）を付与する〔既定 false＝ホストの上限（Kestrel 既定 30MB）が効き超過は 413。任意サイズの本文を受けるのは DoS 面のため認可併用推奨・RouteGroupBuilder で別値へ上書き可。フラグはマップ時に評価しハンドラ署名は不変＝バイナリブロックを持つ per-entity の `Map{X}Endpoints` だけが引数を受ける〕。**413 は宣言長の事前チェックで出す**（`EffectiveRequestBodySizeLimit`＝書き込みは本文を読む前に宣言長ぶんの領域を確保する〔SQLite の `zeroblob(宣言長)`〕のに対しホストの上限は本文を読んだときにしか発火しないため、`Content-Length` を書き込み前に上限と突き合わせる）。**上限の解除は feature でなくエンドポイントのメタデータで判別する**＝feature の `null` は「意図的な解除」と「feature を公開しないホスト（IIS アウトオブプロセス等）」の両方で起き、後者を「上限なし」と読むとオプトインしていないエンドポイントで事前チェックが無言で消える。判定順は `IRequestSizeLimitMetadata` があればその値（`null`＝解除）→ feature の上限 → **Kestrel 既定 30,000,000 のフォールバック**（ホストが上限を全体解除していてもオプトインなしならこの既定が効く＝安全側・docs 明記）。**解除側の取引**＝宣言長の検査も外れるため、本文を 1 バイトも送らずに宣言長ぶんの blob 先行確保を毎回させられる（確保はロールバックされるが I/O は消費）＝認可併用推奨の理由。DELETE は `Write(id,null)`＝NULL 化（0 バイト PUT＝空ボディと構造的に区別）。クライアント（`Http{Entity}RemoteRepository`）は `ResponseHeadersRead`＋`CopyToAsync`／`StreamContent`（Content-Length 付き）＋非シークは送信前 `ArgumentException` 検証。**渡された Stream の所有権は呼び出し側**＝直結エンジンと同じく閉じも破棄もしない（HttpClient は送信後にリクエストコンテンツを破棄し、`StreamContent` の破棄は内側の Stream まで届くため、非クローズのラッパー `NonClosingStream`〔Core パッケージ収載・Dispose/DisposeAsync だけ無視して他は委譲〕を挟む）、キー直列化は `RemoteJson.Options`（VO 内包値）を URL エンコードしサーバーが同設定で復元。クライアント固定部（`FormatKeyForQuery`・Download/Upload ヘルパー）は `HttpRemoteRepositoryCore` 基底＝Core パッケージ収載。`WithUnboundedBinary()`/`Query()`/生 SQL のリモート化はスコープ外。実 DB 全経路検証は BinaryColumnAdo/EfCore/Remote/InMemory 各テスト（WithUnboundedBinary の取得・Include 併用例外・取得後 Update ガード・Stream の往復/SET NULL/行なし/非シーク length 必須/ファイル糖衣往復・**リモートは HTTP 越しの往復/404 false/DELETE→NULL/空ボディ 0 バイト/送信前 length 例外/`allowUnboundedUploads: true` での 31MB 転送成功と既定での 413 拒否の対照**を含む・CI 常時実行）
- **DB アクセスの排他と単独出力**: Repository バケットは「共通契約」（GenerateRepositories || GenerateEfCoreRepositories で出力）と「QuickER 版 Repository 実装」（GenerateRepositories 時のみ）に分割されており、**GenerateEfCoreRepositories 単独の生成物には QuickER 版 Repository 実装の ADO 依存（Microsoft.Data.SqlClient / Microsoft.Data.Sqlite）が一切出ない**（ガードテストあり）。QuickER 版 Repository は `RepositoryDialects` で方言を選ぶ多DB対応（未指定は既定 `sqlserver` の単一へフォールバック。**未対応方言を含む指定は GenerateRepositories=true なら検証エラー・false なら `sqlserver` へ丸ごとフォールバック**＝CLI が `--repository-dialects` 未指定時に図の方言名を焼き込むため、これがないと PostgreSQL / MySQL / Oracle の図で Repository 非生成の generate まで落ちる（GUI の空リスト時と同一出力）。`sqlserver`＝FOR JSON／`sqlite`＝プレーン SELECT＋IncludeLoader マルチクエリ・LIMIT/OFFSET・strftime。旧単数キー `RepositoryDialect` は廃止済み）で、単一指定では片方言のみを出力し方言間の ADO 依存も排他（ガードテスト双方向）。`RepositoryDialects` に複数方言を指定するとマルチターゲット出力になり、中立契約（IRepository / I{Entity}Repository / ISqlExecutor など）を `RepositoryNamespace` に 1 回・方言別実装を `.SqlServer` / `.Sqlite` の方言別 namespace に出力する（分割時のファイルは `Repositories.g.cs`＋`Repositories.SqlServer.g.cs` 等＝いずれも per-entity 実装と DI だけで、方言エンジンの固定 infra は `Runtime.{方言}.g.cs` 側にある＝ランタイム配布節を参照）。**EF Core 実装（GenerateEfCoreRepositories）も分割時は同じ流儀で `Repositories.EntityFrameworkCore.g.cs`・`{RepositoryNamespace}.EntityFrameworkCore`（契約 namespace のサブ名前空間へ導出専用＝個別の名前空間オプションは持たない。サフィックスはパッケージ名に揃えた `EntityFrameworkCore` で、C# 型名 `EfCore{Entity}Repository` は不変）に出力する**（インメモリ実装も同流儀の `Repositories.InMemory.g.cs`・`{RepositoryNamespace}.InMemory`）。DI は方言別拡張＋keyed 版（AddGeneratedSqlServerRepositories / AddGeneratedSqliteRepositories・`object? serviceKey` 付き）で、単一契約型を keyed 解決して同一プロセスで複数 DB へ書き分けられる（**`ISqlExecutor` はリポジトリの省略可能な第 3 引数**＝DI 登録が `GetService<ISqlExecutor>()`／keyed 版は `GetKeyedService<ISqlExecutor>(key)` を渡す。従来はフィールド初期化子で `new SqlExecutor(connectionFactory)` を固定生成しており、DI へ登録したラッパー実装が生 SQL 経路に一切効かなかった。省略時は従来どおり既定実装を組むため手動 new は不変・EF Core 版も同型・2026-08-12）（実 DB 結合は MultiTargetRepositoryRuntimeTests＝Docker 依存・keyed 解決の型検証は MultiTargetRepositorySqliteKeyedResolutionTests＝CI 実行）。マルチターゲット（実効方言 2 つ以上）と GenerateEfCoreRepositories は排他で、併用指定は診断エラーになる。GUI の生成ダイアログは DB アクセスをラジオ3択（なし既定／QuickER 版 Repository＝常時選択可・対象 DB チェック群 [SQL Server / SQLite] で選択（既定=図の方言、未対応方言の図では空・最低1つ必須）／EF Core）で排他選択させる。両方 ON はパリティ検証用に CLI／オプション直指定でのみ可能
- **GUI の配布（Velopack・2 チャンネル）**: release.yml（手動トリガ）が win-full（self-contained）/ win-lite（フレームワーク依存）の 2 チャンネルで Setup.exe＋Portable zip を生成する。エントリポイントは `Program.Main`（`VelopackApp.Build().Run()` を WPF 初期化より前に実行。csproj の `StartupObject`＋`EnableDefaultApplicationDefinition=false` とセットで崩さない）。起動時自動更新のフィードは `UpdateFeed.GitHubRepositoryUrl`（`https://github.com/kokko-labs/QuickER`＝本リポジトリの GitHub Releases。空文字にするとチェック無効）で、環境変数 `QUICKER_UPDATE_FEED` による差し替えは **`#if DEBUG` 限定**＝配布ビルドでは分岐ごとコンパイルされない（更新元は利用者の環境変数で差し替えられてよい設定ではなく、任意のフィードを指せると更新の受け口がそのまま任意コードの配布経路になる。テストも `#if DEBUG` / `#else` で両アームを固定する）。**利用者のオプトアウトは gui-settings.json の `CheckForUpdatesOnStartup`**（既定 true＝キーを持たない旧ファイルも「確認する」で読む）で、`UpdateService` は設定を見る `Func<bool>` を受けてフィード解決より前に短絡する＝off ならネットワークへ一切出ない。UI はツールバーの設定ボタン（⚙・`Settings_Caption`）の ContextMenu で、言語切替はその下のサブメニューへ移した（`Language_*` の resx キーは不変＝`StringsLocalizationTests` が値を固定している）。**配布物は Authenticode 未署名**（SmartScreen が発行元不明の警告を出す）で、この非署名と更新チェックの開示は README の Install 節が担う。Velopack 依存は `IAppUpdater` seam の裏に隔離・UpdateService は単体テスト済み
- **アプリ設定・作業状態の保存先はローカル（`%LOCALAPPDATA%\QuickER`）**: `JsonSettingsStore` の既定コンストラクタ（gui-settings.json / ai-settings.json / codegen-settings.json）・`SqlConnectionProfileStore`（connections.json＋DPAPI シークレット）・`ApiKeyStore`・自動保存（last_diagram.json）・クラッシュログの 5 箇所がすべて `SpecialFolder.LocalApplicationData` を使う。ローミング（`%APPDATA%`）へ置かないのは、ここにあるのが機械固有の作業状態と DPAPI（CurrentUser）で保護した秘密で、ドメインのローミングプロファイルで他端末へ複製されても復号できず・別端末のパスを運ぶだけになるため（Windows のローミング指針どおり）。**移行処理は書かない**＝ローミング側の旧ファイルは読まず、既存ユーザーの設定は一度リセットされる（CHANGELOG に明記済み）

## 不変条件（ビルド・型検査では検出できず、壊すと静かに回帰する）

- 生成テンプレートは保守性のため `Templates/CSharpRuntime/*.scriban` の部品ファイル群へ物理分割されており（`_00_HeaderAttributes` 〜 `_10_Sync` の 11 本）、`ScribanCSharpRenderer.LoadTemplate` が `TemplatePartResourceNames` の固定順で単純連結して 1 本のテンプレート本文へ復元する（Scriban の include は使わず連結結果を 1 回だけ解析）。各部品は元の連続行範囲そのままで、**並べ替え・境界での行編集をしない**（連結結果が分割前とバイト完全一致であることが不変条件＝生成コードのバイト一致の前提）。部品は全て **CRLF 固定**（.gitattributes の `*.scriban text eol=crlf` で強制）。部品を編集する場合は連結順（番号順）と CRLF を崩さないこと
- **テンプレート変更時は** `tests/QuickER.Tests/GeneratedFixture/` の固定フィクスチャ **17 個**（GeneratedFixture.g.cs＝SQL Server 全カバレッジ・PortableFixture.g.cs＝方言可搬な EF Core 単独出力・SqlitePortableFixture.g.cs＝SQLite 方言のQuickER 版 Repository＋EF Core 併存・MultiTargetPortableFixture.g.cs＝sqlserver/sqlite マルチターゲットQuickER 版 Repository・EF Core なし・keyed DI（自己参照テーブル nodes を含む＝IncludeGraph edge-skip の実 DB 検証用。図の部品は SelfReferenceTableDefinition を InMemoryFixture / QueryFixture と共有）・InMemoryFixture.g.cs＝インメモリ Repository（同じく自己参照テーブル nodes を含む）・QueryFixture.g.cs＝名前付きクエリ入りの SQLite 方言＋EF Core＋InMemory 併存（DSL クエリのパリティを 3 実装先で取るため InMemory を含む。自由 SQL 実装のクエリは InMemory では manual＝テスト側 partial が担う。customers→orders→order_lines の 3 階層チェーンと自己参照テーブル nodes を含む＝IncludeGraph ランタイム検証と自己参照×EF Core〔FK 列の VO 型統一で成立〕の検証用）・RemoteContractFixture.g.cs＝QueryFixture と同一図で GenerateRemoteContracts=true＝リモート面追加の比較対照・RemoteServiceFixture＝同図で GenerateRemoteServices=true。**本体＋RemoteServiceFixture.RemoteServer.g.cs の 2 ファイル構成**＝サーバーファイルはテストプロジェクトで実コンパイルされ ASP.NET Core 参照の検証を兼ねる・BinaryFixture＝ExcludeUnboundedBinaryColumns=true の SQLite 方言＋EF Core＋InMemory＋リモート併存＝無制限バイナリ除外の全経路検証。**本体＋BinaryFixture.RemoteServer.g.cs の 2 ファイル構成**・SqlServerBinaryFixture.g.cs＝BinaryFixture と同一図の SQL Server 方言版（QuickER 版 Repository 単独＝最小構成）＝SQL Server ストリーミングエンジン・除外の FOR JSON 縮小 SELECT・WithUnboundedBinary のプレーン SELECT 経路の実 DB 検証用（SqlServerBinaryColumnRuntimeTests・Testcontainers・Docker 依存）。図は BinaryFixtureDefinition.Build() へ委譲・BinaryVoFixture.g.cs＝「VO 有効 × 無制限バイナリ除外 × EditModel/Mapper」の最小構成（SQLite 方言のQuickER 版 Repository 単独・1 テーブル vault_items＝NOT NULL／NULL 許容の除外 blob 各 1 本）＝非 NULL VO 除外列（初期化子 `= null!`）の EditModel 往復（必須検証除外＋非 null ゲート代入）を固定。BinaryVoEditModelRuntimeTests（実ファイル SQLite・CI 常時）が使う・ConcurrencyFixture＝rowversion 列 × GenerateValueObjects=true の SQL Server 方言＋EF Core＋InMemory＋リモートサービス併存＝版番号が VO 型（RowVerValue）になる経路の全カバレッジ。**本体＋ConcurrencyFixture.RemoteServer.g.cs の 2 ファイル構成**・親子とも rowversion 列を持つ最小図（gadgets / gadget_notes）で ConcurrencyVo{InMemory,SqlServer,EfCore,Remote}RuntimeTests が使う・MultiTargetRowVersionFixture.g.cs＝rowversion 列 × sqlserver/sqlite マルチターゲット（VO・EditModel・EF Core なしの最小構成）＝型統合（byte[]）と「SQL Server だけが書き込み除外＋版ガード／SQLite は通常列」の非対称を固定。図は 1 テーブル sync_items で MultiTargetRowVersion{,Sqlite}RuntimeTests が使う・SqliteValueConversionFixture.g.cs＝SQLite 方言 × GenerateValueObjects=true の最小構成（EditModel・Mapper・EF Core・InMemory・リモートなし）＝`Convert.ChangeType` が扱えない CLR 型（TimeSpan / Guid / DateTimeOffset＝いずれも SQLite では TEXT 格納・IConvertible 非実装）の読み戻し経路を固定。図は 1 テーブル time\_probes で SqliteValueConversionRuntimeTests が使う・UniquenessSqlServerFixture.g.cs＝QueryFixture と同一図から名前付きクエリだけを外し SQL Server 方言のQuickER 版 Repository 単独（VO 有効）で生成＝重複事前チェックのランタイムスイートを SQL Server でも同一アサーションのまま流すための最小構成。UniquenessCheckSqlServerRuntimeTests・IncludeGraphSqlServerRuntimeTests（いずれも Docker）が使う・SyncFixture＝双方向同期支援（GenerateSyncSupport=true）× sqlserver/sqlite マルチターゲット × **GenerateRemoteServices=true** × 親子 2 テーブル（sync_orders / sync_order_lines・rowversion 列は NULL 許容）＋**版なし子テーブル sync_notes（rowversion なし＝後勝ち専用・版あり親を FK 参照する混在トポロジ）**＝同期エンジンの固定部・同期記述子（SyncTable/VersionlessSyncTable の両派生）・ジャーナル記録デコレータ・SyncGraphRecorder・直結／HTTP 差分ソース（キー順ページング含む）・DI 登録（構築時除外含む）・同期専用エンドポイント（SyncPage 含む）を固定。**本体＋SyncFixture.RemoteServer.g.cs の 2 ファイル構成**（リモートサービスを同じ図へ載せたのは、直結と HTTP の実行時テストが同じ型・同じシナリオを転送経路だけ差し替えて共有できるため。別フィクスチャへ分けると生成 namespace が割れて共通基底が書けない）。FK 順（親→子）の DI 登録と方言別クォートの差分 SQL を守る。Sync{Sqlite,Http,SqlServer,SqlServerHttp}RuntimeTests が使う・SyncVoFixture.g.cs＝「VO 有効 × GenerateSyncSupport × sqlserver/sqlite マルチターゲット」の最小構成（親子 2 テーブル syncvo_orders / syncvo_lines・**rowversion 列は NOT NULL 宣言**＝版読み生成式が無条件 `?.Value` であることを固定する側のアーム。EditModel・EF Core・リモートなし）＝VO×Sync 初のランタイム検証。SyncVoRuntimeTests（サーバー役も SQLite・Docker 不要・CI 常時＝オフライン新規行のアップロード・キーだけのスタブのグラフ削除記録・版書き戻し）が使う）の再生成が必要。各ドリフトテスト（GeneratedFixtureDriftTests / PortableFixtureDriftTests / SqlitePortableFixtureDriftTests / MultiTargetPortableFixtureDriftTests / InMemoryFixtureDriftTests / QueryFixtureDriftTests / RemoteContractFixtureDriftTests / RemoteServiceFixtureDriftTests / BinaryFixtureDriftTests / SqlServerBinaryFixtureDriftTests / BinaryVoFixtureDriftTests / ConcurrencyFixtureDriftTests / MultiTargetRowVersionFixtureDriftTests / SqliteValueConversionFixtureDriftTests / UniquenessSqlServerFixtureDriftTests / SyncFixtureDriftTests / SyncVoFixtureDriftTests＝出力ファイル構成そのものも検知対象）が「再生成→差分ゼロ」を検証し、失敗メッセージに再生成手順がある。再生成は次の 1 コマンドでまとめて処理する（環境変数 `QUICKER_REGEN_FIXTURES` を立てるとドリフト検知と同一経路で上書き。実処理は FixtureDriftHarness に集約）：

  ```powershell
  $env:QUICKER_REGEN_FIXTURES=1; dotnet test tests/QuickER.Tests/QuickER.Tests.csproj --filter "FullyQualifiedName~Drift"; $env:QUICKER_REGEN_FIXTURES=$null
  ```

  再生成後は環境変数なしで同じテストを流し、緑（ドリフトなし）を確認する（MultiTargetPortableFixture はマルチ辞書オーバーロードで生成するため、FixtureDriftHarness の該当オーバーロード経由で再生成される）。同じ 1 コマンドで**ランタイムパッケージ用ソース 7 本**（`src/QuickER.Runtime*/Runtime*.g.cs`＝Core/SqlServer/Sqlite/EntityFrameworkCore/InMemory/AspNetCore/Sync・RuntimePackageSourceDriftTests が検証・.gitattributes で eol=crlf 固定）と、**動くサンプルのチェックイン生成物 6 本（2 サンプル）**（ec-order＝`EcOrder.g.cs`（SQLite 方言・QuickER 版 Repository・VO なし）・`EcOrder.g.md`（--generate-api-docs 同梱出力・英語正本）・`EcOrder.sql`／ec-order-remote＝`EcOrderRemote.g.cs`＋`EcOrderRemote.RemoteServer.g.cs`（GenerateRemoteServices の 2 ファイル構成）・`EcOrderRemote.sql`。EcOrderSampleDriftTests / EcOrderRemoteSampleDriftTests が検証。生成コードは**実 CLI と同一経路**で照合＝実 `quicker generate` が生成した物と同一であることを保証。DDL は非決定的な `-- 生成日時:` 行のみ正規化して比較・.gitattributes で eol=crlf 固定）も再生成される
- ランタイムパッケージ 7 csproj の依存集合は**完全一致で固定**（RuntimePackageProjectDependencyGuardTests）: Core=PackageReference ゼロ／SqlServer=Microsoft.Data.SqlClient のみ／Sqlite=Microsoft.Data.Sqlite＋SQLitePCLRaw ピンのみ／EntityFrameworkCore=Microsoft.EntityFrameworkCore.Relational のみ／InMemory=PackageReference ゼロ（Core への ProjectReference のみ）／Sync=PackageReference ゼロ（Core への ProjectReference のみ）／AspNetCore=PackageReference ゼロ＋FrameworkReference は `Microsoft.AspNetCore.App` の 1 つだけ（他 6 つは FrameworkReference を持たない逆表明も固定＝共有フレームワーク参照は参照側プロジェクトの種別を縛るため）。DI 系・ADO・他方言を足すとガードが落ちる（依存を足す前に「それは本当にパッケージの責務か（スキーマ依存物ではないか）」を疑う）
- ランタイムパッケージ 7 種の**公開 API 面はスナップショットで固定**（`tests/QuickER.Tests/PublicApi/{アセンブリ名}.approved.txt`＝リフレクションで型・基底/実装インターフェイス・public/protected メンバーのシグネチャ・省略可能引数の既定値・定数値・列挙体の値を正規化した CRLF テキスト。属性・XmlDoc・NULL 許容注釈・コンパイラ生成物は対象外＝正規化仕様の正本は `PublicApiSnapshot` の XmlDoc）。ロックステップ版で「同一メジャー内は互換」を SemVer で宣言している以上、メンバーの削除・シグネチャ変更は利用者のコンパイル済みコードを黙って壊すため、依存集合（RuntimePackageProjectDependencyGuardTests）・型集合（SplitRuntimeSymmetryTests）に加えて面そのものを固定する。承認ファイルの更新は既存フィクスチャと同じ 1 コマンド（`QUICKER_REGEN_FIXTURES=1` ＋ `--filter "FullyQualifiedName~Drift"`）で、**再生成は承認であって検証ではない**＝差分が出たら先に「利用者を壊す変更か」を判断する。スナップショットはコンパイル済みアセンブリを読むため、**テンプレート変更がパッケージの公開 API 面を変えるときは「regen → 再ビルド → regen」の 2 回が要る**（1 回目は旧アセンブリの面で承認してしまい、環境変数なしの検証実行でスナップショットだけが赤になるのが正常な合図。2 回目の承認前に差分の目視判断を飛ばさない）
- DDL 出力の先頭コメントは `-- DDL auto-generated by QuickER`（DdlGeneratorBase.cs／SqliteDdlGenerator.cs で同一文言）。生成 SQL に乗る固定文（DDL ヘッダ・生成日時行・多対多コメント・Oracle の ON UPDATE 注意コメント・同期スクリプトのセクション見出し／スキップコメント）は英語が正本で、`DdlOutputEnglishGuardTests`（ASCII のみの図・差分から生成した DDL／同期スクリプトに CJK 混入がないことを検証）が回帰を検知する
- `PasswordBoxBehavior`（QuickER.Gui.Common）の DP 既定値は **null 必須**。string.Empty にすると空文字バインド時に PasswordChanged が購読されず入力が VM に届かない（PasswordBoxBehaviorTests が守る）。バインド先 VM プロパティは空文字で初期化する
- 生成ランタイムの SqlParameter は `[SqlColumnType]` 属性由来の明示型。文字列 Size の「宣言長超過なら値長」ガードを固定 Size にするとサイレントなデータ破損を招く
- SQLite 方言の生成 `SqlConnectionFactory` は、接続文字列に `Foreign Keys` キーワードが**無いときだけ** `True` を補う（`SqliteConnectionStringBuilder.ForeignKeys` が `bool?` ＝ null で「未指定」を判別。明示指定は `False` も含めてそのまま尊重）。SQLite は既定で FK を検査しないため、補わないと生成 DDL が宣言した FK が黙って無効化される（存在しない親を参照する子行が入り、親の削除で子が残る）。この既定に**サンプル（PRAGMA を撤去済み）と統合テストが依存**しており、外すと「FK 違反が例外にならない」形で静かに回帰する（SqliteForeignKeyDefaultRuntimeTests が守る）
- 生成 Repository のトランザクション失敗経路は、catch から**共有ヘルパー `SqlTransactions.RollbackQuietlyAsync`** だけを呼ぶ（Save 単一/複数・SQLite の BulkInsert / blob write・ExecuteDelete の 5 箇所）。ヘルパーは「完了済み（`Connection is null`）なら何もしない・Rollback 自身の例外は握り潰す・常に `CancellationToken.None`」で、**コミット後処理（`versions.Apply()` / `AcceptChanges`）は try の外**に置く。完了済みトランザクションへの Rollback は `InvalidOperationException` になり、catch から出た例外が伝播中の元の例外を置き換えて**失敗原因が完全に消える**（ビルドでも型検査でも出ない）。SqlTransactionRollbackTests が実 SQLite の挙動と生成コードの構造を固定する
- インメモリ Repository の**直接 CRUD（Insert / BulkInsert / Update）は引数の `RowState` を触らない**（他 3 実装＝SqlServer / EF Core / HTTP と同じ契約。確定させるのはグラフ保存成功後の `SaveAsync` だけ）。触ると Added のまま Insert したエンティティが続く `SaveAsync` で「変更なし」に化け、実 DB なら主キー重複で失敗するケースがインメモリだけ黙って通る（`Store.Insert` の版書き戻しは別機構なので無関係）。`InMemoryFixtureRuntimeTests` の「直接 CRUD は RowState を触らない」が守る
- **スキーマ取込・同期のコマンドは必ず `DbCommands.Create` で作る**（5 方言の `*SchemaImporter` / `*SchemaSyncExecutor` 計 10 ファイル）。コマンド生成箇所は取込 3〜7 箇所・同期 1〜3 箇所と散在し、生成箇所ごとに `CommandTimeout` を書く運用ではクエリを 1 本足したときに設定が静かに漏れる（実際、SQLite の同期実行器だけ 3 箇所とも未設定＝ADO 既定 30 秒のままだった）。タイムアウトの正本は `DbConnectionSettings.CommandTimeoutSeconds`（既定 60・`0` は ADO の規約どおり無制限・負値は `DbCommands.Create` が `ArgumentOutOfRangeException`）で、GUI の接続プロファイル／CLI `scaffold --command-timeout` から流れる。素の `connection.CreateCommand()` / `new XxxCommand(...)` が 1 つでも現れたら `SchemaCommandTimeoutGuardTests` が落ちる
- **SQLite のテーブル再構築は意味モデルに無い列レベル属性を落とす**（`AUTOINCREMENT` / `DEFAULT` / `CHECK` / `COLLATE` / 生成列）。インデックス・トリガーは `SchemaAuxiliaryObject` が CREATE 文全文で温存し、暗黙の rowid 別名（`INTEGER` 主キー）も保たれるので、消えるのはこの 5 種だけ。喪失を黙らせないため、SQLite の取込は `sqlite_master` の `type='table'` の `sql` を `SchemaImportResult.TableCreateSql` として持ち帰り、`SyncPlanner` が再構築対象（`CreateOnly=false`）に限って `TableRebuildAttributeDetector` で検出し `SyncPlanWarningKind.TableRebuildDropsColumnAttribute` を積む（実行確認ダイアログに表示）。検出は文字列レベル（最外括弧内・引用/リテラル/コメント除去・語境界・大文字小文字非依存）の割り切りで、警告のため過剰検出側に倒す。`TableCreateSql` を合成に使わないこと（合成の正本は `LiveEntities`）
- **ドライバが返した生の値の型変換は `Convert.ChangeType` 単独に任せない**（2026-08-16）。`TimeSpan` / `Guid` / `DateTimeOffset` / `DateOnly` / `TimeOnly` は `IConvertible` を実装しないため、これらを TEXT で返すドライバ（Microsoft.Data.Sqlite は 5 種すべて TEXT 格納）では**値の中身に関係なく必ず** `InvalidCastException` になる。SQL Server の ADO は型付きで返すので型一致で素通りし、単体テストでも露見しない。共有変換は `RawValueConverter.ConvertRaw`（Core パッケージ収載）で、値オブジェクトの再ラップ（`SqlValueObjectActivator.ResolveFactory`）・生 SQL スカラー（`RawSqlMapper.ConvertSingleValue`）・生 SQL 射影 DTO（`RawSqlMapper.CoerceProjectionValue`）の 3 経路がここを通る。方言エンジン側の `EntitySaveMetadata.CoerceScalar`（SQLite 専用・素の列用）は `DateTime` を `RoundtripKind` で読む分だけ意図的に別実装で、共有する 3 型は同じテキストを受け付ける（相互参照コメントで固定。**片方だけ変えないこと**）。行マッピング `SetColumnValue` は「VO は `Wrap`・それ以外は `CoerceScalar`」で分岐する（`MapEntityObject` と同じ規則。VO 有効時に無条件 `Wrap` にすると、`ISqlExecutor.QueryBySqlAsync<TEntity>` へ渡された手書きエンティティの素の列に格納値の文字列がそのまま渡り `ArgumentException` になる）。実 DB 検証は SqliteValueConversionRuntimeTests（CI 常時）

- **図の名前・型文字列の信頼境界（2026-09-11）**: テーブル名・列名・制約名・クエリ名と `Column.DataType` は GUI 自由入力・DB 取込・DBML/Excel 取込・MCP/AI 経由の**任意文字列**で、Description と違い出力側でエスケープされない箇所が層をまたいで散っていた。方針は「**生成前診断（名前の改行・制御文字のみ Error）＋出力側は方言ヘルパーへ集約**」で、適用面は 4 つ。**(1) 動的 SQL の二段エスケープ**＝`{SqlIdentifier,PgIdentifier,MySqlIdentifier,OracleIdentifier}.QuoteForDynamicSql`（クォート＋リテラル用 `'` 二重化。MySQL は `\` も）。制約名不明の PK / FK 削除は `EXEC('…')` / `DO $$ … EXECUTE '…'` / `PREPARE`＋`CONCAT` / `EXECUTE IMMEDIATE '…'` で組み立てるため、クォートだけでは名前の `'` が外側のリテラルを閉じる。SQLite は再構築方式で動的 SQL を持たないため**このヘルパーを持たない**（対称性の意図的な欠落）。**MySQL は実行時に読んだ値を動的 SQL へ埋める面が 3 つあり、それぞれ別の規則が要る**＝(a) 逆引きした制約名 `@fk` は `CONCAT` のバッククォートへ入るため SQL 側で `REPLACE(@fk, '`', '``')` を通す（生成時の `Escape` では届かない。`@pk` は存在判定にしか使わず埋めないので対象外）・(b) `information_schema` 検索のスキーマは操作対象と一致させる（`MySqlIdentifier.SchemaNameOnly` が非 null ならその名前・null なら `DATABASE()`＝`SchemaPredicate`。`DATABASE()` 固定だと修飾名 `other.t` の説明／列順同期がカレント DB の同名テーブルの定義を `other.t` へ焼き付ける）・(c) 実行器 `MySqlSchemaSyncExecutor` はスクリプト前にセッションの `sql_mode` から `NO_BACKSLASH_ESCAPES` を外す（`EscapeStringLiteral` 全体がバックスラッシュエスケープ前提。外さないと説明の `\` が無言で二重化され、`'` を含む生成列式は構文エラーで中断＝部分適用）。あわせて **MySQL のライブ再構成は「列の不在」と「再構成の失敗」を `@exists` / `@def` の 2 変数で分ける**（`@def` の NULL だけで `DO 0` へ倒すと、列順同期で移動列の 1 本が畳まれても中途半端な列順のまま成功報告になる。在るのに組めないときは PREPARE が落ちる文を組み立てて必ず失敗させる＝`SIGNAL` はプリペアド文では使えない）。**(2) `--` コメント行のサニタイズ**＝`SqlComment.Sanitize`（改行・制御文字 U+0000〜U+001F・U+007F **＋ Unicode の行区切り U+0085 / U+2028 / U+2029** を空白 1 つへ・長さ不変）。FK / 一意制約のスキップコメント（`SyncScriptBuilderHelper`）・主キー非対応コメント（`SyncScriptBuilderBase`）・SQLite の対象外コメントと説明コメントと**テーブル再構築の見出し**・MySQL の列順見出しと列スキップ・多対多コメント（`DdlGeneratorBase` と `SqliteDdlGenerator` の**両方**）が経由する。Gui 側（Mermaid / DBML の識別子・ラベル）は `ExportTextSanitizer.Sanitize` が同じ実装へ 1 行委譲する。**埋め込み箇所の漏れは `SqlCommentSanitizeGuardTests` がソース上で全数検査する**（`--` で始まる文字列リテラルを含む文の補間穴が `SqlComment.Sanitize` か明示の安全リスト〔enum・int・固定書式〕のいずれかであること。文は「文字列リテラル・コメントの外の `;`」で区切り、穴の抽出はリテラルの中身だけから行う＝コードブロックの波括弧を穴と誤認しない。SQLite の再構築見出しと SQLite の多対多コメントはこのガードで発見された）。**(3) 型文字列の安全パターン**＝`SqlTypeText.IsSafe` / `Validate(ErDiagram)` / `Validate(SyncPlan)`。型は識別子でも文字列リテラルでもなく素通しで補間するしかないため、`DdlGeneratorBase.Build` / `SqliteDdlGenerator.Build` / `SyncScriptBuilderBase.Build` / `OracleSyncScriptBuilder.Build` / `SqliteSyncScriptBuilder.Build` の 5 入口で検証し、不一致は「テーブル.列 = 型」を名指しして `InvalidOperationException`。**ホワイトリストは構造的（キーワード許可集合ではない）**＝`;` / 引用符 / バッククォート / `\` / コメント記号 / 改行を通さない一方、語の並び＋括弧引数（`(max)`・`(n)`・`(p,s)`・Oracle の単位語）＋後続語＋末尾 `[]`＋MySQL の値リスト（`enum('a','b')` だけ引用符を許す）を通す。キーワード許可集合にすると 5 方言の取込が verbatim で持ち帰る実在型（`LONG RAW` / `SDO_GEOMETRY` / `VARYING CHARACTER(255)` / 別名型の非 ASCII 名）を弾き、セキュリティ修正が実 DB でのデータ欠落へ化ける。**語の構成文字から `#` を外す**（MySQL の `#` は行末コメント開始＝`int#` が通ると `MODIFY COLUMN` の後続句〔NOT NULL / COMMENT / AFTER〕が黙って消える）・**前後に空白や改行が付いた表記は拒否**（`IsSafe` が Trim 後に判定し生成側が生を補間する不一致を塞ぐ＝`"\nGO\n"` は Trim 後 `GO` に見えて PASS し、出力されると SQL Server のバッチ分割 `^\s*GO\s*$` を誤作動させ得た。空・空白のみ＝未設定は従来どおり safe）・**2 語目以降の列定義句キーワードを拒否**（`NOT` / `NULL` / `DEFAULT` / `PRIMARY` / `KEY` / `REFERENCES` / `CHECK` / `GENERATED` / `IDENTITY` / `COLLATE` / `UNIQUE` / `ON`＝型は列定義の途中へ補間されるため `int NOT NULL` は構文エラーにならず図の宣言と静かに食い違う。判定は括弧の中を除いた語単位。5 方言カタログ 137 表記・取込の実在表記とは 1 件も衝突しない＝全数テストで確認済み）。**(4) C# リテラルのエスケープ**＝`EscapeNameForCSharpString`（名前用・空白のみを空へ畳まない）/ `EscapeForCSharpString`（説明用）/ `EscapeForCSharpInterpolatedString`（`$"…"` のリテラル部＝波括弧も二重化）。適用面は `[Table]` / `[Column]` / `[NavigationReference]` の 4 引数 / EF Core の `ToTable` / `HasColumnName` / `SyncTableDescriptor.TableName` / `Journal.RecordAsync` / インメモリのサンプル値式で、同期 SQL の識別子は `CSharpGenerationModelBuilder.Sync.QuoteIdentifier`（終端クォート二重化）を通す。**改行畳み込みは C# 言語仕様の new-line 全部**（CR / LF / NEL U+0085 / U+2028 / U+2029）＝`FoldNewLines`。**XmlDoc の定型文へ載る名前は専用の `*XmlDoc` フィールドで XML エスケープする**（`CSharpClassModel.TableNameXmlDoc` / `CSharpPropertyModel.ColumnNameXmlDoc` / `CSharpValueObjectModel.ColumnNameXmlDoc` / `CSharpEditModelClassModel.TableNameXmlDoc` / `CSharpSyncTableModel.TableNameXmlDoc`＝説明が無いときのフォールバック文 5 箇所。テンプレートの `*_xml_doc` 参照サイトは 6（_01 VO / _02 Entity・プロパティ / _03 EditModel / _07 インメモリシーダー / _10 同期記述子）で、Sync の 1 箇所だけはフォールバックでなく無条件文＝「5 フォールバック」と数が違って見えるのは数え方の差。C# リテラル用のエスケープを XmlDoc へ流用すると `<` が生のまま残って CS1570 になり、生のまま流すと U+2028 で `///` 行を抜けて任意の C# を注入できる）。**(5) 名前の入口検証**＝`SqlNameText.IsSafe` / `Validate(ErDiagram)` / `Validate(SyncPlan)` を `SqlTypeText.Validate` と同じ 5 入口へ並べ、テーブル名・列名・制約名の改行・制御文字（判定は `SqlComment.ContainsControlCharacter` と共有）を場所つきで拒否する（`TABLE 'x'` / `COLUMN 'x'.'y'` / `UNIQUE 'x'.'y'` / `FOREIGN KEY 'x'`）。**説明は対象外**（改行が正当＝コメント側の Sanitize が唯一の防壁なので上記のガードテストが要る）。C# 生成側の同規則は `ValidateNameControlCharacters`（生成前診断 Error・`IsControlCharacter` は Provider 側と**意図的に二重定義**＝依存方向が Provider → CodeGen.CSharp のため。片方だけ変えないこと）。**(6) 合成する制約名のクォート**＝`PK_{SafeName(テーブル名)}` は `SafeName` が終端クォート文字を二重化しないため、埋め込み側で必ず方言の `Escape` を通す（SQLite の DDL＋逐次 DDL 4 方言の同期の `CREATE TABLE` / `ADD CONSTRAINT` の計 8 箇所。UNIQUE / FK の合成名は当初から通っており、主キーだけが非対称だった）。**PostgreSQL の `DO` ブロックのドルタグは本文から決定的に選ぶ**＝`PgIdentifier.DollarQuoteTag(body)`（本文に現れない限り `$$`、現れるなら `$q$` → `$qq$` …。ドルクォートはタグ文字列そのもので終端するため、`$$` 固定だと `$$` を含む名前がブロックを閉じる。リテラルエスケープは `'` しか見ないので無関係。乱数不可＝出力の決定性）。**生成ランタイムの識別子クォートは `EntitySaveMetadata.QuoteIdentifier` / `QuoteTableName`**（テンプレート `_06` 在住・`internal` なので PublicApi 面は不変。`QuoteIdentifier` の emit ガードは `repositories || ef_core`＝EF Core 単独出力の `columnList` も呼ぶ）で、**テーブル名は最初の `.` で分割クォート**（DDL 側と揃える＝まとめてクォートすると dbo 以外のスキーマの図で全 CRUD が実行時エラー）・列名は終端クォート文字を二重化する。`SqlExpressionTranslator.RawColumnName` は二重化を解いて返す（解かないと `[SqlColumnType]` の逆引きが外れて型付きパラメータが静かに落ちる）。**DBML のリテラルは `DbmlLiteral.Escape` / `Unescape` の対**（`\` 二重化 → `'` エスケープ・復元は左から 1 パス。逐次 `Replace` にすると解いた結果がもう一方のエスケープに見えて二重に解ける。改行は空白へ畳む＝複数行 Note の `'''` ブロックは `DbmlImporter` が行単位解析で非対応のため）。**ガードテスト**＝`SqlTypeTextTests`（5 方言カタログの全表記＋取込表記の通過を実カタログから走査）/ `SqlNameTextTests`（入口検証を 5 方言の DDL・同期へ同一ケースで当てる）/ `SqlCommentTests` / `SqlCommentInjectionTests`（入口で止めない説明の実出力＋共有スキップコメントヘルパー）/ `SqlCommentSanitizeGuardTests`（埋め込み箇所のソース全数検査）/ `ConstraintNameEscapingTests`（合成 PK 名を 5 方言まとめて）/ `IdentifierSymmetryTests`（5 方言へ**同一ケース集合**）/ 4 方言の `*SyncScriptBuilderTests` の動的 SQL・ドルタグのケース / `NameTrustBoundaryGenerationTests`（Roslyn 実コンパイル＋診断＋XmlDoc の XML エスケープ）/ `GeneratedIdentifierQuotingTests`（ランタイムパッケージを実行してクォートを見る）/ `ExportNameInjectionTests` / `SchemaSyncDialogViewModelTests`（プレビュー生成の失敗はステータス行へ＝`UpdatePreview` は `IsSelected` 購読から直接呼ばれるので例外が抜けると WPF の未処理例外になる）。mutation 実証済み＝`QuoteForDynamicSql` 素通し・`EscapeNameForCSharpString` 除去・`IsSafe` 常に true・テーブル名の分割無効化・`Sanitize` の畳み込み除去・`DbmlLiteral.Escape` のバックスラッシュ二重化除去・**合成 PK 名の `Escape` 除去**・**ドルタグの `$$` 固定化**・**`SqlNameText.Validate` の除去**・**`IsSafe` の Trim 判定戻し**・**SQLite 多対多コメントの `Sanitize` 除去**・**XmlDoc の `EscapeForXmlDocSummary` 除去**・**プレビューの catch 狭め**の 13 点で、いずれも該当群だけが赤

- **値オブジェクトの `Create` / `TryCreate` / `Validate` の本体は `ValueObjectBaseCore<TSelf, TValue>` に 1 回だけ**（2026-08-26・第 8 次）＝各型は `New`＋`ValidateCore` の明示的実装と partial フック宣言（`OnValidate` / `Customize*`・2026-08-29 から `GetDefinedInstance` / `ConvertCustomInput` のブリッジ込み）だけを持つ（基底クラスから継承した静的メソッドが `static abstract` インターフェイスメンバを満たすことに依存。手書き VO を 3 メンバで書ける公開レシピの正本は docs/code-generation.md（.ja）の「手書きの値オブジェクト」節）。この構造のため **Create をリフレクションで名前解決する箇所（`SqlValueObjectActivator.ResolveFactory`・materializer の `ResolveValueObjectReader`＝SqlServer / Sqlite / EF Core の 3 エンジンに展開）は `BindingFlags.FlattenHierarchy` 必須**＝外しても基底宣言の静的メンバが見えなくなるだけでビルドは通り、`Wrap` は生値を素通しし・高速経路は静かに `SetColumnValue` フォールバックへ落ちる。HandwrittenValueObjectTests（mutation 実証済み＝フラグ除去で A-1 の 4 ケースだけが赤）が固定する

- **値オブジェクトの検証規則の本体は Runtime の共有ルールクラス 4 つに 1 回だけ（2026-09-03・移設 P1）**＝`ValueObjectRules`（必須チェック・bool 返しで後続規則を打ち切る）/ `ValueObjectStringRules`（MaxLength・半角英数＋許可記号・メール実用検証）/ `ValueObjectNumberRules`（整数桁数・範囲）/ `ValueObjectDecimalRules`（precision/scale・Pow10 テーブル比較）。per-VO の `ValidateCore` は「共有ヘルパ呼び出し（定数は引数渡し＝総称ディクショナリ参照を増やさない）＋ `OnValidate`」の定型のみ。**文言フック 5 種（`Customize{MaxLength,Scale,Precision,ValueRequired}ErrorMessage`＋`CustomizeDisplayName`）は代替の拡張点を新設せず廃止**＝文言の正本は `ValueObjectValidationMessages`（全エントリの第 1 引数が displayName。**既定文面には埋め込まない**＝差し替え側が分岐・埋め込みに使う材料として渡す）。型ごとの文言・表示名は `displayName == XxxValue.DisplayName` 比較／`GeneratedDisplayNames.Resolve` の `nameof(Entity.Prop)` 比較で行う（いずれも型・プロパティが消えるとビルドエラー＝コンパイル安全。レシピの正本は docs 値オブジェクト節）。**拡張点の一般基準は「他の手段で実現不能な生成パイプラインへの挿入のみ・ただし上位基準は利用者の使いやすさ」**（grill-me 2026-09-03 確定）＝意味系フック（`OnValidate` / `GetDefinedInstance` / `ConvertCustomInput`）は第 8 次設計のまま存続。汎用検証ヘルパ 4 種（桁数・範囲・半角英数・メール〔'@' 1 つ・前後非空・空白なし＝実用最小で RFC 完全検証は意図的にしない〕）は利用者の `OnValidate` から呼ぶ public API。成功経路の割り当てゼロ（`ref List<string>?` 素通し）は共有後も維持＝割り当て実測テストが固定・意味論等価は mutation 実証済み（scale 判定を壊すと既存 VO テストだけが赤）

- **生値変換の型独自入力（`TryConvertCustomInput`）は基底の `TryCreateFrom` が `TSelf.` 経由で照会する**（2026-08-29）。具象型名で 4 引数 `TryCreateFrom` を呼んでもフックが効くのはこの照会があるからで、per-VO 側の実装だけで受ける形へ変えると型名呼びが基底の継承メソッドへ静的束縛されて迂回が再発する（旧設計「`TryCreateFrom` を明示的実装で override」の実バグ＝4 引数の型名呼びだけが override を飛ばした）。利用者拡張は生成 VO では partial フック（`GetDefinedInstance` / `ConvertCustomInput`）経由＝`TryGetDefined` / `TryConvertCustomInput` の明示的実装を利用者 partial に直書きすると生成ブリッジと衝突してコンパイルエラーになる（意図的＝シグネチャ間違いが静かな no-op にならない）。フックの中から `Create` / `TryCreate` / `TryCreateFrom` / `CreateFrom` を呼ぶと再帰＝catch 不能の StackOverflow（XmlDoc と docs に明記済み）。ValueObjectDefinedInstanceTests の「変換フックは全呼び形で効く」「実生成 VO のブリッジが partial フックへ配線されている」が固定する（mutation 実証済み＝基底の照会除去で該当テストだけが赤）

- **GuidKey 値オブジェクトの長さ検証は素通しした MaxLength が駆動する（2026-09-03）**。旧実装は GuidKey のとき MaxLength を無条件 null 化しており、「長さ検証は VO が担うから Entity へ `[MaxLength]` を出さない」内部契約が破れて検証がスタック全体から消えていた（宣言幅超過が INSERT の切り詰めエラーで初めて露見）。素通しにより `_01_ValueObjects.scriban` の既存 emission（`vo.max_length` 駆動）がそのまま効く＝テンプレート無変更。列幅が正の値かつ 36 未満は生成時 **Warning**（`CodeGen_Warning_GuidKeyColumnShorterThanGuid`＝引数なし `Create()` の採番 36 文字が必ず検証例外になるため名指しで告知・生成は続行）。**Error にしない**＝`UseGuidKeyForStringPrimaryKey` は図の全 string PK に効くグローバル指定で、GUID を意図しない短い string PK（コード表等）が混在する図で機能ごと使えなくなる（手動キー供給の運用は正当・rowversion 2 本の Error 前例とは「生成物が使用不能」の条件が違う）。GuidKeyValueObjectExecutionTests＋生成テスト 3 本が固定・mutation 実証済み（null 化復活で 4 件・警告条件潰しで 1 件だけが赤）

- **`MockProjectEmitTools.ResolveEmitPath` は拒否リストへ戻さない**（2026-08-22）。AI が提出したファイルは直後に同じ出力フォルダで `dotnet build` されるため、MSBuild が自動 import するファイル（`Directory.Build.props`/`.targets`・`Directory.Packages.props`・`Directory.Solution.*`・`*.rsp`・`obj/*.nuget.g.props`）を 1 つでも書けると**ビルド検証がそのままユーザー権限の任意コード実行**になる。自動 import 面は MSBuild の版で増えるため名前の列挙では原理的に追随できず、「UI 層のソース拡張子だけを通す」ホワイトリストであることが防御の本体（`obj/`・`bin/` のフォルダ拒否は、拡張子集合をいじるたびの再検証を不要にするための二重化）。守るべき性質は「**ユーザーが明示的にアプリを起動する前に、ビルド検証の瞬間に AI のコードが走らないこと**」で、`.cs` を許可してよいのはビルドでは実行されないから（ソースジェネレータは csproj 経由でしか追加できず csproj は保護済み）。プロファイルの宣言が中央上限集合の部分集合であることは `MockProjectEmitToolsTests` の全数列挙テストが固定する

- **`.cmd` / `.bat` シムの起動は必ず `BatchShimProcessGuard` を通す**（2026-09-11）。`ProcessStartInfo.FileName` が `.cmd` / `.bat`（npm 導入の CLI の典型）のとき CreateProcess は内部で `cmd.exe` を経由し、.NET が C ランタイム規約で組んだコマンドラインを cmd が**もう一度**自分の構文で解釈する＝引数の `"` や `&` が引用の外へ抜けて任意コマンドの実行につながる（BatBadBut 型）。ガードには**入口が 2 つあり、引数の形が違えば取り得る防御も違う**: (1) `WrapCommandLine`＝連結済みの 1 本の文字列（Codex の app-server 引数。トークン境界が分からず個別に引用できないため `" & | < > ^ %` を含んだら起動前に拒否）・(2) `Apply(ProcessStartInfo)`＝`ArgumentList`（Claude Code。トークン境界が分かるので各トークンを `"` で包み内側の `"` を `""` へ倍加し、`"` 直前の連続 `\` を C ランタイム規約どおり倍にする。引用の内側では `& | < > ^ ( )` は cmd にとってただの文字になるため、残る危険は `%` の展開〔展開結果が `"` を持ち込める〕だけで**これだけを拒否**し、遅延展開の `!` は `/v:off` を明示して無効化する）。**(2) をメタ文字の一律拒否にしてはいけない**＝Blazor のシステムプロンプトは `<Routes @rendermode="InteractiveServer" />` を含み、引数で渡る以上モックプロジェクト生成が `.cmd` 導入環境で丸ごと動かなくなる。**改行だけは引数・パスのどちらでも両入口で拒否する**（2026-09-11 に方針転換）＝cmd は `%*` を展開する段階で最初の改行から後ろを捨て、**その引数の残りだけでなく後続の引数もすべて**エラーなしに消える（実機プローブで確認。コマンド実行には至らないので挿入ではなく「黙った切り捨て」だが、黙る分むしろ危うい）。引用しても防げない＝`cmd.exe /d /s /v:off /c` へ包み直す前の段階で起きるためガード層で拒否するしかない。**複数行の内容はファイル経由で渡す**＝`ClaudeCodeProcessClient.AppendSystemPromptArguments` がシステムプロンプトを `%TEMP%\QuickER\claude-code\{GUID}.txt`（BOM なし UTF-8）へ書いて `--append-system-prompt-file` で渡し、ターン終了後の finally でベストエフォート削除する（旧実装の `--append-system-prompt <本文>` は 1 行目で切れ、後ろに積んだ `--model` / `--resume` ごと消えていた＝resume 消失で毎ターン新規セッション。防御の二重化として引数の積み順も「`--model` / `--resume` が先・プロンプト系が後」へ変更済み）。**Copilot は例外で、起動が GitHub.Copilot.SDK の内側＝QuickER が引数を組み立てない**ため掛けられるのは解決パスの検証だけ（`CopilotCliLocator.EnsureSafeToLaunch`＝**起動直前に置く**。解決側へ置くと `IsAvailable` の検出だけで例外になる）。残余は docs/ai-chat.md の注意節に明記。文言 resx はバックエンドごと（どの CLI が止まったかを名指しするため）で、ポリシーだけが共有
- **内蔵チャットの CLI バックエンドは作業フォルダを `%TEMP%\QuickER\{backend}\<guid>` へ隔離する**（2026-09-11 に Codex も追随）。Claude Code / Copilot は元からこの形で、Codex だけ `Cwd = Environment.CurrentDirectory`（＝アプリの実行フォルダ）のまま `ApprovalPolicy = never` で走っており、承認なしにその配下を読めた。`CodexChatEngine.BuildThreadStartOptions` は一時フォルダを遅延生成して `Cwd` に据え、あわせて `Sandbox = "read-only"` を**明示**する（既定任せにしない）。チャットの仕事は `dynamicTools` 経由の図の編集だけでファイルを書く必要が無いのが read-only にできる根拠で、**モック生成側（`CodexMockProjectAgent`）は出力フォルダへ書くのが目的なので `workspace-write` のまま**＝ここが両者の唯一の差。破棄時の `TryDeleteWorkingDirectory` は子プロセス停止の後に呼ぶ（掴まれたままだと消せない）
- **モーダルは必ずオーナー付きで出し、実行中フラグを落としてから出す**（2026-08-22）。`MessageBoxDialogService` の 7 メソッド（IDialogService 実装の全部）はすべて `ResolveOwner()`（アクティブなウィンドウ→`MainWindow`）を通す。オーナーを与えない `MessageBox.Show` は、モードレスで開いた機能ウィンドウ（AI モック生成・`MockGenerationLauncher` が `Owner = null; Show()`）の**背面へ回り込んで見えなくなる**——モーダルは呼び出し元スレッドを止めるので、画面はダイアログを出す直前の状態のまま固まって見える（実際にモック生成の完了ダイアログで発生。`StatusMessage` だけ完了になり「生成を実行中です。」とボタン無効化が残った）。**Z 順序は WPF 実行時の挙動でテストでは守れない**ため、オーナー付与を外さないこと。あわせて完了・失敗のモーダルは `finally` で実行中フラグを解除した**後**に出す（背面に回り込んでも画面は完了状態として正しく見える。順序は `GenerateMockProject_ResetsProgressBeforeShowingDialog` が固定）

これらのファイルに触るときは、対応するテスト（各方言の *DdlGeneratorTests / PasswordBoxBehaviorTests / CSharpCodeGenerationServiceTests / GeneratedFixtureDriftTests）を必ず実行する。

## コーディング規約

- 既存コードと同様、ビヘイビアや主要メソッドに分かりやすい日本語コメントを付ける（**samples/ 配下の手書きコードは例外で英語コメント**）
- `if` / `for` / `switch` / `try` 等のブロック前後には空行を1行入れる（最終判断は CSharpier の挙動を優先）
- 文字列リテラルの定数化は一貫性重視：一部の表示文言だけを定数化せず、やるなら同種をまとめて整理する
- ビルド警告は可能な限り解消する

## ワークフロー

- `git add` / `git commit` は**ユーザーの明示的な指示があるまで実行しない**（変更はワーキングツリーに残してレビューを待つ）。ブランチ作成はタスク開始時に行ってよい
- **コミットメッセージは「件名 70 字前後の要約＋空行＋本文に詳細」**。設計判断・検証結果の全文は本文へ書く（情報量は減らさない）。件名は切り詰めでなく要約として完結させ、履歴から識別できる固有性（機能名・対象コンポーネント名）を持たせる。Co-Authored-By トレーラは本文末尾
- 計画・教訓は `tasks/todo.md` / `tasks/lessons.md` に記録する運用。**tasks/ はリポジトリに含めない**（メンテナのローカルで非公開管理・.gitignore 済み）。tasks/ が存在する環境では、セッション開始時に lessons.md を確認する
- レイアウト等ビジュアルに直結する機能は、機能テストに加えて見た目の品質指標（占有面積・充填率・交差数・線長）を実測してから完了とする（lessons.md 参照）
- **本ファイルの「不変条件」節へ項目を追記するときは、その不変条件を壊すと赤になるガードテストを同時に追加する**（テストの名前を項目内に書く）。文章だけの不変条件は静かに嘘になる——2026-09-11 の全体レビューで、記述と実装が食い違ったまま残っていた例（性能の doc・ServerWins の docs 乖離）が確認されたための運用化
- **GitHub Actions のアクションは commit SHA でピンする**（`uses: owner/action@<sha> # vX.Y.Z`）。タグは可変で、上流のタグ付け替え・乗っ取りで実行内容が黙って差し替わる（公式アクションも例外にしない）。更新は `.github/dependabot.yml`（github-actions・weekly）が SHA とバージョンコメントを両方書き換える PR として持ってくるので、手で追わない。NuGet は Directory.Packages.props の中央管理＋ロックファイルで別管理のため Dependabot の監視対象に入れていない
