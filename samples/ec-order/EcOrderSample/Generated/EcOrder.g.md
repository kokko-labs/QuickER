# EcOrderSample.Generated API Reference

<!-- API reference auto-generated by QuickER. Do not edit by hand (it is overwritten on regeneration). -->

This document summarizes the API of the code generated from this diagram (namespace `EcOrderSample.Generated`). It lists the properties of each entity and, when generated, how to use the data access API (Repository contract).

## Entities

| Class | Table | Description |
| --- | --- | --- |
| `CustomerEntity` | `customers` | 顧客。注文の発注元となる購入者マスタ |
| `ProductEntity` | `products` | 商品マスタ。販売対象となる商品の定義 |
| `OrderEntity` | `orders` | 注文ヘッダ。1 顧客の 1 回の注文を表す |
| `OrderLineEntity` | `order_lines` | 注文明細。注文と商品を多対多で結ぶ明細行 |

## Entity details

### CustomerEntity

顧客。注文の発注元となる購入者マスタ

| Property | C# type | DB type | PK | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `CustomerId` | `int` | `int32` | ✓ | ✓ | 顧客ID（主キー。アプリ側で採番） |
| `Name` | `string` | `string(50)` |  | ✓ | 顧客名 |
| `Email` | `string?` | `string(100)` |  |  | 連絡先メールアドレス（任意） |

Navigations:

| Property | Kind | Target entity |
| --- | --- | --- |
| `Orders` | Child collection | `OrderEntity` |

Repository contract: `ICustomerRepository` (primary key type `int`) is generated.

### ProductEntity

商品マスタ。販売対象となる商品の定義

| Property | C# type | DB type | PK | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `ProductId` | `int` | `int32` | ✓ | ✓ | 商品ID（主キー。アプリ側で採番） |
| `Name` | `string` | `string(50)` |  | ✓ | 商品名 |
| `UnitPrice` | `decimal` | `decimal(10,2)` |  | ✓ | 商品マスタ上の販売単価 |

Navigations:

| Property | Kind | Target entity |
| --- | --- | --- |
| `OrderLines` | Child collection | `OrderLineEntity` |

Repository contract: `IProductRepository` (primary key type `int`) is generated.

### OrderEntity

注文ヘッダ。1 顧客の 1 回の注文を表す

| Property | C# type | DB type | PK | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `OrderId` | `int` | `int32` | ✓ | ✓ | 注文ID（主キー。アプリ側で採番） |
| `CustomerId` | `int` | `int32` |  | ✓ | 発注した顧客ID（customers への外部キー） |
| `OrderedAt` | `DateTime` | `datetime` |  | ✓ | 注文日時 |
| `Memo` | `string?` | `string(100)` |  |  | 注文に添える備考（任意） |

Navigations:

| Property | Kind | Target entity |
| --- | --- | --- |
| `Customer` | Parent reference | `CustomerEntity` |
| `OrderLines` | Child collection | `OrderLineEntity` |

Repository contract: `IOrderRepository` (primary key type `int`) is generated.

### OrderLineEntity

注文明細。注文と商品を多対多で結ぶ明細行

| Property | C# type | DB type | PK | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `OrderLineId` | `int` | `int32` | ✓ | ✓ | 注文明細ID（主キー。アプリ側で採番） |
| `OrderId` | `int` | `int32` |  | ✓ | 所属する注文ID（orders への外部キー） |
| `ProductId` | `int` | `int32` |  | ✓ | 対象の商品ID（products への外部キー） |
| `Quantity` | `int` | `int32` |  | ✓ | 注文数量 |
| `UnitPrice` | `decimal` | `decimal(10,2)` |  | ✓ | 注文時単価（商品マスタの改定に影響されないよう注文行に保持） |

Navigations:

| Property | Kind | Target entity |
| --- | --- | --- |
| `Order` | Parent reference | `OrderEntity` |
| `Product` | Parent reference | `ProductEntity` |

Repository contract: `IOrderLineRepository` (primary key type `int`) is generated.

## Data access API

Each generated repository implements `IRepository<TEntity, TKey>`. The main methods are as follows.

| Method | Description |
| --- | --- |
| `GetByIdAsync(id, ct)` | Gets a single entity by primary key (null when not found). |
| `GetAllAsync(ct)` | Gets all entities. |
| `InsertAsync(entity, ct)` | Inserts a single entity. |
| `UpdateAsync(entity, ct)` | Updates the single entity matching the primary key (returns whether it succeeded). |
| `DeleteAsync(id, ct)` | Deletes the single entity matching the primary key (returns whether it succeeded). |
| `BulkInsertAsync(entities, ct)` | Inserts multiple entities at once (returns the number inserted). |
| `Query()` | Builds queries such as filtering, ordering, and row limiting. |

For the supported scope and details of `Query()`, see [docs/code-generation.md](https://github.com/kokko-labs/QuickER/blob/main/docs/code-generation.md) (documentation in Japanese).

## Usage

### DI registration

Registers the QuickER Repository (Sqlite) with the DI container.

```csharp
services.AddGeneratedSqliteRepositories(connectionString);
```

### CRUD and queries

Using `CustomerEntity` (primary key type `int`) as an example, here are the representative operations.

```csharp
// Insert
var entity = new CustomerEntity();
await repository.InsertAsync(entity);

// Get by primary key
var found = await repository.GetByIdAsync(1);

// Get all
var all = await repository.GetAllAsync();

// Query (filter / order)
var results = await repository
    .Query()
    .OrderBy(x => x.CustomerId)
    .ToListAsync();

// Update / delete
if (found is not null)
{
    await repository.UpdateAsync(found);
    await repository.DeleteAsync(1);
}
```

## Generated file layout

| File | Namespace | Contents |
| --- | --- | --- |
| `EcOrder.g.cs` | `EcOrderSample.Generated` | Entity / EditModel / Mapper / Repository / Runtime |
