# 変更履歴

*[English](CHANGELOG.md) | 日本語*

QuickER の利用者に影響する変更を記録します。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います（0.x の間の版上げルールとリリース手順は [CONTRIBUTING.ja.md](CONTRIBUTING.ja.md) を参照）。

## [Unreleased]

このリリースの要点:

- 複合主キーの列順を、DB 取込・編集・DDL・生成コードのすべてで保つようになりました
- DB 取込が型と制約をより忠実に持ち帰り、持ち帰れなかったものを報告するようになりました
- 生成コードの基底クラスを、どの出力モードでも `partial` で拡張できるようになりました
- 双方向同期でデータが失われる不具合と、AI CLI の起動・モック生成の安全性の問題を直しました
- アップグレードの前に「Breaking changes」を確認してください（設定の保存先の移動を含みます）

### Breaking changes

#### 配布・設定

- **設定と作業状態の保存先が `%LOCALAPPDATA%\QuickER` に移りました** — 旧フォルダ `%APPDATA%\QuickER` は読まないため、表示言語・保存済み接続（パスワードを含む）・API キー・コード生成の設定・復旧用スナップショットは初回起動で既定に戻ります。引き継ぐには、初回起動の前に旧フォルダの中身を新フォルダへコピーしてください

#### コード生成ダイアログ・CLI

- **API リファレンスの言語は `ApiDocsLanguage`（CLI は `--api-docs-lang`）で指定するようになりました** — `English` / `Japanese` / `Both` から選び、日本語版だけも出力できます。旧キー `IncludeJapaneseApiDocs` と `--api-docs-ja` は読み替えないため、`"ApiDocsLanguage": "Both"` へ書き換えてください。GUI で日本語版を併せて出していた場合は「両方」を選び直してください
- **`generate` / `scaffold` と MCP の `generate_csharp` が、手で編集された生成ファイルを上書きしなくなりました** — 置き換え対象の `.g.cs` が生成後に編集されていると、CLI は何も書かずに一覧を出して終了コード `2` で終わり、`generate_csharp` は同じ一覧を返して失敗します（GUI は確認を出します）。整形や改行コードの変換は編集とみなさず、この版より前に生成したファイルは照合しません。生成後に生成ファイルを書き換えるスクリプトや CI がある場合は、再生成で上書きし続けるよう `--force`（MCP は `force: true`）を付けてください。詳しくは [docs/code-generation.ja.md](docs/code-generation.ja.md#生成ファイルのヘッダー) を参照してください

#### DB 同期・DDL

- **列の型と名前に置ける文字を制限しました** — DDL 生成と同期スクリプト生成は、`;`・引用符・コメント記号・改行を含む型や、`int NOT NULL` のように列定義の句を含む型を、場所を名指しして拒否します。テーブル名・列名・制約名・クエリ名に改行や制御文字があると、C# 生成を含むすべての生成が止まります。該当する図は型や名前を直してください

#### 生成コード

- **ランタイムの基底型に `Core` サフィックスが付きました** — `EntityBaseCore`・`EditModelBaseCore`・`ValueObjectBaseCore`・`IValueObjectCore`・`IRepositoryCore` / `IRemoteRepositoryCore`・各バックエンドの `…RepositoryCore`・`MapperBaseCore` です。生成コードが書く名前（`: EntityBase` など）は変わりません。ランタイムの型を直接名指ししている自前のコード（`where TEntity : EntityBase` の制約や `is IValueObject` の判定など）は `Core` 付きへ改名してください
- **文言と表示名のフックを廃止しました** — EditModel の `Customize…ErrorMessage` / `CustomizePropertyDisplayName`、値オブジェクトの `Customize…ErrorMessage` / `CustomizeDisplayName`、Entity の `CustomizeDisplayName` です。文言は `EditModelMessages` / `ValueObjectValidationMessages`、表示名は `GeneratedDisplayNames.Resolve` で差し替えます。型ごとに変えたいときは、各エントリが新しく受け取る第 1 引数（プロパティ名・表示名）で分岐してください。既定の文面は変わりません
- **Mapper の `includeRemoved` 引数が必須になりました** — 保存用のグラフを作る呼び出しには `true`、表示用には `false` を渡してください（省略時の `false` が、削除した行を保存から漏らしていました）。`MapperBase<,>` を手書きで継承して `CreateEntityCore` などを override していたコードは、`LoadColumns` / `CompleteLoad` へ移してください
- **`CheckUniquenessAsync` の宣言が共通面 `IRemoteRepositoryCore<TEntity, TKey>` へ移りました** — 呼び出し側は変わりません。リポジトリの契約を手書きで実装しているテストダブルやアダプタは、このメンバーの実装が必要になります
- **値オブジェクト有効時、C# 型が食い違う同名列は生成エラーになりました** — 一方の `payload` 列が `varbinary`・もう一方が `varchar` のような図は、従来は警告つきで 1 つの値オブジェクトへ統一され、狭い側の列の読み出しが実行時例外になっていました（型によっては比較の意味だけが黙って変わりました）。図で列の型を揃えるか、別の列として扱えるよう列名を変えてください。長さ・精度だけの食い違いは従来どおり警告＋統一です
- **EditModel を生成する構成で、列名 `row_state` は生成エラーになりました** — Mapper の状態転送（`entity.RowState = editModel.RowState;`）は名前で束縛するため、この列があると転送先が列プロパティへ静かに差し替わり、編集した行が「変更なし」として保存されていました。図で列名を変えてください
- **名前付きクエリにリモート操作と同じ名前を付けられなくなりました** — `SaveMany`・`Ping`・`SyncCeiling` / `SyncChanges` / `SyncKeys` / `SyncPage`（大文字小文字を問わず）と、大小だけ違う 2 つのクエリ名（重複扱い）が生成エラーになります。リモートエンドポイントは両者を同じルートへ張るため（ルートは大小を区別しません）、どちらを呼んでも 500 になっていました。図でクエリ名を変えてください

#### 双方向同期

- **同期の生成クラスとインターフェイスの形が変わりました** — テーブルごとの同期クラス（`{Entity}SyncTable` / `{Entity}DirectSyncSource` / `Http{Entity}SyncSource`）は生成されなくなり、固定クラスへ `GeneratedSyncTables` の記述子を渡して作ります（DI 登録を使っていれば変更は不要です）。`SyncTableBase.OnLastWriteWinsUploadedAsync` は廃止し、`ISyncTable.UploadAsync` と `PropagateDeletesAsync` のシグネチャが変わりました。ローカル DB には受理記録のテーブル `quicker_sync_ack` が増えます（`SyncJournal.EnsureCreatedAsync` が作成します）
- **未送信のローカル変更がある行で、同期のダウンロードが止まるようになりました** — その行をサーバーの内容で上書きしないためです。止まった場所は `SyncResult.Truncations` が報告するので、競合を解決するか `SyncJournal.RemoveTableAsync` で捨ててから再実行してください

### Added

#### 図の編集・ファイル

- **複合主キーの列順を保ち、編集できるようになりました** — 5 方言の DB 取込が `PRIMARY KEY (b, a)` の順序を読み、DDL・同期スクリプト・EF Core の `HasKey` もその順で出力します。主キーが 2 列以上のテーブルでは、プロパティパネルの「主キーの順序」カードで並べ替えられます。AI ツールには、主キーを列順ごと設定する `set_primary_key` を追加しました。一度順序を明示した図では、列を並べ替えてもキーの順序は変わりません。DBML / Mermaid / Excel 定義書・C# コード取込は順序を運ばず、0.1.0 で開いて保存すると順序は失われます

- **左右のパネルを畳めるようになりました** — ツールバーに新設した「表示」グループの「ツールボックス」（F9）・「プロパティパネル」（F10）で左右のパネルを隠し、キャンバスをウィンドウいっぱいに使えます。プロパティパネルの幅をドラッグで調整していれば、隠して戻してもその幅が戻ります。2 つの状態は次回起動時に復元されます。リレーション作成中にツールボックスを畳むと、作成モードも中断されます（案内とキャンセルボタンがそのパネルにあるため）

- **F11 で全画面表示にできるようになりました** — ウィンドウの枠を隠して画面いっぱいに広げます。ツールバーとステータスバーは残るので、パネルの表示切替（F9 / F10）と組み合わせればキャンバスだけの画面になります。ツールバーの「表示」グループからも切り替えられ、この状態だけは次回起動時に復元されません。タイトルバーが隠れるぶん、その × の代わりにツールバー右端へ「終了」ボタンが出ます

#### DB 取込・接続

- **PostgreSQL・MySQL の接続で、TLS の要求水準を選べるようになりました** — 接続ダイアログの「暗号化 (SSL Mode)」欄で指定します。既定の `Unspecified` は従来どおりの接続です。ドライバの既定は証明書を検証しないため、検証させるには `VerifyCa` / `VerifyFull` を選びます。Oracle を含む方言ごとの扱いは [docs/database.ja.md](docs/database.ja.md) にまとめました

#### コード生成ダイアログ

- **手で編集された生成ファイルは、上書きする前に確認するようになりました** — 置き換え対象の `.g.cs` が生成後に編集されていると、ダイアログがそのファイルの一覧を示し、OK を選ばない限り何も書きません。整形や改行コードの変換は編集とみなさず、この版より前に生成したファイルは照合しません

#### 生成コード

- **生成される基底クラスを、どの出力モードでも `partial` で拡張できるようになりました** — `EntityBase`・`EditModelBase<TSelf>`・値オブジェクトの基底とマーカー `IValueObject`・リポジトリの契約と実装基底・`MapperBase<,>` がソースとして出力され、メンバーやインターフェイスを足すと生成物全体へ届きます。ランタイムをパッケージ参照にしても同じコードがそのまま通ります。リポジトリ契約への追加は既定実装付きに限ります。書き方は [docs/code-generation.ja.md](docs/code-generation.ja.md) の「生成される基底クラスの拡張」節を参照してください
- **列挙型の値オブジェクトを属性 1 つで宣言できるようになりました — `[DeclaredInstance]`** — 生成（または手書き）の値オブジェクトの `static readonly` インスタンスへ付けるだけで、列挙型としての挙動一式がそろいます: DB 読み出し・JSON 復元を含む全生成経路が定義済みインスタンスを返し、集合外の値は検証エラーとして拒否され（文言は `ValueObjectValidationMessages.ValueNotDeclared` で差し替え可能）、`GetDeclaredInstances()` が宣言順の一覧を返し、フィールドの `[Display(Name = ...)]` は `DeclaredDisplayName` で引けて `DisplayValue` の override が 1 行で済みます。定義済みインスタンスは `TryCreateFrom` / `CreateFrom` の入力表記も宣言で引き取れます: `InputText = "○"` はその固定文字列を、`ClaimsAbsent = true` は空欄セル（null / DBNull / 空文字）をそのインスタンスとして読みます——「○ か空欄か」のフラグ列が宣言 2 行で閉じ、固定表記のための `ConvertCustomInput` / `ConvertAbsentInput` のボイラープレートが不要になります（フックは残り、併用時はフック優先）。よくあるケースでは手書きの表・`GetDefinedInstance`・メンバーシップ検証の `OnValidate` が不要になり、フックはそのまま残って属性と併用できます（引き当てはフック優先・独自の `OnValidate` は追加で走ります）。フィールドの初期化は `Create` でなく `new(...)` で書いてください。書き方は [docs/code-generation.ja.md](docs/code-generation.ja.md) の値オブジェクト節を参照してください
- **値オブジェクトが空欄セルを自分の値として取り込めるようになりました — `ConvertAbsentInput` フック** — `TryCreateFrom` / `CreateFrom` は空欄入力（`null` / `DBNull` / 空文字）に対して従来どおり「成功＋null」を返しますが、「○ か空欄か」のように空欄が値の片割れである記法を持つ型は、partial フック `ConvertAbsentInput`（手書き型は interface の `TryConvertAbsentInput`）を実装すると空欄を null でなく `False` などのインスタンスとして取り込めます。効くのは `TryCreateFrom` / `CreateFrom` だけで、EditModel の空欄入力と DB の NULL は変わらず、フックを実装しない型の挙動も従来のままです。書き方は [docs/code-generation.ja.md](docs/code-generation.ja.md) の値オブジェクト節を参照してください
- **値オブジェクトが `IFormattable` を実装しました** — `price.ToString("N2")`、文字列補間、WPF の StringFormat の書式指定が内包値に効きます
- **値オブジェクトの検証ルールを公開し、汎用ルールを 4 種追加しました** — 生成コードが使う必須・最大長・精度の検証を `ValueObjectRules` などから呼べます。`OnValidate` 向けに、桁数・範囲・ASCII 英数字・メールアドレスの検証を追加しました
- **生成ファイルに QuickER の版と内容ハッシュを記録するようにしました** — すべての `.g.cs` の `// <auto-generated />` の直後に `// Generated by QuickER x.y.z` と `// Content hash: sha256:…` の 2 行が入り、API リファレンスにも版の行が入ります。最初の再生成で全ファイルにこの 2 行が追加されます

#### 配布・設定

- **設定ボタン（⚙）に「バージョン情報」を追加しました** — 版・.NET ランタイム・著作権表示・リポジトリとドキュメントへのリンクを表示し、不具合報告に貼る版情報をボタン 1 つでコピーできます

### Changed

#### 図の編集・ファイル

- **ツールバーの表示トグルを「表示」グループへまとめました** — 「説明表示」「NULL表示」「簡易表示」は、新しく追加した 2 つのパネルトグルと一緒に、「表示」ボタンを押すと開くポップアップの中へ移りました。切り替えてもポップアップは開いたままなので続けて操作でき、トグルが増えてもツールバーは 1 行に収まります

- **図を開いたときの表示倍率が 100% になりました** — 従来はウィンドウに収まらない図を 80% まで縮めていましたが、文字が読みにくいため等倍のまま表示し、収まらないぶんはスクロールに委ねます。開く・取込・DB 取込・AI 生成・復元が対象です。一方、利用者が並べ直した操作（整列の 3 種と「全体を表示」）は結果を確かめる操作なので、全体が収まるまで縮めます（下限は手動ズームと同じ 50%。従来の「全体を表示」は 80% で止まり、大きい図では全体が入りませんでした）。収まらない図は、中央でなく左上から映ります

#### ライセンス

- **Entity / EditModel / Mapper の基本生成に対する商用利用の恒久許諾を、今後のバージョンから廃止しました** — [LICENSE-NC.md](LICENSE-NC.md)（QuickER 固有条件の第 2.0 版）の追加許諾は 1 つの暫定許諾にまとまり、将来のバージョンではどの機能も有償化の対象になり得ます。現行リリースは、商用利用を含めて引き続き全員が無料で利用できます。QuickER 0.1.0 は第 1.0 版の条件のままです

#### 配布・設定

- **起動時の更新確認をオフにできるようにしました** — ツールバーの設定ボタン（⚙）から切り替えます（言語の選択もこのメニューへ移りました）。オフにすると起動時に GitHub へ接続しません

#### コード生成ダイアログ

- **完了ダイアログが、書き出したファイルを一覧するようになりました**
- **層別フォルダ出力を ON にしたままでも「1 ファイルにまとめる」を選べるようになりました** — 選ぶと層別のチェックが外れます

#### 生成コード

- **インメモリ Repository が図の UNIQUE 制約を強制するようになりました** — 実 DB が拒否する書き込みは、インメモリでも `InvalidOperationException` で拒否します。`CollectCustomUniquenessChecks` のユーザー定義チェックとシードは対象外です
- **NOT NULL の除外バイナリ列は、新規行のときだけ必須入力になりました** — DB で必ず失敗していた INSERT を、EditModel の検証が列名つきで止めます。INSERT してから `Write{Column}Async` で本体を書く場合は、INSERT に空の値を載せてください
- **`[Column]` 属性は `IncludeDataAnnotations` に依らず常に出力されます** — ランタイムは、この属性が付いたプロパティだけを列として扱います
- **クエリの繰り返し実行が速くなりました** — 呼び出しのたびに式ツリーをコンパイルしなくなりました。射影クエリや値オブジェクト条件のクエリでは、SQLite の実測で約 2.5〜3 倍速くなっています
- **生成コードの量を減らしました** — EditModel と Mapper（約 3 割減）、リポジトリ、DI 登録、リモートサーバーのエンドポイント、同期の定型を固定ランタイムへ移しました。観測できる挙動は変わりません

### Security

#### AI チャット・モック生成

- **npm で入れた AI CLI（`.cmd`）の起動で、引数が別のコマンドとして実行されないようにしました** — 各引数を `cmd.exe` の解釈から守り、守りきれない `%` と改行を含む引数は拒否します。CLI と `cmd.exe` もフルパスで起動するようにしました。Copilot は起動を SDK が行うため、確認できるのは実行ファイルのパスだけです（[docs/ai-chat.ja.md](docs/ai-chat.ja.md) の注意節）
- **Codex のチャットが、QuickER を起動したフォルダの中身を読めないようにしました** — 使い捨ての一時フォルダを読み取り専用で与えます（Claude Code・Copilot のチャットと同じ扱い）
- **Codex の認証 URL は、`http` / `https` のときだけ開くようにしました** — それ以外の URL で既定のアプリが起動することはなくなり、チャットのステータス行で知らせます
- **モックプロジェクトの最終検証ビルドが、AI の置いたビルド設定を黙って実行しないようにしました** — ビルドの前に `obj` / `bin` を削除し、MSBuild が自動で読み込むファイルの経路を追加で塞ぎます。Codex と Copilot では、UI のソース・静的資産以外のファイルが追加・変更されていれば一覧を見せ、ビルドしてよいかを確認します（キャンセルすると未検証のまま終了します）
- **モック生成で AI に自動で許す操作を絞りました** — Copilot では、パスを読み取れないコマンドは `dotnet` だけのときに限り承認し、拒否したコマンドを生成ログに残します。API キー方式では、綴りを変えたパス（末尾の `.` や空白、`GENERA~1` のような短縮名）で保護フォルダへ書き込めないようにしました

#### CLI・MCP

- **MCP の `generate_ddl` ツールは `.sql` ファイルにしか書き込まないようにしました** — 親フォルダが既に存在することも必要です。書き込みは原子的になりました

#### 生成コード

- **バイナリ列のアップロードで、上限を超える長さの宣言を書き込み前に 413 で拒否するようにしました** — 本文を送らずに巨大な長さを宣言するだけで、領域の確保を走らせられました
- **テーブル・列の説明文の書式送り文字（U+000C）が、生成コードを行またぎに壊せなくなりました** — 改行と同じく空白 1 つへ畳みます。従来は畳み込みを素通りして描画時に実改行へ変わり、XmlDoc コメントや属性リテラルがそこで切れ、構成によっては説明文から任意のメンバー宣言を生成コードへ差し込めました

#### 配布

- **配布物を作るワークフローのアクションを commit SHA で固定しました** — 上流のタグが差し替えられても、ビルド・公開の処理が変わらないようにするためです。配布物の中身は変わりません
- **更新フィードを差し替える `QUICKER_UPDATE_FEED` は、Debug ビルドでしか効かなくなりました** — 配布版は常に公式の更新フィードだけを見ます

### Fixed

#### 図の編集・ファイル

- **開いているファイルへの外部変更を取りこぼしたり、確認なしに上書きしたりしなくなりました** — 上書き保存の前にディスクの内容と照合し、外部で変わっていれば確認します。新しいフォーマットで保存されたファイルへの書き戻しも確認します（Schema JSON のエクスポート先がそうしたファイルの場合も含みます）。変更が続くと監視が止まる問題と、保存直後の外部の書き込みを見逃す問題も直しました
- **ドラッグ中に外部変更が届いても、図と Undo 履歴が壊れなくなりました** — 進行中の移動・リサイズ・範囲選択は、開始時点へ戻して打ち切ります。すでに開いているダイアログは閉じません
- **テーブルや列の識別子が重複した図ファイルは、読み込まずに拒否するようになりました** — 従来は読み込めても保存できず、対象 DBMS の切替でアプリが終了していました。メッセージは、テーブル名・列名・重複した識別子を名指しします。読み込めないファイルについても、問題の種類（JSON として不正など）を区別して報告します
- **電源断の直前に保存したファイルが空にならなくなりました**
- **図を開き直したり取り込んだりするたびに、メモリ使用量が増え、Undo 履歴に不要な項目が混ざる問題を直しました**
- **Undo・Redo が失敗しても、その操作が履歴から消えなくなりました**

#### DB 取込

- **PostgreSQL 取込が、列の実際の宣言型を読むようになりました** — `numeric(10,-2)`・`bit(8)` の長さ・`interval` の単位・配列の要素型が正しく取り込まれます。接続ロールに列の権限が無いテーブルが列ゼロで取り込まれることも、パーティションテーブルが丸ごと落ちることもなくなりました。該当する列は、古い図へ取り込み直したときに 1 度だけ変更として現れます
- **Oracle 取込が、DISABLE された制約・一時表・マテリアライズドビューを取り込まなくなり、`NUMBER(10,-2)` と `VARCHAR2(50 CHAR)` を保つようになりました** — 負のスケールを持つ列を SQL Server / MySQL / SQLite へ切り替えると、変換できない型として報告します
- **取込範囲の外を参照する外部キーと、大文字小文字だけが違う同名テーブルを正しく扱うようになりました** — MySQL は範囲外の外部キーを同名の別テーブルへ誤って結び付け、PostgreSQL / Oracle / MySQL は同名テーブルの列を混ぜていました。どちらも該当するものを取り込まずに警告します
- **DB 取込が、写し取れなかったものを完了時に一覧するようになりました** — ドメイン型の平坦化・範囲外の外部キー・読めなかった列・同名テーブル・パーティション・DDL に書き出せない型が対象です。`quicker scaffold` は同じ内容を標準エラーへ出力します
- **SQLite で参照先列を省略した複合外部キーが、正しい列の組で取り込まれるようになりました**

#### DB 同期・DDL

- **MySQL で説明や列順を同期しても、列の他の属性が消えなくなりました** — `DEFAULT`・`AUTO_INCREMENT`・照合順序などは実 DB の定義から組み立て直します。選択していない型変更まで適用されることもなくなりました
- **MySQL の同期スクリプトが、頼んでいない外部キーを削除しなくなりました**
- **同期で作ったテーブルに UNIQUE 制約が付くようになりました**（SQL Server / PostgreSQL / MySQL / Oracle）
- **説明に `GO` や `/` だけの行があっても、同期スクリプトが途中で分割されなくなりました**（SQL Server / Oracle）
- **記号や改行を含む名前でも、出力が壊れなくなりました** — クォート文字・`'`・`$$`・`"`・`\`・`<`・改行を含むテーブル名や列名が、DDL・同期スクリプト・生成コード・XML doc コメント・DBML / Mermaid の出力を壊していました。DBML のバックスラッシュも往復できるようになりました

#### C# コード取込

- **取り込めなかったものを黙って落とさなくなりました** — 列を 1 つも復元できないテーブルは列ゼロで残し、主キー列やリレーションの端点を落としたときも名指しで警告します。数値で書かれた参照アクションの誤読と、多対多のメタデータが 1 対多へ移る問題も直しました
- **列の型表記が、コード取込の往復で変わらなくなりました** — `datetime` や `numeric` のように別の表記へ畳まれる型は `[DbColumnMeta]` に元の表記も記録し、取込で元へ戻します。以前は型が書き換わり、次の DB 同期で不要な `ALTER COLUMN` が出ていました

#### 生成コード

- **スキーマ修飾したテーブル（`sales.orders` など）への CRUD が、実行時エラーにならなくなりました**
- **`partial` で足したプロパティが、列として SQL に載らなくなりました** — 列として扱うのは `[Column]` 付きのプロパティだけです。EF Core を使う構成では、足したプロパティに `[NotMapped]` を付けてください
- **秒未満を持つ日時列の端数が、別の列の編集で失われなくなりました** — EditModel の入力文字列が端数まで表示するようになりました
- **値オブジェクト無効の数値・日時・bool・バイナリ列も、空欄入力で値を消せるようになりました** — 確定値が null になり、null を許すかは必須チェックが決めます（値オブジェクト列が従来から持っていた規則と同じです）。以前は空欄が変換エラー（バイナリ列は空配列）になり、NULL 許容列を画面から NULL へ戻せませんでした
- **必須欄が空の削除行で、保存が例外にならなくなりました** — Mapper が削除行の未入力の非キー列を必須扱いにせずスキップするようになり、`Validate()` が true を返した保存はそのまま通ります。キーは従来どおり必須です（削除に要るため）
- **`AcceptChanges` が保存済みの削除の後始末をするようになりました** — `MarkRemoved()` でコレクションに残した行は通常行として復活せずコレクションから外れ、`Remove()` で退避した行はカスケード子孫ごと Added に戻るため、同じインスタンスを戻せばグラフ全体が挿入になります（以前は Removed のまま残り、永久に保存されませんでした）。単一のカスケード子は Removed のまま残るので、保存後にアプリ側で null を代入してください
- **行編集中にロードが走っても、`CancelEdit` がロード済みの値を巻き戻さなくなりました** — Mapper のロードが進行中の行編集を破棄するため、その後のキャンセルはロード前の古いスナップショットを復元せず no-op になります
- **MySQL の `tinyint`（`sbyte`）列の値オブジェクトが比較演算子を持つようになりました** — 従来は等価のみの基底から派生し、その列の `OrderBy` が実行時例外になっていました
- **バイナリ値オブジェクトを `TryCreateFrom` / `CreateFrom` で Base64 文字列から生成できるようになりました** — EditModel が受け付けるのと同じ記法です。不正な文字列は例外でなく通常の変換エラーになります
- **`foo` と `foo_snapshot` のような列名の組を持つ図が再び生成できるようになりました** — 生成器がもう存在しない per-列スナップショットメンバー（行編集のスナップショットは基底の配列 1 本）を予約し続けており、正当な組が衝突として弾かれていました
- **`HasErrors` が `PropertyChanged` を発火するようになりました** — 保存ボタンの IsEnabled のような `HasErrors` へのバインディングが、エラーの発生・解消に追従します（`HasChanges` と同じ扱いです）
- **アクセサが null を返す子コレクション登録は空として扱われるようになりました** — 遅延初期化のコレクションが null の間も、検証・エラー収集・受理が例外になりません（単一子と同じ規則です）
- **`CreateEntity(editModel, includeRemoved: false)` が削除マークした単一カスケード子を null として外すようになりました** — コレクション要素は除外されるのに単一子だけ載る非対称でした
- **`LongTimePattern` を差し替えたクローンカルチャで、日時の入力文字列がそのパターンで書式化されるようになりました** — パターンキャッシュのキーをパターン文字列にし、クローンが元カルチャのキャッシュに潰されなくなりました
- **`GetDeclaredInstances()` が内部配列を公開しなくなりました** — 戻り値を配列へキャストして書き換えることで共有集合を壊せてしまう形でした
- **EditModel コレクションの `Clear()` で、削除を取り消したはずの行が保存時に消える問題を直しました**
- **`ValidateUniqueAsync` が、削除マークした行を DB へ照会しなくなりました**
- **名前付きクエリへの不正なページング引数が、500 ではなく 400 になりました**
- **1 ファイル出力でリモートサービスを生成したとき、サーバー側のファイル名が本体とずれていた問題を直しました**
- **双方向同期の対象テーブルに射影クエリがあると生成コードがコンパイルできない問題を直しました** — ジャーナル記録デコレータがクエリメソッドを 2 回目に組み立て直し、射影 DTO 名がビルド全体の重複判定に引っかかって委譲メソッドだけが静かに抜けていました（CS0535）
- **親の主キーでない列を参照する外部キーが、EF Core でもその列へ結合するようになりました** — Fluent 構成に `HasPrincipalKey` を明示します。EF Core の既定では主キーへ黙って結合され、CLR 型が同じなら `Include` の結果が誤り、違えば DbContext 全体がモデル検証で使えなくなっていました。他のバックエンドは元から列ペアどおりに結合します
- **主キーの無いテーブルがあっても、EF Core の DbContext 全体が初回使用時に落ちなくなりました** — そのテーブルをモデルから除外し（DbSet なし・そのテーブルへのナビゲーションも無視）、Warning 診断がテーブルを名指しします。Repository 生成が同じテーブルをスキップするのと同じ線引きです
- **別テーブルの名前付きクエリどうしで、サーバー側リクエストレコード名が衝突しなくなりました** — レコード名を `{リポジトリ名}_{操作名}Request` にしました（private のためワイヤ上の変化はありません）
- **`RepositoryDialects` を大小違いの綴り（`SQLite`）で書いてもコンパイル不能な出力が出なくなりました** — 綴りを正規形へ揃えます。従来は片方の方言の基底ともう片方の using が診断なしで混ざっていました
- **NULL 許容の値型列への IN 検索がコンパイルできるようになりました** — メソッド冒頭でリストを NULL 許容の要素型へ一度だけ持ち上げます（`IReadOnlyList<int>` × `int?` 列は生成コードが CS1929 になっていました）
- **簡易 DSL の条件式が型不整合でも、生成アセンブリ全体が壊れなくなりました** — エミットされる C# が必ずコンパイルエラーになる条件（string 列への `name > 'M'`・数値列への `CONTAINS`・整数内包の値オブジェクトへの小数リテラル・別の列の値オブジェクトで型付けした引数との比較など）を生成時に検出し、クエリ名・テーブル名・列名・オペランドを名指しした警告つきでそのクエリだけをスキップします（他のクエリとファイルは従来どおり生成＝列参照切れと同じ扱い）。従来コンパイルできていた形は新たに拒否されません（整数列と `1.5` の比較は double への暗黙拡大でそのまま通ります）。MCP の `set_query` はこうした条件を従来どおり保存します（ファイルベースのサーバは列型を解決する方言型マッパーを持たないため）＝検出されるのは生成時です（[docs/mcp.ja.md](docs/mcp.ja.md) に明記）
- **Repository を生成しない構成では、方言指定（`RepositoryDialects`）が出力へ影響しなくなりました** — EF Core 単独の出力が、方言辞書の有無に依らず単一方言の出力とバイト一致します。従来は `[SqlColumnType]` や型統合の Info 診断が混ざり得ました
- **NULL 許容の値オブジェクト列への `<` / `<=` / `>` / `>=` が、インメモリ実行器でも NULL 行を返さなくなりました** — 値オブジェクトの比較演算子は null を最小として順序付けるため、SQL / EF Core（NULL 行は脱落）とインメモリだけ結果が割れていました。生成される条件式が「列が NULL でないこと」の前提を含むようになります
- **インメモリ Repository の `byte[]` 列への等値比較が、内容（バイト列）で一致するようになりました** — `Query()` の述語・簡易 DSL の等値・一意性チェックの `byte[]` 構成列が対象です。従来は参照比較のため、ストアが取得のたびに複製を返す設計と相まって 1 行も一致しませんでした
- **生 SQL のパラメータ照合が大文字小文字を区別するようになりました（実行時の束縛と同じ規則）** — 従来は `@CustomerID` が宣言 `customerId` と照合され、生成は通るのに実行時に必ず失敗していました。いまは未宣言（警告＋そのクエリだけスキップ）と未使用（警告のみ）として報告されます
- **ワイルドカードなしの `NOT LIKE 'abc'` が、NULL 行を一致に含めなくなりました** — 従来は等値の否定へ畳まれて列側の NULL 補償が効き、LIKE の意味論（NULL 行はどちらの向きでも一致しない）から外れていました。前置 `NOT` の連鎖も偶奇で正しく畳まれます
- **値型の一意制約構成列が未入力でも、`ValidateUniqueAsync` が CLR 既定値（0）で DB 照合しなくなりました** — 未入力の値型列は確定値を Entity へ写せないため、照合せず true を返します（必須検証が保存を止めている状態の助言的チェックのため）
- **コンパイル不能な生成物になる名前付きクエリの定義を、生成時に診断するようになりました** — C# の予約語（`class` など）のパラメータ名・リポジトリの `Query()` を隠すパラメータ名 `Query`・結果型名と同名の射影フィールドは診断エラーになります。IN のリスト持ち上げ変数がパラメータ名（`idsValues` など）と衝突するときは、名前を自動で回避します
- **条件式の数値リテラルは ASCII の数字だけを受け付けるようになりました** — 全角数字（`５`）は従来そのまま C# のリテラルへ出てコンパイル不能でした。いまは条件式の検証エラーになります。あわせて、文字列リテラル内の改行に化ける文字（U+2028 など）はエスケープして出力します
- **参照アクション（ON DELETE）が EF Core の DeleteBehavior へ実挙動どおり写像されるようになりました** — 従来は Cascade 以外がすべて `Restrict` に畳まれていました。いまは `SET NULL` → `SetNull`・`NO ACTION` → `NoAction`（クライアント挙動は Restrict と同一）・`SET DEFAULT` → `ClientNoAction` を出します。従来は SET DEFAULT の図で、EF 経由の親削除が追跡中の子の外部キーを既定値でなく NULL へ更新していました
- **生 SQL の `DECLARE` で宣言したローカル変数が、未宣言パラメータとして誤検知されなくなりました** — 複数宣言（`DECLARE @a int, @b int`）・初期化子つき・テーブル変数に対応します。従来は正当な SQL のクエリが偽陽性でスキップされていました
- **生 SQL / 手動実装のクエリに残った条件・並び順が、黙って無視されず警告で告げられるようになりました** — WHERE / ORDER BY は SQL 側（実装側）の責務のため生成は続行します
- **生 SQL の逐語リテラル内の連続空行が、生成時に書き換えられなくなりました** — SQL の文字列リテラルに連続空行を含むユーザーの SQL が黙って変わっていました（リテラルの外の連続空行は従来どおり 1 空行へ畳まれます）
- **極端に複雑な条件式（AND / OR / NOT / 括弧の合計 200 超）が、プロセスごと落ちる StackOverflow でなく検証エラーになりました**
- **DSL 射影で NULL 許容の値型列を非 NULL 指定したクエリが、生成アセンブリ全体を壊さなくなりました** — `int?` → `int` の変換が無く必ず CS0266 になる組み合わせを、警告つきでそのクエリだけスキップします（DSL の型整合検査と同じ流儀）
- **生 SQL × 単一戻り形の本体ローカル（`items`）が、同名の引数と衝突しなくなりました** — 衝突するときは `_` を後置してリネームします
- **API リファレンス（.g.md）の表セルに、C# リテラルエスケープの痕（`\"` など）が出なくなりました** — テーブル名・説明のセルは図の生の値に Markdown の安全化（`|` のエスケープ・改行の畳み込み）だけを掛けます
- **層別出力で未対応の Repository 方言を指定したとき、例外でなく診断エラーで報告されるようになりました**（ライブラリ直呼びのみで到達する経路）
- **非分割の単一方言では、使われない `RepositoryNamespace` の不正値で生成が止まらなくなりました** — カテゴリ別名前空間は出力に実際に使われる構成（分割時・非分割はマルチ方言レイアウトのみ）でだけ検証します

#### 双方向同期

- **同期でサーバーの変更やローカルの編集が失われる不具合を直しました** — アップロードした行の新しい版が未取得の変更を追い越し、その変更が二度と降りてこなくなることがありました。競合として報告した行のローカル編集を同じランのダウンロードが上書きする問題と、途中で落ちたアップロードが偽の競合や古い値での上書きを起こす問題も直しました
- **`SyncConflictPolicy.ServerWins` が、捨てた変更の行をサーバーの内容へ戻すようになりました** — 従来は他の誰も触っていない行のローカル編集が、捨てたはずなのに残り続けていました
- **decimal 主キーのテーブルで、削除伝搬と受理記録の照合が外れなくなりました** — キーの文字列化がスケールを正規化（末尾ゼロなし）します。SQL Server は宣言スケールへ揃えて `1.50`・ローカルの SQLite ミラーは書いたままの `1.5` を返すため、同じ行を別キーとして扱っていました
- **スキーマ修飾が二重のテーブル名（`dbo.sync.orders` の形）でも、同期 SQL のクォートが CRUD・DDL と同じ規則（最初のドットだけで分割）になりました**

#### AI チャット・モック生成

- **npm で入れた `claude` で、Claude Code チャットが毎ターン新規セッションになる問題を直しました** — 複数行のシステムプロンプトが途中で切れ、後ろの `--resume` / `--model` まで消えていました
- **AI ツールが、どのホスト経由でも主キー列を NOT NULL にするようになりました** — 外部 MCP サーバだけが、NULL 許容の主キー列を図へ書き込めていました
- **既にプロジェクトがある出力フォルダへのモックプロジェクト生成で、上書きの前に確認するようになりました**
- **モックプロジェクト生成の中断・タイムアウト・アプリ終了を正しく扱うようになりました** — Claude Code での中断を中断として報告し、最終検証ビルドは 10 分で打ち切ります。アプリを終了すると、AI チャットとモック生成の子プロセスも中断します
- **モックプロジェクトの成果物判定とログ表示を直しました** — 判定は生成したプロジェクトフォルダの中だけを見て、読めないサブフォルダでは落ちません。長い生成でもログ表示が重くなりません

#### CLI・MCP

- **失敗した MCP ツール呼び出しが、エラー（`isError`）として返るようになりました**
- **`quicker reverse` が、出力先のフォルダを作るようになりました**
- **`quicker reverse` が、新しいフォーマットで保存された図を上書きしなくなりました** — 何も書かずに終了コード `2` で止まります。それでも上書きする場合は `--force` を付けます

## [0.1.0] - 2026-08-30

初回公開リリース。

### Added

- **ビジュアル ER 設計** — crow's foot 記法、1 対 1 / 1 対多 / 多対多、複合主キー、FK 参照アクション、包括的な Undo/Redo、大規模図向けのキャンバス UX（ズーム / パン / 検索 / ミニマップ）
- **マルチ DB 対応** — SQL Server / PostgreSQL / MySQL / Oracle / SQLite の 5 方言で、スキーマ取込・差分同期・DDL 生成・方言切替時の型自動変換
- **C# コード生成** — Entity / EditModel / Mapper と、DB アクセス 3 択（なし / QuickER 版 Repository / EF Core 版 Repository。同じインターフェイスを実装し、DI 登録 1 行で差し替え可能）。値オブジェクト、名前付きクエリ、リモート契約と HTTP + JSON サービス、ランタイム NuGet パッケージ参照モードはオプション
- **双方向同期** — ローカルの SQLite を SQL Server と同期させるエンジンを任意で生成（転送は直結／HTTP のどちらでも可）。初回構築・復旧向けの高速な洗い替えも生成
- **AI チャットとモック生成** — 対話による図の生成・編集（OpenAI API / Anthropic API / OpenAI 互換のローカル LLM / Codex / Claude Code / Copilot）、ER モデルからの Web 画面モック生成、任意で実行可能な Blazor / WPF モックプロジェクトの生成
- **MCP サーバ** — `quicker mcp` が図の編集とコード生成を外部 AI エージェントへ stdio で公開
- **入出力** — DBML / Mermaid / Excel 定義書 / HTML 定義書 / スキーマ JSON / PNG / SVG / ベクタ印刷
- **CLI** — `quicker generate` / `quicker scaffold` / `quicker reverse` / `quicker mcp`
- **動くサンプル** — `samples/ec-order`（SQLite・外部データベース不要）と `samples/ec-order-remote`（HTTP + JSON の 3 階層構成）

配布形態は、GUI（Setup.exe と Portable zip。自己完結の full チャンネルとフレームワーク依存の lite チャンネル）と NuGet パッケージ（dotnet tool の `QuickER.Cli` と、ランタイムパッケージ `QuickER.Runtime` / `.SqlServer` / `.Sqlite` / `.EntityFrameworkCore` / `.InMemory` / `.AspNetCore` / `.Sync`）です。

本リポジトリは混合ライセンスです。コアは MIT、AI 機能・コード生成・CLI・MCP ツール実行ホストの 8 プロジェクトは PolyForm Noncommercial 1.0.0 ＋追加許諾で、現在は商用利用を含め全員無料、基本コード生成の商用利用は恒久的に許諾されています。条文は [LICENSE-NC.md](LICENSE-NC.md)、平易な解説は [LICENSING.ja.md](LICENSING.ja.md) にあります。

[Unreleased]: https://github.com/kokko-labs/QuickER/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/kokko-labs/QuickER/releases/tag/v0.1.0
