# 変更履歴

*[English](CHANGELOG.md) | 日本語*

QuickER の利用者に影響する変更を記録します。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います（0.x の間の版上げルールとリリース手順は [CONTRIBUTING.ja.md](CONTRIBUTING.ja.md) を参照）。

## [Unreleased]

### Added

- **アプリ — PostgreSQL・MySQL の接続で TLS を要求できる** — 接続ダイアログにこの 2 方言向けの**暗号化 (SSL Mode)** 欄を追加しました（`Unspecified` / `Disable` / `Prefer` / `Require` / `VerifyCa` / `VerifyFull`・接続プロファイルごとに保存）。既定は `Unspecified` で、接続文字列へキーワードを一切書かず判断をドライバへ委ねます＝既存プロファイルの接続内容は変わりません。これが重要なのは、両ドライバの既定が「暗号化はするが**サーバー証明書を検証しない**」＝盗聴は防げても中間者は防げない状態だからです。証明書を検証させるには `VerifyCa` / `VerifyFull` を選びます。SQL Server は同じ判断を従来から「サーバー証明書を信頼する」として出しており、SQLite には暗号化する通信がありません。Oracle は欄を設けず文書で案内します——管理対象ドライバの TLS はキーワードではなくデータソースのプロトコル接頭辞（`tcps://host:2484/service`）とリスナーポートで切り替わり、`Prefer` や `Require` に当たる形も無いためです。ダイアログで Oracle を選ぶとその旨を注記します（欄が無いことと「黙って暗号化されている」ことは画面上で区別が付かないため）。`quicker scaffold` は接続文字列をそのまま受け取るので、`SSL Mode=VerifyFull` や Oracle の `tcps://` データソースは `--connection` へ直接書けます。方言ごとの既定の挙動は [docs/database.ja.md](docs/database.ja.md) の表にまとめました

- **アプリ — 複合主キーが制約自身の列順序を保つようになりました** — スキーマ取込（5 方言すべて）が `PRIMARY KEY` 制約の宣言した列順序を読むようになり、`PRIMARY KEY (b, a)` は `(b, a)` のまま図に入ります。従来はテーブルの列順へ潰されていたため、取り込んで DDL を再生成すると裏付けインデックスの列順が黙って変わっていました。生成 DDL と同期スクリプトは制約自身の順序で出力し、差分同期は順序だけの違いも主キー変更として検出し、再取込時の置換確認はキー順序だけが違う図を構造差ありとして扱います。複合キーの順序がテーブルの列順と食い違うときは、AI ツールの図サマリーに `Primary key order:` 行が付きます。挙動変化が 1 つあります＝取込由来のキー順序を持つ図では、列の並び替えが主キーの順序を変えなくなりました（順序を一度も明示していないキーは従来どおり列順に追従します）。順序の表示と編集の手段は本リリースの別エントリになります＝プロパティパネルの「主キーの順序」カードと AI ツールの `set_primary_key` です。DBML / Mermaid / Excel 定義書と C# コード取込はこの順序を運びません（各ドキュメントに制限として明記しています）。既知の制限＝この順序は図ファイルの新しいフィールドに保存され、形式バージョンは 1 のままのため、QuickER 0.1.0 で開いて保存するとそのフィールドが失われ（0.1.0 は知らないプロパティを読み捨てます）、キーの順序はテーブルの列順へ戻ります

- **アプリ — AI ツールからテーブルの主キーを設定できるようになりました** — 外部 MCP サーバと内蔵チャットの双方に `set_primary_key` ツールを追加しました。テーブル名と列名の配列を受け取り、そのテーブルの主キーを「挙げた列を挙げた順で」丸ごと置き換えます（それ以外の列は主キーから外れます）。`set_unique_constraint` と違って列の順序に意味があり、生成される `PRIMARY KEY` 制約と裏付けインデックスの列順そのものになります。従来はエージェントがこれを表現する手段がありませんでした＝`add_column is_primary_key=true` は順序について何も言えず、いったん主キーにした列を外す手段もなかったためです。挙げた列は NOT NULL になります（主キー列は NULL を取れないため）。主キーから外れた列の NULL 許容は据え置かれます（外したことは「NULL を許してよくなった」を意味しないため）。列は最低 1 つ必要なので、このツールは主キーの移動・作り直しはできてもテーブルを主キーなしにはできません＝主キーを丸ごと解除するのは従来どおりエディタの PK チェックの役目です

- **アプリ — プロパティパネルで複合主キーの列順を並べ替えられるようになりました** — 選択中のテーブルの主キー列が 2 本以上のとき、カラム一覧と UNIQUE 制約カードの間に「主キーの順序」カードが出ます。キーの構成列を実効順に並べ、↑ / ↓ で移動できます。どの列を主キーにするかは従来どおりカラム一覧の PK チェックが握り、カードが扱うのは順序だけです。1 回の移動が 1 回の Undo 単位で、主キーの構成・それに伴う NOT NULL・順序がまとめて戻ります。順序を明示するまではグリッドの列の並びに追従する点も従来どおりです。一度明示すると（カードでの並べ替え・`set_primary_key`・データベースからの取込のいずれでも）順序はそこで固定され、以後はグリッドで列を並べ替えてもキーの列順は動かず、あとから PK チェックを付けた列は主キーの末尾に入ります

- **生成コード — 値オブジェクトが `IFormattable` を実装** — `price.ToString("N2")`（culture 指定のオーバーロードあり）が内包値を書式化するほか、文字列補間・`string.Format`・WPF バインディングの StringFormat の書式指定子が内包値へ届くようになります。書式指定子が無いときの結果は常に `ToString()` と同じ（`ToString()` の override が書式なしの表示を従来どおり支配）で、内包値が書式化できない型（string・byte[]・bool）も同様に書式を無視します（合成書式の一般規約と同じ）

- **生成コード — EditModel の定型を「列テーブル」駆動にしました** — 生成される EditModel は列ごとに `EditModelColumn<TSelf>`（2 つの名前・必須かどうか・確定値と入力文字列のコンパイル済みアクセサ）を新設の `EditModelBaseCore<TSelf>.EditModelColumns` で公開し、必須入力チェック・入力の書き戻し・行編集のスナップショット・重複エラーの割り当ては基底クラスに 1 回だけ書かれるようになりました（従来はクラスごと・列ごとに展開）。生成コードが委譲する新しい基底メンバーはほかに `EditModelBaseCore.BeginLoad` / `EndLoad`・`GetChildren` / `SetChildren`・`CheckDatabaseUniquenessAsync` です。格納の仕方（確定値フィールド・入力文字列フィールド・バインディング setter）は一切変えておらず、観測できる意味論もすべて同じで、EditModel と Mapper の生成量が 30〜36% 減ります（EditModel クラス 1 つあたり約 190 行、Mapper 1 つあたり約 28 行）
- **生成コード — 生成された値オブジェクトが走らせる検証ルールを公開し、再利用できるようにしました** — `ValueObjectRules.ValidateRequired` / `ValueObjectStringRules.ValidateMaxLength` / `ValueObjectDecimalRules.Validate` を値オブジェクトごとの展開から固定 infra（パッケージ参照モードでは `QuickER.Runtime`）へ移しました。生成される `ValidateCore` は規則 1 つにつき 1 行の委譲になり、手書きの値オブジェクトからも同じ規則を呼べます
- **生成コード — `OnValidate` から呼べる汎用の検証ルールを 4 種追加しました** — `ValueObjectNumberRules.ValidateMaxDigits`（整数の桁数）・`ValueObjectNumberRules.ValidateRange`（閉区間。`IComparable<T>` なら何でも）・`ValueObjectStringRules.ValidateAsciiAlphanumeric`（ASCII 英数字＋許可した記号）・`ValueObjectStringRules.ValidateEmailAddress`（実用最小。RFC 5322 の完全検証は意図的にしない）。文言は `ValueObjectValidationMessages` の新エントリ（`DigitsExceeded` / `OutOfRange` / `InvalidCharacters` / `InvalidEmailAddress`）です
- **生成コード — どの出力モードでも生成基底クラスを拡張できるようになりました** — 生成コードが継承・実装する基底は**例外なく** 2 層になりました＝実装は固定ランタイムの `*Core` に置き、素の名前（`EntityBase` / `EditModelBase<TSelf>` / 値オブジェクトの基底と新設の非ジェネリックマーカー `IValueObject` / `IRepository<TEntity, TKey>` / `IRemoteRepository<TEntity, TKey>` / `SqlServerRepository<TEntity, TKey>` / `SqliteRepository<TEntity, TKey>` / `EfCoreRepository<TEntity, TKey, TContext>` / `InMemoryRepository<TEntity, TKey>` / `HttpRemoteRepository<TEntity, TKey>` / `MapperBase<TEntity, TEditModel>`）は、**per-型コードと同じ場所へソースとして**（`--use-runtime-packages` を含むどの出力モードでも）出力される `partial` になります。ここへメンバーやインターフェイスを足せば、生成された全 Entity・全 EditModel・全値オブジェクト・全リポジトリ・全 Mapper へ一度に届きます。しかもランタイムがインライン同梱でもパッケージ参照でも同じファイルがそのままコンパイルできる＝パッケージモードの切り替えで拡張コードを書き直す必要がなくなりました（従来はコンパイル済みアセンブリの型だったため書けませんでした）。値オブジェクトには、値の形に依らず全部へ届く面が 2 つあります＝マーカー `IValueObject`（`public partial interface IValueObject : IMyInterface { … }` と書けば、既定実装込みで全値オブジェクトがその面を満たします）と、クラス側のルート `ValueObjectBase<TSelf, TValue>`（全値オブジェクトがここから派生し、`Value` と `TSelf` を参照できます）。値の形ごとの基底（`ValueObjectOrderedBase` / `StringBase` / `BooleanBase` / `DateTimeBase` / `BinaryBase` / `GuidKeyBase`）も生成コードで、いずれもそのルートから派生します＝形を絞った拡張は対応するクラスへ書きます。リポジトリ契約 `IRepository<TEntity, TKey>` / `IRemoteRepository<TEntity, TKey>` へ**既定実装付きの**メンバーを足すと、DI で受け取ったリポジトリ参照から——エンティティにもバックエンドにも依らず、デコレータ越しでも——そのまま呼べます（既定実装は仮想ディスパッチなので `this` へのインターフェイス呼び出しがデコレータの実装へ落ちます）。本体のないメンバーを足すと生成リポジトリが全滅するため、**契約への追加は既定実装付きに限ります**。バックエンドの実装基底 5 本は共通の親を持たない独立した枝で、`{Entity}Repository` の手書き `partial` から使う `protected` 共有ヘルパーの置き場に向きます。ジェネリック契約の `IValueObject<TSelf>` / `IValueObject<TSelf, TValue>` と `IStringMatchValueObject<TSelf>` はランタイム側のままで、拡張面ではありません。`*Core` 型も同様に拡張面ではなく、いずれも `partial` を名乗りません＝メンバーを足す場所は拡張面だけで、そこがパッケージ参照モードでも通用する唯一の場所です
- **生成コード — 同期対象テーブルを「クラス」でなく「データ」で記述するようにしました** — `SyncTableDescriptor<TEntity, TKey>` がテーブル固有の情報（名前・方言別 SQL・キー／ミラー版／無制限バイナリ列の読み書き）をすべて持ち、生成される `GeneratedSyncTables` がテーブルごとの静的記述子 1 つと `All`・共有の `GraphRecorder` を公開します。`SyncGraphRecorder` は `QuickER.Runtime.Sync` の固定クラスになり、走査は `EntityBaseCore.EnumerateCascadeChildren` 1 つで、保存側（グラフ保存）の走査と同じカスケードナビゲーション集合を辿ります（エンティティ型ごとの再帰メソッド生成をやめたので「同じ走査の 2 実装」が乖離しようがありません）。そのために `EntityBaseCore.EnumerateCascadeChildren` を public にしました。無制限バイナリ列のコピー面も `SyncRepositoryBinaryColumns<TEntity, TKey>` 1 実装（ローカル・直結サーバー共通）になります
- **生成コード — EF Core の `HasKey` が主キー自身の列順序に従うようになりました** — 順序がテーブルの列順と食い違う複合キー（今回から表現可能＝上記の複合主キーのエントリを参照）は、キーの順序どおりの `HasKey(e => new { e.B, e.A })` を生成します。キー順序が列順と一致する図——このリリース以前に存在し得たすべての図——の生成物はバイト単位で不変です

### Changed

- **ライセンス — Entity / EditModel / Mapper の基本生成に対する商用利用の恒久許諾を、今後のバージョンから廃止しました** — [LICENSE-NC.md](LICENSE-NC.md)（QuickER 固有条件の第 2.0 版）の追加許諾は 1 つの暫定許諾にまとまり、基本生成を含む NC 対象プロジェクトのすべての機能の商用利用をこれが対象にします。したがって、将来のバージョンではどの機能も有償化の対象になり得ます。現時点で変わることはなく、現行リリースは商用利用を含めて全員が無料で利用できます。残りの約束（既存機能の個人・非商用利用は無料のまま・公開済みバージョンの許諾は取り消さない・有償化は事前告知と移行期間つき）は変わりません。QuickER 0.1.0 は条件の第 1.0 版で公開しており、基本生成の恒久許諾をそのまま保持します
- **アプリ／CLI — 破壊的変更: API リファレンスを日本語版だけ出力できるようになり、言語の指定方法が変わりました** — 設定キー `IncludeJapaneseApiDocs` と CLI の `--api-docs-ja` を廃止し、言語を 1 つのキー `ApiDocsLanguage`（`English` / `Japanese` / `Both`、既定 `English`）と CLI の `--api-docs-lang` で選ぶようにしました。GUI の「日本語版を出力する」チェックも「言語: 英語 / 日本語 / 両方」の選択に変わります。日本語版だけのときもファイル名は `{ベース名}.ja.g.md` です。**旧設定は読み替えません**: `quicker.json` の `"IncludeJapaneseApiDocs": true` は未知キーとして警告され、英語版だけが出力されます。`"ApiDocsLanguage": "Both"` へ書き換えてください。GUI の保存設定も、日本語版の併産を有効にしていた場合は一度「両方」を選び直す必要があります
- **アプリ — C# コード生成の完了ダイアログが、書き出したファイルを一覧するようになりました** — 生成コードと API リファレンス (`.g.md`) は別々のサブフォルダへ出せるため、完了時点でどこに何が出たかを絶対パスで確かめられます。これに伴い、診断もパッケージ案内も無い生成でも、1 行の完了メッセージではなく詳細つきの完了ダイアログになります
- **アプリ — 列の型は素の型表記でなければならなくなりました** — DDL 生成とスキーマ同期スクリプト生成は `Column.DataType` をそのまま SQL へ書き出します（型は識別子でも文字列リテラルでもなく、クォートで包む場所がないため）。安全に書き出せない型がこれまで素通りしていたのを改め、両生成は出力全体を拒否し、該当する「テーブル.列」と型文字列を名指しして失敗します。型に置けるのは、単一スペース区切りの語（英字・各国語の文字・数字・`_`・`$`）の並びで、各語の直後に `(max)`・`(50)`・`(10,2)`・Oracle の `(10 BYTE)` のような括弧引数を置け、末尾に `[]` を付けられ、あるいは MySQL の値リスト（`enum('a','b')`）です。5 方言の型カタログが提供する全表記と、5 方言のスキーマ取込が実 DB から組み立てる表記はすべて通ります（`double precision`・`timestamp with time zone`・`TIMESTAMP(6) WITH LOCAL TIME ZONE`・`INTERVAL DAY(2) TO SECOND(6)`・`decimal(10,2) unsigned`・`UNSIGNED BIG INT`・`integer[]` など）。通らないのは、`;`・引用符・バッククォート・バックスラッシュ・コメント記号（`--`・`/*` と MySQL の `#`）・改行を含む型、前後に空白や改行が付いた型（検証した文字列と書き出す文字列を一致させるため。`"\nGO\n"` は Trim すると語 1 つの `GO` に見えます）、そして 2 語目以降に列定義の句を密輸した型（`int NOT NULL`・`int DEFAULT 0`・`varchar(50) COLLATE …`。型は列定義の途中へ補間されるため、そのまま有効な DDL になり、図の宣言と静かに食い違います）です。空・空白のみは従来どおり「型が未設定」として通ります。型の**解釈**は従来どおりで、方言カタログが解析できない型はそのまま保持され、中立トークンが付かないだけです
- **アプリ — 改行・制御文字を含む図の名前は、どの生成も拒否するようになりました** — テーブル名・列名・制約名・クエリ名に改行や制御文字が含まれていると、C# 生成はコンパイルできないファイルを出す代わりに生成前に場所を名指しして報告し、DDL 生成とスキーマ同期スクリプト生成も同じように出力全体を拒否して場所を名指しします（`TABLE 'x'`・`COLUMN 'x'.'y'`・`UNIQUE 'x'.'y'`・`FOREIGN KEY 'x'`）。対象は C0 制御文字・DEL に加えて Unicode の行区切り 3 種（U+0085・U+2028・U+2029）です——この 3 種は C# にとって改行なので、含む名前は `///` コメントを抜けてコードになり得ました。コメントの畳み込みと識別子のエスケープはそのまま残してあり、入口で拒否するのは「1 箇所の通し忘れが致命傷にならない」ようにするための二重化です。クォート文字は意図的にエラーにしません（`"`・`'`・`]` は実 DB に存在し得る名前で、エスケープで扱えます）。説明は拒否しません（説明の改行は正当で、`--` コメントへ載る箇所で畳まれます）

- **生成コード — 破壊的変更: 固定ランタイムの基底型に `Core` サフィックスが付きました** — ランタイム側の型は `EntityBaseCore`・`EditModelBaseCore`・`EditModelBaseCore<TSelf>`・`ValueObjectBaseCore<TSelf, TValue>`・`IValueObjectCore`・`IRemoteRepositoryCore<TEntity, TKey>`・`IRepositoryCore<TEntity, TKey>`・`SqlServerRepositoryCore<TEntity, TKey>`・`SqliteRepositoryCore<TEntity, TKey>`・`EfCoreRepositoryCore<TEntity, TKey, TContext>`・`InMemoryRepositoryCore<TEntity, TKey>`・`HttpRemoteRepositoryCore<TEntity, TKey>`・`MapperBaseCore<TEntity, TEditModel>` になり、サフィックスなしの名前は上記の生成された拡張面のものになりました。値の形ごとの基底（`ValueObjectOrderedBase` / `StringBase` / `BooleanBase` / `DateTimeBase` / `BinaryBase` / `GuidKeyBase`）は生成側の型になったため名前は変わりません。**利用者が書く名前は従来どおり**です（生成 Entity は `: EntityBase`、生成 EditModel は `: EditModelBase<TSelf>`、生成リポジトリ契約は `: IRepository<CustomerEntity, int>`、生成リポジトリ実装は `: SqlServerRepository<CustomerEntity, int>(…)`、生成 Mapper は `: MapperBase<CustomerEntity, CustomerEditModel>`、手書きの値オブジェクトは `: ValueObjectStringBase<ContactMailValue>, IValueObject<ContactMailValue, string>`）。壊れるのはランタイム型を直接名指ししているコードです＝ランタイムが宣言する位置で `EntityBase` を受けるシグネチャ（`ISqlExecutor.QueryBySqlAsync<TEntity> where TEntity : EntityBaseCore`）・非ジェネリックの `EditModelBase` を型に使ったヘルパー・自前インフラの `is IValueObject` 判定・`ISyncServerSource<,>.Writer` のように**ランタイムが宣言する位置**で `IRemoteRepository<,>` / `IRepository<,>` を名乗る自前実装・これらに対して書いた `partial`。いずれも `Core` 付きへ改名してください。`IValueObject<TSelf>` / `IValueObject<TSelf, TValue>` は名前不変で、`IValueObject<TSelf>` の継承元だけが `IValueObjectCore` になります
- **生成コード — 秒未満を持つ日時列を、その端数まで表示するようにしました** — EditModel の入力文字列は確定値からカルチャ既定の書式で導出しますが、この書式は秒までで止まるため、それより細かい値は切り捨てて表示されていました。バインド setter は入力文字列から確定値を作り直すので、同じ行の**別の**列を編集しただけで切り捨てた値が保存対象へ戻ってしまいます。端数があるときは秒の直後へ不変カルチャの `.` 区切り（末尾ゼロなし）で足すようにし、どのカルチャでも tick まで完全に往復するようにしました。端数を持たない値の表示は 1 文字も変わらないため、既存の画面は精度を落としていた箇所だけが変わります。生成される列テーブルは `DateTime` / `DateTimeOffset` / `TimeSpan` / `TimeOnly` について固定 infra の `EditModelInputFormat` を呼びます（`date` 列は従来どおり短い日付書式）
- **生成コード — インメモリ Repository が図の UNIQUE 制約を強制するようになりました** — 直接の Insert / Update / BulkInsert とグラフ保存は、図が宣言した制約について「同じ値の組を持つ行が 2 つ残る」書き込みを拒否します。実 DB が拒否する書き込みが、インメモリでだけ黙って通ることがなくなります。照合規則は重複事前チェック（`CheckUniquenessAsync`）と同じで、値に null を含む組は判定対象外・書き込む行自身は主キーで除外します。BulkInsert はバッチ全体を事前検証してから適用するため部分投入を残さず、グラフ保存は「保存が残していく状態」で判定するので、同じ保存単位で削除する行の値を別の行が引き継げます（違反時は保存単位ごと何も公開しません）。違反は `InvalidOperationException`（主キー重複と同じ扱い）です。実 DB ではプロバイダ固有の例外（SQL Server ならエラー 2627 の `SqlException` など）として表面化し、インメモリのどの型もその例外にはなり得ないため、プロバイダ例外を catch するコードは実 DB で確かめる必要があります。`CollectCustomUniquenessChecks` で足すユーザー定義チェックは強制の対象外（アプリ側の規則であって DB が持つ制約ではないため）で、`InMemoryDataStore.Put` / `InMemorySampleData.Seed` によるシードも対象外です
- **生成コード — NOT NULL の除外バイナリ列を、新規行のあいだだけ必須入力にしました** — `ExcludeUnboundedBinaryColumns` が ON のとき、除外列は従来どおり必須入力チェックの対象外ですが、EditModel が `RowState.Added` のあいだだけは例外です。INSERT は全列を書くため、空のままだと NOT NULL 列へ値が届かず DB に拒否されます。この INSERT はもともと必ず失敗していたもので、NOT NULL 違反として返ってくる代わりに、EditModel の検証が列名つきで捕まえるようになっただけです。2 段構え（INSERT してから `Write{Column}Async` で本体を流し込む）は、INSERT に空の値を載せていれば従来どおり通ります（値オブジェクト有効なら `ThumbValue.Create([])`・無効なら `[]`＝値オブジェクト無効の列では Entity の初期値がそもそもこれです）。行が DB にできた後の挙動・NULL 許容の除外列・オプション OFF の構成は変わりません
- **生成コード — 破壊的変更: EditModel ごとの文言・表示名フックを廃止しました** — 生成される EditModel は `CustomizeRequiredErrorMessage` / `CustomizeParseErrorMessage` / `CustomizeDuplicateErrorMessage` / `CustomizePropertyDisplayName` を宣言しなくなったため、既存の実装はコンパイルできなくなります。文言は対応する `EditModelMessages` のエントリ、表示名は `GeneratedDisplayNames.Resolve` で差し替え、いずれも新たに受け取るプロパティ名で分岐してください。値オブジェクトのフックを廃止したときと同じ移動で、文言が「型ごとに 1 か所」でなく「メッセージごとに 1 か所」へまとまり、再生成後に生成側クラスへ実装し直す必要もなくなります。意味系のフック（`OnValidate` / `OnBeginEdit` / `OnEndEdit` / `OnCancelEdit` / `On{Property}Changing` / `Changed`）は変わりません
- **生成コード — 破壊的変更: `EditModelMessages` の `Required` / `ParseFailed` / `DuplicateValue` が第 1 引数にプロパティ名を取るようになりました** — `Required` は `(propertyName, displayName)`、`ParseFailed` は `(propertyName, displayName, inputValue, typeName)`、`DuplicateValue` は `(propertyNames, displayNames)` です。この第 1 引数が `nameof` で分岐できる安定キーで、クラスごとのフックの役目をリゾルバ 1 つで果たせるのはこの引数のおかげです。既定の文面は変えていないため、差し替えていないアプリの見た目は変わりません
- **生成コード — 破壊的変更: ロードの手順を `MapperBase<TEntity, TEditModel>` が持つようになりました** — `CreateEntity()` / `CreateEditModel(entity)` / `ApplyToEditModel(entity, editModel)` は abstract をやめて基底の実装になり、生成 Mapper は `LoadColumns`（列コピー）と `CompleteLoad`（カスケード子とロード後フック）、および `OnEntityCreated` / `OnEditModelCreated` の partial への橋渡し 2 つを override するだけになりました。`CreateEntityCore` / `CreateEditModelCore` は廃止です。呼び出し側への影響はありません（利用する公開面 `CreateEntity` / `CreateEntity(editModel, includeRemoved)` / `CreateEntities` / `CreateEditModel` / `CreateEditModels` / `ApplyToEntity` / `ApplyToEditModel` は同じ、ロードの順序も完全に同じ、partial フックの発火位置も同じ）。壊れるのは `MapperBase<,>` を手書きで継承し、virtual でなくなったメンバーを override していたコードです
- **生成コード — 破壊的変更: 値オブジェクトごとの文言・表示名フックを廃止しました** — 生成される値オブジェクトは `CustomizeDisplayName` / `CustomizeMaxLengthErrorMessage` / `CustomizeScaleErrorMessage` / `CustomizePrecisionErrorMessage` / `CustomizeValueRequiredErrorMessage` を宣言しなくなったため、既存の実装はコンパイルできなくなります。表示名は `GeneratedDisplayNames.Resolve`、文言は対応する `ValueObjectValidationMessages` のエントリで差し替え、型を絞りたいときは名前で分岐してください（`memberName == nameof(CustomerEntity.Name)` / `displayName == NameValue.DisplayName`）。文言が「型ごとに 1 か所」でなく「メッセージごとに 1 か所」へまとまり、再生成後に生成側クラスへ実装し直す必要もなくなります。`OnValidate` / `GetDefinedInstance` / `ConvertCustomInput` は変わりません
- **生成コード — 破壊的変更: Entity の表示名フックを廃止しました** — `EntityBaseCore` は `CustomizeDisplayName` を宣言しなくなったため既存の override はコンパイルできなくなり、`DisplayName` は `DefaultDisplayName` をそのまま返します。表示名の差し替えは `GeneratedDisplayNames.Resolve` を置き換え、受け取る実行時クラス名で分岐してください（`static (memberName, description) => memberName == nameof(CustomerEntity) ? "顧客" : description ?? memberName;`）。既定のリゾルバは説明を優先するので、テーブルの説明へ書くだけでも足ります。値オブジェクト・EditModel のフックを廃止したときと同じ移動で、表示名の差し替えが「Entity クラスごとの override」でなく 1 か所へまとまります。`DisplayName` という名前の列を持つエンティティへの診断も、もうフックには触れません（省略されるのは `DefaultDisplayName` の override だけです）
- **生成コード — 破壊的変更: `ValueObjectValidationMessages` の各エントリが第 1 引数に表示名を取るようになりました** — `MaxLengthExceeded` は `(displayName, maxLength, actualLength)`、`ScaleExceeded` / `PrecisionExceeded` は `(displayName, …)`、`ValueRequired` は `(displayName)`、`InputNotConvertible` は引数順を `(displayName, raw)` へ入れ替えました。1 つの差し替えで型ごとに文言を変えられるのはこの引数のおかげです。既定の文面は変えていないため、差し替えていないアプリの見た目は変わりません
- **生成コード — 破壊的変更: `CheckUniquenessAsync` の宣言をランタイム共通面 `IRemoteRepositoryCore<TEntity, TKey>` へ移しました** — 重複事前チェックは従来エンティティごとの `I{Entity}RemoteRepository`（リモート契約なしなら `I{Entity}Repository`）が宣言し、生成される全 Repository クラスが同じ実装を持っていました。宣言はランタイム共通面へ、実装は各バックエンドの Repository 基底（方言・インメモリ・EF Core・HTTP クライアント）へ 1 回ずつ移しています。呼び出し側への影響はありません（`I{Entity}Repository` は継承で同じメンバーを提供し、シグネチャ・戻り値・`CollectCustomUniquenessChecks` フックは不変＝既存の手書きフックは無修正で動きます）。壊れるのは `IRemoteRepositoryCore<,>` / `IRepositoryCore<,>`（と、それを継承する拡張面 `IRemoteRepository<,>` / `IRepository<,>`）を**手書きで実装**しているコード（テストダブルやアダプタ）で、このメンバーの実装が必要になります（生成基底を継承していれば自動的に満たされます）。生成 Repository が持つのは制約テーブル（`{Entity}UniquenessConstraints.Set`）とフックへの 2 行の橋渡しだけになり、エンティティ 1 つ × バックエンド 1 つあたり約 50 行減ります
- **生成コード — DI 登録がエンティティ 1 つにつき 1 行になりました** — `AddGenerated{SqlServer,Sqlite}Repositories` と `AddGeneratedEfCoreRepositories` は `services.AddScoped<IContract, Implementation>()` で登録し、実体の生成は同じ呼び出しが登録した依存からコンテナに任せます（`AddGeneratedInMemoryRepositories` が元から使っていた形）。keyed オーバーロードと `AddGeneratedHttpRemoteRepositories` は、全エンティティで共通の配線を 1 つのローカル関数へ畳みました。登録のライフタイム・`ISqlExecutor` の keyed / 非 keyed 解決・`TryAddScoped<ISaveHookRegistry>` の既定・リモート面が同一インスタンスへ転送されること、はいずれも変わりません
- **生成コード — 破壊的変更: テーブルごとの同期クラスを廃止しました** — `{Entity}SyncTable` / `{Entity}DirectSyncSource` / `Http{Entity}SyncSource` は生成されなくなりました。これらが継承していた固定クラスが記述子を受け取る形になり、直接インスタンス化します（`new SyncTable<OrderEntity, int>(local, localSql, source, GeneratedSyncTables.Order)` / `new VersionlessSyncTable<…>` / `new DirectSyncSource<OrderEntity, int>(serverSql, serverRepository, GeneratedSyncTables.Order)` / `new VersionlessDirectSyncSource<…>` / `new HttpSyncServerSource<OrderEntity, int>(httpClient, GeneratedSyncTables.Order)`）。これに伴い `SyncTable<,>` / `VersionlessSyncTable<,>` / `DirectSyncSource<,>` / `VersionlessDirectSyncSource<,>` / `HttpSyncServerSource<,>` は abstract から sealed へ変わり、`JournalingRepositoryBase<,>` と 2 つの派生は記述子と共有 `SyncGraphRecorder` をコンストラクタ引数に取ります。DI 登録（`AddGeneratedSyncSupport` / `AddGeneratedSyncEngine` / `AddGeneratedDirectSyncSources` / `AddGeneratedHttpSyncSources`）・エンジン・`ISyncTable` / `ISyncServerSource<,>` / `ISyncBinaryColumns<TKey>`・生成される `Journaling{Entity}Repository`（引き続き生成・`(inner, journal)` のまま）を使うコードに影響はありません。壊れるのは廃止クラスを名指ししているコードと、sealed 化したクラスを継承しているコードです。テーブルごとの生成コードは約 70〜80% 減ります（3 テーブルの同期フィクスチャで 779 行 → 210 行）
- **生成コード — サーバー側のエンドポイントマッピングを汎用化しました** — `RemoteServerEngine.MapCrud<TEntity, TKey, TRepository>` が CRUD・グラフ保存に加えて重複事前チェックも張り、新設の `RemoteServerEngine.MapBinaryColumn<TKey>` が無制限バイナリ列 1 本の 3 動詞をまとめて張ります。生成側の `Map{Entity}Endpoints` に残るのは名前付きクエリのハンドラと、バイナリ列 1 本につき 1 呼び出しだけです。同期ソースの起動時検査もテーブル（`SyncSourceServices`）を 1 周する形になり（従来はテーブルごとに `if` 1 つ）、登録漏れの報告名は従来と同一です。ルート・リクエストボディ・ステータスコード・400／409／500 の分類・`Response.HasStarted` ガード・404 marker・`allowUnboundedUploads` のオプトインはいずれも不変で、同じワイヤ形式をより少ない行数で書いたものです（生成行数はエンティティあたり 26 → 9 行、バイナリ列あたり 57 → 11 行、サーバーファイルのスキーマ依存部は図により 21〜71% 減）。1 点だけ明記します: 重複事前チェックは Insert と同じエンベロープ（`RemoteEntityRequest<TEntity>`）を読むようになったため、per-entity の `private sealed record {Entity}CheckUniquenessRequest` は廃止されました。ワイヤ形式にも公開 API にも影響はなく、気づき得るのは `GeneratedRemoteEndpoints` の partial を自分で書き足してこのレコードを名指ししていたコードだけです
- **生成コード — `[Column]` は `IncludeDataAnnotations` に依らず常に出力されます** — この属性は文書ではなく永続化の構造マッピングなので、データアノテーションを OFF にした構成でも生成される全列プロパティへ付きます（OFF の出力は列 1 つにつき 1 行増え、ON の出力は従来とバイト単位で同一です）。`IncludeDataAnnotations` が制御するのは以後、文書・検証系の属性（`[Table]` / `[Key]` / `[Required]` / `[MaxLength]` / `[DbTableMeta]` / `[DbColumnMeta]`）だけになります。ランタイムが `[Table]` / `[Key]` をリフレクションで読むため、Repository 契約の生成に ON が必要な点は従来どおりです
- **生成コード — 破壊的変更: 未送信のローカル変更が同期のダウンロードを打ち切るようになり、`SyncResult` がその場所を報告します** — テーブルのダウンロードは、ジャーナルにエントリが残っているキーの行に当たった時点で止まり、外部キー順でそれより後ろのテーブルも次のランへ回します。打ち切り点より後ろの行は、止めていたエントリが決着する（判断して再実行する／`SyncJournal.RemoveTableAsync` で捨てる）までサーバーに残ります。その行だけ飛ばして先へ進む選択肢はありません＝再開点はテーブルのミラー版の最大値なので、守ったはずの行を追い越してしまうからです。`SyncResult.Truncations` / `HasTruncations` が、テーブル名と「止まったキー」または「自分を止めたテーブル名」のどちらか一方を添えて各打ち切りを報告します。ランが止まる理由は「未送信のジャーナルエントリ」であって、多くは直前に収集した競合ですが、中断したアップロードが残したエントリでも同じです。エントリをすべて決着させたランは打ち切りを報告しないため、アップロードが通る構成では何も変わりません。変わるのは、エントリを残したランが以前より降ろす量が減り、そのことを報告するようになる点です。版なしテーブルは打ち切らずその行を飛ばします（毎回テーブル全体を読み直すため）
- **生成コード — 破壊的変更: 同期エンジンがローカルテーブルをもう 1 つ持つようになり、`SyncTableBase<TEntity, TKey>.OnLastWriteWinsUploadedAsync` を廃止し、同期の各面の形が変わりました** — `SyncJournal.EnsureCreatedAsync` が作るローカルテーブルは 2 つになりました。従来どおりの `quicker_sync_journal` と、「サーバーが受理したアップロードに採番した版」を行ごとに記録する `quicker_sync_ack` です（これは受理記録であって再開点ではなく、ここからアンカーを導く処理はどこにもありません）。廃止したフックは後勝ちアップロードの後にミラー版を書くためのもので、アップロードが版を書かなくなった以上やることが無いため、`override` していたコードはコンパイルできなくなります（置き換え先はありません）。シグネチャの変更: `ISyncTable` / `SyncTableBase<,>` の `UploadAsync` がキャンセルトークンの手前に `byte[]? acknowledgedRowVersion` を取り、`PropagateDeletesAsync` は削除した件数ではなく削除したキー（`IReadOnlyList<string>`）を返します。追加分: `ISyncTable` と `SyncTableBase<,>` に `SyncDownloadContext`（そのテーブルの未送信キーと未決着の受理記録）を受け取り `SyncDownloadOutcome` を返す `DownloadAsync` オーバーロード、`SyncJournal` の `RecordAckAsync` / `ReadAcksAsync` / `RemoveAcksAsync` / `RemoveTableAcksAsync`（行の型は `SyncAckRow`）、`SyncUploadOutcome` の `ServerRowVersion` と `UploadedWith`、`SyncResult` の `Truncations` / `HasTruncations`、新しい `SyncDownloadTruncation`。変わらないもの: 既存の 4 引数 `DownloadAsync`（テーブル面）は従来どおり全行を適用し、`SyncJournal.RemoveTableAsync` / `RemoveAllAsync` は従来どおりジャーナルのエントリだけを扱います（未送信のローカル変更を捨てることは、サーバーが既に受理した内容について何も言っていないためです）。`SyncEngine.UploadAsync` と `DownloadAsync` を別々に呼ぶ形も `SyncAsync` と同じだけ健全になりました＝受理記録はそれを書いた呼び出しより長く生きるため、単独のダウンロードでも「エコーがまだ降りていないアップロード」の行を自分のエコーとして認識します
- **生成コード — 破壊的変更: 生成 Mapper の `includeRemoved` が必須引数になりました** — `CreateEntity(editModel, includeRemoved)` / `CreateEntities(collection, includeRemoved)` / `ApplyToEntity(editModel, entity, includeRemoved)` の既定値 `false` を撤廃したため、呼び出しごとに「保存用のグラフを作るのか（`true`）表示用なのか（`false`）」を明示します。省略できた頃は削除追跡中の行がグラフから漏れ、保存してもユーザーが消したはずの行が黙って残っていました。移行はコードを再生成し、コンパイルエラーになった呼び出しへ保存経路なら `includeRemoved: true`、表示経路なら `includeRemoved: false` を付けてください
- **アプリ — 層別フォルダ出力を ON にしたままでも「1 ファイルにまとめる」を選べるようになりました** — C# コード生成ダイアログの出力モードのラジオは常に操作でき、「1 ファイルにまとめる」を選ぶと「層別フォルダへ振り分けて出力する」のチェックが外れます（単一ファイルは層へ割れないため。含意が両方向になっただけで、チェックを ON にすると分割へ切り替わる従来の動きはそのままです）。従来はチェックが ON の間ラジオが操作不可で、1 ファイル出力へ戻すには先にチェックを外す必要がありました。生成される成果物は変わりません
- **生成コード — クエリの呼び出しごとの式ツリーのコンパイルをやめました** — 呼び出しのたびに新しい式ツリーを `Expression.Compile()` へ渡す（＝そのたびに動的メソッドを生成する）箇所が 2 つありました。(1) 射影クエリの選択式は、メソッド本体へ直書きする代わりにリポジトリの `private static readonly Expression<Func<TEntity, TResult>>` フィールドへ保持するようになり、デリゲート化が必要なバックエンド（方言実行器・インメモリ実行器・EF Core のフォールバック経路）は新しい `QuerySelectorCache` から受け取ります。C# は式ツリーをキャッシュしないため、直書きのラムダは呼び出しのたびに別インスタンス＝別のコンパイルになっていました。(2) 述語の値側は、可能な限り解釈で評価するようになりました。新しい `QueryValueEvaluator` が、従来から直読みしていた定数・クロージャ参照に加えて、メソッド呼び出し・値を変えない変換（参照型のアップキャスト・`object` へのボクシング）・インライン配列を直接読み、残る形だけがコンパイルへ落ちます。効くのは主に値オブジェクト有効の図で、生成クエリは値側に必ず `XxxValue.Create(arg)` を置くため、全クエリの全呼び出しがコンパイルのフォールバックを踏んでいました。結果と例外は変わりません——ファクトリが投げた例外はリフレクションの `TargetInvocationException` ではなく元の型のまま伝播し、null レシーバへのインスタンス呼び出しは従来どおり `NullReferenceException` になります。SQLite・20 万行での実測では、射影クエリ 10,000 回が 5.27 秒 / 208 MB から 1.60 秒 / 110 MB へ、値オブジェクト条件のエンティティクエリ 10,000 回が 4.36 秒 / 188 MB から 1.74 秒 / 147 MB へ短縮し、全件実体化は変わりません。`QuerySelectorCache` と `QueryValueEvaluator` は `QuickER.Runtime` の公開面への追加です
- **アプリ — 起動時の更新確認をオフにできるようにしました** — ツールバーの言語ボタンを**設定**ボタン（⚙）に変え、言語の選択はその下のサブメニューへ移したうえで、隣に**起動時に更新を確認する**のトグルを置きました。オフにすると起動時に GitHub Releases へ一切接続しません。設定は `gui-settings.json` に保存され、既定はオンなので、変えない限り従来と同じ動きです（更新確認は以前からダウンロードの前に必ず確認を出しますし、Portable 版はそもそも確認しません）。なお、インストーラと更新パッケージには Authenticode 署名が付いていません（SmartScreen が「発行元不明」と警告するのはこのためです）。この 2 点は README のインストール節に明記しました
- **アプリ — 破壊的変更: 設定と作業状態の保存先を `%APPDATA%` から `%LOCALAPPDATA%` へ移しました** — `gui-settings.json` / `ai-settings.json` / `codegen-settings.json`・`connections.json` と DPAPI で保護したパスワードファイル・保存済み API キー・自動保存ファイル（`last_diagram.json`）・クラッシュログのすべてが `%LOCALAPPDATA%\QuickER` を読み書きします。ここにあるのはこの PC に固有の状態と、CurrentUser の DPAPI で保護した秘密で、ドメインのローミングプロファイルが他端末へ複製しても復号できないものだからです。**移行処理はありません**＝`%APPDATA%\QuickER` のファイルは読まないため、表示言語・保存済み接続（パスワードを含む）・API キー・コード生成の設定・復旧用スナップショットは一度だけ既定値に戻ります。引き継ぎたい場合は、初回起動の前に `%APPDATA%\QuickER` を `%LOCALAPPDATA%\QuickER` へコピーしてください。旧フォルダはそれ以外では削除して構いません
- **アプリ — `QUICKER_UPDATE_FEED` を Debug ビルド限定にしました** — 更新フィードを差し替えるこの環境変数は E2E 検証のためのもので、配布ビルドでは「環境変数が指した先」が更新元になっていました。Release では分岐ごとコンパイルされなくなり、配布された QuickER は常に定数のフィードだけを見ます

- **アプリ・生成コード — 列の型表記が C# リバースの往復で変わらなくなりました** — `[DbColumnMeta]` の方言中立トークンは型の「意味」だけを運ぶため、同じ意味の複数表記が 1 つの代表表記へ畳まれ、`datetime` は `datetime2`、`numeric(10,2)` は `decimal(10,2)`、`ntext` / `text` / `image` は `nvarchar(max)` / `varchar(max)` / `varbinary(max)`、`smallmoney` は `money` として戻っていました。そのまま取り込むと図が黙って書き換わり、次の DB 同期が該当列すべてへ `ALTER COLUMN` を出します。`[DbColumnMeta]` は**そうした列に限って**元の表記も `NativeType` として記録し、取込側は現在の方言でそれを解析した型がトークンと一致するときだけ採用します（一致しなければトークンを採って警告します＝手編集や別方言向けの生成物でも嘘をつきません）。ここでいう「綴り」は DB 同期の型比較と同じ基準で、前後の空白と大文字・小文字の差は違いとして数えません。したがって、代表表記だけで書かれた図の生成物はバイト単位で従来どおりで、変わるのは属性そのものの宣言（プロパティが 1 つ増える）だけです。畳まれる表記を含む図では、対象の列を情報診断で名指しします。rowversion 列は元からトークンを刻まない仕様のため対象外です

### Security

- **アプリ — `.cmd` シムで入った CLI の起動で、引数が引用の外へ抜けないようにしました** — Windows では npm で入れた `codex` / `claude` / `copilot` が `.cmd` ファイルになり、Windows はこれを `cmd.exe` 経由で起動します。.NET が組み立てたコマンドラインは `cmd` にもう一度解釈されるため、引数中の引用符や `&` が引用の外へ抜けて別のコマンドを始められます。`codex` は元から自前で `cmd.exe` のコマンドラインを組み立て、`cmd` が解釈する文字を含む引数を拒否していましたが、`claude` は無防備で、しかもその引数にはシステムプロンプトが載ります。両者を 1 つの共有ガードへ通し、`claude` 側は引数がリストで来る（＝トークン境界が分かる）ため各引数を個別に引用したうえで、引用の内側でも `cmd` が作用する `%` を拒否します。引用では閉じ込められない改行も同様に拒否します。`copilot` は GitHub Copilot SDK の内側で起動されコマンドラインを QuickER が組み立てないため、確認できるのは解決したパスだけで、この残余は `docs/ai-chat.md` に明記しました
- **アプリ — Codex チャットが QuickER 自身のフォルダで動かないようにしました** — Codex のチャットスレッドは作業フォルダがアプリのカレントディレクトリで、サンドボックスの指定も無いまま承認ポリシーが `never` だったため、そのフォルダの中身をモデルが無確認で読めました。`%TEMP%\QuickER\codex` の使い捨てフォルダを与え、`read-only` サンドボックスを明示します（Claude Code / Copilot のチャットは元からこの形でした）。モック**プロジェクト**生成は対象外で `workspace-write` のままです（出力フォルダへ書くのが目的のため）
- **アプリ — GitHub Actions を commit SHA でピンしました** — ワークフローは `actions/checkout@v7` のようにタグで参照していましたが、タグは移動できます。すべてのアクションを commit で指定し、バージョンは末尾のコメントに残し、更新を持ってくる Dependabot の設定を追加しました。配布物は何も変わりません——上流のタグが乗っ取られたときに release / publish ワークフローの実行内容が差し替わる経路を塞ぐ変更です
- **アプリ — モックプロジェクトの最終検証ビルドが、AI の置いた `obj` のコードを実行しないようにしました** — MSBuild は `obj/{プロジェクト}.csproj.*.targets` / `*.props` をワイルドカードで import するため、生成中に `obj` へ書かれたターゲットが QuickER 自身の検証ビルドで実行されていました。検証ビルドはプロジェクトの `obj` / `bin` を先に削除し（そこがジャンクションならリンクだけを外し、リンク先の中身には触れません）、`-noAutoResponse` を渡して `Directory.Build.rsp` などの自動応答ファイルが `-logger:` でアセンブリをビルドへ読み込ませられないようにし、`-nodeReuse:false` と `-p:UseSharedCompilation=false` で AI が書いたビルドタスク・アナライザを常駐プロセスに残さないようにします。祖先フォルダの `global.json` と `NuGet.Config` は従来どおり効くため、`docs/ai-chat.md` は「置き場所から完全に独立」と言わずその事実を明記するようにしました
- **アプリ — モックプロジェクトの最終検証ビルドが、AI の書いたビルド設定を実行する前に確認するようにしました** — この検証ビルドは、QuickER がエージェントのサンドボックスの外側へ出る唯一の地点です＝Codex や Copilot がサンドボックスの内側で書いたファイルに対して、あなたの権限で `dotnet build` を実行するため、置き残された csproj・`global.json`・`*.csproj.user` は読み込まれて記述どおりに実行されます。この 2 つのバックエンドでは、スキャフォールド直後の出力フォルダを記録し、ビルドの直前に比べ直して、UI 層のソース・静的資産（`.cs` / `.razor` / `.xaml` / `.css` / `.js` / `.html` / 画像）以外の追加・変更があれば一覧を全件見せて、検証ビルドを実行してよいかを確認します。キャンセルするとビルドは実行せず、プロジェクトを**未検証**のまま終了します（生成物とログは出力フォルダに残ります）。比較は更新日時やサイズではなくファイルの内容で行います（エージェントは書き込んだあとで日時を戻せるため）。通す側も危険な名前の列挙ではなくホワイトリストです（MSBuild・dotnet が自動で読むファイルはバージョンとともに増えるため）。Claude Code と API キー方式では確認しません＝前者は元から `Bash` で任意のコマンドを実行でき、後者はそもそもビルド設定ファイルを提出できません
- **アプリ — Copilot のモック生成で、パスを読み取れないコマンドを無条件に自動承認しないようにしました** — シェル実行の自動承認は出力フォルダ配下に限る建て付けでしたが、コマンド文からパスが 1 つも取れない場合（`powershell -c …`・`curl … | sh` など）は無条件で承認しており、しかもヘッドレス実行のため確認も出せませんでした。この場合は「全要素が `dotnet`（生成プロジェクトに必要な唯一のコマンド）で、URL 参照もファイルへの書き込みリダイレクトも無い」ときだけ承認し、それ以外は拒否して、拒否したコマンドを生成ログへ残します。パスが出力フォルダ配下と判断できるコマンドは従来どおり承認します
- **アプリ — CLI と `cmd.exe` をフルパスで起動するようにしました** — `codex` は名前だけで起動しており（`.cmd` シムのときだけ場所を解決していました）、どのバックエンドでも `.cmd` シムを包む `cmd.exe` は名前で渡していたため、どの実行ファイルが起動するかをプロセス起動の探索順に委ねていました。`codex` を `claude` / `copilot` と同じ規則で `PATH` から解決してフルパスで起動し、`cmd.exe` は Windows のシステムフォルダから取るようにしました。3 バックエンド共通の `PATH` 走査は `.` などの相対要素も読み飛ばすため、起動する実体がカレントフォルダに左右されません。防御の二重化です（.NET 10 のプロセス起動はカレントフォルダ・作業フォルダを探さないことを実測で確認しています）
- **アプリ — API キー方式のモック生成で、別の綴りのパスが保護フォルダへすり抜けないようにしました** — Windows はフォルダ名末尾の `.` や空白を落として開くため `Generated./Pages.cs` は `Generated/` へ書き込まれ、`GENERA~1` のような 8.3 短縮名でも同じフォルダを指せました。末尾が `.` または空白のセグメントと `~` を含むセグメントを拒否し、保護フォルダ（`Generated/`・`design/`・`obj/`・`bin/`）の照合を正規化後のパスでももう一度行います。拡張子のホワイトリストは元から効いていたため、この経路でビルド設定ファイルを提出できたわけではありません

### Fixed

- **生成コード — 出力ファイル名が `.g.cs` で終わらないとき、サーバー実装のファイル名が本体とずれていた** — 1 ファイル出力でリモートサービスを生成し、出力ファイル名を `EcOrder.cs` のように指定すると、本体は `EcOrder.g.cs` になるのにサーバー実装は `EcOrder.cs.RemoteServer.g.cs` になっていました。いまは `EcOrder.RemoteServer.g.cs` です。API リファレンス (`.g.md`) の生成ファイル構成表も、正規化前の名前ではなく実際に書き出すファイル名を載せます
- **アプリ — 長時間のモックプロジェクト生成でログ表示が重くならないようにしました** — ストリームで届く断片ごとにログ文字列全体を作り直して変更通知を出していたため、大きなログを出す生成（最長 30 分続きます）ではログの長さの 2 乗に比例する時間を使い、断片のたびに表示をスクロールしていました。断片はバッファへ溜めて最短 200 ms ごとにまとめて表示します。生成の完了・失敗・中断の時点では必ず全量が表示され、ログの内容は変わりません
- **アプリ — Claude Code でのモックプロジェクト生成の中断が、中断として報告されるようにしました** — Claude Code クライアントはキャンセルをただの失敗結果へ畳みます（チャットのターン中断を「エラー」と表示しないための規約）が、モックプロジェクト側はそれをそのまま返していたため、タイムアウトも利用者自身の中断も「詳細不明」として報告され、ファイルがたまたま在れば最終ビルドまで進んでいました。他の 3 バックエンドと同じくキャンセルを投げ直すようにしました
- **アプリ — モックプロジェクトの最終検証ビルドに時間上限を設けました** — 全体の時間上限は AI の実行までしか掛かっていなかったため、`dotnet build` が返ってこないと生成が永久に終わりませんでした。10 分で打ち切り、専用の文言でタイムアウトとして報告します（NuGet キャッシュが空の状態で同等の Blazor ソリューションは約 7 秒でビルドできることを実測しています）
- **アプリ — 既にプロジェクトがある出力フォルダへの生成で、先に確認するようにしました** — 出力フォルダが空でないときの案内は、ログへ書いた直後に 1 行あとのログクリアで消えており一度も見えず、しかも文言は「既存ファイルは残したまま」でしたが、実際にはソリューション・csproj・`README-QuickER.md`・`Generated/`・`design/mock/` を上書きしていました。生成前に既にあるもの（同じフォルダに同居する別のソリューション・プロジェクトを含む）を一覧で示して確認を取り、キャンセルすれば出力フォルダには一切書き込みません。検証ビルドが自分のソリューションをパスで名指しするようになったため、同居する別のソリューションでビルドが壊れること（MSB1011）もなくなりました
- **アプリ — 成果物チェックが生成したプロジェクトフォルダの中だけを見るようにしました** — 出力フォルダ全体を走査していたため、隣に置かれた無関係な `.csproj` / `.xaml` でも「AI が生成できた」と判定され、前回実行の `obj` / `bin` も数えていました。あわせて、現在のユーザーが読めないサブフォルダがあると走査ごと例外になり生成が丸ごと落ちていたのも直しました
- **アプリ — アプリ終了時に、実行中の AI チャットのターンとモック生成を中断するようにしました** — メインウィンドウを閉じても設定を保存するだけで、`claude` / `codex` / `copilot` / `dotnet` の子プロセスが孤児として残っていました。どちらのウィンドウも終了時に中断を要求します
- **アプリ — AI ツールが、どのホスト経由でも主キー列を NOT NULL にするようになりました** — `add_column is_primary_key=true is_nullable=true` と、主キー列に対する `set_column_property is_nullable=true` は、ツールを実行したホストによって違う図を作っていました＝内蔵チャットはエディタと同じビューモデルを編集するため主キー列は元から NOT NULL へ強制されるのに対し、外部 MCP サーバはファイルを直接書くため指定をそのまま受け取っていたためです。同じツール呼び出しが両ホストで同じ正規化を行うようになり、外部エージェントが編集した図が「NULL 許容の主キー列」を宣言することはなくなりました。生成 DDL への影響はありません（`PRIMARY KEY` 句は図の宣言に依らず構成列を NOT NULL にするため）。変わったのは、図・プロパティパネル・定義書・スキーマ差分がその事実と食い違わなくなったことです

- **アプリ — SQLite の参照先列を省略した外部キーが、複合主キーへ正しく対応付くようになりました** — 列リストを書かない `FOREIGN KEY (a, b) REFERENCES parent` は親の主キーをキー自身の列順で参照しますが、取込は全構成列を親の**先頭**の主キー列と対応付けていたため、この形の複合外部キーは列ペアが壊れた状態で取り込まれていました。各構成列が同じ位置の主キー列と対応付くようになりました。単一列の暗黙参照は元から正しく、変わりません

- **アプリ — PostgreSQL 取込が、列の実際の宣言型を読むようになりました** — 列定義の情報源を `information_schema` から `pg_catalog` と `format_type()` へ変えました。前者はいくつかの修飾を表現できなかったためです。`numeric(10,-2)` の列は `numeric(10,2046)` として取り込まれていました。これは PostgreSQL 自身が受け付けない表記で、その図からはそのテーブルの DDL すら生成できませんでした。`bit(8)` と `bit varying(16)` は長さを失い（切り詰められた `bit` を列へ当て直すと既存データで失敗します）、`interval day to second(3)` は単位を、`time(3) with time zone` は精度を失い、配列は内部名（`_int4`。`varchar(20)[]` に至っては長さごと落ちて `_varchar`）で返っていました。取り込む表記は従来どおり QuickER の短い語彙（`varchar(50)`・`timestamptz(3)`）なので、健全な列の型文字列は変わらず、新たな差分も出ません。上記に当たる列は、古いバージョンで作った図へ取り込み直したときに 1 度だけ列の変更として現れます。また `pg_catalog` は列権限でフィルタされないため、接続ロールが権限を持たないテーブルも、黙って列ゼロで届くのではなく列つきで取り込まれ、パーティションテーブルは（消えるのではなく）親テーブルとして取り込まれます（パーティション自体は対象外）
- **アプリ — 取込範囲の外を参照する外部キーが、消えるのではなく報告されるようになりました** — 取込範囲は PostgreSQL が `public` スキーマ、MySQL が接続先データベース、Oracle が接続ユーザーのスキーマで、親がその外にある外部キーはリレーションにできません。MySQL は参照先スキーマをまったく見ていなかったため、他データベースへの外部キーを接続先の同名テーブルと突き合わせて**誤ったテーブルへ結び付けて**いました。Oracle と PostgreSQL は無言で落としていました。3 方言とも、その外部キーを取り込まず、取込警告で名指しするようになりました
- **アプリ — Oracle が、設計の一部ではないものを取り込まなくなりました** — DISABLE された主キー・一意制約・外部キーは取り込みません（DISABLE された制約は何も強制しないため、取り込むと図がデータベースには無い保証を宣言することになります）。一時表とマテリアライズドビューのコンテナ表も除外し、「ビューは取り込まない」という文書上の宣言と実装が一致しました
- **アプリ — Oracle が、列の丸め位取りと長さの単位を保つようになりました** — `NUMBER(10,-2)`（100 の倍数へ丸める）は `NUMBER(10)` として取り込まれており、これは単なる 32 ビット整数として読まれて丸めが消えていました。`VARCHAR2(50 CHAR)` は `VARCHAR2(50)` として取り込まれており、マルチバイト文字セットでは別の型です。どちらもそのまま持ち帰るようになりました。PostgreSQL の `numeric(10,-2)` と、リバースが読み戻す中立トークン `decimal(10,-2)` の負のスケールも同様です。なお、こうした列を SQL Server / MySQL / SQLite へ切り替えると、実行できない DDL を書き出す代わりに変換不能として報告します（前 2 つは負のスケールをそもそも受け付けず、SQLite の `DECIMAL` は丸めを持ちません）
- **アプリ — 大文字小文字だけが違う同名テーブルが、1 つに混ざらなくなりました** — PostgreSQL と Oracle は引用識別子で、MySQL も `lower_case_table_names` が 0（Linux の既定）なら `"Dup"` と `dup` を共存させられますが、取込は両者を 1 つのエンティティへ潰し、両テーブルの列を混ぜていました。片方だけを取り込み、もう片方を取込警告で名指しするようになりました。どちらが残るかも、3 方言とも並び順をバイト値で決めるようにしたためデータベースの照合順序に左右されません
- **アプリ — DB 取込が、写し取れなかったものを報告するようになりました** — PostgreSQL のドメイン型を基底型へ平坦化したとき・取込範囲外を参照する外部キーがあったとき・テーブルの列を読み取れなかったとき・大文字小文字だけが違う同名テーブルがあったとき・パーティションテーブルをパーティションなしで取り込んだとき・**列の型が SQL へ安全に書き出せない表記だったとき**に、完了ダイアログが対象を一覧します。最後の 1 つがとくに効きます。そうした型は取込こそ通りますが、後で DDL 生成・スキーマ同期が**図全体**を拒否するため、取込時点が一番早く分かる場所です（SQLite は宣言された型のテキストをそのまま保持するので、この検査は 5 方言すべてに掛けます）。`quicker scaffold` は同じ内容を標準エラーへ出力します。報告するものが無い取込は、従来どおり 1 行の完了メッセージです

- **アプリ — C# コード取込が、落としたものを黙って落とさなくなりました** — 復元できなかったもののいくつかは、これまで何の痕跡も残していませんでした。全列が `[DbColumnMeta]` を持たないテーブル（Oracle の `SDO_GEOMETRY` のように型カタログが解析できない方言固有の型だけで出来た小さなマスタ表）はテーブルごと落ち、関連するリレーションまで道連れになっていました。こうしたテーブルは**列ゼロ**で取り込んで名指しで警告するようになり、リレーションも残ります。スキップした主キー列には「テーブルが主キーを失う」ことを伝える専用の警告が出ます。端点の列を解決できなかったリレーションは列ペアなしへ退化し（線は図に残る一方で、その外部キーは DDL 生成とスキーマ差分から消えます）、この退化も、端点のテーブルがファイルに無いリレーションも、名指しで通知します。引数を読み取れない `[NavigationReference]` は無視ではなく通知になり、宣言と違う順序の `name:` 形式の引数は（位置だけで読んで両端が黙って入れ替わるのではなく）検出されるようになりました。同名のテーブル・同名の列も通知します。1 つのクラスが同一ファイル内で複数の `partial` 宣言に分かれている場合は、`[Table]` を持つ部分だけを読むため、他の部分にある列プロパティの件数を通知します。取込対象の判定に `[DbColumnMeta]` 付きの列は要らなくなり、`[Table]`（生成器がエンティティクラスにだけ付ける属性）だけで判定します
- **アプリ — 数値で書かれた参照アクションを受け付けなくなりました** — `[NavigationReference]` の `OnUpdate = "1"` が列挙体の検査を通って無言で `Cascade` になっていました。トークンは列挙体の「名前」と照合するようになり、読み取れない他のトークンと同じく未知として通知し、未指定のままにします
- **アプリ — C# コード取込で、多対多のメタデータが 1 対多へ移らなくなりました** — 多対多は列ペアを持たないため端点キーが両端エンティティだけに退化し、同じ 2 エンティティ間にある「列を解決できなかった 1 対多」と衝突していました。1 対多が多対多の制約名・参照アクションを引き継いだうえで多対多も追加され、同じ制約名を名乗るリレーションが 2 本残りました。多対多の温存は従来どおりですが、未指定フィールドの補完元にはなりません
- **アプリ — `quicker reverse` が出力先ディレクトリを作るようになりました** — 存在しないフォルダを `--out` に指定すると、原子的書き込みの一時ファイル名を含む素の `DirectoryNotFoundException` で失敗していました（`quicker generate` は以前から出力ディレクトリを作っていました）
- **アプリ — 開いている図の置き換えで、前の図の要素が変更追跡器に残り続けなくなりました** — ファイルを開く・取込・外部変更の自動再読込は、いずれもコレクションの `Clear()` で図を置き換えますが、その Reset 通知は「何が消えたか」を運ばないため、旧エンティティ・列・一意制約・リレーションの切り離しが一度も走っていませんでした。追跡器のスナップショット辞書は置き換えのたびに図 1 枚分ずつ増え続け（MCP クライアントがファイルを編集していれば呼び出しごと）、生き残った購読経由で、切り離されたはずの要素への変更が生きている Undo スタックへ幽霊コマンドを積み、きれいな文書を未保存扱いにすることがありました。すべての置き換え経路が、1 件削除の経路と同じ明示的な切り離しを通るようになりました
- **アプリ — ドラッグ中に外部変更が届いても、文書が壊れなくなりました** — 進行中のテーブル移動・リサイズ・グループ移動・範囲選択は、再読込や確認ダイアログへ進む前に開始時点へ戻して打ち切られます。従来は、再読込の後でマウスを離すと破棄済みの要素から移動操作が組み立てられて履歴へ積まれ、読み込み直したばかりの文書が即座に未保存扱いになり、意味を失った Undo 項目が残りました。また確認ダイアログがマウスをつかんだまま開くことがあり、離した通知が届かず、ダイアログを閉じた後の最初のマウス移動でテーブルがポインタの位置へ確定していました。「このまま編集を続ける」を選んだ場合、ドラッグ中のテーブルは開始位置へ戻ります——動いた位置は Undo 履歴のどこにも無いため、残すと取り消せない変更になるからです。すでに開いているダイアログ（DB 同期・クエリ定義など）は閉じません（既知の制限としてドキュメントに記載）
- **アプリ — 保存が「他人の書き込み」を「自分が保存した内容」として記録し得なくなりました** — 保存後に覚えておく内容ハッシュをディスクの読み直しで採っていたため、書き込み完了からその読み直しまでの隙間に外部プロセスが書くと、その内容が「自分の保存」として記録され、変更イベントは一時停止中の監視に捨てられていました。以後は次の保存まで、どの検知経路もメモリの図とファイルの食い違いに気づけません。ハッシュは実際に書き出した JSON 文字列そのものから計算し、監視の再開直後にもう 1 回だけファイルと突き合わせるようになったので、この隙間に届いた書き込みも他の外部変更と同じように検知されます
- **アプリ — Undo・Redo の失敗で、その操作が履歴から消えなくなりました** — 実行中に例外が出た操作は両方のスタックから消え、やり直す手段も先へ進める手段も同時に失われていました。例外を伝える前に元のスタックへ戻すようになったので、履歴には残り続け、再試行できます

- **アプリ — MySQL で列の説明を同期しても、その列の他の属性が消えなくなりました** — MySQL には説明だけを変える文が無く、同期は `ALTER TABLE ... MODIFY COLUMN` を通ります。この文は列定義をまるごと言い直すものですが、その定義を図の型と NULL 制約だけから組み立てていたため、図が表現できない属性が実 DB の列から軒並み消えていました——`DEFAULT`（リテラル・式の両方）、`AUTO_INCREMENT`、照合順序、`ON UPDATE CURRENT_TIMESTAMP`、`INVISIBLE`、空間参照系の `SRID`、生成列の式。MySQL の DDL は暗黙にコミットされるため、どれも取り消せませんでした。同じ文は図側の型も一緒に持ち込むので、差分一覧で**選択していない**型変更まで黙って適用されてもいました。列定義はスクリプトの実行時に `information_schema.COLUMNS` から組み立て直し、末尾の `COMMENT` だけを差し替えるようになったので、説明の同期は説明だけを変えます。組み立て直しは文が実際に対象とするスキーマのカタログを読むため、スキーマ修飾したテーブルは接続先の既定データベースにある同名テーブルの定義ではなく自分自身の定義を取ります。`BIT` とバイナリ系のリテラル既定も、文字列ではなくリテラルのまま書き戻します——さもないと `varbinary` の既定は自分の 16 進表記の文字列へ黙って置き換わり、`binary(n)` の既定はスクリプトを途中で止めていました。実行時点で列が無くなっていれば no-op に畳まれ、列は在るのに定義を組み立て直せなければ黙って通さずスクリプトを失敗させます
- **アプリ — MySQL で列の並び順を同期しても、動かした列の属性と説明が消えなくなりました** — `ALTER TABLE ... MODIFY COLUMN ... AFTER` も説明の変更と同じく列定義をまるごと言い直す文で、同じように図から組み立てていたため、列を動かすだけで上と同じ属性が落ちていました。こちらも実 DB のカタログから——同じスキーマの絞り込みと同じリテラルの扱いで——再構成するようになり、その列に付いていた説明も一緒に引き継ぎます

- **生成コード — 生成 Repository がスキーマ修飾テーブルを正しく指すようになりました** — `sales.orders` のようなテーブル名をまとめてクォートしていたため（`[sales.orders]` / `"sales.orders"`）、「ドットを含む 1 つのテーブル名」を指すことになり、既定スキーマ以外のテーブルに対する SELECT / INSERT / UPDATE / DELETE がすべて実行時に「無効なオブジェクト名」で失敗していました。最初の `.` で分割して各部をクォートする形（`[sales].[orders]`）になり、DDL 生成が元から書いていた形と揃います。ドットを含まないテーブル名の SQL は従来とまったく同じです
- **アプリ・生成コード — 方言のクォート文字を含むテーブル名・列名・制約名が、生成コードの組み立てる SQL を壊さなくなりました** — 名前に `]`（SQL Server）や `"`（SQLite）が含まれるとクォートがそこで閉じ、以降が SQL として解釈されていました。DDL 生成と同じ規則（終端クォート文字の二重化）で、生成ランタイムが識別子を組み立てる全箇所（CRUD 文・列一覧・`Query()` の翻訳・Include ローダー・バイナリ列アクセサ）がエスケープするようになりました。同じ穴が合成する主キー制約名 `PK_{テーブル名}` にもあり、8 箇所（SQLite の DDL と、逐次 DDL 方言 4 つの同期スクリプトの `CREATE TABLE` / `ADD CONSTRAINT`）が手書きのクォートへ未エスケープで埋め込んでいました——すぐ隣の UNIQUE 制約名・外部キー制約名はエスケープ済みだったので、見た目では気付けない非対称でした
- **生成コード — `"` や `\` を含む名前がコンパイルできないコードを生み出さなくなりました** — DB 取込で入り得るこれらの文字を含むテーブル名・列名が、`[Table("…")]`・`[Column("…")]`・`[NavigationReference(…)]`・EF Core の `ToTable` / `HasColumnName`・同期記述子へ未エスケープで埋め込まれ、生成ファイルがコンパイルできませんでした。説明の属性が既にそうしていたのと同じくエスケープするようになりました。`{` や `}` を含む列名がインメモリのサンプルシーダー（サンプル値が補間文字列）を壊す問題も同時に解消しています
- **アプリ — テーブル名に `'` が含まれるときスキーマ同期スクリプトが壊れなくなりました** — 制約名が不明な主キー・外部キーの削除は動的 SQL で行い、クォート済みのテーブル名をそこの文字列リテラルへ埋め込みます（SQL Server の `EXEC('…')`・PostgreSQL の `DO` ブロック内 `EXECUTE '…'`・MySQL の `PREPARE` / `CONCAT`・Oracle の `EXECUTE IMMEDIATE '…'`）。名前のシングルクォートがそのリテラルを閉じ、良くても構文エラーになっていました。各方言の識別子ヘルパーに `QuoteForDynamicSql`（クォートしてからリテラル用にエスケープする）を追加し、該当箇所はすべてこれを通します

- **アプリ — テーブル名に `$$` が含まれるとき PostgreSQL の同期スクリプトが壊れなくなりました** — 主キー・無名の外部キーを削除する `DO` ブロックはドルクォートのタグを `$$` 固定にしていましたが、ドルクォートは<b>タグ文字列そのもの</b>で終端するため、`$$` を含む名前がブロックを閉じ、以降が最上位の SQL になっていました（リテラルエスケープは `'` を二重化するだけで、ブロックの終端とは無関係です）。タグは本文を組み立てたあとに決定的に選ぶようになりました＝本文に現れない限り `$$`、現れるなら `$q$`・`$qq$` … と伸ばします。`$$` を含まない名前のスクリプトは 1 バイトも変わりません
- **アプリ — 改行を含む名前・説明が SQL のコメント行を突き破らなくなりました** — `--` は行末までのコメントなので、名前の中の改行が残りを独立した行に出し、実行される SQL として解釈されていました。図のテキストを `--` コメントへ載せる全箇所（2 つの DDL 生成器の多対多の注記・SQLite のテーブル／列の説明コメント・同期スクリプトのスキップコメントと見出し）が、改行・制御文字を空白 1 つへ畳むようになりました。とくに SQLite のテーブル再構築の見出しは、SQLite の実行器がスクリプト全文を 1 コマンドで送るため影響が大きい箇所でした

- **生成コード — `<` や Unicode の行区切りを含む名前が、生成コードの XML doc コメントを壊さなくなりました** — テーブル・列に説明が無いとき、5 つの `///` サマリー（Entity クラス・その列プロパティ・値オブジェクト・EditModel・インメモリのシーダー）が名前をそのまま載せる定型文になりますが、うち 2 つが未エスケープでした。`<` を含む名前は XML として不正になり（`GenerateDocumentationFile` を有効にしてビルドすると CS1570）、U+2028 を含む名前はコメント行そのものを抜けていました。5 箇所とも、説明文が既に通っていた XML エスケープを通すようになりました
- **アプリ — 改行を含む名前が Mermaid / DBML 出力の行構造を壊さなくなりました** — どちらも 1 行 1 宣言の形式なので、テーブル名・列名・制約名の中の改行が宣言を分断し、残りが別の宣言として読まれていました。出力時に名前とラベルを 1 行へ畳むようになりました
- **アプリ — バックスラッシュを含む DBML の説明・制約名が往復できるようになりました** — 出力側が `'` だけをエスケープしてバックスラッシュを二重化していなかったため、元からの `\` とエスケープ由来の `\` が区別できず、取込で別の文字列に戻っていました（`a\\b` が `a\b` になる）。出力でバックスラッシュを二重化し、取込では両方のエスケープを 1 パスで解くようになりました
- **アプリ — npm で入れた `claude` で、Claude Code チャットが毎ターン新規セッションになる問題を修正しました** — `.cmd` シムは `cmd.exe` 経由で起動され、`cmd` はシムの `%*` 展開を最初の改行で切って、その引数の残りと**それ以降の引数すべて**をエラーも出さずに捨てます。QuickER が渡すシステムプロンプトは複数行のため 1 行目で切れ、後ろに積んでいた `--model` と `--resume` ごと消えていました＝会話を継続できず、モデル指定も無視され、どこにも問題が報告されないままでした。システムプロンプトは一時ファイルへ書いて `--append-system-prompt-file` で渡すようになり（パスに改行は含まれません）、改行を含む引数は黙って切り捨てる代わりに起動前に拒否します（`claude` / `codex` の両起動経路）。`claude.exe` として解決される導入では元から影響がなく、`.exe` 経由のモックプロジェクト生成も同様です。シム導入環境でのモックプロジェクト生成のプロンプト切れも同時に解消します

- **生成コード — 生成基底やエンティティ自身の `partial` へ足したプロパティが列として扱われなくなりました** — ランタイムは列を除外リスト（読み書き可能でナビゲーションでなくランタイム基底の宣言でもないもの）で判定していたため、拡張面 `EntityBase` や自分で書いた `partial class CustomerEntity` へ足した読み書き可能なプロパティが「実在しない列」になっていました＝INSERT / SELECT / UPDATE に載り、実 DB では "Invalid column name" で失敗する一方、インメモリのバックエンドは緑のままでした。列として扱われるのは `[Column]` を持つプロパティだけになり（この属性は生成される全列に付きます）、足したプロパティは SQL・`HasSameValues` / `GetValueHashCode`・インメモリの列コピーのいずれからも外れます。JSON 経路（`ToJson` / `Clone`・リモート転送）は公開プロパティを見るため引き続き載りますので、外したければ `[JsonIgnore]` を付けてください。`Query()` の述語でそのようなプロパティを参照した場合も、SQL 方言バックエンドでは実在しない列を出す代わりに `NotSupportedException` で明示的に失敗するようになりました（インメモリバックエンドは述語を通常の C# として評価します）。EF Core はこの修正の対象外です——規約が公開の読み書きプロパティをすべて写像するため、`GenerateEfCoreRepositories` 構成では足したプロパティに `[NotMapped]` が必要です。逆に列として扱いたい場合は、生成プロパティがまだ写像していない実在列を指す標準の `[Column("実在する列名")]` を付けます（これがオプトインです）
- **生成コード — `ValidateUniqueAsync` が削除マーク行を DB へ照会しなくなりました** — 検証状態の読み出しは他がすべて削除マーク行を飛ばす（`Validate`・エラー収集・要素どうしの重複検証）のに、DB 照合だけが対象にしていたため、削除予定の行に重複値があると `false` を返しながら `CollectErrors` は空＝表示できるエラーが無いまま保存が止まっていました。削除マークされた EditModel は照会せずに `true` を返します。エラーストアには触れないので、削除を取り消せば次の検証が判断し直します
- **生成コード — 同期のランが、自分のアップロードで追い越したサーバー変更を取りこぼさなくなりました** — アップロードは送った行にサーバーが採番した版をローカルのミラーへ書き戻しており、ダウンロードの再開点はテーブル内のミラー版の最大値なので、その 1 行の新しい版が「まだ降ろしていないサーバー変更」の版を軒並み追い越していました。追い越された変更は以後ずっと再開点より下に沈みます——そのランでも、次のランでも、二度と降りてきません。サーバーに未取得の変更がある状態でのアップロードならいつでも起こり得た不具合で、オフライン編集 1 件で無関係な行の更新が恒久的に失われました。アップロードが得た版は代わりに `quicker_sync_ack` へ記録され（再開点を導かない、行ごとの受理記録です）、その行が版の順番で降りてきた時点でダウンロードがミラーへ書きます。エコーがまだ降りていない行をさらに編集したときの再生も、この受理記録を original に採ります——同じクライアントが自分で書いた版に対する競合として報告されることはありません

- **生成コード — バイナリ列のアップロードが、ホストが拒否する長さ分の領域を先に確保しなくなりました** — `PUT {prefix}/{エンティティ}/{列名}` のエンドポイントは宣言された `Content-Length` をそのまま書き込みへ渡しており、書き込み側は本文を読む前にその長さ分を確保します（SQLite はちょうどその大きさの `zeroblob` を先に割り当てます）。一方でホストのリクエストサイズ上限は「本文を実際に読んだとき」にしか発火しないため、巨大な長さを宣言してデータを 1 バイトも送らない要求だけで、上限が止める前に確保が走っていました。書き込みを呼ぶ前に宣言長と上限を照合し、超過していれば他の分類済み拒否と同じ `RemoteError` 本文つきの 413 で返すようになりました。`allowUnboundedUploads: true` でマップしたグループ（およびホストが上限を解除している場合）は照合相手の上限が無いため、従来どおり素通しします
- **生成コード — 名前付きクエリの不正なページング引数が 500 でなく 400 になりました** — ページングを持つクエリのリモートエンドポイントは `Take` / `Skip` を未検証のまま渡しており、クエリパイプラインは `take <= 0` / `skip < 0` を `ArgumentOutOfRangeException` で拒否するため、呼び出し側が誤った値がサーバー側の未処理例外として表に出ていました＝HTTP 500・サーバーログ・`OnServerError` の呼び出しが、サーバー側は何も間違えていないのに走ります。エンドポイントへ到達できるクライアントなら誰でも、このアラート経路を好きなだけ焚けました。両方の値はリクエストを読む箇所（必須フィールド検証・`ConcurrencyMode` 検証と同じ位置）で検証され、`RemoteError.Type` が "BadRequest" の 400 として返るようになりました。正常な値の挙動は従来どおりです
- **生成コード — `Collect` で報告した競合のローカル行が、同じランのダウンロードで上書きされなくなりました** — 食い違いを報告したうえで、そのままサーバー行をローカルへ適用していたため、ジャーナルに残ったエントリが指す行の内容がサーバーのものに化けていました。次のランはサーバー自身の値をサーバーへ送り返すだけになり、ローカルの編集は「あったことすら分からない形で」消えていました。ダウンロードは、未送信のローカル変更が残っている最初の行で止まるようになり、外部キー順でそれより後ろのテーブルも次のランへ回します（下の Changed 項目を参照）
- **生成コード — 途中で落ちたアップロードが、サーバーへ届いた変更のジャーナルエントリを残さなくなりました** — エントリの削除はラン末尾の一括だったため、その手前で落ちるとサーバーへ届いた分のエントリまで残っていました。次のランはそれを再生します＝版ガード付きの更新は「自分で書いた行」に対して失敗し、解決しようのない競合を報告します。後勝ちの再生なら、より新しいサーバー行を古いローカル行で上書きします。エントリはサーバーが受け取ったその場で 1 件ずつ削除するようになりました
- **生成コード — `SyncConflictPolicy.ServerWins` が、捨てた変更の行をすべてサーバーの内容へ戻すようになりました** — ドキュメントは「ローカル変更を捨て、ダウンロードがサーバー行で上書きする」と書いていましたが、実装はランのジャーナルを落とすだけで、あとはダウンロードに任せていました。ダウンロードが降ろすのは「前回ミラーしてから版が進んだ行」だけなので、<b>他の誰も触っていない行へのローカル編集は、それを捨てろと言われたそのランを生き延びていました</b>。エントリも消えているため以後どのランも直さず、呼び出し側が明示的に捨てると言った内容がローカルに黙って残り続けます。捨てる各エントリの行はサーバーから読み直してローカルへ上書きするようになりました＝ローカルで削除した行も戻ります。サーバーがもう持っていないキーは適用するものが無いので削除伝搬に委ねます（つまり `PropagateDeletes` を切っているとその行は残ります）。戻した行のミラー版はサーバーの版でなく<b>元のまま</b>にするので、ダウンロードの再開点がこの端末の未取得の変更を追い越すことはありません。代わりに適用した行は受理記録へ残し、その行への次のローカル編集はそこから original の版を読みます——残さないと、ダウンロードが既に通り過ぎた版のもとで戻った行には二度と到達できず、その行を編集すると「サーバーが一度も見たことのない行」として再生されて、端末自身のコピーを相手にした競合として報告されてしまいます

- **アプリ — スキーマ同期で作ったテーブルに一意制約が付くようになりました** — 逐次 DDL 方言 4 つ（SQL Server / PostgreSQL / MySQL / Oracle）は `AddTable` 差分の `CREATE TABLE` に主キーしか書いていなかったため、DDL 生成なら UNIQUE 制約込みで作られるテーブルが、同じ図を同期で適用したときだけ制約なしで出来上がっていました——選べる差分項目も警告も無く、黙って落ちていました。`CREATE TABLE` の制約行（`PRIMARY KEY` → `UNIQUE` の順・合成名 `UQ_{テーブル}_{列…}` を含む）は共通ヘルパー 1 つが組み立て、DDL 生成と 4 方言の同期スクリプト生成がそれを呼ぶ形になりました＝2 つの経路が再び食い違うことはありません。SQLite は元から影響を受けていません（テーブル再構築方式の同期が既に UNIQUE 句を書いていました）
- **アプリ — MySQL の同期スクリプトが、頼んでいない外部キーを削除しなくなりました** — 制約名が不明な外部キーの削除は `SELECT … INTO @fk` で名前を逆引きしますが、MySQL は該当行が無いときユーザー変数を書き換えないため、1 つのスクリプト内の 2 件目以降が直前の逆引きが残した名前を読んでいました。主キー側の `@pk` と同じく、逆引きの前に `@fk` を `NULL` で初期化するようになりました。同じ逆引きにあった 2 つの穴も塞がりました。どちらもテーブル名がどのスキーマを指していても接続先の既定データベースを検索していたため、修飾名のテーブルは別スキーマの同名テーブルの制約を引き当てるか、何も見つけられませんでした。外部キー名も、含まれ得るバッククォートを二重化しないまま `DROP` 文へ埋めていました。外部キーの逆引きは制約名順に並べるようにもなったので、同じ親子を結ぶ外部キーが 2 本あっても落ちる側が一定します
- **アプリ — 文字列リテラル内の `GO` / `/` だけの行で同期スクリプトが分割されなくなりました** — SQL Server は `GO` のみの行でバッチを、Oracle は `/` のみの行で文を分割しますが、どちらの分割器も文字列リテラル・引用識別子・ブロックコメントの内外を追跡していませんでした。そのような行を含む説明（説明には改行を入れられます）が文を 2 つに切り、SQL Server では構文エラー、DDL が暗黙コミットされる Oracle では部分適用になっていました。両方の分割器が、MySQL の分割器が既にそうしていたように、各方言のリテラル・コメント規則に従うようになりました

- **生成コード — EditModel コレクションの Clear が、退避していた要素を削除対象のまま残さなくなりました** — 既存要素を Remove するとその要素は削除対象として退避され `Removed` になり、同じインスタンスを戻すと削除が取り消されて削除前の状態へ復元されます。`Clear()` は何も復元せずに退避リストを捨てていたため、Remove → `Clear()` → 同じインスタンスを Add の順に操作すると、引き当てる先を失った要素が `Removed` のままコレクションに居座り、次の保存でその行が消えていました。`Clear()` は退避リストを捨てる前に、各要素を削除前の状態へ戻すようになりました。それ以外の `Clear()` の挙動（追跡のリセット・所有の解除・位置通知）は変わりません
- **アプリ — MCP の `generate_ddl` ツールが `.sql` 以外の出力先へ書き込まなくなりました** — 渡されたパスへそのまま DDL スクリプトを書いていたため、stdio MCP サーバを操作するエージェントが任意の種類の任意のファイルをこのツール経由で上書きできました。出力先は `.sql` で終わること、親ディレクトリが既に存在すること（親ディレクトリを黙って作らない点は `create_diagram` と同じ規則です。拡張子の制限は `generate_ddl` 固有）が必要になりました。書き込みも原子的になり、途中で中断しても書きかけのスクリプトではなく従来のファイルがそのまま残ります。`.sql` への書き出しは、BOM 付き UTF-8 という符号化も含め従来どおりです

- **アプリ — 外部変更検知が以後のセッション全体で止まらなくなりました** — 開いている図を他のプログラム（stdio MCP サーバ・エディタ・同期クライアント）が書き換えたことを検知する監視が、監視自身のエラーを受け取っていなかったため、OS の通知バッファが溢れるほどの変更が連続すると無言のまま止まっていました（以後の外部変更は一切気づかず、ファイルを開き直すまで復帰しません）。エラーを受けたら監視を張り直し、復帰直後に内容を 1 回照合して、止まっていた間に起きた内容の変更も通知するようになりました（その間の削除・リネームは対象外です）

- **アプリ — 電源断の直前に保存したファイルが空にならなくなりました** — 保存は一時ファイルへ全量を書いてから差し替えますが、書き込み自体は OS のキャッシュに留まるため、中身が永続化されないまま差し替えだけが永続化され得ました。差し替えの前に一時ファイルをディスクへフラッシュするようになりました。保証の範囲は広がっていません＝クラッシュ後に残るのは新旧どちらかの完全な内容であって、書き途中の内容にはならない、という形のままです（どちらが残るかは引き続き OS の裁量です）

- **失敗した MCP ツール呼び出しが通常の出力ではなくエラーとして返るようになりました** — MCP サーバ（stdio の `quicker mcp` と、内蔵チャット自身の MCP エンドポイントの両方）は失敗テキストを成功結果として返していたため、外部エージェントは本文を読んで解釈しない限り、たとえば `add_entity` が実際に追加したのかを判定できませんでした。失敗時はプロトコルの `isError` を立てるようになりました（メッセージ本文は従来どおり）

- **アプリ — 外部のプログラムが変更したファイルを、確認せずに上書きしなくなりました** — 開いているファイルがディスク上で変わったとき、未保存の変更が無ければ QuickER は自動で読み直します。しかしその再読込は、破損したファイル・より新しいフォーマットで保存された文書・型の合わないプロパティのいずれでも失敗し、5 秒の一時通知を残すだけでした。文書はクリーンのまま・QuickER が覚えている内容も古いままなので、次の Ctrl+S が外部の作業内容を無言で上書きしていました（保存の<b>後</b>に走る照合は、自分がいま書いた内容と突き合わせるため、この上書きには気づけません）。いま開いているファイルへの保存は、書き込む前にディスクの内容と照合し、上書きしてよいか確認するようになりました。キャンセルすればファイルも未保存の変更もそのまま残ります。新規保存・別ファイルへの保存は従来どおりで、**名前を付けて保存**で同じファイルを選び直した場合（大文字小文字だけが違う場合を含む）も同じファイルとして扱います

- **アプリ — 新しいフォーマットで保存された文書を、確認せずにこの版の形式で書き戻さなくなりました** — そうした文書を開くときには「このバージョンが表現できないデータは失われる」と警告しますが、失われるのは QuickER がメモリに読み込んだ内容からだけで、ファイル側は上書きされるまでそのデータを保持しています。ファイル自体は開いてから変わっていないため上記の照合では何も報告されず、次の保存が黙って現行フォーマットで書き直していました。上書き保存・名前を付けて保存は、これから上書きするファイルの版番号を読み、将来のフォーマットなら確認するようになりました（図ファイルの保存だけが対象で、JSON エクスポートなど保存先をその場で選ぶ出力は従来どおりです）。QuickER が覚えている状態ではなくファイル自体を見るため、図を丸ごと置き換えた後（置換では現在のファイルは変わりません）・QuickER を起動し直した後・そのファイルを別のものが将来のフォーマットで書いた場合にも同じように確認が出ます（そして、ファイルがこの版のフォーマットになれば出なくなります）

- **アプリ — 2 つのテーブル・2 つの列が同じ識別子を使っている図ファイルを、読み込まずに拒否するようになりました** — この状態は JSON を手で編集したときにしか起こりませんが、従来は何も言わずに読み込めてしまい、そのあと保存が一切できなくなっていました＝Ctrl+S は毎回失敗し、復旧用の自動保存はエラーを握り潰すため一度も成功せず、列の識別子が重複している状態で対象 DBMS を切り替えるとアプリケーションが終了し（緊急保存も同じ理由で失敗するため）最後に成功した自動保存以降の作業がすべて失われました。読込は、テーブル名・列名・重複した識別子を名指しするメッセージで失敗するようになりました（手で直せるようにするためで、識別子を自動で振り直すことはしません）。検査は、エディタ・MCP サーバ・CLI・前回の作業状態の復元が共通して通る読込経路 1 箇所に置いています。リレーション・一意制約・名前付きクエリの識別子は、参照されないため検査しません

- **アプリ — 読み込めない図ファイルについて、どの種類の問題かを言うようになりました** — 同じキーを 2 つ持つ JSON オブジェクトと、フォーマットに合わない型のプロパティは、どちらも分類をすり抜けて呼び出し側へ素の失敗として届いていたため、「読み取れません」以上のことを言えませんでした。いずれも「JSON として不正」として報告します。`Version` が 0 や負数の文書は、初回リリースより古い文書として読むのではなく、図ファイルではないものとして拒否します

- **Web アドレスでない Codex の認証 URL を OS へ渡さなくなりました** — `codex app-server` が返した URL をシェル経由で開いていたため、scheme に紐づく既定アプリが起動します。`file://` や独自 scheme のアドレスではブラウザ以外が起動し得ました。開くのは `http` / `https` のときだけとなり、それ以外はチャットのステータス行で知らせます

## [0.1.0] - 2026-08-30

初回公開リリース。

### Added

- **ビジュアル ER 設計** — crow's foot 記法、1 対 1 / 1 対多 / 多対多、複合主キー、FK 参照アクション、包括的な Undo/Redo、大規模図向けのキャンバス UX（ズーム / パン / 検索 / ミニマップ）
- **マルチ DB 対応** — SQL Server / PostgreSQL / MySQL / Oracle / SQLite の 5 方言で、スキーマ取込・差分同期・DDL 生成・方言切替時の型自動変換
- **C# コード生成** — Entity / EditModel / Mapper と、DB アクセス 3 択（なし / QuickER 版 Repository / EF Core 版 Repository。同じインターフェイスを実装し、DI 登録 1 行で差し替え可能）。値オブジェクト、名前付きクエリ、リモート契約と HTTP + JSON サービス、ランタイム NuGet パッケージ参照モードはオプション
- **双方向同期** — ローカルの SQLite を SQL Server と同期させるエンジンを任意で生成（転送は直結／HTTP のどちらでも可）。初回構築・復旧向けの高速な洗い替えも生成
- **AI チャットとモック生成** — 対話による図の生成・編集（OpenAI API / Anthropic API / OpenAI 互換のローカル LLM / Codex / Claude Code / Copilot）、ER モデルからの Web 画面モック生成、任意で実行可能な Blazor / WPF モックプロジェクトの生成
- **MCP サーバ** — `quicker mcp` が図の編集とコード生成を外部 AI エージェントへ stdio で公開
- **入出力** — DBML / Mermaid / Excel 定義書 / HTML 定義書 / スキーマ JSON / PNG / SVG / ベクタ印刷
- **CLI** — `quicker generate` / `quicker scaffold` / `quicker reverse` / `quicker mcp`
- **動くサンプル** — `samples/ec-order`（SQLite・外部データベース不要）と `samples/ec-order-remote`（HTTP + JSON の 3 階層構成）

配布形態は、GUI（Setup.exe と Portable zip。自己完結の full チャンネルとフレームワーク依存の lite チャンネル）と NuGet パッケージ（dotnet tool の `QuickER.Cli` と、ランタイムパッケージ `QuickER.Runtime` / `.SqlServer` / `.Sqlite` / `.EntityFrameworkCore` / `.InMemory` / `.AspNetCore` / `.Sync`）です。

本リポジトリは混合ライセンスです。コアは MIT、AI 機能・コード生成・CLI・MCP ツール実行ホストの 8 プロジェクトは PolyForm Noncommercial 1.0.0 ＋追加許諾で、現在は商用利用を含め全員無料、基本コード生成の商用利用は恒久的に許諾されています。条文は [LICENSE-NC.md](LICENSE-NC.md)、平易な解説は [LICENSING.ja.md](LICENSING.ja.md) にあります。

[Unreleased]: https://github.com/kokko-labs/QuickER/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/kokko-labs/QuickER/releases/tag/v0.1.0
